# V3 Topics — Design Memos and Open Questions

This directory holds **v3 design memos**: open questions, model-gap notes, and
design syntheses. Unlike [`../research/`](../research/README.md) (which reads
*other* systems), these documents work on v3's own design — but they are not
yet part of the converged model contract in
[`../approach.md`](../approach.md).

## Where a topic belongs

- Touches an **already-designed part** of the core model → fold it into
  [`../../model/core-model-todo.md`](../../model/core-model-todo.md).
- Belongs to a **not-yet-designed level** → fold it into
  [`../../model/core-model-future-topic.md`](../../model/core-model-future-topic.md)
  under its owning level.
- Needs its own working document (too big, still moving, or cross-cutting) →
  it lives here as a memo, and the convergence docs link to it.

A memo with a settled direction stays here as the record of *why*; the
decision itself must land in the convergence docs to be binding.

**Naming convention:** `_open-` = the memo may still move (an open question,
a pending review, or payload not yet fully bound). `_closed-` = settled AND
every binding piece has landed in the convergence docs — nothing in the file
is pending; it is purely the record of why. Rename (`git mv`) when a memo
crosses that bar. Most `_open-` memos below are SETTLED in direction but keep
the prefix until their payload is fully anchored.

## Memo index

| Memo | Status | Feeds into |
|---|---|---|
| [`_closed-emit-contract.md`](_closed-emit-contract.md) | **CLOSED (2026-07-07) — landed AND the landing review absorbed.** The last Block A model backlog as one slice: todo Parts E + F + the A1 digest, as the paper test's 11 declarations with both review rounds folded (help under the generic validator; same-function offer; 4 new rejection names, 81→85; digest scoped to the actor-emit path). Inventories exactly as pre-declared (140/116/85), mirror clean; two ratified behavior deltas (digest + offer); F-EC-1 applied (todo F4 two-concept fix); F-EC-2 (the pre-hardening collision trace + digest storage) caught in the landing review and fixed before ratification. E6 + E1's extended fields stay as named Absents. | Section EC in `core-model.html`; `core-model-todo.md` Parts A1/E/F REALIZED statuses; v1 parity incl. the contract machine. |
| [`_open-private-data-boundary-vs-federation.md`](_open-private-data-boundary-vs-federation.md) | **Settled direction** — the MVP needs a controlled private-data contribution boundary, not kernel federation; stay federation-ready. | `approach.md` L10 scope note; `core-model-future-topic.md` L10. |
| [`_open-v3-storage-architecture.md`](_open-v3-storage-architecture.md) | **Settled direction** — storage as explicit authority planes; canonical truth = instance + transcript + idempotency ledger under CAS. Plus instance homing: authority is per-instance (one home store each); cross-kernel = events + durable refs only, never multi-master. | Core-model storage shape; `core-model-todo.md` Part A / shared kernel-shape guardrails. |
| [`_open-v3-core-api-surface.md`](_open-v3-core-api-surface.md) | **Settled direction** — the core API is a typed command/query/observe kernel API; the CLI is one thin client. | Cross-level; the core-model `RECEIVE` ingress shape. |
| [`_open-v3-workflow-inspector-ui.md`](_open-v3-workflow-inspector-ui.md) | **Settled direction** — the UI is a kernel read-model UI over typed projections; operator actions re-enter through normal ingress. | Cross-level; pairs with the core-API memo. |
| [`v3-gate-policy-config-design-synthesis.md`](v3-gate-policy-config-design-synthesis.md) | **Captured synthesis** — two-surface gate config: authoring profile compiled to a normalized, typed `GatePipeline`. | `core-model-future-topic.md` L2/L2a (authoring profiles, packaged policy). |
| [`_open-agent-runtime-and-pane-layout.md`](_open-agent-runtime-and-pane-layout.md) | **Settled direction (2026-07-07)** — Q1 (per-adapter cleanest channel; tmux = observe/takeover only), Q2 (pane binds **per-runtime-context**: one pane = one workspace; per-role is a one-bubble-era legacy that breaks under concurrency), Q3 (layout config = presentation layer) all settled. Config form RESOLVED at the v1-operability close-out: **no layout config in v1** — discovery via the visibility floor, attach = per-runtime-context verb on the adapter's observe/takeover channel; tree view = the parked inspector UI. Remaining: MVP scope sub-decision → `implementation-contract.md` PI-8. | L0e runtime-context, L0c ActorAdapter, observe-seam; `core-model-future-topic.md` L0e; `implementation-contract.md` PI-8. |
| [`_open-creation-identity.md`](_open-creation-identity.md) | **Settled direction (2026-07-07)** — kernel-edge construct: `CREATE_INSTANCE` gains an optional `creation_key` (`{namespace, external_id}`, caller-encoded generations) with mint-or-return-existing semantics over a kernel-owned forever-lived key registry; required by contract on machine ingress (L6/L8 clients), template-declarable as required. `creation_key` is to CREATE what `op_id` is to everything after. Binding form = a later F-W1-2-style touch. | `CREATE_INSTANCE` ingress touch; `core-model-future-topic.md` L6/L8 (as clients); GAP-16 series-grain inherits the pattern. |
| [`_open-kernel-floor.md`](_open-kernel-floor.md) | **Settled direction (2026-07-07)** — all four boundaries ratified: the weight floor is a *rule* ("kernel entry when the wait must survive the performer or the ask needs independent audit"; lighter form deliberately deferred), cross-instance exclusion/lease stays runtime-side by design (kernel's grain = creation-key task exclusivity), the outermost pager is external by design, the not-a-workload canon preserved. Enforcement: written canon → later L12 review gate → observability symptoms. Includes the kernel-vs-product framing and the "three homes of below-floor work" record. | Template-design canon; `core-model-future-topic.md` L9 #7 (R8); the L12 definition-review checklist (later). |
| [`_open-runtime-capability-surface.md`](_open-runtime-capability-surface.md) | **Settled direction (2026-07-07)** — three rules ratified: the surface exists and is projected into the context packet (visibility); every state-touching op goes through normal kernel ingress as a committed fact (no side door); policy/definition mutation is propose-only. Op list + naming → implementation plan (after the dynamic-orchestrator decision) — explicitly NOT the first round (Block B era, GAP-15 registry; Block A's packet already projects `available_ops`/`op_contracts` — see the scope-out note under `implementation-contract.md` PI). Addendum: the **pull form** named — capability-query op family (`list_my_ops` / `list_spawnable_actors` w/ capability flags / `list_addressable_helpers`) as the GAP-15 actor registry's actor-facing read surface; goal→tool matching stays in the actor's head. | `core-model-future-topic.md` L0b (structured emit affordances family); L12 propose-only discipline; Identity seam GAP-15. |
| [`_dynamic-orchestrator-workflow.md`](_dynamic-orchestrator-workflow.md) | **Settled direction (2026-07-07)** — no first-class dynamic-orchestrator mode: the shape is a template pattern over **six constructs** (declared child · detached spawn · data-driven fan-out · help-ask · below-floor tool calls · **the multi-round child**: a child instance parked between rounds, re-instructed through normal ingress, addressed by instance id). Q1: sessions are NOT kernel objects (adapter-side handles, issued intent; the durable address is the instance). Consequence: L4 #3 intermediate lifecycle subscriptions promoted to load-bearing. Verified by three adversarial Opus sweeps of the Omnigent codebase (examples 5/5, runtime source, design docs) — no live steering, no worker mesh, no kernel plan object found; the transient-inbox bug history is fresh no-side-door evidence. | `core-model-future-topic.md` L4 #3/#11/#12, L5 #9; `_open-runtime-capability-surface.md` rule 2. |
| [`_closed-v1-operability.md`](_closed-v1-operability.md) | **CLOSED (2026-07-07)** — settled AND fully anchored (IC-D/IC-E, PI-1…PI-9, T1 realized, approach.md scope binding) — the practical-usability round for the Block A implementation, all four questions ratified. **Q1:** the CLI-first read-only visibility floor ships WITH Block A — listInstances / getInstanceDetail / getTimeline + the live tail (promoted to required at ratification); observe seam + inspector UI stay parked. **Q2:** a "test kit" chapter as a peer of the IC chapter (scripted actor / fake egress adapter / fixture convention / deterministic gate-process fixtures) + the contract line that ingress never assumes a particular adapter implementation. **Q3:** the kernel never reads the clock directly — single injected time source (explicitly NOT replay). **Q4:** the ledger = the model↔code contract surface — three drift-tested name-spaces (85 rejections / domain registry / 158 pseudocode units→code mapping) unconditional; chapter traces as golden tests mandatory core + rejection-branch traces as scoped extension; invariant post-condition suite; the domain-registry lift recorded as a model-side tooling touch for the model-build thread. **External-review addendum (ratified):** the debug bundle as the floor's fifth piece (one-run structured diagnostic export, with a redaction boundary — secrets/runtime env omitted); the operator CLI's command+dev verbs ship with Block A too (thin client, normal ingress); runner-MVP chapter sharpened to the worktree-provider / one-real-adapter / process-gate-runner trio. **Addendum 2 (third review, ratified):** the rejection-visibility seam resolved — `getTimeline` is committed-rows-only; a named non-authoritative diagnostic channel (structured kernel log + rejection-audit stream, per IC-A1's "audit, not ledger" allowance) feeds the tail's rejection visibility and the debug bundle's "rejected inputs"; the Q2/Q3 contract lines landed as binding **IC-D/IC-E**; the domain-registry lift anchored as `core-model-todo.md` T1 **and REALIZED (ledger §4: 51 aggregate blocks · 121 entities, per Domain-lens slice, `check.sh`-guarded — the Q4 type-layer drift test is now unconditional)**. Plus reserved implementation-plan chapters (template format, bootstrap, storage pick+migration stance, runner MVP scope, operator recourse card, kernel diagnostics & structured logging); the full plan-facing payload mirrored as the `PI-*` plan-intake checklist in `implementation-contract.md`. | The implementation plan's scope + its IC/test-kit chapters; `_open-v3-core-api-surface.md` query family; `implementation-contract.md` IC-D/IC-E + PI-1…PI-9; `core-model-todo.md` T1 (domain registry §4). |
| [`_open-kernel-primitives.md`](_open-kernel-primitives.md) | **COMPLETE — the rebaseline executed as waves 1–5, all ratified (2026-07-06)** — the five kernel primitives (Errand · ChoicePoint · Admission · Warrant · Directive) are named contracts in the corpus at their earned birth points (Admission@L0d · Warrant@L1 · Errand@LC2 · ChoicePoint+Directive@L3) with instance/phase labels corpus-wide; behavior-neutrality machine-checked per wave (registries + per-block rejection multisets; one deliberate ratified delta, F-W4-2). §9 is the per-wave findings log. The named debt (the dedicated F-W1-2 ingress/idempotency hardening touch) was discharged and ratified 2026-07-06 — the strand is fully closed. | Realized in `core-model.html` (model-src units); `core-model-todo.md` Parts A/B/D/E cross-refs. |
