OpenAI Codex v0.146.0
--------
workdir: /Users/felho/dev/pairflow
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 019fe1a4-e437-7843-811d-4dd11048ceac
--------
user
# QA review charter — schema-lock round 2: re-check on the folded bytes

You are a fresh-context reviewer with full repo access. Read the repo
yourself; do not trust any summary in this charter over the bytes.

SECOND round of a bounded three-round frame on the contract-draft
lint's schema lock. Round 1 found two in-scope defects; both were
folded at commit `20b73f6a` (dims-first). This round verifies the folds
and their neighbourhoods and confirms nothing else moved. NOT a fresh
falsification hunt — outside the scope below, CARRIED-SCOPE, one line.

## 0. The time budget

**HARD 20-minute wall-clock budget, external kill; unwritten findings
are lost.** Write each finding in full immediately; stop at ~12 min;
over-2-minute questions go down as `unrun`.

## 1. The review object

`tools/v3-plan/check_packet.py` (3515 lines, sha256 first16
`367a598eb6cfdb2b`) at commit `20b73f6a` (with the docs commit
`0d58283e` on top; HEAD may sit there — the lint bytes are `20b73f6a`'s).
The standards: the file's D3/D5b docstring claims, the template's §3
schema-lock clause, and the fold's commit message.

## 2. The two folds under verification

**Fold 1:** the shape check refuses `..` as a path SEGMENT, not as a
substring — `schema..fixture.ts` (a regular file, true hash) is green;
`sub/../x.ts` refuses at shape. Exercise the neighbourhood: `../x` at
the head; a trailing `/..`; a filename that is exactly `..` alone;
backslashes and other separators the split does not treat.

**Fold 2:** the resolved target must stay under the resolved root — a
symlink inside the root pointing outside refuses with "resolves outside
the draft's repository", even with the outside bytes' true hash.
Exercise the neighbourhood: a symlink pointing INSIDE the root (must
stay green with the target's true hash); a symlinked intermediate
DIRECTORY escaping the root; a relative symlink; the fixture root
itself being under a symlinked /tmp path (macOS `/tmp` → `/private/tmp`
— a false escape here would red every legitimate lock).

## 3. Scope fence

In scope: the two folds, their neighbourhoods, regression on the five
schema-lock dims and the live drafts. Also verify: selftest register
now 140 red dims + the new green, 0 failures; live lint unchanged (6
drafts, 0 errors).

## 4. Finding format

Threat model: ACCIDENT and SLOPPINESS in the fold. Format as before:

```
### <CLASS> <n> — <one-line title>
CLASS: IN-SCOPE | CARRIED-SCOPE | UNRUN
WHAT / WHERE / EVIDENCE (exact command + output) / EXPECTED
```

No fixes. No repo modification — work under `/tmp`; a guard runs
before and after. Routine QA vocabulary throughout.

## 5. Closing message

Only: HEAD; counts; neighbourhoods full/partial; selftest + live lint
results; one sentence — do the folds hold?

codex
A 20 perces keretet most indítom. Először rögzítem a HEAD-et, a worktree-guardot és a vizsgált bájtok hashét/diffjét; utána kizárólag a két fold környékét, az öt dimenzió regresszióját, a selftest-regisztert és a live draft lintet futtatom. A repót nem módosítom, minden ideiglenes fixture `/tmp` alatt lesz.
exec
/bin/zsh -lc "date '+START %Y-%m-%d %H:%M:%S %z'; git rev-parse --short=12 HEAD; git log -3 --oneline --decorate; git status --short --branch; shasum -a 256 tools/v3-plan/check_packet.py; git diff --stat 20b73f6a"'^ 20b73f6a -- tools/v3-plan/check_packet.py; git diff --check 20b73f6a''^ 20b73f6a -- tools/v3-plan/check_packet.py' in /Users/felho/dev/pairflow
 succeeded in 0ms:
START 2026-08-08 15:52:02 +0200
0d58283eeb1a
0d58283e (HEAD -> main) docs(v3): tag closure both directions — C1 cites the root's growth, C11 owns the profile channel
20b73f6a fix(v3): schema-lock round-1 fold — a parent segment is not a substring, and the lock stays home
ed86ac24 feat(v3): the schema lock — one hash line per act (template §3 + lint D5b)
## main...origin/main [ahead 119]
367a598eb6cfdb2b7994ae7cab04d9d2b29170f59da0b5a06d9169b9d9bdbad0  tools/v3-plan/check_packet.py
 tools/v3-plan/check_packet.py | 61 +++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 59 insertions(+), 2 deletions(-)

exec
/bin/zsh -lc "git show --format=fuller --stat --oneline 20b73f6a; git show --format=full --no-ext-diff 20b73f6a -- tools/v3-plan/check_packet.py; rg -n \"schema.lock|schema lock|D5b|D3|selftest|140|draft's repository|resolves outside|parent segment|schema\\.\\.fixture\" tools/v3-plan/check_packet.py v3/implementation/task-packet-template.md v3/implementation -g '*.md' -g '*.py'" in /Users/felho/dev/pairflow
 succeeded in 0ms:
20b73f6a fix(v3): schema-lock round-1 fold — a parent segment is not a substring, and the lock stays home
 tools/v3-plan/check_packet.py | 61 +++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 59 insertions(+), 2 deletions(-)
commit 20b73f6a265cde31cb94c68b08083326e55708b5
Author: Gergely Hodicska <gergely.hodicska@gmail.com>
Commit: Gergely Hodicska <gergely.hodicska@gmail.com>

    fix(v3): schema-lock round-1 fold — a parent segment is not a substring, and the lock stays home
    
    Two findings from the schema-lock arm's first round, folded dims-first.
    
    F1: the shape check refused any path CONTAINING ".." — a regular file
    named schema..fixture.ts with its true hash went red. The check now
    tests path SEGMENTS, and the dotted filename is the green twin.
    
    F2: a symlink inside the root pointing outside it satisfied the lock
    with the outside bytes' true hash. The resolved target must now stay
    under the resolved root — the ratified bytes are the draft's own
    working tree's — and the escape is a red dim with the exact message.
    
    Register 139 to 140 red dims plus one new green; live lint unchanged
    at 6 drafts, 0 errors.
    
    Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

diff --git a/tools/v3-plan/check_packet.py b/tools/v3-plan/check_packet.py
index f9ee0eec..f6c01e8f 100644
--- a/tools/v3-plan/check_packet.py
+++ b/tools/v3-plan/check_packet.py
@@ -683,7 +683,9 @@ def check_ratification_shape(name: str, index: int, block: object, checker: Chec
                 not isinstance(lock_path, str)
                 or not lock_path.strip()
                 or lock_path.startswith("/")
-                or ".." in lock_path
+                # a PARENT SEGMENT, not a substring — "schema..fixture.ts"
+                # is a legal filename (round-1 F1's false positive)
+                or ".." in lock_path.split("/")
             ):
                 checker.error(f"{label}.schema.path must be a nonempty repo-relative path")
             if not isinstance(schema["sha256"], str) or not SHA256_RE.match(schema["sha256"]):
@@ -769,6 +771,15 @@ def check_schema_lock(path: Path, schema: object, checker: Checker) -> None:
         return
     root, _rel = located
     target = root / lock_path
+    # round-1 F2: a symlink inside the root pointing OUTSIDE it must never
+    # satisfy the lock — the ratified bytes are the draft's own working
+    # tree's, so the RESOLVED target must stay under the resolved root.
+    if not target.resolve().is_relative_to(root.resolve()):
+        checker.error(
+            f"{name}: schema lock path '{lock_path}' resolves outside the draft's "
+            f"repository — the lock binds the draft's own working tree bytes"
+        )
+        return
     if not target.is_file():
         checker.error(
             f"{name}: schema lock path '{lock_path}' does not exist in the working "
@@ -2067,7 +2078,7 @@ def build_superseded_fixture() -> tuple[tempfile.TemporaryDirectory, Path, Path,
 
 # The pinned size of the red-fixture register (see the check at the end
 # of run_selftest for why a printed number was not enough).
-EXPECTED_RED_DIMS = 139
+EXPECTED_RED_DIMS = 140
 
 
 def run_selftest() -> int:
@@ -2860,6 +2871,52 @@ def run_selftest() -> int:
         None,
     )
 
+    def _schema_lock_symlink_escape() -> None:
+        # round-1 F2: a symlink inside the root pointing OUTSIDE it must
+        # never satisfy the lock, even with the outside bytes' true hash.
+        fixture = build_green_fixture()
+        if fixture is None:
+            failures.append("selftest fixture setup failed: schema-lock-symlink-escape")
+            return
+        tmp, root, draft, _packet, green = fixture
+        outside = root.parent / (root.name + "-outside.ts")
+        outside.write_text("outside declaration\n", encoding="utf-8")
+        (root / "schema-link.ts").symlink_to(outside)
+        true_sha = hashlib.sha256(outside.read_bytes()).hexdigest()
+        locked = green.replace(
+            '"arms": ["a", "b"], "commit"',
+            f'"arms": ["a", "b"], "schema": {{"path": "schema-link.ts", "sha256": "{true_sha}"}}, "commit"',
+        )
+        draft.write_text(locked, encoding="utf-8")
+        errors, _ = lint(root / "packets", root / "contracts", ADR_DIR)
+        assert_red("schema-lock-symlink-escape", errors, "resolves outside the draft's repository")
+        outside.unlink()
+        tmp.cleanup()
+
+    _schema_lock_symlink_escape()
+
+    def _schema_lock_dotted_filename_green() -> None:
+        # round-1 F1: ".." INSIDE a filename is not a parent segment — a
+        # regular file named schema..fixture.ts with its true hash is green.
+        fixture = build_green_fixture()
+        if fixture is None:
+            failures.append("selftest fixture setup failed: schema-lock-dotted-filename")
+            return
+        tmp, root, draft, _packet, green = fixture
+        decl = root / "schema..fixture.ts"
+        decl.write_text("export const declaration = { frozen: true };\n", encoding="utf-8")
+        true_sha = hashlib.sha256(decl.read_bytes()).hexdigest()
+        locked = green.replace(
+            '"arms": ["a", "b"], "commit"',
+            f'"arms": ["a", "b"], "schema": {{"path": "schema..fixture.ts", "sha256": "{true_sha}"}}, "commit"',
+        )
+        draft.write_text(locked, encoding="utf-8")
+        errors, _ = lint(root / "packets", root / "contracts", ADR_DIR)
+        expect_green("schema-lock-dotted-filename-green", errors)
+        tmp.cleanup()
+
+    _schema_lock_dotted_filename_green()
+
     def _tree_sha_fixture() -> None:
         # a TREE sha satisfies `git show <sha>:<path>` but is not an
         # auditable ratification point — must be red, not green
v3/implementation/task-packet-template.md:229:- **Lint:** `pnpm v3:packet-lint` (selftest + live; wired into the CI
tools/v3-plan/check_packet.py:10:Claims — the selftest derives from THIS list; each claim proves itself
tools/v3-plan/check_packet.py:135:  D3. ratification = {date, arms, commit}: exact keyset; YYYY-MM-DD
tools/v3-plan/check_packet.py:145:  D5b. The SCHEMA LOCK (template §3; user-ratified 2026-08-08): when
tools/v3-plan/check_packet.py:198:           naming a real calendar day (D3's rule); and `superseded` is
tools/v3-plan/check_packet.py:262:Modes: default lints everything; --selftest proves the claims red on
tools/v3-plan/check_packet.py:417:    """D3/D8.3: a declared date is a REAL day, not merely a
tools/v3-plan/check_packet.py:663:    """D3: strict shape for EVERY ratification block. The OPTIONAL
tools/v3-plan/check_packet.py:665:    every block that carries it; its BYTE check (D5b) runs on the
tools/v3-plan/check_packet.py:671:            f" (plus the optional schema lock)"
tools/v3-plan/check_packet.py:686:                # a PARENT SEGMENT, not a substring — "schema..fixture.ts"
tools/v3-plan/check_packet.py:723:    # D3/D5 say COMMIT sha — `git show <sha>:<path>` happily resolves a
tools/v3-plan/check_packet.py:754:def check_schema_lock(path: Path, schema: object, checker: Checker) -> None:
tools/v3-plan/check_packet.py:755:    """D5b: the working tree's declaration file hashes to the recorded
tools/v3-plan/check_packet.py:758:    ones. Runs only when the shape (D3) already holds; a malformed
tools/v3-plan/check_packet.py:762:        return  # shape already reported by D3
tools/v3-plan/check_packet.py:765:        return  # shape already reported by D3
tools/v3-plan/check_packet.py:769:            f"{name}: not inside a git repository — the schema lock cannot be verified"
tools/v3-plan/check_packet.py:779:            f"{name}: schema lock path '{lock_path}' resolves outside the draft's "
tools/v3-plan/check_packet.py:785:            f"{name}: schema lock path '{lock_path}' does not exist in the working "
tools/v3-plan/check_packet.py:1002:        # D5b the schema lock — the LATEST block only, same statuses,
tools/v3-plan/check_packet.py:1005:            check_schema_lock(path, latest["schema"], checker)
tools/v3-plan/check_packet.py:1880:    """The full lint pass as data — the selftest asserts on the error
tools/v3-plan/check_packet.py:1948:# ---------------------------------------------------------------- selftest
tools/v3-plan/check_packet.py:2005:GIT_BASE = ["git", "-c", "user.email=lint@selftest", "-c", "user.name=lint-selftest"]
tools/v3-plan/check_packet.py:2080:# of run_selftest for why a printed number was not enough).
tools/v3-plan/check_packet.py:2081:EXPECTED_RED_DIMS = 140
tools/v3-plan/check_packet.py:2084:def run_selftest() -> int:
tools/v3-plan/check_packet.py:2090:        print("selftest FAIL: git fixture setup failed", file=sys.stderr)
tools/v3-plan/check_packet.py:2100:            failures.append(f"selftest dim NOT red for its claim: {name} (no error containing '{substr}')")
tools/v3-plan/check_packet.py:2115:            failures.append(f"selftest fixture setup failed: {name}")
tools/v3-plan/check_packet.py:2128:            failures.append(f"selftest green NOT green: {name} ({errors[0]})")
tools/v3-plan/check_packet.py:2776:    # ---- D3 ratification shape + dates
tools/v3-plan/check_packet.py:2801:            failures.append("selftest fixture setup failed: ratification-dates-decreasing")
tools/v3-plan/check_packet.py:2817:    # ---- D3/D5b the schema lock (user-ratified 2026-08-08)
tools/v3-plan/check_packet.py:2818:    def _schema_lock_fixture(name: str, schema_json, write_file: bool, substr: str | None) -> None:
tools/v3-plan/check_packet.py:2819:        """A green draft whose latest block carries a schema lock over a
tools/v3-plan/check_packet.py:2824:            failures.append(f"selftest fixture setup failed: {name}")
tools/v3-plan/check_packet.py:2843:    _schema_lock_fixture(
tools/v3-plan/check_packet.py:2844:        "schema-lock-bad-keyset",
tools/v3-plan/check_packet.py:2849:    _schema_lock_fixture(
tools/v3-plan/check_packet.py:2850:        "schema-lock-bad-sha-shape",
tools/v3-plan/check_packet.py:2855:    _schema_lock_fixture(
tools/v3-plan/check_packet.py:2856:        "schema-lock-path-missing",
tools/v3-plan/check_packet.py:2861:    _schema_lock_fixture(
tools/v3-plan/check_packet.py:2862:        "schema-lock-hash-mismatch",
tools/v3-plan/check_packet.py:2867:    _schema_lock_fixture(
tools/v3-plan/check_packet.py:2868:        "schema-lock-green",
tools/v3-plan/check_packet.py:2874:    def _schema_lock_symlink_escape() -> None:
tools/v3-plan/check_packet.py:2879:            failures.append("selftest fixture setup failed: schema-lock-symlink-escape")
tools/v3-plan/check_packet.py:2892:        assert_red("schema-lock-symlink-escape", errors, "resolves outside the draft's repository")
tools/v3-plan/check_packet.py:2896:    _schema_lock_symlink_escape()
tools/v3-plan/check_packet.py:2898:    def _schema_lock_dotted_filename_green() -> None:
tools/v3-plan/check_packet.py:2899:        # round-1 F1: ".." INSIDE a filename is not a parent segment — a
tools/v3-plan/check_packet.py:2900:        # regular file named schema..fixture.ts with its true hash is green.
tools/v3-plan/check_packet.py:2903:            failures.append("selftest fixture setup failed: schema-lock-dotted-filename")
tools/v3-plan/check_packet.py:2906:        decl = root / "schema..fixture.ts"
tools/v3-plan/check_packet.py:2911:            f'"arms": ["a", "b"], "schema": {{"path": "schema..fixture.ts", "sha256": "{true_sha}"}}, "commit"',
tools/v3-plan/check_packet.py:2915:        expect_green("schema-lock-dotted-filename-green", errors)
tools/v3-plan/check_packet.py:2918:    _schema_lock_dotted_filename_green()
tools/v3-plan/check_packet.py:2925:            failures.append("selftest fixture setup failed: ratification-tree-sha")
tools/v3-plan/check_packet.py:2993:        failures.append("selftest fixture setup failed: reopen-choreography")
tools/v3-plan/check_packet.py:3011:            failures.append("selftest claim NOT held: reopened drafts not listed by name")
tools/v3-plan/check_packet.py:3062:            failures.append(f"selftest fixture setup failed: {name}")
tools/v3-plan/check_packet.py:3079:        failures.append("selftest fixture setup failed: supersede-choreography")
tools/v3-plan/check_packet.py:3094:            failures.append("selftest claim NOT held: superseded drafts not listed by name")
tools/v3-plan/check_packet.py:3209:            failures.append("selftest git fixture setup failed (post-build)")
tools/v3-plan/check_packet.py:3241:                    f"selftest green NOT green: valid post-build audit ({checker.errors[0]})"
tools/v3-plan/check_packet.py:3276:                failures.append("selftest git fixture setup failed (merge)")
tools/v3-plan/check_packet.py:3292:            failures.append("selftest git fixture setup failed (post-build-malformed)")
tools/v3-plan/check_packet.py:3314:            failures.append("selftest git fixture setup failed (post-build-shape)")
tools/v3-plan/check_packet.py:3338:            failures.append("selftest git fixture setup failed (post-build-root)")
tools/v3-plan/check_packet.py:3346:                failures.append("selftest git fixture setup failed (post-build-empty)")
tools/v3-plan/check_packet.py:3396:            failures.append("selftest git fixture setup failed (adr-015 alias)")
tools/v3-plan/check_packet.py:3410:                    f"selftest green NOT green: alias-ancestor-audit ({checker.errors[0]})"
tools/v3-plan/check_packet.py:3445:            failures.append("selftest git fixture setup failed (adr-015 alias d5)")
tools/v3-plan/check_packet.py:3453:                    f"selftest green NOT green: alias-d5-ancestor ({checker.errors[0]})"
tools/v3-plan/check_packet.py:3468:    # selftest green. Deliberately one level: a red fixture per claim, a
tools/v3-plan/check_packet.py:3472:            f"selftest register count {len(red_dims)} != pinned {EXPECTED_RED_DIMS} "
tools/v3-plan/check_packet.py:3478:        print(f"selftest FAIL: {f}", file=sys.stderr)
tools/v3-plan/check_packet.py:3480:        f"selftest: {len(red_dims)} red dims exercised, green fixture + reopen "
tools/v3-plan/check_packet.py:3488:    parser.add_argument("--selftest", action="store_true")
tools/v3-plan/check_packet.py:3497:    if args.selftest:
tools/v3-plan/check_packet.py:3498:        return run_selftest()
v3/implementation/adr-015-migration-report.md:2322:   "content": "| GAP-1 | Creation-grain idempotency (trigger/event → `CREATE_INSTANCE` exactly-once binding) | S2–S17 (all 16 simulations after S1; S16 also at timer-series grain) | `CREATE_INSTANCE` carries no idempotency/external-identity key and `UNIQUE(instance_id, op_id)` is per-instance scope, so a redelivered event or a re-claimed L6 fire mints twin instances. L8 §1's store-enforced correlation covers the *channel-borne* path as written; the L6 fire→create seam and the bare operator/API ingress have no story. **Two documented real-world casualties, dual failure faces: BitSafe's 26 duplicate findings pages (S9, capture 1404 — too many instances) and the 16 stuck production threads (S17, capture 1480 — zero instances); L8 §1's mint-or-return-existing oracle kills both with one mechanism, and v3-as-built would reproduce both.** | Lifted → `topics/_open-creation-identity.md` (design-fork memo) |",
v3/implementation/adr-015-migration-report.md:4176:   "line": 140,
v3/implementation/adr-015-migration-report.md:4221:   "line": 140,
v3/implementation/adr-015-migration-report.md:5572:   "reason": "kept — alias machinery (selftest fixture enacting the migration)",
v3/implementation/adr-015-migration-report.md:5581:   "reason": "kept — alias machinery (selftest fixture)",
v3/implementation/adr-015-migration-report.md:5590:   "reason": "kept — alias machinery (selftest fixture)",
v3/implementation/contract-draft-template.md:175:- **The schema lock (schema-first contracts, ADR-019 D4):** a
v3/implementation/process-v2-design.md:221:  plan-boundary family (`2:draft-split` in the D3 token registry),
v3/implementation/process-v2-design.md:237:  approve, per D3; a genuinely new ADR-class decision mid-loop → STOP 1,
v3/implementation/process-v2-design.md:257:### D3 — Autonomy envelope
v3/implementation/process-v2-design.md:382:  (+ STOP per D3). The verdict set is an ADAPTATION of v1's, renamed along
v3/implementation/process-v2-design.md:404:  never silently "fixed"); `packet_plan_drift` BIFURCATES per D3
v3/implementation/process-v2-design.md:530:  Policy`) → D3.
v3/implementation/process-v2-design.md:637:   only, it is not a separate commit): the D3 autonomy envelope + STOP
v3/implementation/process-v2-design.md:664:   to the D3 verdict-action matrix (STOPs and flag-bearing approves are
v3/implementation/process-v2-design.md:718:metrics + in-place archival, the D3 verdict-action matrix + split
v3/implementation/process-v2-design.md:774:(`packet_plan_drift` bifurcates per D3); the draft loop got its verdict
v3/implementation/process-v2-design.md:801:member, and D3 gained the **canonical STOP member-token registry**
v3/implementation/process-v2-design.md:840:1330 lines and 45 selftest dimensions — guarding a gate ZERO live v2
v3/implementation/process-v2-design.md:866:D3 STOP list + verdict-action matrix, the D2 contract-draft LAYER, the
v3/implementation/process-v2-design.md:1023:  duties. Corollary: selftest armor scales with the declared surface,
v3/implementation/process-v2-design.md:1044:selftest culture (fixtures + red dims, re-derived from the shrunk
v3/implementation/process-v2-design.md:1053:selftest fixtures); the `sha256` ratification key.
v3/implementation/process-v2-design.md:1062:with the retired selftest dimensions replaced by a smaller claim-derived
v3/implementation/task-packet-template.md:229:- **Lint:** `pnpm v3:packet-lint` (selftest + live; wired into the CI
v3/implementation/ch13-rederivation-arm/p3-build/phase-close-report.md:15:  (vocabulary 384 · engine 1183 · declaration 545 · normalizer 140 ·
v3/implementation/ch13-rederivation-arm/p3-build/phase-close-report.md:30:| `normalizer.ts` | D3's second, separately-named capability: derivation. |
v3/implementation/ch13-rederivation-arm/p3-build/arm-round2.md:20:| `v3/src/definition/schema/normalizer.ts` | 140 | `30754f4b7aa93199` |
v3/implementation/ch13-rederivation-arm/p3-build/arm-round3.md:19:| `v3/src/definition/schema/engine.ts` | 1183 | `c60b692db5301402` |
v3/implementation/ch13-rederivation-arm/p3-build/arm-round3.md:21:| `v3/src/definition/schema/normalizer.ts` | 140 | `30754f4b7aa93199` |
v3/implementation/README.md:42:   140 named Absents (counts at the ch11 model alignment; the plan §1.4
v3/implementation/README.md:307:(PI-3's own split), and the 140 named Absents are *not* implemented but
v3/implementation/README.md:555:selftest armor scales with the declared surface — shrinking the
v3/implementation/README.md:566:floors (claim-derived negative tests, §4 step 2; the selftest armor
v3/implementation/README.md:574:analysis) stay at the selftest floor — the trigger is verification
v3/implementation/packets/ch12-p1b-activation-machinery.md:547:| D3 | The ledger is BYTE-IDENTICAL; the drift lanes are green before AND after — any drift-lane movement is a STOP, never a packet-local fix (anchored: plan §12.2; PI-3) |
v3/implementation/packets/ch12-p1b-activation-machinery.md:800:      { "id": "D3", "class": "anchored", "refs": ["prose:plan §12.2", "prose:PI-3 standing rule"] },
v3/implementation/packets/ch12-p1b-activation-machinery.md:937:- Drift tests green (standing, unconditional — PI-3): D3's rule.
v3/implementation/process-log.md:622:  comparison), selftest 15→19 red dims. Lesson line for the boundary:
v3/implementation/process-log.md:624:  — a checker's selftest must derive from what the checker CLAIMS to
v3/implementation/process-log.md:633:  bootstrap paradox), with a self-referential selftest (the "claim" it
v3/implementation/process-log.md:709:  packets, D1–D7 drafts — the round-6 selftest-derives-from-claims
v3/implementation/process-log.md:728:  (1) the draft selftest fixtures re-used the SHARED fixture's green
v3/implementation/process-log.md:731:  commit error could mask a dead D3/D7 check: the selftest's
v3/implementation/process-log.md:893:  structural selftest assertion. Selftest 72→74
v3/implementation/process-log.md:907:  show); the selftest's own audits now pass rev-parse'd shas, with
v3/implementation/process-log.md:910:  · (1) the P8 audit had RED-ONLY selftest coverage — a false-red
v3/implementation/process-log.md:913:  claim-derivation rule cuts both ways: a gate's selftest proves it
v3/implementation/process-log.md:935:  lock at approve, selftest fixture invalidity, wrong-commit audit,
v3/implementation/process-log.md:950:  the BUILD-CLOSE gate point. Three selftest proofs: the exact
v3/implementation/process-log.md:1522:  the template §1 twin rule line; 78 selftest dims, live packets
v3/implementation/process-log.md:2965:to FAIL before commit — the checker-selftest discipline ("each claim
v3/implementation/process-log.md:2973:commit, on the checker-selftest precedent.
v3/implementation/process-log.md:3343:  unknown>>` and D3 claiming that RENDERS `projection-never-the-ref` at the
v3/implementation/process-log.md:3350:  is type-enforced, the collapse making D3/Acceptance §T's compile-error
v3/implementation/process-log.md:3560:  template.ts 84/140/172/181/183, main.ts 308). Cost: the retirement SWEEP can
v3/implementation/process-log.md:3692:  type check + red/green selftest cases; REQUIRED by template rule from
v3/implementation/process-log.md:3976:  adversarial selftests per the extended R-CLAIM-FORM-PROBES):
v3/implementation/process-log.md:4085:  (C20's D6/F3, C27's O4, C32's O5, C39's D3, C8's K2/T1, C18's
v3/implementation/process-log.md:4342:  writing the sidecar, selftests exercise REAL refusal/timeout
v3/implementation/process-log.md:4358:  negatives §4/2, selftest armor, the review taxonomy's TOOLING
v3/implementation/process-log.md:4365:  use; convenience scripts stay at the selftest floor; disputed
v3/implementation/process-log.md:5219:  (selftest red dims, live-tree lint, an executed tsc probe), and a
v3/implementation/process-log.md:5225:  selftest that stayed green when the arm DELETED two of the
v3/implementation/process-log.md:5227:  the round-6 selftest-derives-from-claims lesson reproduced on fresh
v3/implementation/process-log.md:5323:  lines, ~700 of them a four-level selftest register tower. THE
v3/implementation/process-log.md:5325:  build (274df3a6, its own selftest 131/0 green) plus the re-applied
v3/implementation/packets/ch9-p4a-spawn-machinery.md:182:  KILL escalation — is the C19/SD3 escalation re-hosted at pane
v3/implementation/packets/ch9-p4a-spawn-machinery.md:482:| TX5 | Session timeout: the channel owns the timer at tmux grain (the seam's timer only bounds the short-lived CLIENT invocations): on `timeoutMs` firing it sets `timedOut`, sends SIGTERM to `pane_pid` (the wrapper forwards to the actor and arms its own inner grace — P6c/P6f, RS1 unchanged), and arms the outer backstop SIGKILL to `pane_pid` at `graceMs + backstopMarginMs` (SD3's two-tier margin re-hosted: the inner escalation provably concludes first on the normal path; the orphan residual stays F2(P3b)'s ratified acceptance). If `pane_pid` was never obtained (the session died pre-observation) no signal is sent — death is already the observable. LAST RESORT: a session still alive after the backstop window + one poll interval (a signal-delivery fault) → `tmux kill-session -t <s>` (a seam invocation like every client call), and the conclusion then proceeds through TX7's SAME precedence over the result file: a valid record ALREADY WRITTEN governs (completed work is never reclassified — a completed-but-stuck-session attempt lands its recorded outcome after a bounded latency penalty), absence lands `own_timeout` via the flag — bounded, never an unbounded wait. THE LAST RESORT'S OWN FAILURE LANE (label-symmetric — it binds WHEREVER the kill-session backstop runs, the flagged timeout path AND TX6's unflagged abort path alike): a kill-session invocation concluding nonzero/infra while the poll STILL reports the session alive is retried ONCE with the next poll; a session still alive after that concludes BOUNDED under the path's own label — `own_timeout` via the flag on the timeout path, `spawn_infra` on the unflagged abort path — WITH the ORPHAN-SESSION RESIDUAL DECLARED on EITHER conclusion (flag F6(d) — the P3b-F2 orphan-acceptance precedent extended to session grain): a tmux server refusing signals AND kill-session is the trusted-local substrate-fault class; the attempt's conclusion stays bounded, the errand budget bounds retries, and the orphan stays findable by its session name — never an unbounded wait, never an undeclared orphan on ANY conclusion label. All channel timer knobs are validated at construction via the seam's SHARED validator (`validateTimerKnobs` — the P3b-T2 rule). DERIVATION: C19's escalation + C23's result-seam letter force pane-grain TERM (the wrapper must RECEIVE the TERM to write the result — kill-session-first would forfeit the record, P7c is the delivery proof); the kill-session backstop is the bounded residual. |
v3/implementation/plan.md:295:- **140 Absents — NOT implemented.** Realization rule (fail-closed, scoped to
v3/implementation/plan.md:1819:module, `src/definition/` (D3, ratified 2026-07-10):
v3/implementation/packets/ch11-p1-l1-authority.md:484:| D3 | The emission contract is unchanged IN KIND: one classified event per non-success final outcome, nothing on committed, bare `diag.emit` calls (REV-DIAG-FAILOPEN), digest THREADED never recomputed — the new reasons ride the EXISTING kernel `rejected` keyset (anchored: prose:packet ch7-P1 emission contract) |
v3/implementation/packets/ch11-p1-l1-authority.md:761:      { "id": "D3", "class": "anchored", "refs": ["prose:packet ch7-P1"] },
v3/implementation/packets/ch11-p2c-round-alignment.md:362:| D3 | The admitted-form completeness (D2's "COMPLETE") is an ADMISSION POSTCONDITION, not a type claim — the optional field keeps every existing literal compiling (dimension 7); `admitTemplate` is the only producer of complete maps, and the kernel reads the admitted form by the store port contract (P2a C20/D6 — only admitted templates reach it). DERIVATION: the P2a effective-config precedent (materialized into the same field shape, guaranteed by the single producer, not by the type) (derived: prose:packet ch11-p2a A1, contract:ch11-gate-format#C39) |
v3/implementation/packets/ch11-p2c-round-alignment.md:377:| K1 | The commit-path predicate reads the CURRENT step's flag for the committed event type — `step.advancesRound?.[envelope.type] === true` on the pinned ADMITTED template — replacing `target === template.start` VERBATIM at the one existing site (`kernel.ts`, the `newRound` expression); the retired comparison survives NOWHERE in round logic (a grep lane: no `template.start` comparison outside navigation/terminal semantics). The `=== true` form is explicit-flag consumption, never a defensive default — an admitted template's map is complete (D3), and the optional chain exists only because the TYPE is shared with the raw form (anchored: contract:ch11-gate-format#C39, prose:l2-pseudocode/HANDLE) |
v3/implementation/packets/ch11-p2c-round-alignment.md:430:   unadmitted template and hide D3's postcondition from the
v3/implementation/packets/ch11-p2c-round-alignment.md:539:      { "id": "D3", "class": "derived", "refs": ["prose:packet ch11-p2a", "contract:ch11-gate-format#C39"] },
v3/implementation/contracts/ch9-runner-contract.md:215:{"realized_map": {"C1": "ch9-P1 W1/W3/F3/K1/K2/T1/T2 (the second `RUNTIME_CONTEXT_FAILED` completion on the provider seam, built SM deliver-all seam, the FAILED admission rungs, testkit failure script, the reason/detail types + R-ABSENCE sweep) + ch9-P2 PB1 (the worktree provider fires it) — kernel/lifecycle.ts, kernel/kernel.ts, ports/runtimeContextProvider.ts, domain/instance.ts, domain/outcome.ts, testkit/scriptedRuntimeContextProvider.ts; lifecycle.test.ts / ingress.test.ts / l0eTrace.test.ts", "C2": "ch9-P1 F1/F2/F4 (FAILED admission runs READY's ordered rungs — terminal-sink then correlation — and an admitted completion commits the existing kernel FAIL atomically, marker kept diagnostic) — kernel/lifecycle.ts, kernel/kernel.ts; lifecycle.test.ts fail-admission lanes", "C3": "ch9-P1 G1/G2/D1/T1 (the closed two-member provisioning-reason enum `sys:provision_rejected`/`sys:provision_failed`, validated at the transport gate) + ch9-P2 S2/S3/J2/U2 (the provider MINTS the two reasons on config-reject vs git-mechanics-fail) — domain/outcome.ts, ports/runtimeContextProvider.ts, providers/worktreeProvider.ts; lifecycle.test.ts / worktreeProvider.test.ts; ADR-018", "C4": "ch9-P1 G3/G4 (`detail` is optional untrusted free text — diagnostic/audit only, never parsed, never matched, never into `failure_reason`) + ch9-P2 DG2/PB3 (the provider's stderr-tail detail rides the diag surface) + ch9-P3B DG1 (the errand-side diagnostic mirror) — kernel/lifecycle.ts, providers/worktreeProvider.ts, diag/sqliteDiagStore.ts; lifecycle.test.ts / worktreeProvider.test.ts", "C5": "ch9-P1 W1/W3/G2/G3/SM1/SM2 (the FAILED completion rides READY's transport rules PLUS the reason-domain membership gate; ordered-after-commit hold/release identical, unknown token = transport-gate integrity throw) — kernel/lifecycle.ts, ports/runtimeContextProvider.ts; lifecycle.test.ts SM hold/release lanes / l0eTrace.test.ts", "C6": "ch9-P1 X1 (the failure→FAIL channel realized BEFORE registration — the ordering that discharges the ch12-C15 D5 gate) + ch9-P2 R1/R2/J1/T1 (the production `ProviderRegistry` gains `pairflow.worktree`) — providers/worktreeProvider.ts, providers/index.ts, kernel/kernel.ts; worktreeProvider.test.ts / worktreeJourney.test.ts", "C7": "ch9-P2 M1/S1/S2/S3 (the `pairflow.worktree` spec grammar — closed `config` keyset `repo`/`base`/`dir`, the sibling `dir` default, config evaluated AT PROVISION with unknown-key/missing-repo/non-repo → `FAILED(sys:provision_rejected)`) — providers/worktreeProvider.ts; worktreeProvider.test.ts config-grammar lanes", "C8": "ch9-P2 N1/N2/N3/N4 (composite worktree identity — directory + branch keyed by BOTH ids through the sanitizing injective host-safe `enc`, reserved `--`/`/` delimiters, loud-degrade on over-length, orphan persistence) — providers/worktreeProvider.ts, runner/enc.ts; worktreeProvider.test.ts / enc.test.ts", "C9": "ch9-P2 M1/M2/M3/M4 (provisioning mechanics — new branch at `base`/committish, dirty host does not block, provision-from-linked-worktree works, explicit workdir never touching the host index/tree) — providers/worktreeProvider.ts; worktreeProvider.test.ts mechanics lanes", "C10": "ch9-P2 M1/RP1/RP2/RP3 (the opaque kernel ref `{kind,locator:{path,branch,repo,base_commit}}` and the field-omission actor projection `{kind,path,branch}`) + ch9-P4B DT1 (the floor detail's runtime-context projection summary consumes it) — providers/worktreeProvider.ts, cli/runtime.ts; worktreeProvider.test.ts projection lanes", "C11": "ch9-P1 W2 (the CHANNEL half — the unconditional-detach obligation as port contract, the ch12-C18 pre-commit breach lane reserved for programming errors) + ch9-P2 PB1 (the worktree provider's own failure ROUTING onto the channel) — ports/runtimeContextProvider.ts, providers/worktreeProvider.ts; lifecycle.test.ts / worktreeProvider.test.ts", "C12": "ch9-P3A ES1/ES5/ES6/L6 (the runner-owned host-local SQLite errand store — WAL, `BEGIN IMMEDIATE`, physically separate from the kernel and diag stores, kernel state written ONLY through ingress) — runner/errandStore.ts, runner/index.ts; errandStore.test.ts; ADR-016", "C13": "ch9-P3A D1/D2/D3/D4/D6/D7/ES2/K4 (the delivery-errand ledger — one row per committed dispatch keyed `(instance_id, context_packet_id)`, `context_packet_id = <instance_id>@v<expected_version>`, INSERT-or-ignore on discovery, polling the committed transcript through the read seam) + ch9-P3B EM2/H3 (the adapter's dispatch consumption) + ch9-P4B RR4 (the respawn verb reads the ledger row) — runner/errandStore.ts, runner/deliveryLoop.ts; errandStore.test.ts / deliveryLoop.test.ts", "C14": "ch9-P3A L1/L2/L3/L4/L5/L6/L7/D3/D5/D7/B5/ES2/K4 (the errand lifecycle enum + transitions, the `unconfirmed` non-terminal single errand-level exit, the `mooted` run-level sink, disposition precedence `confirmed > mooted > (unconfirmed|exhausted)`, the stale-claim reclaim edge clearing the active-attempt marker) + ch9-P3B CB1 (the confirmation-vs-budget precedence at attempt conclusion) + ch9-P4A TX3 (the respawn edge under frozen budget) — runner/errandStore.ts, runner/deliveryLoop.ts; errandStore.test.ts / deliveryLoop.test.ts / ctA2.test.ts; ADR-016", "C15": "ch9-P3A CF1/CF2/CF3/CF4/B3/ES2 (CONFIRMATION = committed kernel evidence never self-report — the committed-row check independent of attempt liveness, `Duplicate` → `confirmed`, the total/disjoint exit-0 ingress-outcome set under C14 precedence, `unconfirmed` = exit-0-without-committed-evidence-on-a-still-active-run) + ch9-P3B CL1/EM3 (the adapter classifies the exit-0 lane from the emitted op) — runner/errandStore.ts, runner/deliveryLoop.ts, runner/actorAdapter.ts; errandStore.test.ts / ctA2.test.ts / actorAdapter.test.ts; realizes CT-A2-CONFIRM; ADR-016", "C16": "ch9-P3A B1/B2/B3/B4/B5/B6/CF3/ES2/ES3/ES4/K2/K3/K4 (the DURABLE retry budget — decrement-on-attempt-start folded into the attempt-start transaction that mints the fresh `attempt_id` + records session name + active-attempt marker BEFORE the spawn, the CAS-on-active-marker demotion of stale negatives, budget-exhaust → `exhausted` only after the full C14 precedence + read-time re-check) + ch9-P3B CL1/CL2/RS4 (the adapter's own-timeout/foreign-kill attempt consumption) + ch9-P4A TX3 (the unbudgeted respawn instance of the transaction) + ch9-P4B RR4 (the respawn verb) — runner/errandStore.ts, runner/deliveryLoop.ts, runner/actorAdapter.ts; errandStore.test.ts / ctA2.test.ts; realizes CT-A2-RETRY-DURABLE + CT-A2-CRASH; ADR-016", "C17": "ch9-P3B H2/H4 (the actor adapter's delivery effect — the dispatched ContextPacket materialized as canonical JSON at the ATTEMPT-SCOPED handoff dir `<cwd>/.pairflow/<enc(attempt_id)>/packet.json`, the one path variable covering the worktree lane and the per-run `default_cwd` subdirectory lane) — runner/actorAdapter.ts, runner/enc.ts; actorAdapter.test.ts handoff lanes", "C18": "ch9-P3B AV1 (the adapter maps `effective_agent_config` to a spawnable argv through the adapter-owned composition-injected template mapping; an unresolvable mapping is a spawn-infra failure lane, `*_refs` left uninterpreted) — runner/actorAdapter.ts; actorAdapter.test.ts argv-mapping lanes", "C19": "ch9-P3B SD3 (the ONE spawn discipline for the actor consumer — explicit `cwd`, env allowlist, timeout SIGTERM + bounded-grace SIGKILL escalation, captured stdio, full env replacement) + ch9-P4A GR1/TX2/TX5 (the process-gate consumer shares the identical discipline) — runner/spawn.ts, runner/spawnChannel.ts, runner/actorAdapter.ts; spawn.test.ts / spawnChannel.test.ts; ADR-017", "C20": "ch9-P3B EM1/EM2/H4 (actor-emit capture — after exit 0 the adapter reads `<cwd>/.pairflow/<enc(attempt_id)>/emit.json`, derives the content-addressed `op_id` via emit-lib on the actor's behalf, submits through normal ingress; no readable emit.json = the C15 `unconfirmed` lane) — runner/actorAdapter.ts, emit/opId.ts, emit/index.ts; actorAdapter.test.ts / opId.test.ts", "C21": "ch9-P0 S3 (the kernel-side `sys:` reason tokens appear in the classification output, never minted runner-side) + ch9-P3A B3 (the delivery-path `runner_error` KIND consumption) + ch9-P3B CL1/CL2 (the adapter's faithful outcome derivation) + ch9-P4A GR1 (the real `ProcessGateRunner` — faithful `ProcessResult`, the spawn side NEVER classifies, TOTAL kind production over child terminations, the MEASUREMENT half persisting the ch11-C26 evidence record before `run()` returns) + ch9-P4B CW1 (the composition wiring) — runner/processGateRunner.ts, runner/spawn.ts, kernel/processGate.ts, gates/process.ts; processGateRunner.test.ts / actorAdapterClassify.test.ts", "C22": "ch9-P1 G1 (the uniform `sys:` convention instantiated in the provisioning-reason domain — every system-minted token carries `sys:`, authored tokens can never contain `:`, disjointness by construction) + ch9-P2 U2 (the convention held across the worktree provider's reason mints) — domain/outcome.ts, providers/worktreeProvider.ts; lifecycle.test.ts / worktreeProvider.test.ts; ADR-018", "C23": "ch9-P3A B1/ES2 (the attempt-start transaction records the live attempt's session name BEFORE the spawn — the ledger, not the opaque ref, is attach's resolution source) + ch9-P3B CL1/H4/RS1/RS2/RS3/RS4 (THE EXIT-RESULT SEAM on the direct-spawn path — the thin result wrapper atomically writing `result.json` with the echoed `attempt_id`, the adapter deriving the C15/C16 lane after session death, the fail-closed missing/unparseable/foreign-`attempt_id` lanes) + ch9-P4A TX2/TX4/TX5/TX6/TX7 (the tmux wrap preserving the result seam — the ephemeral per-activation session `pairflow-<enc(instance_id)>--<enc(attempt_id)>`, no remain-on-exit, liveness = `has-session`) + ch9-P4B CW3 (the composition wiring) — runner/actorAdapter.ts, runner/attemptWrapper.mjs, runner/tmuxChannel.ts, runner/enc.ts; actorAdapter.test.ts / tmuxChannel.test.ts / ctBRealRunner.test.ts", "C24": "ch9-P4B AT1 (the per-runtime-context attach verb — `observe` = `tmux attach -r`, `takeover` = writable, resolving `instance_id → the errand's live attempt's recorded session name` via a runner-ledger read and EXECing tmux; missing session = a clean `not running` CLI error; default `observe`) — cli/runnerVerbs.ts, cli/runtime.ts; runnerCli.test.ts attach lanes; ADR-017", "C25": "ch9-P3A L5 (the `runner respawn` errand exit under the frozen-budget rules) + ch9-P4B DT1/J1/RR4 (the CLI surface — `runner run` foreground composition, `attach <instance> [--takeover]`, `runner respawn <instance>`, the ch6 JSON contract + exit lanes 0/2/3, the floor instance-detail additions: projection summary, errand state + remaining budget, attach availability) — cli/runnerVerbs.ts, cli/runtime.ts, cli/main.ts; runnerCli.test.ts / runnerJourney.test.ts / cli.test.ts", "C26": "ch9-P1 G4 (provisioning-completion events) + ch9-P2 DG1/DG2/DG3/DG4 (provisioning + provider diagnostics) + ch9-P3A DG1/DG2/DG3/ES5 (every errand state transition emits a structured diagnostic event) + ch9-P3B DG1/DG2 (spawn/emit outcomes) + ch9-P4A DG3 (spawn-outcome events) — runner-plane observability rides the EXISTING ch7 diagnostic channel (best-effort, fail-open, non-authoritative); the errand LEDGER is the authoritative current state, the diag stream best-effort history; no new observability machinery minted — diag/sqliteDiagStore.ts, ports/diagnostics.ts, runner/deliveryLoop.ts; sqliteDiagStore.test.ts", "C27": "ACT-REALIZED — the ch11-C31 REOPEN CARRIER: the `ch11-gate-format` `sys:` rename reopen rode THIS draft's 2026-07-23 ratification act (one GO, two named acts) under the template-§4 realized-reopen choreography (commit 1 = row edits + status `reopened` + map lifted; commit 2 = re-ratification block + reopen-record prose + map RESTORED-AS-UPDATED + the ch11 Close-metrics reopenings 2 → 3 dated update), resolving ch11-C10/C11/C17/C25/C31 + C26's classification reference to the `sys:` forms and ch11-C31's disjointness claim to the POSITIONAL narrowing (ADR-018, the ratifier's option-A) — with the CODE realization delegated to ch9-P0 S3 (the kernel classification→`sys:`-reason mapping, the `ProcessResult.kind` domain left bare); ADR-016/ADR-017/ADR-018 born `proposed` with the content commit and flipped `accepted` by the same act — gates/process.ts, gates/threshold.ts, gates/previousReviewerVerdict.ts, kernel/processGate.ts; process.test.ts / l2aTrace.test.ts"}}
v3/implementation/ch13-rederivation-arm/p3-aftermath/arm-round4-b1.md:31:| `v3/src/definition/schema/normalizer.ts` | 140 |
v3/implementation/ch13-rederivation-plan.md:53:P11 build pattern: selftest-red per claim, arm-obliged because it is
v3/implementation/contracts/ch11-gate-format-contract.md:253:"C3": "ch11-P2a A4/D3 — definition/admit.ts nonempty-list admission + authored pipeline order; admit.test.ts (+ ch11-P2b K1 ordered first-block-wins eval, kernel/kernel.ts)",
v3/implementation/contracts/ch11-gate-format-contract.md:289:"C39": "ch11-P2c A1/K1/D2 + D3 (admitted-form completeness as an admission postcondition) + T1 (the checkRoundReconstruction checker lane + the AdmittedTemplate harness narrowing) + ch11-P4 F8 (the file-grain membership expansion) — definition/admit.ts advancesRound expansion + kernel consumes flags only (replaces target===start heuristic) + testkit/storeCheckers.ts reconstructable checker; admit.test.ts / kernel.test.ts",
v3/implementation/contracts/ch13-context-block-v2-contract.md:19:contract-draft template's schema lock). A structural rule visible in
v3/implementation/contracts/ch13-context-block-v2-contract.md:106:| C13 | The admitted-form surfaces — DERIVED, named measurer: the P5 build's acceptance family plus the parity gate. On admission success the admitted template carries: the catalog record (always present — the declared default covers absence; a present catalog arrives validated, C1 having refused every non-map form); and normalized ref-list fields FOR EVERY ref position (empty list where nothing was authored), produced by the normalizer (ADR-019 D3 — derivation is never validation), which requires a NEW normalizer hook: that hook is D7 format growth landing at P5's own act, never silently. REALIZATION DUTY, stated so P5 cannot forget it: the effective-config hook's carry list must grow `contextBlockRefs`, or the admitted binding drops the authored refs at the rebuild — the declared carry list is the admitted binding's whole keyset. Rendering consumes these admitted surfaces and nothing rawer — no re-validation at dispatch. The raw config maps retain the authored key untouched for the ch12 cascade to read (C5). No authored keyset widens: admission output owns the normalized positions exclusively, and an author writing one meets the standing unknown-key refusal. |
v3/implementation/flip-claims.md:211:  parked proposals batch-ratified at approve (D3); watchpoint STATUS
v3/implementation/flip-claims.md:504:  autonomy envelope IN FULL — the D3 principle sentence ("the loop
v3/implementation/packets/ch5-p2-invariant-disposition-map.md:91:their slices). `--selftest` gains one red fixture per new dimension
v3/implementation/packets/ch5-p2-invariant-disposition-map.md:151:- `--selftest` green with the new red dimensions (1–5 above) and the
v3/implementation/packets/ch9-p3b-actor-adapter.md:361:| P6i | Node timer-delay collapse (arm-run probe, 2026-07-24) | `setTimeout(NaN)`, `setTimeout(Infinity)`, and `setTimeout(2147483648)` each fired at ~1 ms — T2's knob-validation basis (an unvalidated margin silently defeats SD3's two-tier arithmetic) |
v3/implementation/packets/ch9-p3b-actor-adapter.md:367:fire — SD3's two-tier margin keeps the normal path orphan-free
v3/implementation/packets/ch9-p3b-actor-adapter.md:391:pinned pass-through — under SD3's escalation), so the full host
v3/implementation/packets/ch9-p3b-actor-adapter.md:450:| SD3 | Escalation ownership on the WRAPPED lane: the ACTOR child's SIGTERM→grace→SIGKILL escalation lives IN the wrapper (RS1 — TERM forwarded on receipt, wrapper-armed grace KILL; P6c/P6f), because a KILL delivered to the wrapper can never be forwarded (P6e). The seam's own backstop on the wrapper is scheduled PROVABLY LATER: the adapter passes the seam `graceMs + backstopMarginMs` (T2's margin knob, default 5 000 ms) as the wrapper-spawn grace while the wrapper's own actor-grace is `graceMs` — so on the NORMAL TERM-ignoring-actor path the inner KILL and the result write complete BEFORE the outer backstop fires (C19's "a SIGTERM-ignoring child is killed" holds through the wrapper, no orphan). The backstop ORPHANS the actor ONLY on a genuinely WEDGED wrapper — the narrowed residual, the C19 uninterruptible-I/O class's two-process analog (flag F2; the wrapper is repo-shipped, trivially small, own-tested; teardown/process-health is the named Absent; ADR-017's trusted-local-host stance). DERIVATION: C19's escalation contract composed over the two-process spawn shape the result seam requires; probes P6c/P6e/P6f are the basis; the two-tier deadline is timer arithmetic, no new substrate. |
v3/implementation/packets/ch9-p3b-actor-adapter.md:457:| H2 | The attempt handoff: the adapter creates `<cwd>/.pairflow/<enc(attempt_id)>/` and writes `packet.json` = the dispatched `ContextPacket` in the emit-lib CANONICAL serialization (sorted keys — ONE canonicalization authority in the tree: the emit-lib's `canonicalize` serializer becomes a PUBLIC export in this change — a new name on the existing implementation, internals untouched, selftest riding `emit/opId.test.ts`), via the same tmp+rename atomic write discipline as the result file; the packet value is canonicalizable BY CONSTRUCTION (ingress-admitted `handoff`, template strings, the admission-bound `effectiveAgentConfig`, the PR4-gated projection or the `"none"` literal; construction discipline: optional fields are OMITTED, never set to `undefined` — `canonicalize` rejects a present-but-undefined key) — a canonicalization throw is therefore UNREACHABLE and undriven; defensively, an uncaught throw is consumed by the loop's K2 catch as a bounded `spawn_infra` attempt failure (EM3's principle — never a loop crash; grid row). DERIVATION: C17 fixes the path and "canonical JSON"; the serializer choice (emit-lib) and the atomic write are representation under it. |
v3/implementation/packets/ch9-p3b-actor-adapter.md:473:| RS1 | The wrapper asset (`runner/attemptWrapper.mjs`, NEW — a plain `.mjs` spawned as `process.execPath <wrapperPath> …`, path resolved from the adapter module via `import.meta.url`; no TS loader dependency): spawns the mapped actor argv as its child WITH ITS OWN already-confined environment — ZERO additions, zero widening (the transitive-confinement pin: the seam replaced the wrapper's env, the wrapper passes exactly that through) — its OWN cwd (the seam set the wrapper's cwd to C17's value; the actor inherits exactly it) and pass-through stdio; forwards SIGTERM on receipt and arms its OWN bounded-grace SIGKILL on the child (SD3; P6c/P6f — the grace value arrives through the wrapper's own argv, beside the result path and the echoed attempt id); and on the child's conclusion ATOMICALLY writes `result.json` into the attempt directory (tmp + rename, same directory — P6b/P6g) with the CLOSED keyset `{ attemptId (echoed from its argv), exitCode: number \| null, signal: string \| null, termForwarded: boolean }`, then exits 0. The actor's stdout/stderr pass through the wrapper into the seam's capture — diagnostic-only for actors (C23: the load-bearing handoff is file-based; DG1's detail tail). DERIVATION: C23 fixes the seam (atomic write, echoed attempt id, record exit-or-signal); the asset form, keyset spelling, and stdio pass-through are representation under it (flag F1's module-home element covers the asset). |
v3/implementation/packets/ch9-p3b-actor-adapter.md:482:| CL1 | The TOTAL fail-closed precedence — every attempt execution that RETURNS concludes to EXACTLY ONE K1 member (a rejecting execution is EM3's K2 lane), decided in this ORDER (each row fires only when no earlier row did; `timedOut` is SD1's seam-carried own-timer flag): (1) the seam reports `infra` (wrapper spawn setup/ENOENT) → `infra_failure(spawn_infra)`; (2) the wrapper concluded by SIGNAL (`exit` with `code: null`) → `timedOut` ? `infra_failure(own_timeout)` (our escalation killed it; the result file — normally absent, P6e — is not consulted) : `infra_failure(foreign_kill)` (C21's class; the orphan residual is SD3/F2's); (3) the wrapper exited NONZERO → `timedOut` ? `infra_failure(own_timeout)` (a wrapper misbehaving inside our kill window is our timeout's fallout — fail-closed) : `infra_failure(spawn_infra)` (wrapper-integrity — the wrapper's contract is exit 0 after the write); (4) the wrapper exited 0 → read `result.json` under RS2/RS3: invalid/missing/foreign → `timedOut` ? `infra_failure(own_timeout)` : `infra_failure(spawn_infra)`; else the RECORDED ACTOR conclusion decides — actor `signal` present → (`timedOut` AND `termForwarded`) ? `infra_failure(own_timeout)` (C23's letter: the result records the runner's OWN forwarded kill — the NORMAL own-timeout path, P6c's shape) : `infra_failure(foreign_kill)` (C21's class — a foreign hand killed the work, directly (P6d) or via a forwarded foreign TERM; `termForwarded` discriminates ONLY under a fired own timer, and the residual grey case — a foreign kill landing inside our fired-timer window — resolves own_timeout, a bounded same-cost ambiguity between two attempt-consuming classes); actor `exitCode` nonzero → `infra_failure(nonzero_exit)` (a nonzero exit is the actor's own conclusion, never a kill record — even under a fired timer); actor `exitCode` 0 → the EM lanes (`submitted` or `no_output`) — COMPLETED WORK IS NEVER RECLASSIFIED by a late timer (the P6h delayed-exit race). DERIVATION: C15/C16/C21/C23 composed over the two-process observation surface with SD1's `timedOut` attribution flag (probes P6c/P6d/P6e/P6h); the order is forced — wrapper integrity before actor record (an untrustworthy wrapper cannot vouch for its record), attribution before class (C21's "attributable" letter). |
v3/implementation/packets/ch9-p3b-actor-adapter.md:505:| T2 | (NEW-DECISION — flag F8) The adapter's deps contract: `createActorAdapter(deps, options)` — `deps = { ingress: Pick<Ingress, "submit">, argvMapper (AV1), diag }`; `options = { defaultCwd: string (REQUIRED — C17's composition-configured value, no default), envAllowlist (default `{ PATH: <host PATH> }` — P6a-viable; composition widens it for real actor CLIs), timeoutMs (default 1 800 000 — 30 min, sized to real LLM-actor runs, ratifier-decided; the PAIRING RULE binds the COMPOSITION: any composition binding the real adapter sets the loop's `leaseMs` a margin ABOVE its effective timeout — C14's RATIFIED 15-min loop default stands untouched (the lease is a composition-configured knob by C14's own words; the CB1 test composition sets 2 700 000, and the operator-facing `runner run` composition carries and VALIDATES the pair as a NAMED P4 obligation); flag F8), graceMs (default 10 000 — C19's ratified default; the wrapper's actor-grace), backstopMarginMs (default 5 000 — SD3's two-tier rule: the seam's wrapper-spawn grace = graceMs + this margin, provably later than the inner escalation; EVERY consumer factory of the seam VALIDATES its timer knobs FAIL-CLOSED at construction through the seam's SHARED validator (SD1's export; R-NUMERIC-LADDER at the config boundary): `timeoutMs`, `graceMs`, and `backstopMarginMs` each a FINITE SAFE INTEGER at or above its NAMED floor (`timeoutMs` ≥ 1 000, `graceMs` ≥ 1 000, `backstopMarginMs` ≥ 1 000), and EVERY derived timer value — `graceMs + backstopMarginMs` included — below Node's 2³¹ timer bound (a NaN, Infinity, or overflowing delay silently COLLAPSES to 1 ms on the substrate, P6i — the probed hazard that would let the outer backstop outrun the inner escalation); any violation is a construction-time config-integrity throw at BOTH current consumers (`createActorAdapter` AND the worktree provider factory — SD2's unchanged-pin is thereby SCOPED to valid configurations), so SD3's orphan-freedom guarantee rests on validated arithmetic, never on trusted luck) }`. All knobs composition-configured per C19; the injected-seam culture (CliDeps/K4 precedent). |
v3/implementation/packets/ch9-p3b-actor-adapter.md:554:| Escalation ownership + orphan residual | SD3 | RS1's forward-and-grace clause, T2's two-tier margin clause, CL1 row 2's parenthetical, flag F2, probe P6e's note |
v3/implementation/packets/ch9-p3b-actor-adapter.md:656:    selftest (sorted keys, equality with the digest path's bytes).
v3/implementation/packets/ch9-p3b-actor-adapter.md:732:- **F2 — the wrapper-KILL orphan residual (SD3/CL1 row 2).** A
v3/implementation/packets/ch9-p3b-actor-adapter.md:734:  WEDGED wrapper (the SD3 two-tier deadline keeps the normal
v3/implementation/packets/ch9-p3b-actor-adapter.md:881:    grep-driven)}. Membership: SD1–SD3 (owner: this packet; driven
v3/implementation/packets/ch9-p3b-actor-adapter.md:1032:selftest), `sqliteDiagStore.test.ts` +25 (DG2 round-trips + both-direction
v3/implementation/packets/ch9-p3b-actor-adapter.md:1133:   SD3/two-tier escalation, the 2 000 ms drain-deadline-with-live-orphan,
v3/implementation/packets/ch9-p3b-actor-adapter.md:1257:      { "id": "SD3", "class": "derived", "refs": ["contract:ch9-runner#C19", "prose:probes P6c/P6e/P6f (in-session, ch9p3b-probes)"] },
v3/implementation/packets/ch12-p3-provider-contract.md:745:| T2 | `RuntimeContextProjection` is a BRANDED OPAQUE type (the codebase `AdmittedTemplate` idiom, `domain/template.ts:140` — `declare const projectionBrand: unique symbol; export type RuntimeContextProjection = { readonly [projectionBrand]: true }`): a NOMINAL type produced ONLY by `projectForActor` (its impl casts its return), carrying NO value-domain constraint at runtime — the unique-symbol brand is a compile-time TYPE EXPRESSION, ERASED at runtime (the `AdmittedTemplate` "not a runtime mechanism" precedent), so any canonical-JSON-safe value the provider returns is admissible (NON-narrowing; C15's value-safety is the RUNTIME `isCanonicalizable` gate at the `projectForActor` RETURN, PR4/E2, never the static type). The brand makes the type DISTINCT from `RuntimeContextRef` (`{kind, locator}`, UNBRANDED), which is exactly what GENUINELY type-enforces `projection-never-the-ref` (D3, the disposition-map `type/schema`): a raw ref is NOT assignable to `RuntimeContextProjection`, so `packet.runtimeContext ← ref` is a COMPILE ERROR — a STRUCTURAL type (`Record<string, unknown>` OR `unknown`) does NOT catch this (a `{kind, locator}` ref is assignable to both), so the brand is what makes the ratified `type/schema` disposition real. The type lands in `domain/` and is exported; `RuntimeContextRef` (`domain/instance.ts`, `{kind, locator}`) is witnessed. `ContextPacket` gains `runtimeContext: RuntimeContextProjection \| "none"` (non-optional — E1): the value is the branded projection OR the literal `"none"`, NEVER the raw ref (compile-enforced) and never absent (derived: contract:ch12-runtime-core#C17, #C15 + prose:l0e-pseudocode/dispatch_intent + prose:invariant-disposition-map l0e/projection-never-the-ref (type/schema) + prose:AdmittedTemplate brand idiom (`domain/template.ts`) — DERIVATION: the disposition map RATIFIES `projection-never-the-ref` as `type/schema`, which a structural projection type CANNOT deliver (a `{kind, locator}` ref is assignable to both `Record<string, unknown>` and `unknown`), so the FAITHFUL realization is the codebase's declared-unique-symbol brand — a nominal opaque type, NON-narrowing on the value domain (brand erased at runtime, value-safety at the PR4 `isCanonicalizable` gate) yet compile-time DISTINCT from `RuntimeContextRef`; the `"none"` literal is the model's explicit context-free value) |
v3/implementation/packets/ch12-p3-provider-contract.md:755:| D3 | Invariant witnesses realized per the ch-5 disposition map: `context-is-optional` / `requirement-is-template-owned` / `kind-boundary-only` / `projection-never-the-ref` (type/schema — the `RuntimeContextRequirement` union renders `context-is-optional`/`requirement-is-template-owned` at the type; the BRANDED `RuntimeContextProjection` (T2, distinct from the unbranded `RuntimeContextRef`) makes `packet.runtimeContext ← ref` a COMPILE ERROR, genuinely rendering `projection-never-the-ref` at the type; the kind-guard type renders `kind-boundary-only`), `provider-resolved-at-start` (test — S2's pre-commit reject lane), `registry-stable-for-the-run` (test — E2's re-resolve invariant throw lane) (anchored: prose:invariant-disposition-map l0e rows + prose:ledger §2 l0e) |
v3/implementation/packets/ch12-p3-provider-contract.md:872:    `domain/template.ts:140` `admittedBrand` idiom), so a raw
v3/implementation/packets/ch12-p3-provider-contract.md:1250:      { "id": "D3", "class": "anchored", "refs": ["prose:invariant-disposition-map l0e rows", "prose:ledger §2 l0e"] }
v3/implementation/ch13-rederivation-arm/p3-aftermath/arm-round5-b2.md:38:| `v3/src/definition/schema/normalizer.ts` | 140 | `30754f4b7aa93199` |
v3/implementation/packets/ch8-p2-md1-migration-activation.md:327:| D3 | Valid → exit 0 with JSON `{valid: true, ref}` on stdout — exactly that keyset; `ref` is the loaded template's `{id, version}` (anchored: contract:ch8-template-format#C31) |
v3/implementation/packets/ch8-p2-md1-migration-activation.md:520:      { "id": "D3", "class": "anchored", "refs": ["contract:ch8-template-format#C31"] },
v3/implementation/packets/ch8-p2-md1-migration-activation.md:574:    rejections; D2's input-gate lanes; D3's exact success keyset;
v3/implementation/packets/ch12-p1a-lifecycle-axis.md:509:| D3 | Registry rows flipping `realized` at P1a: `l0d/KernelStatus`, `l0d/TerminalDisposition`, `l0d/WaitReason`, `l0d/ActivationMode`, `l0d/RuntimeContext`, `l0d/RuntimeContextRef`, `l0d/WorkflowInstance` (the axis fields land on the instance aggregate). STAYING pending: `l0d/Template` (the `activation` key — P1b's admission-default face + P4's format face), `l0d/OperatorIntent`, `l0d/KernelEvent`, `l0d/ActorEnvelope` (the entry classes — P1b's) (anchored: ledger §4 l0d + the domainRegistry level-axis semantics) |
v3/implementation/packets/ch12-p1a-lifecycle-axis.md:704:      { "id": "D3", "class": "anchored", "refs": ["prose:ledger §4 l0d", "prose:domainRegistry level-axis semantics"] },
v3/implementation/packets/ch12-p1a-lifecycle-axis.md:768:  - **The drift/registry family (D1–D3)** — discipline: the
v3/implementation/packets/ch12-p1a-lifecycle-axis.md:775:    D3's realized flips are witnessed by the typecheck (a missing
v3/implementation/packets/ch12-p1a-lifecycle-axis.md:776:    realized witness is a compile error). Membership: D1–D3
v3/implementation/packets/ch12-p1a-lifecycle-axis.md:805:  (D2/D3) keep every drift lane green before AND after — any
v3/implementation/packets/ch5-p1-drift-suite.md:125:### The script cross-check (the three-way lock) + `--selftest`
v3/implementation/packets/ch5-p1-drift-suite.md:136:- **`--selftest` mode (chapter rule 2 — the check is executed, not
v3/implementation/packets/ch5-p1-drift-suite.md:140:  ownership directions. CI runs `--selftest` before the real
v3/implementation/packets/ch5-p1-drift-suite.md:141:  validation (the `v3:coverage` bridge becomes selftest + validation).
v3/implementation/packets/ch5-p1-drift-suite.md:143:  validations into selftest is NOT this packet (recorded, not smuggled).
v3/implementation/packets/ch5-p1-drift-suite.md:195:  (index row); `package.json` (`v3:coverage` bridge gains `--selftest`);
v3/implementation/packets/ch5-p1-drift-suite.md:212:- Script cross-check: `--selftest` green in CI, one red case per
v3/implementation/packets/ch5-p1-drift-suite.md:239:- **Script:** `--selftest` is the STANDING executed negative — 9 red
v3/implementation/packets/ch12-p2-run-profile.md:524:| D3 | Invariant witnesses realized per the ch-5 disposition map: `deterministic-provenance` (test — C4's byte-identical recompute lane), `config-sources-immutable-per-dispatched-step` (type/schema — the pinned template + the `readonly` `runOverrides` snapshot render the sources immutable at the type), `issued-proven-runtime` (review — C5's disposition note; no code asserts proven runtime) (anchored: prose:invariant-disposition-map + prose:ledger §2 l0c) |
v3/implementation/packets/ch12-p2-run-profile.md:858:      { "id": "D3", "class": "anchored", "refs": ["prose:invariant-disposition-map", "prose:ledger §2 l0c"] }
v3/implementation/packets/ch12-p4-format-operator-surface.md:303:   84/140/172/181/183`, `cli/main.ts:308`). NOTHING else: the kernel,
v3/implementation/packets/ch12-p4-format-operator-surface.md:633:  retirements only — `template.ts:84/140/172/181/183`).
v3/implementation/packets/ch12-p4-format-operator-surface.md:688:    325/335/442`, `domain/template.ts:84/140/172/181/183`,
v3/implementation/packets/ch11-p2a-gate-admission.md:473:| D3 | `GatePipeline` — the ordered NONEMPTY list of bindings at one (step, event) point; authored order IS pipeline order (P2b's rung consumes it first-block-wins); realized as the exported ordered-list type; nonemptiness is A4's admission lane, never a type claim (anchored: prose:ledger §4 l2 GatePipeline, contract:ch11-gate-format#C3) |
v3/implementation/packets/ch11-p2a-gate-admission.md:790:      { "id": "D3", "class": "anchored", "refs": ["prose:ledger §4 l2", "contract:ch11-gate-format#C3"] },
v3/implementation/packets/ch9-p3a-delivery-core.md:416:| P5a | `INSERT OR IGNORE`, second insert on the same PK (single handle) | `changes = 0`, silent no-op; the first row's state survives — D3's idempotency mechanism |
v3/implementation/packets/ch9-p3a-delivery-core.md:492:| D1 | Discovery polls the injected READ seam: `listInstances()` filtered to `kernelStatus === "ACTIVE"` — an ACTIVE run has EXACTLY ONE outstanding committed dispatch (the linear-CAS single-outstanding-dispatch guarantee; CREATED/WAITING/TERMINAL runs have none). Reads only — discovery performs NO kernel write and NO kernel-store write of any kind. DERIVATION: C13 fixes poll-the-committed-transcript-through-the-read-seam; the ACTIVE predicate is the l0d macro-state's dispatch semantics (ch12-P1a: ACTIVE ⇔ the actor's turn), the cheapest committed read exposing exactly the dispatch set; for each NEW key the creation step performs its one per-instance committed read (D3's acquisition basis, D6's class). ANCHOR-LITERAL NOTE (the reclassification challenge, disposed `declined`): C13's "POLLING the committed transcript through the read seam" fixes the READ-ONLY, COMMITTED-STATE character and the seam — not a row-cursor mechanism; the instance list IS the store's committed-transcript projection (every instance field derives from committed rows, ch12-P1a's two-axis truth), and under the single-outstanding-dispatch guarantee the ACTIVE snapshot is EXACTLY the DELIVERABLE committed dispatch set. TOTALITY (flag F9 — a resolved STOP, user-elected): the poll's discovery set is the ACTIVE instances (live discovery — rows born `pending`, `discovery = live`) PLUS the not-yet-reconciled TERMINAL instances (the D7 one-time reconciliation sweep), so C13's one-row-per-committed-dispatch holds as an AUDITABLE EVENTUAL-TOTALITY invariant at its decidable boundary: a live run carries a row for its current dispatch; a reconciled terminal run carries one row per consumed dispatch PLUS one for its final outstanding dispatch exactly when the terminal aggregate proves one existed (D7's TOTAL decision rule). WATCH (successor-row): the predicate rests on WAITING carrying no dispatch-bearing wait kind (true at ch9 — `kickoff_pending` only); a future dispatch-bearing WAITING kind revisits it. |
v3/implementation/packets/ch9-p3a-delivery-core.md:494:| D3 | Idempotent creation: INSERT-or-ignore on the C13 key (probe P5a: a second insert on an existing key is a silent zero-change no-op — the existing row's state survives; P5b: a sibling handle's post-commit insert is the same no-op); the created row is `state = pending`, `remaining_budget` = the configured attempts-per-errand (default 3), no claim, no active attempt, `actor_id` recorded from the committed dispatch's derivation — acquired by the creation step's per-new-key committed read (the dispatched actor = the instance's current-step role binding, the same value CF1 later matches on the confirming envelope). Any number of polls and workers create at most one row per key. |
v3/implementation/packets/ch9-p3a-delivery-core.md:497:| D6 | Committed-state reads beyond the poll: the creation step's per-new-key read (D3) and the attempt-time intent re-derivation — the loop re-derives the `DispatchIntent` from COMMITTED state via the kernel-exported `deriveDispatchIntent` (loadInstance + the pinned template from the injected definition store + the provider registry; `handoff` = the last committed transition's envelope payload, absent at start). A derivation INTEGRITY throw — the LIST, per the transitive call-graph rule (helpers and injected boundaries carry their own sites): the six in-function sites (undefined step, unbound role, NULL task, non-ready context, provisioned-ref-on-a-no-context-template, unresolvable provider), the `resolveRuntimeContextRequirement` helper's own integrity throw (`domain/template.ts`), the definitions port's own failures (load rejection / not-found), and the injected provider's `projectForActor` gate throws AS BOUND (the worktree provider: foreign-kind ref, malformed locator; the scripted testkit provider adds a canonicality gate — the boundary's throw set is the bound implementation's own, enumerated at build) — propagates FAIL-CLOSED out of the loop pass — a kernel/config error, never converted to errand state (the errand row stays as it was; the lease recovers a died worker). DERIVATION: C13's read-seam discovery + the kernel's established integrity-throw culture (`dispatchIntent.ts`); converting config corruption into errand failure would launder an integrity class into a business state. |
v3/implementation/packets/ch9-p3a-delivery-core.md:538:| K3 | The scripted executor (testkit, NEW member — the sizing gate's counted testkit-contract change): plays a script of `AttemptResult`s and rejections in order, records every `execute` input (intent/attemptId/sessionName) for asserts; ADR-005 culture — selftest included, deterministic, no I/O. |
v3/implementation/packets/ch9-p3a-delivery-core.md:655:    selftest.
v3/implementation/packets/ch9-p3a-delivery-core.md:942:    executor's selftest (script order, input recording,
v3/implementation/packets/ch9-p3a-delivery-core.md:1113:      { "id": "D3", "class": "anchored", "refs": ["contract:ch9-runner#C13", "contract:ch9-runner#C14"] },
v3/implementation/packets/ch13-p0-closed-pipe-sink.md:107:| E4 | **Post-closure behavior.** Once a stream is marked closed the sink writes nothing more to it, and the FIRST write ATTEMPTED after closure throws the internal `OutputClosedError` sentinel (its `message` is a fixed constant that matches no message-discriminating catch in the traversal inventory — measured: the prefix test at `main.ts:553`, inside the catch at `main.ts:546`, is the tree's ONLY message discriminator on the path). ORDERING (why the production reach is narrow): a stream is KNOWN closed only AFTER a failed write on it, and the establishing failure is absorbed (E2) — so a single-document verb resolves before the closure is knowable and NEVER reaches the sentinel in production; only the two multi-write verbs do (dimension 4). The sentinel unwinds through the verb's own cleanup, and if a cleanup THROWS, E3's precedence rule governs (the cleanup error wins, loud). The THREE forms REACHABLE from a multi-write verb all run normally — `withStore` (`common.ts:140-142`, plain `tail`), `withStoreAndDiag` (`common.ts:164-167`, `tail --diag` and `dev inject`), and `dev inject`'s inner `finally { processRunner.close(); }` (`dev/main.ts:294-296`). PROOF SPLIT, stated so no obligation is silently unmet: the first two are DRIVEN (their close is observable through the injected deps — family 3); the third is proven STATICALLY — the close sits in a `finally` wrapping the sink write, and a `finally` cannot be skipped by an exception traversing it, so no test asserts it and none is owed. A build that MOVES that close outside its `finally` breaks the static premise without reddening any lane; the build-close cleanup-form sweep is what catches it (a `finally` block whose classification changes is a red). The CLI plane's other cleanup blocks (`withDiagStore` `common.ts:179-181`; `main.ts:360-362`, `741-743`; `runnerVerbs.ts:458-460`, `515-517`, `604-607`, `780-782`; `dev/main.ts:624-627`) sit behind verbs that write at most ONE document — three of them write NONE (the inventory's zero-document verbs) — and are unreachable by this ordering, named so the build-close sweep has a disposition for every block it finds. TRAVERSAL RULE: every per-verb catch between a sink write and `dispatch` must RETHROW the sentinel unchanged — the measured catch-site inventory (Embedding gates) is the current proof, re-run at build close, and a catch that maps unknown errors into a `CliError` between those points is a red. The sentinel is internal: recognized in `dispatch` BEFORE its generic `CliError` wrap, it never reaches a user-visible surface. REPETITION: a stream may report `error` more than once (two writes issued before the first event lands, or a listener firing again on an already-closed stream); every further report CARRYING `EPIPE` is absorbed without state change, and a report carrying any other code takes E6's lane regardless of the stream's closed mark |
v3/implementation/ch13-rederivation-arm/p3-build/arm-round1.md:19:| `v3/src/definition/schema/normalizer.ts` | 140 | `30754f4b7aa93199` |
v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:89:| F5 | impossible calendar dates passed D3/D8.3 | `2026-13-40`, non-leap `2026-02-29`, `0000-00-00` all green |
v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:91:| F10 | the selftest's red-dim count was printed, not pinned | deleting a `assert_red` call: 131 → 130 and still exit 0 |
v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:122:selftest              135 red dims exercised, 0 failure(s)          exit 0
v3/implementation/ch13-rederivation-arm/p3-design/design-review-charter.md:47:| `v3/src/definition/schema/engine.ts` | 1183 | `c60b692db5301402` |
v3/implementation/ch13-rederivation-arm/p3-design/design-review-charter.md:48:| `v3/src/definition/schema/normalizer.ts` | 140 | `30754f4b7aa93199` |
v3/implementation/ch13-rederivation-arm/p1/arm-round1.md:61:python3 tools/v3-plan/check_packet.py --selftest
v3/implementation/ch13-rederivation-arm/p1/arm-round1.md:68:`expect_red_superseded` helper in the selftest section, import the
v3/implementation/ch13-rederivation-arm/p1/infra/arm-round1-timeout.md:38:python3 tools/v3-plan/check_packet.py --selftest
v3/implementation/ch13-rederivation-arm/p1/infra/arm-round1-timeout.md:39:  → selftest: 131 red dims exercised, green fixture + reopen choreography
v3/implementation/ch13-rederivation-arm/p1/infra/arm-round1-timeout.md:181:   (selftest, lint) with their output.

exec
/bin/zsh -lc "sed -n '125,170p' tools/v3-plan/check_packet.py; sed -n '655,800p' tools/v3-plan/check_packet.py; sed -n '160,200p' v3/implementation/contract-draft-template.md; sed -n '3460,3515p' tools/v3-plan/check_packet.py; git diff --name-status 20b73f6a 0d58283e; git diff -- tools/v3-plan/check_packet.py 20b73f6a..HEAD" in /Users/felho/dev/pairflow
 succeeded in 0ms:
      the declared literals — is named as the review-side duty, not
      claimed here.

Drafts (v3/implementation/contracts/*.md, README.md excluded):
  D1. contract_draft = {chapter, surface, status}: exact keyset;
      status in draft|ratified|reopened|realized|superseded; filename
      ch<N>-<surface>-contract.md matches chapter/surface.
  D2. C-row registry: table rows whose first cell is C<n> (fenced code
      excluded); unique ids, no leading zeros (ids are exact strings —
      C01 is never C1); ratified-or-later requires >=1 row.
  D3. ratification = {date, arms, commit}: exact keyset; YYYY-MM-DD
      string date naming a REAL CALENDAR DAY (the shape alone admits
      2026-13-40 and a non-leap 2026-02-29 — a mistyped month is the
      ordinary accident); nonempty string-list arms; 7-40 lowercase-hex
      commit. Dates non-decreasing in document order; "latest block" =
      the LAST block in document order. A block MAY additionally carry
      schema = {path, sha256} (the ADR-019 D4 byte lock for
      schema-first contracts): exact inner keyset; path a nonempty
      repo-relative string; sha256 64 lowercase hex — shape-checked on
      every block that carries it.
  D5b. The SCHEMA LOCK (template §3; user-ratified 2026-08-08): when
      the LATEST block carries schema, the working tree file at its
      path must EXIST and its bytes must hash to the recorded sha256 —
      bound at ratified|realized|superseded, suspended ONLY at
      reopened (with D5 and for the same reason). A missing file or a
      differing hash is a LOUD error, never a skip: an edit of the
      declaration file after ratification is an unratified schema
      change until a new act records the new bytes. Threat model:
      accident and sloppiness (an unratified schema edit, a stale or
      mistyped hash, a wrong path) — deliberate concealment stays with
      human diff review and the arm.
  D4. State consistency (decidable from the current bytes alone):
      ratification block(s) present <=> status in {ratified, reopened,
      realized, superseded}; status draft => no blocks.
  D5. Recorded-commit equality (status ratified|realized|superseded):
      the C-row
      lines in the working tree equal the C-row lines at the latest
      block's recorded commit (git show). The recorded sha must
      resolve to a COMMIT object (git cat-file -t): a tree/blob/tag
      would satisfy git show while being no auditable ratification
      point. An out-of-repo draft or an unresolvable recorded commit
      is a LOUD error, never a skip.
      Suspended ONLY at reopened — the two-commit reopen choreography
      (content+status commit, then record commit) stays green at every
      step. At superseded it binds FOREVER: the flip edits no C-row,
      so equality against the latest ratification's commit is the

def c_row_lines(text: str) -> list[str]:
    """The canonical C-row byte surface: fence-stripped table rows whose
    first cell is C<n>, rstripped, in document order."""
    return [line.rstrip() for line in strip_fences(text).splitlines() if C_ROW_RE.match(line)]


def check_ratification_shape(name: str, index: int, block: object, checker: Checker) -> None:
    """D3: strict shape for EVERY ratification block. The OPTIONAL
    schema key (the ADR-019 D4 byte lock) is shape-checked here on
    every block that carries it; its BYTE check (D5b) runs on the
    latest block only, beside the D5 equality check."""
    label = f"{name}: ratification[{index}]"
    if not isinstance(block, dict) or set(block.keys()) - {"schema"} != RATIFICATION_KEYS:
        checker.error(
            f"{label} must be an object with exactly keys {sorted(RATIFICATION_KEYS)}"
            f" (plus the optional schema lock)"
        )
        return
    if "schema" in block:
        schema = block["schema"]
        if not isinstance(schema, dict) or set(schema.keys()) != SCHEMA_LOCK_KEYS:
            checker.error(
                f"{label}.schema must be an object with exactly keys {sorted(SCHEMA_LOCK_KEYS)}"
            )
        else:
            lock_path = schema["path"]
            if (
                not isinstance(lock_path, str)
                or not lock_path.strip()
                or lock_path.startswith("/")
                # a PARENT SEGMENT, not a substring — "schema..fixture.ts"
                # is a legal filename (round-1 F1's false positive)
                or ".." in lock_path.split("/")
            ):
                checker.error(f"{label}.schema.path must be a nonempty repo-relative path")
            if not isinstance(schema["sha256"], str) or not SHA256_RE.match(schema["sha256"]):
                checker.error(f"{label}.schema.sha256 must be a 64-character lowercase-hex sha256")
    if not is_calendar_date(block["date"]):
        checker.error(f"{label}.date must be a YYYY-MM-DD string naming a real calendar day")
    arms = block["arms"]
    if not isinstance(arms, list) or not arms or not all(isinstance(a, str) and a.strip() for a in arms):
        checker.error(f"{label}.arms must be a nonempty list of nonempty strings")
    if not isinstance(block["commit"], str) or not COMMIT_RE.match(block["commit"]):
        checker.error(f"{label}.commit must be a 7-40 lowercase-hex commit sha")


def check_recorded_equality(
    path: Path,
    commit: str,
    current_rows: list[str],
    checker: Checker,
    migration_parent: str | None | object = _PINNED,
) -> None:
    """D5: the working tree's C-rows equal the C-rows at the recorded
    commit. Failures are LOUD — an unverifiable ratification record is
    a broken record, never a skip. Reads through the ADR-015
    legacy-path alias for pre-migration recorded commits."""
    if migration_parent is _PINNED:
        migration_parent = MIGRATION_PARENT
    located = git_root_and_rel(path)
    if located is None:
        checker.error(
            f"{path.name}: not inside a git repository — the recorded-commit "
            f"equality check cannot run on a ratified-or-later draft"
        )
        return
    root, rel = located
    # D3/D5 say COMMIT sha — `git show <sha>:<path>` happily resolves a
    # TREE sha too, but a tree is not an auditable ratification point
    # (no date, author, or history position).
    typ = subprocess.run(
        ["git", "-C", str(root), "cat-file", "-t", commit],
        capture_output=True,
        text=True,
    )
    obj_type = typ.stdout.strip() if typ.returncode == 0 else "unresolvable"
    if obj_type != "commit":
        checker.error(
            f"{path.name}: recorded sha '{commit}' does not resolve to a COMMIT "
            f"(got '{obj_type}') — the ratification record is broken: only a "
            f"commit is an auditable ratification point"
        )
        return
    shown = show_at_commit(root, commit, rel, migration_parent)
    if shown is None:
        checker.error(
            f"{path.name}: recorded commit '{commit}' does not yield a readable "
            f"draft (git show failed) — the ratification record is broken"
        )
        return
    if c_row_lines(shown[1]) != current_rows:
        checker.error(
            f"{path.name}: C-rows differ from the latest ratification's recorded "
            f"commit {commit[:12]} — a post-ratification row edit needs "
            f"re-ratification (the reopen choreography)"
        )


def check_schema_lock(path: Path, schema: object, checker: Checker) -> None:
    """D5b: the working tree's declaration file hashes to the recorded
    sha256. A broken lock is LOUD, never a skip — a missing file or a
    differing hash means the declaration's bytes are not the ratified
    ones. Runs only when the shape (D3) already holds; a malformed
    schema block was reported there and proves nothing here."""
    name = path.name
    if not isinstance(schema, dict) or set(schema.keys()) != SCHEMA_LOCK_KEYS:
        return  # shape already reported by D3
    lock_path, recorded = schema["path"], schema["sha256"]
    if not isinstance(lock_path, str) or not isinstance(recorded, str) or not SHA256_RE.match(recorded):
        return  # shape already reported by D3
    located = git_root_and_rel(path)
    if located is None:
        checker.error(
            f"{name}: not inside a git repository — the schema lock cannot be verified"
        )
        return
    root, _rel = located
    target = root / lock_path
    # round-1 F2: a symlink inside the root pointing OUTSIDE it must never
    # satisfy the lock — the ratified bytes are the draft's own working
    # tree's, so the RESOLVED target must stay under the resolved root.
    if not target.resolve().is_relative_to(root.resolve()):
        checker.error(
            f"{name}: schema lock path '{lock_path}' resolves outside the draft's "
            f"repository — the lock binds the draft's own working tree bytes"
        )
        return
    if not target.is_file():
        checker.error(
            f"{name}: schema lock path '{lock_path}' does not exist in the working "
            f"tree — the ratified declaration file is missing (a broken lock is "
            f"LOUD, never a skip)"
        )
        return
    actual = hashlib.sha256(target.read_bytes()).hexdigest()
    if actual != recorded:
        checker.error(
            f"{name}: schema file '{lock_path}' bytes differ from the recorded "
            f"sha256 ({recorded[:12]}… recorded, {actual[:12]}… in the working "
            f"tree) — an edit of the declaration after ratification is an "
            f"unratified schema change until a new act records the new bytes"
        )


def _resolve_branch(root: Path, branch: str) -> str | None:
  non-decreasing in document order;
  `arms` a nonempty list of nonempty strings (naming the transitional
  cross-model arms that reviewed — README §5.5); `commit` 7–40
  LOWERCASE-hex — shape-checked on EVERY block, while the
  COMMIT-object resolution (`git cat-file -t == commit`; a tree/blob/
  tag is not an auditable ratification point) and the equality check
  run on the LATEST block only (= the last block in document order),
  in `ratified`, `realized` and `superseded`. An unresolvable recorded
  commit or an out-of-repo draft is a LOUD error, never a skip.
- **The equality check:** the working tree's C-row lines equal the
  C-row lines at the latest block's recorded commit
  (`git show <commit>:<path>`). The recorded sha binds CONTENT, not
  the record — the block lands in a follow-up commit. Older blocks
  are human-readable history verified by diff review, not tier 0
  (the stated threat model, README §5.5).
- **The schema lock (schema-first contracts, ADR-019 D4):** a
  ratification block MAY carry `schema` with exact keyset
  `{path, sha256}` — `path` a repo-relative existing file, `sha256`
  64 LOWERCASE hex. Shape-checked on EVERY block that carries it; the
  BYTE check runs on the LATEST block only, in `ratified`, `realized`
  and `superseded` (suspended ONLY at `reopened`, with the equality
  check and for the same reason): the working tree file's sha256
  equals the recorded value, else LOUD red — an edit of the
  declaration file after ratification is an unratified schema change
  until a new act records the new bytes. Threat model (user-ratified
  2026-08-08, the §3 header's sentence applied): ACCIDENT and
  SLOPPINESS — an unratified schema edit, a stale or mistyped hash, a
  wrong path; deliberate concealment stays with human diff review and
  the arm. Multiple contracts locking ONE file each red on any edit
  of it — designed (a schema edit is an act), revisited as a boundary
  topic when a second locking contract exists.
- **State consistency (decidable from the current bytes alone):**
  ratification block(s) present ⇔ status ∈ {ratified, reopened,
  realized, superseded}; status `draft` ⇒ no blocks; the equality
  check binds `ratified`/`realized`/`superseded` and is suspended
  ONLY at `reopened`.
- **`realized_map`:** exactly one block with this exact top-level
  key — and that key is the block's ONLY top-level key (a sibling key
  in the same fence is red: it would ride along unread, the ordinary
  accident being a copy-paste that merges two blocks); keys exactly
  the C-row id set; every landing site a nonempty
    errors, _ = lint(shared_root / "packets", shared_root / "contracts", ADR_DIR)
    expect_green("GREEN fixture pair", errors)
    shared_tmp.cleanup()

    # The count is PINNED, not merely printed: a deleted assert_red call
    # used to shrink the reported number while the run still exited 0,
    # so an accidentally dropped fixture read as a pass. Raise this
    # constant in the SAME commit that adds a dim — never to make a red
    # selftest green. Deliberately one level: a red fixture per claim, a
    # name list, and this count. Nothing checks THIS check.
    if len(red_dims) != EXPECTED_RED_DIMS:
        failures.append(
            f"selftest register count {len(red_dims)} != pinned {EXPECTED_RED_DIMS} "
            f"— a dim was added or lost; if intended, update EXPECTED_RED_DIMS "
            f"in the same commit"
        )

    for f in failures:
        print(f"selftest FAIL: {f}", file=sys.stderr)
    print(
        f"selftest: {len(red_dims)} red dims exercised, green fixture + reopen "
        f"choreography + fenced-noise greens pass, {len(failures)} failure(s)"
    )
    return 1 if failures else 0


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--selftest", action="store_true")
    parser.add_argument("--packets-dir", type=Path, default=PACKETS_DIR)
    parser.add_argument("--contracts-dir", type=Path, default=CONTRACTS_DIR)
    parser.add_argument("--adr-dir", type=Path, default=ADR_DIR)
    parser.add_argument("--forbid-reopened", action="store_true")
    parser.add_argument("--post-build", metavar="COMMIT")
    parser.add_argument("--packet", type=Path)
    args = parser.parse_args()

    if args.selftest:
        return run_selftest()
    if args.post_build:
        if not args.packet:
            print("--post-build requires --packet <path>", file=sys.stderr)
            return 2
        checker = Checker()
        check_post_build(args.packet, args.post_build, checker)
        for msg in checker.errors:
            print(f"packet-lint FAIL: {msg}", file=sys.stderr)
        print(f"post-build boundary check: {len(checker.errors)} error(s)")
        return 1 if checker.errors else 0
    return run_lint(
        args.packets_dir, args.contracts_dir, args.adr_dir, forbid_reopened=args.forbid_reopened
    )


if __name__ == "__main__":
    sys.exit(main())
M	v3/implementation/contracts/ch13-context-block-v2-contract.md
M	v3/src/definition/schema/templateFormat.ts

codex
Az implementációs diff pontosan két érdemi ágat érint, és a `20b73f6a..HEAD` tartományban a lintfájl nem változott; a felső docs commit csak a live schema/contract párost módosította. Most egy izolált, valódi git-repós fixture-mátrixszal hajtom meg a shape és resolution ágakat, beleértve a symlinkelt `/tmp`-gyökeret is.
exec
/bin/zsh -lc "rg -n \"round 1|round-1|schema-lock.*F1|schema-lock.*F2|schema\\.\\.fixture|symlink.*outside|parent segment\" v3 tools docs . -g '*.md' -g '*.py' --glob '"'!node_modules/**'"' --glob '"'!v3/coverage/**'"'" in /Users/felho/dev/pairflow
 succeeded in 0ms:
docs/modularity-review/2026-05-09-modularity-review-deepening-candidates.md:115:report-only mode. A threshold around 12 direct `.ts` files is a reasonable
tools/v3-plan/check_packet.py:86:      round-1 header wrote its tally from memory and was wrong; this
tools/v3-plan/check_packet.py:686:                # a PARENT SEGMENT, not a substring — "schema..fixture.ts"
tools/v3-plan/check_packet.py:687:                # is a legal filename (round-1 F1's false positive)
tools/v3-plan/check_packet.py:774:    # round-1 F2: a symlink inside the root pointing OUTSIDE it must never
tools/v3-plan/check_packet.py:1597:    # ch7-P3 round-1 header wrote its tally from memory). Skipped when
tools/v3-plan/check_packet.py:2238:        GREEN_PACKET + "\n```text\nround-1 header said: 9 anchored / 9 derived / 9 new-decision\n```\n",
tools/v3-plan/check_packet.py:2875:        # round-1 F2: a symlink inside the root pointing OUTSIDE it must
tools/v3-plan/check_packet.py:2884:        (root / "schema-link.ts").symlink_to(outside)
tools/v3-plan/check_packet.py:2892:        assert_red("schema-lock-symlink-escape", errors, "resolves outside the draft's repository")
tools/v3-plan/check_packet.py:2899:        # round-1 F1: ".." INSIDE a filename is not a parent segment — a
tools/v3-plan/check_packet.py:2900:        # regular file named schema..fixture.ts with its true hash is green.
tools/v3-plan/check_packet.py:2906:        decl = root / "schema..fixture.ts"
tools/v3-plan/check_packet.py:2911:            f'"arms": ["a", "b"], "schema": {{"path": "schema..fixture.ts", "sha256": "{true_sha}"}}, "commit"',
docs/pairflow-initial-design.md:339:3. `pairflow bubble kickoff --id <id> (--task <file-or-text>)` (activates ideation pending bubble to round 1)
docs/pairflow-initial-design.md:381:8. Bubble start injects an initial protocol briefing into implementer/reviewer panes (role, required command set, task/worktree references). Legacy/task bubbles also send implementer kickoff to start round 1 automatically; ideation pending bubbles stay `RUNNING round=0` and require explicit `pairflow bubble kickoff`.
docs/spec-skill-evolution.md:480:After a task reached round 11 in a lifecycle bubble plus ~9 additional direct worktree fixes without converging, and the existing planning apparatus (skill + risk card + simulation) had judged the scope "probably workable", the user asked for a retrospective on why the process itself failed rather than another local bug fix. The agent identified the pattern as excessive closure-breadth: one task bundled six distinct closure families (producer / contract / consumers / schema / status-CLI / diagnostics). The commit codifies that diagnosis into a new `Closure-Budget Gate` with a 7-bucket inventory (reusing the Authority Fan-out vocabulary from `8b57b962`) and six split-forcing policy rules, so future tasks of this shape are blocked at planning time rather than after hours of implementation.
docs/spec-skill-evolution.md:549:- Final state + final round: `DONE at round 13` (`state.json:state`, `state.json:round`)
docs/spec-skill-evolution.md:552:  - Multi-round reviewer CONVERGENCE with advisory findings persisting round-over-round: R4 `advisory_findings_open_total: 4` (`transcript.ndjson:L9`); R5 `3` (`:L14`); R6 `5` (`:L19`); R7 `6` (`:L24`); R8 `3` (`:L29`). The bubble eventually reached `DONE` at round 13, but the pattern through rounds 4-8 matches the user's "15-round" framing at rollout:L186.
docs/spec-skill-evolution.md:558:The `impl_phase2b_remote_create` bubble reached round 13 (with round 4-8 reviewer convergences repeatedly surfacing persistence-write-ordering and rollback-symmetry findings) despite the existing skill having Control-Model Gate, Authority Fan-out Scan, Complexity-Risk Gate, Baseline Preservation, and Closure-Budget Gate. The user's framing at L186 is direct: "we keep hitting the case where after 15 rounds we realize it can't be done in this form and it has to be split into smaller pieces." The agent's L309 diagnosis names the two missing forcing functions (primary-shape classification at plan/task creation; precondition-before-side-effect boundary), and the commit formalizes them into the Bounded-Task-Shape Gate + matching template sections, plus a new ReviewSpec workflow so the same over-wide-task pattern can be caught during planning review rather than at round 15.
docs/spec-skill-evolution.md:834:   - Closure-Budget → bubble at round 11 + 9 direct fixes, passing every prior gate.
./docs/modularity-review/2026-05-09-modularity-review-deepening-candidates.md:115:report-only mode. A threshold around 12 direct `.ts` files is a reasonable
v3/implementation/adr-015-migration-report.md:4484:   "content": "| CS4 | `src/core/convergence/policy.ts` | policy input/evaluator | `validateConvergencePolicy(input: ConvergencePolicyInput) -> ConvergencePolicyResult` | findings evaluation helper + gate messaging | explicit `P0/P1/P2/P3` count + `hasBlocking` + `hasNonBlocking`; structured-first parsing, summary diagnostics fallback; round-1 guardrail policy backref | P1 | required-now | AC7, AC10, AC12, AC14, AC18, AC20, T2, T15, T17 |",
v3/implementation/adr-015-migration-report.md:4502:   "content": "| T15 | Round-1 convergence guardrail precedence | reviewer converged intent round-1 helyzetben | convergence policy evaluated | explicit `ROUND1_CONVERGENCE_GUARDRAIL` reject, initial-design precedence megtartva | P2 | required-now | tests/core/agent/converged.test.ts + tests/core/convergence/policy.test.ts |",
v3/implementation/autonomy-realignment.md:121:  finding that ADDS it — round 1 materializes, the next round
v3/implementation/autonomy-realignment.md:126:- **AL-9 — consequence fixes from the round-1 reviews:** the
v3/implementation/contract-draft-template.md:328:round-1 arm evidence):** status resolution lives on the **manifest**
./docs/pairflow-initial-design.md:339:3. `pairflow bubble kickoff --id <id> (--task <file-or-text>)` (activates ideation pending bubble to round 1)
./docs/pairflow-initial-design.md:381:8. Bubble start injects an initial protocol briefing into implementer/reviewer panes (role, required command set, task/worktree references). Legacy/task bubbles also send implementer kickoff to start round 1 automatically; ideation pending bubbles stay `RUNNING round=0` and require explicit `pairflow bubble kickoff`.
v3/implementation/p3-parity-gate.md:198:the harness" (arm round 1, F7: the exhaustive old↔new replay could not
v3/implementation/p3-parity-gate.md:205:its basis is what arm round 1's F1 and F2 caught.
v3/implementation/p3-parity-gate.md:215:**After arm round 1's fold (2026-08-06, measured at `8d651d3f`):**
./docs/spec-skill-evolution.md:480:After a task reached round 11 in a lifecycle bubble plus ~9 additional direct worktree fixes without converging, and the existing planning apparatus (skill + risk card + simulation) had judged the scope "probably workable", the user asked for a retrospective on why the process itself failed rather than another local bug fix. The agent identified the pattern as excessive closure-breadth: one task bundled six distinct closure families (producer / contract / consumers / schema / status-CLI / diagnostics). The commit codifies that diagnosis into a new `Closure-Budget Gate` with a 7-bucket inventory (reusing the Authority Fan-out vocabulary from `8b57b962`) and six split-forcing policy rules, so future tasks of this shape are blocked at planning time rather than after hours of implementation.
./docs/spec-skill-evolution.md:549:- Final state + final round: `DONE at round 13` (`state.json:state`, `state.json:round`)
./docs/spec-skill-evolution.md:552:  - Multi-round reviewer CONVERGENCE with advisory findings persisting round-over-round: R4 `advisory_findings_open_total: 4` (`transcript.ndjson:L9`); R5 `3` (`:L14`); R6 `5` (`:L19`); R7 `6` (`:L24`); R8 `3` (`:L29`). The bubble eventually reached `DONE` at round 13, but the pattern through rounds 4-8 matches the user's "15-round" framing at rollout:L186.
./docs/spec-skill-evolution.md:558:The `impl_phase2b_remote_create` bubble reached round 13 (with round 4-8 reviewer convergences repeatedly surfacing persistence-write-ordering and rollback-symmetry findings) despite the existing skill having Control-Model Gate, Authority Fan-out Scan, Complexity-Risk Gate, Baseline Preservation, and Closure-Budget Gate. The user's framing at L186 is direct: "we keep hitting the case where after 15 rounds we realize it can't be done in this form and it has to be split into smaller pieces." The agent's L309 diagnosis names the two missing forcing functions (primary-shape classification at plan/task creation; precondition-before-side-effect boundary), and the commit formalizes them into the Bounded-Task-Shape Gate + matching template sections, plus a new ReviewSpec workflow so the same over-wide-task pattern can be caught during planning review rather than at round 15.
./docs/spec-skill-evolution.md:834:   - Closure-Budget → bubble at round 11 + 9 direct fixes, passing every prior gate.
v3/implementation/process-v2-design.md:30:  during the round-1 review.
v3/implementation/process-v2-design.md:510:against these during the round-1 review.
v3/implementation/process-v2-design.md:739:Arm 2 (hunting rifts the round-1 folds themselves minted — the "rule
v3/implementation/process-v2-design.md:896:(fold round 1: silent pass-through admitted typo-bypass, `ADR006`
v3/implementation/process-v2-design.md:906:The one remaining document scan is pinned (fold round 1): the
v3/implementation/process-v2-design.md:933:document order (fold round 1), and block dates must be non-decreasing
v3/implementation/process-v2-design.md:942:two-commit choreography is GREEN** (fold round 1 — both arms found the
v3/implementation/process-v2-design.md:972:a partial map, on any status, is red; fold round 1 closed the
v3/implementation/process-v2-design.md:1011:  (user-stated, fold round 1): a finding the folder disagrees with is
v3/implementation/process-v2-design.md:1013:  declined per finding — the §6 round-1 "consciously narrowed"
v3/implementation/process-v2-design.md:1099:**Fold round 1 (2026-07-09): two arms, both refine; every finding
v3/implementation/schema-expressiveness-audit.md:44:tags serving one C-row are ONE user (arm round 1, F6). A construct
v3/implementation/schema-expressiveness-audit.md:50:to flag a construct and still bank its coverage (arm round 1, F2).
v3/implementation/schema-expressiveness-audit.md:84:exactly (arm round 1, F7 — the earlier wording double-counted
v3/implementation/schema-expressiveness-audit.md:182:construct, per §0's grain (corrected from a tag count at arm round 1,
v3/implementation/schema-expressiveness-audit.md:368:criterion turns on exactly this kind of item. From the arm round-1 fold
v3/implementation/schema-expressiveness-audit.md:684:ratified row).** ONE survives arm round 1; the other two were the
v3/implementation/schema-expressiveness-audit.md:746:| **R6** | substrate-owned behaviour — a PARITY residual, carrying no obligation residual | **Members:** none. **Affected:** ch8-C1/C2/C3/C4/C20 (message TEXTS) and ch8-C5 (the expansion bound) — all `Δlib` | Each obligation IS declared, as a flag with its surfacing stage; what the engine cannot own is the WORDING, which the YAML library emits. Parity here is library-version parity, not engine parity. The ORDER of those findings is NOT in this family — it became `[d-order]` at the round-1 fold (F9). |
v3/implementation/schema-expressiveness-audit.md:903:  round-1 BASIS measurement, not the current state: the fold added
v3/implementation/schema-expressiveness-audit.md:940:rows; and R4's ownership list still incomplete after round 1's F8 — the
v3/implementation/schema-expressiveness-audit.md:943:answers round 1's F8 more accurately than round 1's own fold did.
tools/v3-plan/adr015_sweep.py:230:    # 2026-07-21 xhigh round 1 on the migration implementation).
v3/implementation/README.md:189:panel fold (the ch7-P4 round-1 skip reproduced exactly the
v3/implementation/README.md:305:chapter 1 must define the round-1 inventory explicitly — e.g. chapter traces
v3/implementation/README.md:414:**The spec-vs-build altitude line (binding from round 1):** the
v3/implementation/plan.md:260:Per README §5.4, "in scope" is a plan decision. The round-1 inventory:
v3/implementation/plan.md:309:  E8) covers round-1 needs; the pull form belongs to the GAP-15 registry era.
v3/implementation/plan.md:2806:  could no longer converge in round 1). That is a product decision,
v3/implementation/ch13-rederivation-arm/p3-build/phase-close-report.md:106:monotonically. Round 2 matching round 1 is the honest number, and the
v3/implementation/ch13-rederivation-arm/p3-build/phase-close-report.md:107:class shifted — round 1 found one whole decorative block, round 2 found
v3/implementation/ch13-rederivation-arm/p4/review-record.md:28:commit `08f99ab9`); build `65b1a9e2`; round-1 fold `14f3ab12`. The P4
v3/implementation/ch13-rederivation-arm/p4/review-record.md:62:- Arm round 1 (charter/output beside this file; pin gpt-5.6-sol/high,
v3/implementation/ch13-rederivation-arm/p4/review-record.md:121:  worked example (round 10 measured this; the P4 probes did the same
v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11.md:37:**Fold 1 (round-1 F1).** `collectDependsOn` now descends `map.plain`
v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11.md:46:**Fold 2 (round-1 F2).** `evalMapPlain` now judges the RAW value on the
v3/implementation/ch13-rederivation-arm/p4/arm-round1-d11.md:1:# QA review charter — D11 round 1: the typed subset on a plain map
v3/implementation/packets/ch9-p4a-spawn-machinery.md:1032:        "why_missed": "the authoring-time and lens-2 round-1 entailment checks accepted the substrate-forced framing without enumerating the segmented-timer alternative"
v3/implementation/ch13-rederivation-arm/p3-build/arm-round2.md:109:what round 1 already found.
v3/implementation/process-log.md:396:- 2026-07-08 · ch 7 (P1 refine, round 10) · three mirror-completeness
v3/implementation/process-log.md:408:- 2026-07-08 · ch 7 (P1 refine, round 11) · the field_provenance
v3/implementation/process-log.md:420:- 2026-07-08 · ch 7 (P1 refine, round 12) · four findings folded: the
v3/implementation/process-log.md:426:  the round-11 cas_restart minimal-keyset choice REVERSED after one
v3/implementation/process-log.md:435:- 2026-07-08 · ch 7 (P1 refine, round 13) · three cell-completeness
v3/implementation/process-log.md:440:  group" survived the round-12 per-member split — the map's own
v3/implementation/process-log.md:447:- 2026-07-08 · ch 7 (P1 refine, round 14) · the inventory's Phase
v3/implementation/process-log.md:461:- 2026-07-08 · ch 7 (P1 refine, round 15) · both arms converged on
v3/implementation/process-log.md:462:  the round-14 rename's CROSS-ARTIFACT leftover: the plan §7.2
v3/implementation/process-log.md:464:  PHASE-based — because the round-14 sweep AND its fresh-eyes run
v3/implementation/process-log.md:516:- 2026-07-08 · ch 7 (P2 fresh-session experiment, round 1) · the first
v3/implementation/process-log.md:530:  round 1, provenance line, STOP at the verdict with decision points
v3/implementation/process-log.md:670:  reviewed the proposed amendment; the round-1 blocker — found by
v3/implementation/process-log.md:726:- 2026-07-09 · process-v2 Phase 0.1 (lint review round 1, three
v3/implementation/process-log.md:740:  new red dims. Selftest 58→60 dims, all claim-pinned. The round-1
v3/implementation/process-log.md:894:- 2026-07-09 · process-v2 Phase 0.1 (lint review round 10, one
v3/implementation/process-log.md:909:- 2026-07-09 · process-v2 Phase 0.1 (lint review round 11, two Lows)
v3/implementation/process-log.md:922:  user's decision after fold round 10) · the claim enumeration ran
v3/implementation/process-log.md:978:- 2026-07-09 · post-flip audit round 1 (five findings, all folded
v3/implementation/process-log.md:995:  9 mismatches — 8 folded, 1 already folded in round 1) · the audit's
v3/implementation/process-log.md:1015:  already folded in audit round 1 — the auditor ran against the flip
v3/implementation/process-log.md:1069:  Audit (missing output → refine ADDS it; round 1 materializes, the
v3/implementation/process-log.md:1223:  round-1 lenses caught it independently. The AL-20 measurement rule
v3/implementation/process-log.md:1228:- 2026-07-10 · ch7-P3 round 1: the D1 detector chain WORKED at its
v3/implementation/process-log.md:1256:  for ONE packet's authoring review. Value curve: round 1 caught the
v3/implementation/process-log.md:1365:- 2026-07-10 · ch7-P4 round-1 model-tier visibility friction (the
v3/implementation/process-log.md:1385:- 2026-07-10 · ch7-P4 round-1 route-token generalization (lens-2
v3/implementation/process-log.md:1461:  pass): three packet-coherence findings on the round-1 aftermath
v3/implementation/process-log.md:1467:  the round-1 aftermath fold ran WITHOUT a reconciliation pass — the
v3/implementation/process-log.md:2049:  classification drifted in TWO steps — the internal panel's round-1
v3/implementation/process-log.md:2070:  finding just caught" class recurred on an INVENTORY RULE — round 1
v3/implementation/process-log.md:2792:  validate ok; the gate BLOCKED at round 1 (rejected/gate_blocked/
v3/implementation/process-log.md:2872:**2026-07-19 — ch12-P0 authoring, panel round 1 (boundary-review
v3/implementation/process-log.md:4088:  defects in MY OWN round-1 fold texts (the G5 anchored-vs-derived
v3/implementation/process-log.md:4090:  and one REVERSAL: round 1 dropped V5 from C19 because the manifest
v3/implementation/process-log.md:4819:  on E3's branch 2 was minted at gate-1 round 1, stated canonically,
v3/implementation/process-log.md:5342:  should have been applied at the OBJECT level from round 1, not
v3/implementation/process-log.md:5390:  terminus pattern applied at the OBJECT level from round 1, which the
v3/implementation/packets/ch5-p3-trace-harness.md:168:redelivery): START (implement, round 1, v1) → a1 implementer PASS →
./v3/implementation/adr-015-migration-report.md:4484:   "content": "| CS4 | `src/core/convergence/policy.ts` | policy input/evaluator | `validateConvergencePolicy(input: ConvergencePolicyInput) -> ConvergencePolicyResult` | findings evaluation helper + gate messaging | explicit `P0/P1/P2/P3` count + `hasBlocking` + `hasNonBlocking`; structured-first parsing, summary diagnostics fallback; round-1 guardrail policy backref | P1 | required-now | AC7, AC10, AC12, AC14, AC18, AC20, T2, T15, T17 |",
./v3/implementation/adr-015-migration-report.md:4502:   "content": "| T15 | Round-1 convergence guardrail precedence | reviewer converged intent round-1 helyzetben | convergence policy evaluated | explicit `ROUND1_CONVERGENCE_GUARDRAIL` reject, initial-design precedence megtartva | P2 | required-now | tests/core/agent/converged.test.ts + tests/core/convergence/policy.test.ts |",
./v3/implementation/autonomy-realignment.md:121:  finding that ADDS it — round 1 materializes, the next round
./v3/implementation/autonomy-realignment.md:126:- **AL-9 — consequence fixes from the round-1 reviews:** the
v3/implementation/packets/ch7-p3-diag-consumers.md:8:the `packet_rows` block; the round-1 header had written the tally from
v3/implementation/packets/ch7-p3-diag-consumers.md:11:at panel round 1 by the entailment attack; Case-B semantic trigger →
v3/implementation/packets/ch7-p3-diag-consumers.md:44:Discovered at authoring + round 1 + the STOP verdicts: projection with
v3/implementation/packets/ch7-p3-diag-consumers.md:385:| X1 | CLI bundle verbs (operator `bundle`; dev `bundle`) | pass `unavailableDiagnosticsReader` (new, `diag/index.ts`, beside `noopDiagnosticsSink`): both reads reject `DiagUnavailableError("open_failed")` — operator-visible interim: `rejectedInputs = { status: "unavailable", reason: "open_failed" }` until P4 wires the store on the derived config. **NEW-DECISION row (reclassified at panel round 1):** the UNAVAILABLE direction is anchored (§7.3 duality: an unwired channel is not known-empty; §7.7 gives P4 the config, so real wiring here would mint an ad-hoc config lane — the P2 flag-2 precedent), but the TOKEN pick is underdetermined by ratified text — P2's `open_failed` mapping binds its own store's open sequence, not a never-opened process state; the port doc's "I/O-shaped" is looser but does not decide. Three-way fork (open_failed / read_failed / a new token) closed by judgment, not anchor → new-decision; trips the Case-B semantic trigger → STOP `1:late-b-signal`. **RESOLVED — the user's STOP-1 verdict (2026-07-10): the mapping stands, approve-ratified (flag 1).** Driven at BOTH entrypoints (one pass-through lane each) |
v3/implementation/packets/ch7-p3-diag-consumers.md:597:   Case-B material). Panel round 1 reclassified the row new-decision
v3/implementation/packets/ch7-p3-diag-consumers.md:864:        "what": "X1 (the interim CLI reader's unavailable-token pick) reclassified derived -> new-decision by the round-1 lens-2 entailment attack — the three-way token fork closes by judgment, not anchor",
v3/implementation/packets/ch11-p2a-gate-admission.md:672:  inventory, annotated or not — deepened TWICE: round-1 lens 1
v3/implementation/packets/ch11-p2a-gate-admission.md:903:    the round-1 lens-3 catch closed the `ProcessSpec`/`GateRunner`/
v3/implementation/packets/ch11-p2a-gate-admission.md:948:"fix scoped to the finding" class caught recurring: round 1 fixed
./v3/implementation/contract-draft-template.md:328:round-1 arm evidence):** status resolution lives on the **manifest**
./v3/implementation/p3-parity-gate.md:198:the harness" (arm round 1, F7: the exhaustive old↔new replay could not
./v3/implementation/p3-parity-gate.md:205:its basis is what arm round 1's F1 and F2 caught.
./v3/implementation/p3-parity-gate.md:215:**After arm round 1's fold (2026-08-06, measured at `8d651d3f`):**
v3/implementation/packets/ch7-p2-diag-store.md:344:(flag 6(b)'s ALREADY-WAL mechanism stands as the round-1 record; flag
v3/implementation/packets/ch7-p2-diag-store.md:647:15. **Refine round 10, one finding (round 9's own carve-out was too
v3/implementation/packets/ch7-p2-diag-store.md:674:16. **Refine round 11, one finding (the round-10 write-side residue
v3/implementation/packets/ch7-p2-diag-store.md:676:   whole row-decidable split audited to closure:** round 10 left "the
v3/implementation/packets/ch7-p2-diag-store.md:688:   and, per the round-10 offer, the ENTIRE row-decidable/write-side
v3/implementation/packets/ch7-p2-diag-store.md:705:17. **Refine round 12, one finding (the `internal_failure` residue was
v3/implementation/packets/ch7-p2-diag-store.md:707:   — folded:** round 11 left ALL of `internal_failure`'s
v3/implementation/packets/ch7-p2-diag-store.md:734:18. **Refine round 13, one narrow table-row fold — folded:** the R3
v3/implementation/packets/ch7-p2-diag-store.md:742:   / matrix row / mirror map / acceptance stay POINTERS (the round-12
v3/implementation/packets/ch7-p2-diag-store.md:744:   re-scatter). **Round-13 over-reach — corrected at round 14
v3/implementation/packets/ch7-p2-diag-store.md:750:   round 14 at the reviewer's request — a factual error excised, not a
v3/implementation/packets/ch7-p2-diag-store.md:753:19. **Refine round 14, one finding (round-13's own over-reach) —
v3/implementation/packets/ch7-p2-diag-store.md:754:   folded:** round 13 promoted the ingress non-empty-attribution rule
v3/implementation/packets/ch7-p2-diag-store.md:763:   IS a valid P1 projection and R3 must NOT reject it (the round-13
v3/implementation/packets/ch7-p2-diag-store.md:773:   round 13 promoted on an UNCHECKED assumption about kernel id types;
v3/implementation/packets/ch7-p2-diag-store.md:844:round 12; rounds 13–14 closed and then correctly re-scoped the
v3/implementation/ch13-rederivation-arm/p3-aftermath/arm-round11.md:1:# QA review charter — round 11: a new construct and its neighbourhood
./v3/implementation/process-v2-design.md:30:  during the round-1 review.
./v3/implementation/process-v2-design.md:510:against these during the round-1 review.
./v3/implementation/process-v2-design.md:739:Arm 2 (hunting rifts the round-1 folds themselves minted — the "rule
./v3/implementation/process-v2-design.md:896:(fold round 1: silent pass-through admitted typo-bypass, `ADR006`
./v3/implementation/process-v2-design.md:906:The one remaining document scan is pinned (fold round 1): the
./v3/implementation/process-v2-design.md:933:document order (fold round 1), and block dates must be non-decreasing
./v3/implementation/process-v2-design.md:942:two-commit choreography is GREEN** (fold round 1 — both arms found the
./v3/implementation/process-v2-design.md:972:a partial map, on any status, is red; fold round 1 closed the
./v3/implementation/process-v2-design.md:1011:  (user-stated, fold round 1): a finding the folder disagrees with is
./v3/implementation/process-v2-design.md:1013:  declined per finding — the §6 round-1 "consciously narrowed"
./v3/implementation/process-v2-design.md:1099:**Fold round 1 (2026-07-09): two arms, both refine; every finding
v3/implementation/contracts/ch8-template-format-contract.md:39:the round-1 panel):**
v3/implementation/packets/ch11-p2c-round-alignment.md:222:   activation sets round 1 (start.ts untouched), a full loop-back run
v3/implementation/packets/ch11-p2c-round-alignment.md:223:   ends round 1 (kernel grain) AND the journey's pass-back scenario
v3/implementation/packets/ch11-p2c-round-alignment.md:224:   ends round 1 at the shipped grain (the R-ACTIVATION-JOURNEY
v3/implementation/packets/ch11-p2c-round-alignment.md:277:   round-1 asserts untouched.
v3/implementation/packets/ch11-p2c-round-alignment.md:290:   pin driven end-to-end at round 1→2); CAS restart re-runs on fresh
v3/implementation/packets/ch11-p2c-round-alignment.md:346:| 0 | `start` on the shipped declaration-absent template | round 1 |
v3/implementation/packets/ch11-p2c-round-alignment.md:347:| 1 | `submit` PASS on implement | committed → review; round 1 |
v3/implementation/packets/ch11-p2c-round-alignment.md:349:| 3 | `submit` PASS on implement | committed → review; round 1 |
v3/implementation/packets/ch11-p2c-round-alignment.md:379:| K3 | `gate_projection`'s round consumption PINNED: the projection's `round` stays the loaded instance's canonical round (the P2b V1 pass-through — zero code change); the pin is DRIVEN end-to-end by the l2 trace's threshold gate reading round 1 → block, round 2 → allow under the wrapper declaration. DERIVATION: the plan row names the pin; the built V1 already realizes it — the packet's obligation is the regression drive, not new code (derived: prose:plan §11.4 P2c row, prose:packet ch11-p2b V1) |
v3/implementation/packets/ch11-p2c-round-alignment.md:385:| T1 | `checkRoundReconstruction(detail, template)` joins `storeCheckers.ts`: replay from `template.start` (the checkTerminalSink walk), round₀ = 1 (the post-activation value — `InstanceDetail` exists only for started instances; `startInstance` is the sole creator and sets round 1), +1 per committed row whose replayed-position step carries `advancesRound[type] === true`; the final reconstructed round MUST equal `detail.instance.round` — a mismatch is a violation naming BOTH values (stored and reconstructed — the checkVersionArithmetic message idiom; the tampered lane asserts the message carries both, not just that a violation fired). A non-resolving replay (no step entry at the position; no transition for the row's type) is a VIOLATION, never a skip (the checkTerminalSink corrupt-history precedent). The checker consumes the ADMITTED template's flags — and the CURRENT tree does NOT satisfy that at the harness seam: the built traces pass RAW templates as `seams.template` (`l0aTrace.test.ts:54`, `l0bTrace.test.ts:84`, `l2Trace.test.ts:156` — measured; the raw value satisfies the `WorkflowTemplate`-typed field silently). The fix is TYPE-LEVEL (the arm-gate-1 correction — the C20 letter: "harness types narrow to the admitted type"): `TraceSeams.template` NARROWS to `AdmittedTemplate`, compile-enforcing what a per-call-site discipline would leave to review. The narrowing ripples EXACTLY the measured consumer set: the three wrapped harness traces hand their wrapper's admitted value (one `admit(...)` result → BOTH `seams.template` and `definitions`); `l1Trace.test.ts:107` admits through its EXISTING local `admit()` helper, and `traceHarness.test.ts` GAINS a tiny helper with an INLINE stub catalog — `admitTemplate(template, { resolve: () => null } satisfies GateCatalog)`, the `GateCatalog` type imported from `ports/` and consumed by the `satisfies` — because the G1 eslint ban covers `src/testkit/**` WITHOUT a test exclusion (measured: a lint probe on a gates/ import under testkit fails; the src-root trace tests like l1Trace are outside that dir-block and import `gates/` legally), and its fixtures are GATE-FREE so admission under a never-resolving catalog is vacuous — all-false flags, their round-1 histories stay green (C38's default); the dev CLI's `replay` verb swaps its seam value to the `admitted.template` it ALREADY computes (`cli/dev/main.ts` — a one-token production edit; no verb, flag, or output change, C28 intact). The message obligation on the tampered lane is the packet's own R-LANE-SENSITIVITY discipline (a fired-but-empty violation is a blind lane) plus the kit's checkVersionArithmetic idiom — a test-evidence demand this packet imposes on its own tests, not contract semantics (anchored: prose:ledger §2 l2, contract:ch11-gate-format#C39, prose:v3/src/testkit/storeCheckers.ts, prose:v3/src/testkit/traceHarness.ts, prose:packet ch11-p2a) |
v3/implementation/packets/ch11-p2c-round-alignment.md:405:| absent ⇒ none (round 1 after activation) | A1 (the all-false half) | Claim 2 · dimension 2 · the journey table · T3 |
v3/implementation/packets/ch11-p2c-round-alignment.md:613:    round 1, allows at round 2, under the wrapper declaration).
./v3/implementation/schema-expressiveness-audit.md:44:tags serving one C-row are ONE user (arm round 1, F6). A construct
./v3/implementation/schema-expressiveness-audit.md:50:to flag a construct and still bank its coverage (arm round 1, F2).
./v3/implementation/schema-expressiveness-audit.md:84:exactly (arm round 1, F7 — the earlier wording double-counted
./v3/implementation/schema-expressiveness-audit.md:182:construct, per §0's grain (corrected from a tag count at arm round 1,
./v3/implementation/schema-expressiveness-audit.md:368:criterion turns on exactly this kind of item. From the arm round-1 fold
./v3/implementation/schema-expressiveness-audit.md:684:ratified row).** ONE survives arm round 1; the other two were the
./v3/implementation/schema-expressiveness-audit.md:746:| **R6** | substrate-owned behaviour — a PARITY residual, carrying no obligation residual | **Members:** none. **Affected:** ch8-C1/C2/C3/C4/C20 (message TEXTS) and ch8-C5 (the expansion bound) — all `Δlib` | Each obligation IS declared, as a flag with its surfacing stage; what the engine cannot own is the WORDING, which the YAML library emits. Parity here is library-version parity, not engine parity. The ORDER of those findings is NOT in this family — it became `[d-order]` at the round-1 fold (F9). |
./v3/implementation/schema-expressiveness-audit.md:903:  round-1 BASIS measurement, not the current state: the fold added
./v3/implementation/schema-expressiveness-audit.md:940:rows; and R4's ownership list still incomplete after round 1's F8 — the
./v3/implementation/schema-expressiveness-audit.md:943:answers round 1's F8 more accurately than round 1's own fold did.
v3/implementation/packets/ch7-p1-diag-channel-core.md:49:   round-14: this axis is distinct from the state phase), driven both
v3/implementation/packets/ch7-p1-diag-channel-core.md:77:| `DiagnosticEventBody` | `source: "ingress" \| "kernel"`; `kind: DiagnosticKind`; `instanceId?`; `opId?`; `actorId?`; `type?`; `reason?: RejectionName` (iff kind=`rejected`); `detail?: IngressDetailToken` (iff source=`ingress`); `expectedVersion?`/`currentVersion?: number` (iff kind=`stale`); `payloadDigest?: string` (kernel-source only; presence is DIGEST-POINT-based — never payload-key-based, and distinct from the STATE phase (round-14): present on every POST-digest lane — even when the payload is ABSENT, the ch-5 digest is type-inclusive with arity encoding (ADR-008, `[TAG, type]`) — absent on pre-digest lanes and the digest-throw lane; the emit path NEVER invokes `DigestSource` or any port itself (round-6 finding: a re-call would make the diag emit fallible on the digest-throw lane); the ONLY payload derivative on events); `error?: { name: string; message: string }` (iff kind=`internal_failure`; `message` is UNTRUSTED free text — a thrown error may embed payload/env/path fragments — confined to the diag channel's store and local reads; the marker-scan negative binds the default-BUNDLE path in P3, not the raw event) |
v3/implementation/packets/ch7-p1-diag-channel-core.md:94:| `handle` → CAS restart | `kind:"cas_restart"` + full envelope attribution + `payloadDigest` (keyset canonical in the lane-inventory table; round-12: attribution is uniform wherever an envelope exists) — one per restart, then the final outcome's own lane |
v3/implementation/packets/ch7-p1-diag-channel-core.md:95:| `handle` → any never-committed throw — port-call sublanes listed per the inventory rule (round-6: "store-port rejection" was itself still collapsed): `store.loadInstance` rejection (pre-digest — event lacks `payloadDigest`), `definitions.load` rejection, pinned-template integrity throw, `store.findOp` rejection, `store.commitTransition` rejection (the txn did not land — still never-committed) — EACH driven with a per-call rejecting fake | exact keysets by DIGEST POINT (round-10; axis named at round-14 — the envelope is typed and IN HAND on every `handle` lane, only the digest is digest-point-gated): pre-digest members (`loadInstance` / `definitions.load` / pinned-template) = `{source:"kernel", kind:"internal_failure", instanceId, opId, actorId, type, error}`; post-digest members (`findOp` / `commitTransition`) = same + `payloadDigest` → **rethrow unchanged** |
v3/implementation/packets/ch7-p1-diag-channel-core.md:108:— the axis that gates `payloadDigest`; round-14: the two axes were
v3/implementation/packets/ch7-p1-diag-channel-core.md:112:lane, present on every event** (round-13). **Provenance defaults**
v3/implementation/packets/ch7-p1-diag-channel-core.md:126:| `handle` duplicate | **TWO origins, both driven** (round-12): `findOp` fast-path · `commitTransition` `duplicate_op` result | pre-commit (no new row lands) | post-digest | `{instanceId, opId, actorId, type, payloadDigest}` | attribution: always → typed envelope; `payloadDigest`: post-digest → attempt-computed, threaded | keyset test at EACH origin |
v3/implementation/packets/ch7-p1-diag-channel-core.md:129:| `handle` stale | `handleOnce` version compare | pre-commit | post-digest | `{instanceId, opId, actorId, type, payloadDigest, expectedVersion, currentVersion}` (explicit — no back-reference, round-12) | `expectedVersion`: stale → envelope field; `currentVersion`: stale → the outcome's value | stale keyset test |
v3/implementation/packets/ch7-p1-diag-channel-core.md:131:| `handle` CAS restart | `commitTransition` `cas_conflict` result | pre-commit (the conflicting attempt landed nothing) | post-digest | `{instanceId, opId, actorId, type, payloadDigest}` — FULL envelope attribution (round-12 reversal: attribution is uniform wherever an envelope exists) | fields → envelope + threaded digest | restart count + keyset test |
v3/implementation/packets/ch7-p1-diag-channel-core.md:134:| `handle` post-commit derive throw | `deriveDispatchIntent` | **post-commit — the transition IS persisted** (round-14 split: this is P1's never-committed vs persisted core distinction, no longer blurred in a mixed cell) | post-digest | previous row (attribution + `error` + `payloadDigest`) | as above | corrupted-template fake + persisted-row assert |
v3/implementation/packets/ch7-p1-diag-channel-core.md:136:| `startInstance` post-create derive throw | `deriveDispatchIntent` | **post-create — the instance IS persisted** (round-14 split) | n/a (no envelope) | `{instanceId, error}` | as above | corrupted-template fake + persisted-instance assert |
v3/implementation/packets/ch7-p1-diag-channel-core.md:179:  payload-key-based (round-7; axis named at round-14): pre-digest
v3/implementation/packets/ch7-p1-diag-channel-core.md:229:  comment-only ripple target (round-12).
v3/implementation/packets/ch7-p1-diag-channel-core.md:333:   ambiguous); (c) the round-11 `cas_restart` minimal-keyset choice
v3/implementation/packets/ch7-p1-diag-channel-core.md:356:16. **Round 15 (the cross-artifact half of round 14):** the plan §7.2
v3/implementation/packets/ch7-p1-diag-channel-core.md:358:   "PHASE-based": the round-14 sweep and its fresh-eyes run were
v3/implementation/packets/ch7-p1-diag-channel-core.md:373:  `commitTransition` result — round-12), the no-digest attribution
v3/implementation/contracts/ch9-runner-contract.md:153:reopenings: 0. Size 38.5 KB — +53% over the round-1 baseline, the
v3/implementation/packets/ch6-p4b-dev-cli.md:5:Approved after two refine rounds (round 1: dev config matrix, replay
v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:5:the bytes — including the summary of round 1 below.
v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:35:The document's own §7 records what round 1 found and what each fold
v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:58:**The verification threat model, unchanged from round 1: this
v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:63:  round-1 finding named.
v3/implementation/packets/ch4-p4-bootstrap-floor-trace.md:46:   round 1, version 1, RUNNING; `Started` carries
v3/implementation/packets/ch7-p4-diag-cli.md:502:   alternative is recorded here). Route-token note (panel round 1,
v3/implementation/packets/ch7-p4-diag-cli.md:663:extension; the metrics nuance + scope sentences) — the round-1
v3/implementation/packets/ch8-p1-definition-module.md:684:  panel round 1, live on the target platform): `readFile` on a
v3/implementation/packets/ch8-p1-definition-module.md:729:reclassification. The mechanical residue: typecheck round 1 caught
v3/implementation/packets/ch8-p1-definition-module.md:731:possibly-undefined access; lint round 1 caught two auto-fixable
v3/implementation/packets/ch8-p1-definition-module.md:787:ch7-P4 round-1 skip lesson).
v3/implementation/packets/ch8-p1-definition-module.md:789:**Aftermath round 2 (2026-07-11, the arm's re-check on the round-1
v3/implementation/packets/ch8-p1-definition-module.md:941:        "what": "S1's 'no out-of-directory access exists by construction' overclaimed — a symlink listing entry is a byte-exact match and readFile follows it outside the dir; and the 'non-integer version resolves null' example was false when a matching file exists (typed rejection per invalid-is-not-absent)",
v3/implementation/packets/ch8-p1-definition-module.md:942:        "why_missed": "twelve Opus lens passes accepted the by-construction REF-traversal argument as covering the whole out-of-directory claim — the directory-CONTENT axis (a planted symlink) sat outside every lens's frame; the arm probed the substrate instead of judging the argument. Folded PRE-approve: the packet was corrected before any code existed — zero code impact"
v3/implementation/packets/ch8-p1-definition-module.md:951:        "what": "the round-1 aftermath fix itself regressed: with V15 accumulating, the walk runs on cyclic graphs — and JSON.stringify in finding messages threw on a cyclic value in a scalar slot (role: *a), losing the cycle finding to the G1 internal-failure belt",
v3/implementation/packets/ch8-p1-definition-module.md:952:        "why_missed": "the accumulation fix was scoped to the finding just caught: the walk's HANG-safety was re-derived under the new invariant but the message sites' SERIALIZATION safety was not; the arm's re-check probed a cyclic value in a scalar slot — a combination the round-1 lanes lacked"
v3/implementation/contracts/ch11-gate-format-contract.md:71:re-executed and reproduced by the round-1 substrate lens):
v3/implementation/contracts/ch11-gate-format-contract.md:80:| GP6 | the round-declaration forms (session probe 2026-07-12, the reopen round; same pinned yaml@2.9.0; re-executed and EXTENDED by the round-1 substrate lens)? | flow and block forms parse clean, members are plain strings; an EMPTY `advanceOnArrivalAt` list parses clean (the nonempty rule is validator-owned — GP5's twin); a SCALAR `round:` value parses clean (the container-precondition lane is real); an unquoted numeric member resolves to the NUMBER at the node level (GP2's identity class at list-member grain — and unlike numeric MAP keys, `toJS` does NOT stringify list members); DUPLICATE members parse clean (validator-owned). Lens re-run extensions: a NULL `round:` value (nothing after the colon) parses clean as present-null; a string/map `advanceOnArrivalAt` value parses clean; an empty `round: {}` map parses clean; an unknown key in the `round` map parses clean; a QUOTED numeric member (`"0"`) is a STRING at the node level |
./v3/implementation/ch13-rederivation-arm/p3-build/phase-close-report.md:106:monotonically. Round 2 matching round 1 is the honest number, and the
./v3/implementation/ch13-rederivation-arm/p3-build/phase-close-report.md:107:class shifted — round 1 found one whole decorative block, round 2 found
./v3/implementation/process-log.md:396:- 2026-07-08 · ch 7 (P1 refine, round 10) · three mirror-completeness
./v3/implementation/process-log.md:408:- 2026-07-08 · ch 7 (P1 refine, round 11) · the field_provenance
./v3/implementation/process-log.md:420:- 2026-07-08 · ch 7 (P1 refine, round 12) · four findings folded: the
./v3/implementation/process-log.md:426:  the round-11 cas_restart minimal-keyset choice REVERSED after one
./v3/implementation/process-log.md:435:- 2026-07-08 · ch 7 (P1 refine, round 13) · three cell-completeness
./v3/implementation/process-log.md:440:  group" survived the round-12 per-member split — the map's own
./v3/implementation/process-log.md:447:- 2026-07-08 · ch 7 (P1 refine, round 14) · the inventory's Phase
./v3/implementation/process-log.md:461:- 2026-07-08 · ch 7 (P1 refine, round 15) · both arms converged on
./v3/implementation/process-log.md:462:  the round-14 rename's CROSS-ARTIFACT leftover: the plan §7.2
./v3/implementation/process-log.md:464:  PHASE-based — because the round-14 sweep AND its fresh-eyes run
./v3/implementation/process-log.md:516:- 2026-07-08 · ch 7 (P2 fresh-session experiment, round 1) · the first
./v3/implementation/process-log.md:530:  round 1, provenance line, STOP at the verdict with decision points
./v3/implementation/process-log.md:670:  reviewed the proposed amendment; the round-1 blocker — found by
./v3/implementation/process-log.md:726:- 2026-07-09 · process-v2 Phase 0.1 (lint review round 1, three
./v3/implementation/process-log.md:740:  new red dims. Selftest 58→60 dims, all claim-pinned. The round-1
./v3/implementation/process-log.md:894:- 2026-07-09 · process-v2 Phase 0.1 (lint review round 10, one
./v3/implementation/process-log.md:909:- 2026-07-09 · process-v2 Phase 0.1 (lint review round 11, two Lows)
./v3/implementation/process-log.md:922:  user's decision after fold round 10) · the claim enumeration ran
./v3/implementation/process-log.md:978:- 2026-07-09 · post-flip audit round 1 (five findings, all folded
./v3/implementation/process-log.md:995:  9 mismatches — 8 folded, 1 already folded in round 1) · the audit's
./v3/implementation/process-log.md:1015:  already folded in audit round 1 — the auditor ran against the flip
./v3/implementation/process-log.md:1069:  Audit (missing output → refine ADDS it; round 1 materializes, the
./v3/implementation/process-log.md:1223:  round-1 lenses caught it independently. The AL-20 measurement rule
./v3/implementation/process-log.md:1228:- 2026-07-10 · ch7-P3 round 1: the D1 detector chain WORKED at its
./v3/implementation/process-log.md:1256:  for ONE packet's authoring review. Value curve: round 1 caught the
./v3/implementation/process-log.md:1365:- 2026-07-10 · ch7-P4 round-1 model-tier visibility friction (the
./v3/implementation/process-log.md:1385:- 2026-07-10 · ch7-P4 round-1 route-token generalization (lens-2
./v3/implementation/process-log.md:1461:  pass): three packet-coherence findings on the round-1 aftermath
./v3/implementation/process-log.md:1467:  the round-1 aftermath fold ran WITHOUT a reconciliation pass — the
./v3/implementation/process-log.md:2049:  classification drifted in TWO steps — the internal panel's round-1
./v3/implementation/process-log.md:2070:  finding just caught" class recurred on an INVENTORY RULE — round 1
./v3/implementation/process-log.md:2792:  validate ok; the gate BLOCKED at round 1 (rejected/gate_blocked/
./v3/implementation/process-log.md:2872:**2026-07-19 — ch12-P0 authoring, panel round 1 (boundary-review
./v3/implementation/process-log.md:4088:  defects in MY OWN round-1 fold texts (the G5 anchored-vs-derived
./v3/implementation/process-log.md:4090:  and one REVERSAL: round 1 dropped V5 from C19 because the manifest
./v3/implementation/process-log.md:4819:  on E3's branch 2 was minted at gate-1 round 1, stated canonically,
./v3/implementation/process-log.md:5342:  should have been applied at the OBJECT level from round 1, not
./v3/implementation/process-log.md:5390:  terminus pattern applied at the OBJECT level from round 1, which the
./v3/implementation/ch13-rederivation-arm/p3-build/arm-round2.md:109:what round 1 already found.
v3/implementation/packets/ch12-p4-format-operator-surface.md:416:    activation (currentStep set, round 1); plus a CANCEL lane:
v3/implementation/packets/ch12-p4-format-operator-surface.md:511:| V7 | The activation journey (the R-ACTIVATION-JOURNEY discharge): a subprocess-driven, production-bound (empty registry, C16), DETERMINISTIC context-free deferred-hold lifecycle through all four verbs — (a) `create --mode deferredKickoff` (no task; a context-free template) → `Created` + the instance id on stdout → `start <id>` → `Accepted`, the run `WAITING(kickoff_pending)` → `detail <id>`'s instance shows `kernelStatus: "WAITING"` + the typed `wait` (the emitted read-doc grain, R1) → `kickoff <id> --task <t>` → activation (currentStep = template.start, round 1); (b) a CANCEL lane — `create --mode deferredKickoff` → `start` → held → `cancel <id>` → `TERMINAL(cancelled)`, `detail`'s instance showing `kernelStatus: "TERMINAL"` + `terminalDisposition: "cancelled"` and the compact `list` row's cancelled discriminant. Deterministic by construction (context-free + deferred: no provider leg; the state reads land BEFORE any immediate dispatch would spawn an actor — the P1b context-free deferred-hold journey pattern through the shipped surface). V7 DISCHARGES the R-ACTIVATION-JOURNEY smoke (this packet's acceptance obligation); the §12.5 hand-driven dogfooding checkpoint is a SEPARATE chapter-CLOSE act (run-or-waived at close, the DoD's own disposition — V7's CI-safe automation makes a waive low-risk but does not itself waive it), not this packet's acceptance obligation. DERIVATION NOTE (stays derived — the arm's new-decision challenge reconciled): the ROW is FORCED — R-ACTIVATION-JOURNEY requires a shipped-entrypoint lifecycle smoke, C16's EMPTY production registry + the determinism clause leave the context-free DEFERRED-HOLD shape as the ONLY deterministic full-lifecycle path pre-ch9 (an immediate/provisioned run spawns an actor or needs a provider — both ch9), and covering all four verbs is the DoD's intent; the exact branch structure (two runs vs a single create→start→kickoff→cancel path — both cover the verbs) is fixture-shape BUILD-FREEDOM per R-ALTITUDE-LINE, not a spec-time decision — so the ROW derives while its structure is build work (the arm's single-path alternative is at the fixture altitude the line defers). (derived: prose:template §2 write-time disciplines, prose:packet ch12-p1b, contract:ch12-runtime-core#C19) |
v3/implementation/packets/ch12-p0-gate-field.md:82:- l2 trace row 2 (round 1 threshold block; fixture op-2, read as
v3/implementation/packets/ch11-p1-l1-authority.md:421:| 3 | claude emits `CONVERGED` on review — `{ expectedVersion: 2, expectedRole: "reviewer" }` | `committed`, version 3; terminal — intent is `null` (no dispatch) | currentStep done, round 1, DONE, version 3 |
v3/implementation/packets/ch11-p1-l1-authority.md:1004:    "learned": "the discovered classification arrived in TWO steps: the panel found the missed WRITE SURFACE (round 1), the external arm found the missed DECISION on it (gate 1) — a fresh finder attacks the inventory, an adversarial arm attacks the entailments; the prediction convention should expect classification drift from BOTH directions"
v3/implementation/contracts/ch12-runtime-core-contract.md:92:| RP6 | hostile spec-map forms (the round-1 substrate lens's re-run extensions — the ch11-GP6 pattern)? | a merge key `<<` inside the spec map is a LITERAL key named `<<` under 1.2 core (double fail-closed under C3: unknown key + missing `kind`); anchor/alias spec maps resolve to plain object graphs (no bypass); explicit `!!str none` → the string `none`; capitalized `None` → the string `None` (NOT the requirement — C2's container lane); `NULL`/`Null` join the present-null family; a duplicate `kind` key is a document-wide `DUPLICATE_KEY` error (ch8-C4); the drive-in-tests obligation for these forms is C25's |
v3/implementation/contracts/ch12-runtime-core-contract.md:325:| C11 | THE schema bump (ADR-003 fenced wipe; the prototype schema marker increments): the instances table gains `kernel_status` (TEXT, token ∈ `CREATED\|ACTIVE\|WAITING\|TERMINAL`), `terminal_disposition` (TEXT nullable, token ∈ `done\|failed\|cancelled`, WRITTEN EXACTLY ONCE — the single-write rule the `l0d/terminal-is-a-sink` checker owns), `activation_mode` (TEXT, token ∈ `immediate\|deferred_kickoff` — the STORED tokens are the MODEL's, like every stored token in this row; the authored↔model mapping is C1's), `wait` (TEXT nullable — canonical JSON `{kind, requested_by, resume_events}`; only `kickoff_pending` at ch12; NORMATIVE: `wait` is non-null IFF `kernel_status = WAITING` — every transition leaving WAITING clears it in the SAME atomic move, so no stale wait ever coexists with ACTIVE or TERMINAL — the model's "wait? (when WAITING)" domain statement made binding), `failure_reason` (TEXT nullable), `runtime_context` (TEXT — canonical JSON of the discriminated state `none \| requested{request_id} \| ready{ref}`; a provisioned ready stores the opaque `ref{kind, locator}`, and the context-free `ready(∅)` stores `ready` with `ref: null` — the ∅ encoding, DECIDED HERE), `run_overrides` (TEXT — canonical JSON map of step-id → agent-config map, the create-snapshotted C9 surface; an absent input stores `{}`). `task` becomes NULLABLE (absent until kickoff in deferred mode); `current_step` becomes NULLABLE (position is meaningless until ACTIVE); the ch-4 `status` column (`LifecycleStatus`) is RETIRED — `kernel_status` + `terminal_disposition` are the named replacement (DONE ≡ TERMINAL(done)). Creation initializes `version: 1`, `round: 0`, `current_step: NULL`, `kernel_status: CREATED`; ACTIVATION sets `current_step: template.start` and `round: 1` (the model's fixed lifecycle, ch11-C38's stated basis — the ch-4 round-1-at-start init moves accordingly). |
./v3/implementation/contracts/ch8-template-format-contract.md:39:the round-1 panel):**
./v3/implementation/contracts/ch9-runner-contract.md:153:reopenings: 0. Size 38.5 KB — +53% over the round-1 baseline, the
v3/implementation/packets/ch11-p2b-gate-rung.md:473:| 0 | start `inst-l2`, task | `started`, version 1, currentStep `implement`, round 1 |
v3/implementation/packets/ch11-p2b-gate-rung.md:474:| 1 | codex `PASS` on implement `{expectedRole: implementer}` (v1) | committed v2 → review; round 1 |
v3/implementation/packets/ch11-p2b-gate-rung.md:475:| 2 | claude `CONVERGED` on review `{expectedRole: reviewer}` (v2) | **`Rejected(gate_blocked)`, gateReason `round_below_min`** — threshold: round 1 < 2; NO commit, version stays 2, still on review, round 1, no transcript row |
v3/implementation/ch13-rederivation-plan.md:152:fixed — from round 1); BOUNDED TOOLING LOOPS (verification loops on
v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:54:| `12c97c53` | the round-1 arm record — one verdict, one preserved infra failure |
v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:55:| `08c61552` | the round-1 fold — calendar dates, sole-key records, a pinned register |
./v3/implementation/contracts/ch11-gate-format-contract.md:71:re-executed and reproduced by the round-1 substrate lens):
./v3/implementation/contracts/ch11-gate-format-contract.md:80:| GP6 | the round-declaration forms (session probe 2026-07-12, the reopen round; same pinned yaml@2.9.0; re-executed and EXTENDED by the round-1 substrate lens)? | flow and block forms parse clean, members are plain strings; an EMPTY `advanceOnArrivalAt` list parses clean (the nonempty rule is validator-owned — GP5's twin); a SCALAR `round:` value parses clean (the container-precondition lane is real); an unquoted numeric member resolves to the NUMBER at the node level (GP2's identity class at list-member grain — and unlike numeric MAP keys, `toJS` does NOT stringify list members); DUPLICATE members parse clean (validator-owned). Lens re-run extensions: a NULL `round:` value (nothing after the colon) parses clean as present-null; a string/map `advanceOnArrivalAt` value parses clean; an empty `round: {}` map parses clean; an unknown key in the `round` map parses clean; a QUOTED numeric member (`"0"`) is a STRING at the node level |
v3/implementation/flip-claims.md:33:Source of every row, one of THREE sanctioned sets (fold round 1 — the
v3/implementation/flip-claims.md:236:  availability-class semantics — the D1 list in FULL, fold round 1:
v3/implementation/flip-claims.md:374:  today (the Amendment-1 carrier), the FULL registry (fold round 1 —
v3/implementation/flip-claims.md:390:  skip (fold round 10: the D5 loudness clause was the one member the
v3/implementation/flip-claims.md:468:  declined-reason completion (fold round 10, the FC-G1 formula: the
v3/implementation/flip-claims.md:531:  approve-time slice syntax is the panel's duty (fold round 10: the
v3/implementation/flip-claims.md:655:  two provably stale neighbours to the landed state (fold round 10,
v3/implementation/flip-claims.md:824:**Fold round 1 (2026-07-09): two arms, both refine; 14 consolidated
v3/implementation/flip-claims.md:1160:State: awaiting the arms' round-10 run on these bytes.
v3/implementation/flip-claims.md:1162:**Fold round 10 (2026-07-09): one arm, four findings; all folded —
./v3/implementation/ch13-rederivation-arm/p3-aftermath/arm-round11.md:1:# QA review charter — round 11: a new construct and its neighbourhood
v3/implementation/packets/ch8-p2-md1-migration-activation.md:695:round-1 skip lesson).
./v3/implementation/contracts/ch12-runtime-core-contract.md:92:| RP6 | hostile spec-map forms (the round-1 substrate lens's re-run extensions — the ch11-GP6 pattern)? | a merge key `<<` inside the spec map is a LITERAL key named `<<` under 1.2 core (double fail-closed under C3: unknown key + missing `kind`); anchor/alias spec maps resolve to plain object graphs (no bypass); explicit `!!str none` → the string `none`; capitalized `None` → the string `None` (NOT the requirement — C2's container lane); `NULL`/`Null` join the present-null family; a duplicate `kind` key is a document-wide `DUPLICATE_KEY` error (ch8-C4); the drive-in-tests obligation for these forms is C25's |
./v3/implementation/contracts/ch12-runtime-core-contract.md:325:| C11 | THE schema bump (ADR-003 fenced wipe; the prototype schema marker increments): the instances table gains `kernel_status` (TEXT, token ∈ `CREATED\|ACTIVE\|WAITING\|TERMINAL`), `terminal_disposition` (TEXT nullable, token ∈ `done\|failed\|cancelled`, WRITTEN EXACTLY ONCE — the single-write rule the `l0d/terminal-is-a-sink` checker owns), `activation_mode` (TEXT, token ∈ `immediate\|deferred_kickoff` — the STORED tokens are the MODEL's, like every stored token in this row; the authored↔model mapping is C1's), `wait` (TEXT nullable — canonical JSON `{kind, requested_by, resume_events}`; only `kickoff_pending` at ch12; NORMATIVE: `wait` is non-null IFF `kernel_status = WAITING` — every transition leaving WAITING clears it in the SAME atomic move, so no stale wait ever coexists with ACTIVE or TERMINAL — the model's "wait? (when WAITING)" domain statement made binding), `failure_reason` (TEXT nullable), `runtime_context` (TEXT — canonical JSON of the discriminated state `none \| requested{request_id} \| ready{ref}`; a provisioned ready stores the opaque `ref{kind, locator}`, and the context-free `ready(∅)` stores `ready` with `ref: null` — the ∅ encoding, DECIDED HERE), `run_overrides` (TEXT — canonical JSON map of step-id → agent-config map, the create-snapshotted C9 surface; an absent input stores `{}`). `task` becomes NULLABLE (absent until kickoff in deferred mode); `current_step` becomes NULLABLE (position is meaningless until ACTIVE); the ch-4 `status` column (`LifecycleStatus`) is RETIRED — `kernel_status` + `terminal_disposition` are the named replacement (DONE ≡ TERMINAL(done)). Creation initializes `version: 1`, `round: 0`, `current_step: NULL`, `kernel_status: CREATED`; ACTIVATION sets `current_step: template.start` and `round: 1` (the model's fixed lifecycle, ch11-C38's stated basis — the ch-4 round-1-at-start init moves accordingly). |
v3/implementation/ch13-rederivation-arm/p1/carried-scope.md:3:Findings the round-1 arm produced that are REAL but not P1's to build
v3/implementation/ch13-rederivation-arm/p1/carried-scope.md:6:never fixed — from round 1") and the user's ratification of option A
./v3/implementation/ch13-rederivation-plan.md:152:fixed — from round 1); BOUNDED TOOLING LOOPS (verification loops on
v3/implementation/packets/ch12-p1b-activation-machinery.md:528:| J1 | The context-free deferred-hold JOURNEY (C25's staging — the P1 acceptance vehicle; ingress-driven over PRODUCTION bindings: real SQLite store, real ingress, real kernel, deterministic; a dedicated suite): `create` (mode `deferredKickoff`, NO task) → `Created` + the genesis floor-read (CREATED, round 0, null step, `none` context) → `start` → `Accepted` + WAITING(kickoff_pending) + `ready(∅)` + the STARTED fact on the timeline → an actor-envelope probe → `not_active` (the WAITING leg of J3) → the SAME start replayed → `Duplicate` → `kickoff` (task) → `Activated` + ACTIVE + round 1 + the task set + the TASK_SUPPLIED fact + `wait` NULL → `cancel` → `Terminated(cancelled)` + TERMINAL + `cancelled` + the CANCELLED fact + `runAllCheckers` green — the full hold/kickoff/cancel machinery with no provider leg (anchored: contract:ch12-runtime-core#C25 — the journey's leg list verbatim, the Duplicate and floor-read legs its stated machinery driven end-to-end) |
v3/implementation/packets/ch12-p1b-activation-machinery.md:585:- The ACTIVATION write-set (ACTIVE + `template.start` + round 1) is
v3/implementation/ch13-rederivation-arm/p1/infra/arm-round1-timeout.md:1:# QA review charter — draft-lint `superseded` status machinery (ch13 re-derivation, phase P1, round 1)
./v3/implementation/flip-claims.md:33:Source of every row, one of THREE sanctioned sets (fold round 1 — the
./v3/implementation/flip-claims.md:236:  availability-class semantics — the D1 list in FULL, fold round 1:
./v3/implementation/flip-claims.md:374:  today (the Amendment-1 carrier), the FULL registry (fold round 1 —
./v3/implementation/flip-claims.md:390:  skip (fold round 10: the D5 loudness clause was the one member the
./v3/implementation/flip-claims.md:468:  declined-reason completion (fold round 10, the FC-G1 formula: the
./v3/implementation/flip-claims.md:531:  approve-time slice syntax is the panel's duty (fold round 10: the
./v3/implementation/flip-claims.md:655:  two provably stale neighbours to the landed state (fold round 10,
./v3/implementation/flip-claims.md:824:**Fold round 1 (2026-07-09): two arms, both refine; 14 consolidated
./v3/implementation/flip-claims.md:1160:State: awaiting the arms' round-10 run on these bytes.
./v3/implementation/flip-claims.md:1162:**Fold round 10 (2026-07-09): one arm, four findings; all folded —
v3/implementation/packets/ch11-p4-format-extension.md:206:   declaration effect (the journey pass-back round 1→2 — Y8's
v3/implementation/packets/ch11-p4-format-extension.md:276:9. **Journey lanes (Y5/Y6):** the gated journey (block at round 1 →
v3/implementation/packets/ch11-p4-format-extension.md:377:| Y5 | The journey extension (the R-ACTIVATION-JOURNEY discharge): (a) the existing pass-back scenario's final-round assert flips 1 → 2 with its comment re-grounded (the restoration observable end-to-end — Y8's declared golden edit; the C38 DEFAULT remains driven at the kernel/admission grain and at F8's file grain, so no coverage is lost); (b) a NEW gated scenario through the shipped subprocess channel: a test-AUTHORED gated template file (a staged temp templates dir — the operator-authored input artifact; a `declarative.threshold` `round >= 2` gate on the review step's CONVERGED transition + the round declaration) driven full-lifecycle: CONVERGED at round 1 → the submit exits NONZERO with the rejected outcome (`gate_blocked`, reason `round_below_min` — the ch6 rejection surface); PASS pass-back → round 2; CONVERGED → allow → DONE; the timeline rows carry the retained ordered `gateDecisions` (the C27 read surface, first driven from a FILE-authored gate). DERIVATION NOTE: the rule's trigger (a shipped entrypoint newly reaching built foundation) is measured — P3b W3 named this packet as the journey's deferral home. (derived: prose:template §2 write-time disciplines, prose:packet ch11-p3b W3, contract:ch11-gate-format#C38) |
v3/implementation/packets/ch6-p4a-operator-cli.md:6:(round 1: production activation/config, dev under-specification →
./v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:54:| `12c97c53` | the round-1 arm record — one verdict, one preserved infra failure |
./v3/implementation/ch13-rederivation-arm/p1/phase-close-report.md:55:| `08c61552` | the round-1 fold — calendar dates, sole-key records, a pinned register |
./tools/v3-plan/check_packet.py:86:      round-1 header wrote its tally from memory and was wrong; this
./tools/v3-plan/check_packet.py:686:                # a PARENT SEGMENT, not a substring — "schema..fixture.ts"
./tools/v3-plan/check_packet.py:687:                # is a legal filename (round-1 F1's false positive)
./tools/v3-plan/check_packet.py:774:    # round-1 F2: a symlink inside the root pointing OUTSIDE it must never
./tools/v3-plan/check_packet.py:1597:    # ch7-P3 round-1 header wrote its tally from memory). Skipped when
./tools/v3-plan/check_packet.py:2238:        GREEN_PACKET + "\n```text\nround-1 header said: 9 anchored / 9 derived / 9 new-decision\n```\n",
./tools/v3-plan/check_packet.py:2875:        # round-1 F2: a symlink inside the root pointing OUTSIDE it must
./tools/v3-plan/check_packet.py:2884:        (root / "schema-link.ts").symlink_to(outside)
./tools/v3-plan/check_packet.py:2892:        assert_red("schema-lock-symlink-escape", errors, "resolves outside the draft's repository")
./tools/v3-plan/check_packet.py:2899:        # round-1 F1: ".." INSIDE a filename is not a parent segment — a
./tools/v3-plan/check_packet.py:2900:        # regular file named schema..fixture.ts with its true hash is green.
./tools/v3-plan/check_packet.py:2906:        decl = root / "schema..fixture.ts"
./tools/v3-plan/check_packet.py:2911:            f'"arms": ["a", "b"], "schema": {{"path": "schema..fixture.ts", "sha256": "{true_sha}"}}, "commit"',
./v3/implementation/ch13-rederivation-arm/p1/carried-scope.md:3:Findings the round-1 arm produced that are REAL but not P1's to build
./v3/implementation/ch13-rederivation-arm/p1/carried-scope.md:6:never fixed — from round 1") and the user's ratification of option A
v3/implementation/packets/ch12-p3-provider-contract.md:423:activation write-set (L7 — ACTIVE + `template.start` + round 1 + the
v3/implementation/packets/ch12-p3-provider-contract.md:518:      kernel_status ACTIVE, current_step implement, round 1, version → 3
v3/implementation/packets/ch12-p3-provider-contract.md:551:kickoff(task): admit (WAITING ∧ kickoff_pending) → task set, wait NULL, activate: ACTIVE, round 1, TASK_SUPPLIED{op2}, v→4 → Activated
v3/implementation/packets/ch12-p3-provider-contract.md:1292:implementation, sensitivity pass): round 1 → 6 findings incl. a P1 PRODUCT
./tools/v3-plan/adr015_sweep.py:230:    # 2026-07-21 xhigh round 1 on the migration implementation).
./v3/implementation/ch13-rederivation-arm/p1/infra/arm-round1-timeout.md:1:# QA review charter — draft-lint `superseded` status machinery (ch13 re-derivation, phase P1, round 1)
./v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:5:the bytes — including the summary of round 1 below.
./v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:35:The document's own §7 records what round 1 found and what each fold
./v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:58:**The verification threat model, unchanged from round 1: this
./v3/implementation/ch13-rederivation-arm/p3/arm-round2.md:63:  round-1 finding named.
v3/adr/ADR-015-v3-plane-consolidation.md:301:round-13 oscillation): on MARKDOWN surfaces
v3/adr/ADR-015-v3-plane-consolidation.md:949:carriers — closing the round-5 ↔ round-13 oscillation). The run
v3/adr/ADR-015-v3-plane-consolidation.md:974:FINDINGS — a SINGLE finding, F1-deep15 BLOCKER: the round-13
./v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11.md:37:**Fold 1 (round-1 F1).** `collectDependsOn` now descends `map.plain`
./v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11.md:46:**Fold 2 (round-1 F2).** `evalMapPlain` now judges the RAW value on the
./v3/adr/ADR-015-v3-plane-consolidation.md:301:round-13 oscillation): on MARKDOWN surfaces
./v3/adr/ADR-015-v3-plane-consolidation.md:949:carriers — closing the round-5 ↔ round-13 oscillation). The run
./v3/adr/ADR-015-v3-plane-consolidation.md:974:FINDINGS — a SINGLE finding, F1-deep15 BLOCKER: the round-13
./plans/feature-ideas.md:32:    - because a bubble can be "round 11" but operationally already be in a much later recovery state.
./v3/implementation/ch13-rederivation-arm/p4/arm-round1-d11.md:1:# QA review charter — D11 round 1: the typed subset on a plain map
./v3/implementation/ch13-rederivation-arm/p4/review-record.md:28:commit `08f99ab9`); build `65b1a9e2`; round-1 fold `14f3ab12`. The P4
./v3/implementation/ch13-rederivation-arm/p4/review-record.md:62:- Arm round 1 (charter/output beside this file; pin gpt-5.6-sol/high,
./v3/implementation/ch13-rederivation-arm/p4/review-record.md:121:  worked example (round 10 measured this; the P4 probes did the same
v3/adr/ADR-003-storage-substrate-and-migration-stance.md:51:  round 1: an external service dependency in a local-first tool; nothing in
./v3/adr/ADR-003-storage-substrate-and-migration-stance.md:51:  round 1: an external service dependency in a local-first tool; nothing in
./v3/implementation/plan.md:260:Per README §5.4, "in scope" is a plan decision. The round-1 inventory:
./v3/implementation/plan.md:309:  E8) covers round-1 needs; the pull form belongs to the GAP-15 registry era.
./v3/implementation/plan.md:2806:  could no longer converge in round 1). That is a product decision,
./v3/implementation/packets/ch9-p4a-spawn-machinery.md:1032:        "why_missed": "the authoring-time and lens-2 round-1 entailment checks accepted the substrate-forced framing without enumerating the segmented-timer alternative"
./v3/implementation/packets/ch5-p3-trace-harness.md:168:redelivery): START (implement, round 1, v1) → a1 implementer PASS →
./v3/adr/ADR-002-language-and-tooling.md:29:- **A different language (Rust/Go)** — rejected for round 1: the domain
v3/adr/ADR-002-language-and-tooling.md:29:- **A different language (Rust/Go)** — rejected for round 1: the domain
./v3/implementation/packets/ch4-p4-bootstrap-floor-trace.md:46:   round 1, version 1, RUNNING; `Started` carries
./v3/implementation/packets/ch7-p1-diag-channel-core.md:49:   round-14: this axis is distinct from the state phase), driven both
./v3/implementation/packets/ch7-p1-diag-channel-core.md:77:| `DiagnosticEventBody` | `source: "ingress" \| "kernel"`; `kind: DiagnosticKind`; `instanceId?`; `opId?`; `actorId?`; `type?`; `reason?: RejectionName` (iff kind=`rejected`); `detail?: IngressDetailToken` (iff source=`ingress`); `expectedVersion?`/`currentVersion?: number` (iff kind=`stale`); `payloadDigest?: string` (kernel-source only; presence is DIGEST-POINT-based — never payload-key-based, and distinct from the STATE phase (round-14): present on every POST-digest lane — even when the payload is ABSENT, the ch-5 digest is type-inclusive with arity encoding (ADR-008, `[TAG, type]`) — absent on pre-digest lanes and the digest-throw lane; the emit path NEVER invokes `DigestSource` or any port itself (round-6 finding: a re-call would make the diag emit fallible on the digest-throw lane); the ONLY payload derivative on events); `error?: { name: string; message: string }` (iff kind=`internal_failure`; `message` is UNTRUSTED free text — a thrown error may embed payload/env/path fragments — confined to the diag channel's store and local reads; the marker-scan negative binds the default-BUNDLE path in P3, not the raw event) |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:94:| `handle` → CAS restart | `kind:"cas_restart"` + full envelope attribution + `payloadDigest` (keyset canonical in the lane-inventory table; round-12: attribution is uniform wherever an envelope exists) — one per restart, then the final outcome's own lane |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:95:| `handle` → any never-committed throw — port-call sublanes listed per the inventory rule (round-6: "store-port rejection" was itself still collapsed): `store.loadInstance` rejection (pre-digest — event lacks `payloadDigest`), `definitions.load` rejection, pinned-template integrity throw, `store.findOp` rejection, `store.commitTransition` rejection (the txn did not land — still never-committed) — EACH driven with a per-call rejecting fake | exact keysets by DIGEST POINT (round-10; axis named at round-14 — the envelope is typed and IN HAND on every `handle` lane, only the digest is digest-point-gated): pre-digest members (`loadInstance` / `definitions.load` / pinned-template) = `{source:"kernel", kind:"internal_failure", instanceId, opId, actorId, type, error}`; post-digest members (`findOp` / `commitTransition`) = same + `payloadDigest` → **rethrow unchanged** |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:108:— the axis that gates `payloadDigest`; round-14: the two axes were
./v3/implementation/packets/ch7-p1-diag-channel-core.md:112:lane, present on every event** (round-13). **Provenance defaults**
./v3/implementation/packets/ch7-p1-diag-channel-core.md:126:| `handle` duplicate | **TWO origins, both driven** (round-12): `findOp` fast-path · `commitTransition` `duplicate_op` result | pre-commit (no new row lands) | post-digest | `{instanceId, opId, actorId, type, payloadDigest}` | attribution: always → typed envelope; `payloadDigest`: post-digest → attempt-computed, threaded | keyset test at EACH origin |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:129:| `handle` stale | `handleOnce` version compare | pre-commit | post-digest | `{instanceId, opId, actorId, type, payloadDigest, expectedVersion, currentVersion}` (explicit — no back-reference, round-12) | `expectedVersion`: stale → envelope field; `currentVersion`: stale → the outcome's value | stale keyset test |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:131:| `handle` CAS restart | `commitTransition` `cas_conflict` result | pre-commit (the conflicting attempt landed nothing) | post-digest | `{instanceId, opId, actorId, type, payloadDigest}` — FULL envelope attribution (round-12 reversal: attribution is uniform wherever an envelope exists) | fields → envelope + threaded digest | restart count + keyset test |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:134:| `handle` post-commit derive throw | `deriveDispatchIntent` | **post-commit — the transition IS persisted** (round-14 split: this is P1's never-committed vs persisted core distinction, no longer blurred in a mixed cell) | post-digest | previous row (attribution + `error` + `payloadDigest`) | as above | corrupted-template fake + persisted-row assert |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:136:| `startInstance` post-create derive throw | `deriveDispatchIntent` | **post-create — the instance IS persisted** (round-14 split) | n/a (no envelope) | `{instanceId, error}` | as above | corrupted-template fake + persisted-instance assert |
./v3/implementation/packets/ch7-p1-diag-channel-core.md:179:  payload-key-based (round-7; axis named at round-14): pre-digest
./v3/implementation/packets/ch7-p1-diag-channel-core.md:229:  comment-only ripple target (round-12).
./v3/implementation/packets/ch7-p1-diag-channel-core.md:333:   ambiguous); (c) the round-11 `cas_restart` minimal-keyset choice
./v3/implementation/packets/ch7-p1-diag-channel-core.md:356:16. **Round 15 (the cross-artifact half of round 14):** the plan §7.2
./v3/implementation/packets/ch7-p1-diag-channel-core.md:358:   "PHASE-based": the round-14 sweep and its fresh-eyes run were
./v3/implementation/packets/ch7-p1-diag-channel-core.md:373:  `commitTransition` result — round-12), the no-digest attribution
./v3/implementation/packets/ch11-p2c-round-alignment.md:222:   activation sets round 1 (start.ts untouched), a full loop-back run
./v3/implementation/packets/ch11-p2c-round-alignment.md:223:   ends round 1 (kernel grain) AND the journey's pass-back scenario
./v3/implementation/packets/ch11-p2c-round-alignment.md:224:   ends round 1 at the shipped grain (the R-ACTIVATION-JOURNEY
./v3/implementation/packets/ch11-p2c-round-alignment.md:277:   round-1 asserts untouched.
./v3/implementation/packets/ch11-p2c-round-alignment.md:290:   pin driven end-to-end at round 1→2); CAS restart re-runs on fresh
./v3/implementation/packets/ch11-p2c-round-alignment.md:346:| 0 | `start` on the shipped declaration-absent template | round 1 |
./v3/implementation/packets/ch11-p2c-round-alignment.md:347:| 1 | `submit` PASS on implement | committed → review; round 1 |
./v3/implementation/packets/ch11-p2c-round-alignment.md:349:| 3 | `submit` PASS on implement | committed → review; round 1 |
./v3/implementation/packets/ch11-p2c-round-alignment.md:379:| K3 | `gate_projection`'s round consumption PINNED: the projection's `round` stays the loaded instance's canonical round (the P2b V1 pass-through — zero code change); the pin is DRIVEN end-to-end by the l2 trace's threshold gate reading round 1 → block, round 2 → allow under the wrapper declaration. DERIVATION: the plan row names the pin; the built V1 already realizes it — the packet's obligation is the regression drive, not new code (derived: prose:plan §11.4 P2c row, prose:packet ch11-p2b V1) |
./v3/implementation/packets/ch11-p2c-round-alignment.md:385:| T1 | `checkRoundReconstruction(detail, template)` joins `storeCheckers.ts`: replay from `template.start` (the checkTerminalSink walk), round₀ = 1 (the post-activation value — `InstanceDetail` exists only for started instances; `startInstance` is the sole creator and sets round 1), +1 per committed row whose replayed-position step carries `advancesRound[type] === true`; the final reconstructed round MUST equal `detail.instance.round` — a mismatch is a violation naming BOTH values (stored and reconstructed — the checkVersionArithmetic message idiom; the tampered lane asserts the message carries both, not just that a violation fired). A non-resolving replay (no step entry at the position; no transition for the row's type) is a VIOLATION, never a skip (the checkTerminalSink corrupt-history precedent). The checker consumes the ADMITTED template's flags — and the CURRENT tree does NOT satisfy that at the harness seam: the built traces pass RAW templates as `seams.template` (`l0aTrace.test.ts:54`, `l0bTrace.test.ts:84`, `l2Trace.test.ts:156` — measured; the raw value satisfies the `WorkflowTemplate`-typed field silently). The fix is TYPE-LEVEL (the arm-gate-1 correction — the C20 letter: "harness types narrow to the admitted type"): `TraceSeams.template` NARROWS to `AdmittedTemplate`, compile-enforcing what a per-call-site discipline would leave to review. The narrowing ripples EXACTLY the measured consumer set: the three wrapped harness traces hand their wrapper's admitted value (one `admit(...)` result → BOTH `seams.template` and `definitions`); `l1Trace.test.ts:107` admits through its EXISTING local `admit()` helper, and `traceHarness.test.ts` GAINS a tiny helper with an INLINE stub catalog — `admitTemplate(template, { resolve: () => null } satisfies GateCatalog)`, the `GateCatalog` type imported from `ports/` and consumed by the `satisfies` — because the G1 eslint ban covers `src/testkit/**` WITHOUT a test exclusion (measured: a lint probe on a gates/ import under testkit fails; the src-root trace tests like l1Trace are outside that dir-block and import `gates/` legally), and its fixtures are GATE-FREE so admission under a never-resolving catalog is vacuous — all-false flags, their round-1 histories stay green (C38's default); the dev CLI's `replay` verb swaps its seam value to the `admitted.template` it ALREADY computes (`cli/dev/main.ts` — a one-token production edit; no verb, flag, or output change, C28 intact). The message obligation on the tampered lane is the packet's own R-LANE-SENSITIVITY discipline (a fired-but-empty violation is a blind lane) plus the kit's checkVersionArithmetic idiom — a test-evidence demand this packet imposes on its own tests, not contract semantics (anchored: prose:ledger §2 l2, contract:ch11-gate-format#C39, prose:v3/src/testkit/storeCheckers.ts, prose:v3/src/testkit/traceHarness.ts, prose:packet ch11-p2a) |
./v3/implementation/packets/ch11-p2c-round-alignment.md:405:| absent ⇒ none (round 1 after activation) | A1 (the all-false half) | Claim 2 · dimension 2 · the journey table · T3 |
./v3/implementation/packets/ch11-p2c-round-alignment.md:613:    round 1, allows at round 2, under the wrapper declaration).
./v3/implementation/packets/ch11-p4-format-extension.md:206:   declaration effect (the journey pass-back round 1→2 — Y8's
./v3/implementation/packets/ch11-p4-format-extension.md:276:9. **Journey lanes (Y5/Y6):** the gated journey (block at round 1 →
./v3/implementation/packets/ch11-p4-format-extension.md:377:| Y5 | The journey extension (the R-ACTIVATION-JOURNEY discharge): (a) the existing pass-back scenario's final-round assert flips 1 → 2 with its comment re-grounded (the restoration observable end-to-end — Y8's declared golden edit; the C38 DEFAULT remains driven at the kernel/admission grain and at F8's file grain, so no coverage is lost); (b) a NEW gated scenario through the shipped subprocess channel: a test-AUTHORED gated template file (a staged temp templates dir — the operator-authored input artifact; a `declarative.threshold` `round >= 2` gate on the review step's CONVERGED transition + the round declaration) driven full-lifecycle: CONVERGED at round 1 → the submit exits NONZERO with the rejected outcome (`gate_blocked`, reason `round_below_min` — the ch6 rejection surface); PASS pass-back → round 2; CONVERGED → allow → DONE; the timeline rows carry the retained ordered `gateDecisions` (the C27 read surface, first driven from a FILE-authored gate). DERIVATION NOTE: the rule's trigger (a shipped entrypoint newly reaching built foundation) is measured — P3b W3 named this packet as the journey's deferral home. (derived: prose:template §2 write-time disciplines, prose:packet ch11-p3b W3, contract:ch11-gate-format#C38) |
./v3/implementation/packets/ch12-p3-provider-contract.md:423:activation write-set (L7 — ACTIVE + `template.start` + round 1 + the
./v3/implementation/packets/ch12-p3-provider-contract.md:518:      kernel_status ACTIVE, current_step implement, round 1, version → 3
./v3/implementation/packets/ch12-p3-provider-contract.md:551:kickoff(task): admit (WAITING ∧ kickoff_pending) → task set, wait NULL, activate: ACTIVE, round 1, TASK_SUPPLIED{op2}, v→4 → Activated
./v3/implementation/packets/ch12-p3-provider-contract.md:1292:implementation, sensitivity pass): round 1 → 6 findings incl. a P1 PRODUCT
./v3/implementation/packets/ch6-p4b-dev-cli.md:5:Approved after two refine rounds (round 1: dev config matrix, replay
./v3/implementation/packets/ch12-p4-format-operator-surface.md:416:    activation (currentStep set, round 1); plus a CANCEL lane:
./v3/implementation/packets/ch12-p4-format-operator-surface.md:511:| V7 | The activation journey (the R-ACTIVATION-JOURNEY discharge): a subprocess-driven, production-bound (empty registry, C16), DETERMINISTIC context-free deferred-hold lifecycle through all four verbs — (a) `create --mode deferredKickoff` (no task; a context-free template) → `Created` + the instance id on stdout → `start <id>` → `Accepted`, the run `WAITING(kickoff_pending)` → `detail <id>`'s instance shows `kernelStatus: "WAITING"` + the typed `wait` (the emitted read-doc grain, R1) → `kickoff <id> --task <t>` → activation (currentStep = template.start, round 1); (b) a CANCEL lane — `create --mode deferredKickoff` → `start` → held → `cancel <id>` → `TERMINAL(cancelled)`, `detail`'s instance showing `kernelStatus: "TERMINAL"` + `terminalDisposition: "cancelled"` and the compact `list` row's cancelled discriminant. Deterministic by construction (context-free + deferred: no provider leg; the state reads land BEFORE any immediate dispatch would spawn an actor — the P1b context-free deferred-hold journey pattern through the shipped surface). V7 DISCHARGES the R-ACTIVATION-JOURNEY smoke (this packet's acceptance obligation); the §12.5 hand-driven dogfooding checkpoint is a SEPARATE chapter-CLOSE act (run-or-waived at close, the DoD's own disposition — V7's CI-safe automation makes a waive low-risk but does not itself waive it), not this packet's acceptance obligation. DERIVATION NOTE (stays derived — the arm's new-decision challenge reconciled): the ROW is FORCED — R-ACTIVATION-JOURNEY requires a shipped-entrypoint lifecycle smoke, C16's EMPTY production registry + the determinism clause leave the context-free DEFERRED-HOLD shape as the ONLY deterministic full-lifecycle path pre-ch9 (an immediate/provisioned run spawns an actor or needs a provider — both ch9), and covering all four verbs is the DoD's intent; the exact branch structure (two runs vs a single create→start→kickoff→cancel path — both cover the verbs) is fixture-shape BUILD-FREEDOM per R-ALTITUDE-LINE, not a spec-time decision — so the ROW derives while its structure is build work (the arm's single-path alternative is at the fixture altitude the line defers). (derived: prose:template §2 write-time disciplines, prose:packet ch12-p1b, contract:ch12-runtime-core#C19) |
./v3/implementation/packets/ch8-p1-definition-module.md:684:  panel round 1, live on the target platform): `readFile` on a
./v3/implementation/packets/ch8-p1-definition-module.md:729:reclassification. The mechanical residue: typecheck round 1 caught
./v3/implementation/packets/ch8-p1-definition-module.md:731:possibly-undefined access; lint round 1 caught two auto-fixable
./v3/implementation/packets/ch8-p1-definition-module.md:787:ch7-P4 round-1 skip lesson).
./v3/implementation/packets/ch8-p1-definition-module.md:789:**Aftermath round 2 (2026-07-11, the arm's re-check on the round-1
./v3/implementation/packets/ch8-p1-definition-module.md:941:        "what": "S1's 'no out-of-directory access exists by construction' overclaimed — a symlink listing entry is a byte-exact match and readFile follows it outside the dir; and the 'non-integer version resolves null' example was false when a matching file exists (typed rejection per invalid-is-not-absent)",
./v3/implementation/packets/ch8-p1-definition-module.md:942:        "why_missed": "twelve Opus lens passes accepted the by-construction REF-traversal argument as covering the whole out-of-directory claim — the directory-CONTENT axis (a planted symlink) sat outside every lens's frame; the arm probed the substrate instead of judging the argument. Folded PRE-approve: the packet was corrected before any code existed — zero code impact"
./v3/implementation/packets/ch8-p1-definition-module.md:951:        "what": "the round-1 aftermath fix itself regressed: with V15 accumulating, the walk runs on cyclic graphs — and JSON.stringify in finding messages threw on a cyclic value in a scalar slot (role: *a), losing the cycle finding to the G1 internal-failure belt",
./v3/implementation/packets/ch8-p1-definition-module.md:952:        "why_missed": "the accumulation fix was scoped to the finding just caught: the walk's HANG-safety was re-derived under the new invariant but the message sites' SERIALIZATION safety was not; the arm's re-check probed a cyclic value in a scalar slot — a combination the round-1 lanes lacked"
./v3/implementation/packets/ch6-p4a-operator-cli.md:6:(round 1: production activation/config, dev under-specification →
./v3/implementation/packets/ch7-p4-diag-cli.md:502:   alternative is recorded here). Route-token note (panel round 1,
./v3/implementation/packets/ch7-p4-diag-cli.md:663:extension; the metrics nuance + scope sentences) — the round-1
./v3/implementation/packets/ch12-p0-gate-field.md:82:- l2 trace row 2 (round 1 threshold block; fixture op-2, read as
./v3/design/topics/_open-kernel-primitives.md:24:reviewer, on the round-1 text) folded in: the status precision above, the P1
./v3/implementation/packets/ch11-p1-l1-authority.md:421:| 3 | claude emits `CONVERGED` on review — `{ expectedVersion: 2, expectedRole: "reviewer" }` | `committed`, version 3; terminal — intent is `null` (no dispatch) | currentStep done, round 1, DONE, version 3 |
./v3/implementation/packets/ch11-p1-l1-authority.md:1004:    "learned": "the discovered classification arrived in TWO steps: the panel found the missed WRITE SURFACE (round 1), the external arm found the missed DECISION on it (gate 1) — a fresh finder attacks the inventory, an adversarial arm attacks the entailments; the prediction convention should expect classification drift from BOTH directions"
./v3/implementation/packets/ch8-p2-md1-migration-activation.md:695:round-1 skip lesson).
v3/design/topics/_open-kernel-primitives.md:24:reviewer, on the round-1 text) folded in: the status precision above, the P1
./v3/implementation/README.md:189:panel fold (the ch7-P4 round-1 skip reproduced exactly the
./v3/implementation/README.md:305:chapter 1 must define the round-1 inventory explicitly — e.g. chapter traces
./v3/implementation/README.md:414:**The spec-vs-build altitude line (binding from round 1):** the
./v3/implementation/packets/ch12-p1b-activation-machinery.md:528:| J1 | The context-free deferred-hold JOURNEY (C25's staging — the P1 acceptance vehicle; ingress-driven over PRODUCTION bindings: real SQLite store, real ingress, real kernel, deterministic; a dedicated suite): `create` (mode `deferredKickoff`, NO task) → `Created` + the genesis floor-read (CREATED, round 0, null step, `none` context) → `start` → `Accepted` + WAITING(kickoff_pending) + `ready(∅)` + the STARTED fact on the timeline → an actor-envelope probe → `not_active` (the WAITING leg of J3) → the SAME start replayed → `Duplicate` → `kickoff` (task) → `Activated` + ACTIVE + round 1 + the task set + the TASK_SUPPLIED fact + `wait` NULL → `cancel` → `Terminated(cancelled)` + TERMINAL + `cancelled` + the CANCELLED fact + `runAllCheckers` green — the full hold/kickoff/cancel machinery with no provider leg (anchored: contract:ch12-runtime-core#C25 — the journey's leg list verbatim, the Duplicate and floor-read legs its stated machinery driven end-to-end) |
./v3/implementation/packets/ch12-p1b-activation-machinery.md:585:- The ACTIVATION write-set (ACTIVE + `template.start` + round 1) is
./v3/implementation/packets/ch7-p3-diag-consumers.md:8:the `packet_rows` block; the round-1 header had written the tally from
./v3/implementation/packets/ch7-p3-diag-consumers.md:11:at panel round 1 by the entailment attack; Case-B semantic trigger →
./v3/implementation/packets/ch7-p3-diag-consumers.md:44:Discovered at authoring + round 1 + the STOP verdicts: projection with
./v3/implementation/packets/ch7-p3-diag-consumers.md:385:| X1 | CLI bundle verbs (operator `bundle`; dev `bundle`) | pass `unavailableDiagnosticsReader` (new, `diag/index.ts`, beside `noopDiagnosticsSink`): both reads reject `DiagUnavailableError("open_failed")` — operator-visible interim: `rejectedInputs = { status: "unavailable", reason: "open_failed" }` until P4 wires the store on the derived config. **NEW-DECISION row (reclassified at panel round 1):** the UNAVAILABLE direction is anchored (§7.3 duality: an unwired channel is not known-empty; §7.7 gives P4 the config, so real wiring here would mint an ad-hoc config lane — the P2 flag-2 precedent), but the TOKEN pick is underdetermined by ratified text — P2's `open_failed` mapping binds its own store's open sequence, not a never-opened process state; the port doc's "I/O-shaped" is looser but does not decide. Three-way fork (open_failed / read_failed / a new token) closed by judgment, not anchor → new-decision; trips the Case-B semantic trigger → STOP `1:late-b-signal`. **RESOLVED — the user's STOP-1 verdict (2026-07-10): the mapping stands, approve-ratified (flag 1).** Driven at BOTH entrypoints (one pass-through lane each) |
./v3/implementation/packets/ch7-p3-diag-consumers.md:597:   Case-B material). Panel round 1 reclassified the row new-decision
./v3/implementation/packets/ch7-p3-diag-consumers.md:864:        "what": "X1 (the interim CLI reader's unavailable-token pick) reclassified derived -> new-decision by the round-1 lens-2 entailment attack — the three-way token fork closes by judgment, not anchor",
./v3/implementation/packets/ch7-p2-diag-store.md:344:(flag 6(b)'s ALREADY-WAL mechanism stands as the round-1 record; flag
./v3/implementation/packets/ch7-p2-diag-store.md:647:15. **Refine round 10, one finding (round 9's own carve-out was too
./v3/implementation/packets/ch7-p2-diag-store.md:674:16. **Refine round 11, one finding (the round-10 write-side residue
./v3/implementation/packets/ch7-p2-diag-store.md:676:   whole row-decidable split audited to closure:** round 10 left "the
./v3/implementation/packets/ch7-p2-diag-store.md:688:   and, per the round-10 offer, the ENTIRE row-decidable/write-side
./v3/implementation/packets/ch7-p2-diag-store.md:705:17. **Refine round 12, one finding (the `internal_failure` residue was
./v3/implementation/packets/ch7-p2-diag-store.md:707:   — folded:** round 11 left ALL of `internal_failure`'s
./v3/implementation/packets/ch7-p2-diag-store.md:734:18. **Refine round 13, one narrow table-row fold — folded:** the R3
./v3/implementation/packets/ch7-p2-diag-store.md:742:   / matrix row / mirror map / acceptance stay POINTERS (the round-12
./v3/implementation/packets/ch7-p2-diag-store.md:744:   re-scatter). **Round-13 over-reach — corrected at round 14
./v3/implementation/packets/ch7-p2-diag-store.md:750:   round 14 at the reviewer's request — a factual error excised, not a
./v3/implementation/packets/ch7-p2-diag-store.md:753:19. **Refine round 14, one finding (round-13's own over-reach) —
./v3/implementation/packets/ch7-p2-diag-store.md:754:   folded:** round 13 promoted the ingress non-empty-attribution rule
./v3/implementation/packets/ch7-p2-diag-store.md:763:   IS a valid P1 projection and R3 must NOT reject it (the round-13
./v3/implementation/packets/ch7-p2-diag-store.md:773:   round 13 promoted on an UNCHECKED assumption about kernel id types;
./v3/implementation/packets/ch7-p2-diag-store.md:844:round 12; rounds 13–14 closed and then correctly re-scoped the
./v3/implementation/packets/ch11-p2b-gate-rung.md:473:| 0 | start `inst-l2`, task | `started`, version 1, currentStep `implement`, round 1 |
./v3/implementation/packets/ch11-p2b-gate-rung.md:474:| 1 | codex `PASS` on implement `{expectedRole: implementer}` (v1) | committed v2 → review; round 1 |
./v3/implementation/packets/ch11-p2b-gate-rung.md:475:| 2 | claude `CONVERGED` on review `{expectedRole: reviewer}` (v2) | **`Rejected(gate_blocked)`, gateReason `round_below_min`** — threshold: round 1 < 2; NO commit, version stays 2, still on review, round 1, no transcript row |
./v3/design/research/bitsafe-workflow-simulation.md:233:  - *The rework loop and round hygiene.* On `request_rework`, `choice.target` is the draft step; the loop-back trips `advances_round`, so the same commit runs **`clear_stale_decision_context`** — the stale review/approval context is dropped, the fresh rework payload (the instruction + refs) survives as the handoff, and `instance.round` advances in `apply_target_entry_effects`. The re-arrived draft step resumes ACTIVE and the post-commit `DispatchIntent` carries the instruction to the drafter. When round 2's draft re-enters the gate, `park_for_human_decision` mints a **new** `request_ref` — the round-1 ask is dead by correlation, so a late round-1 approve cannot ship round 2's draft. This loop is the exact anchor shape the L3 units were built around; nothing here is planned-level.
./v3/design/research/bitsafe-workflow-simulation.md:419:- *(b) The core question — is "no context from round one" natural in v3?* **Yes, and structurally: the isolation is the kernel's default, not a discipline the template must add.** Walk what step 2 actually receives. The `ContextPacket` (l2b `dispatch_intent`) is exactly: `task` (the pinned contract + positions refs — delivered to *every* step), `instruction` (the checker's own step text), `handoff = payload_of_transition_into` — **the committed emit payload of round 1 and nothing else**, `available_ops`, `context_blocks` — rendered *deterministically from the template catalog only* (`assemble_context_blocks`: rendered, ordered, deduped, with provenance; round 2's declared blocks, not round 1's), `effective_agent_config` (L0c resolve: role default + step override + run override), and the runtime-context projection. Round 1's *reasoning* — the session, the conversation, the chain of thought — never appears in this list because **produce-not-perform keeps it out of the kernel entirely**: the kernel sees only the actor envelope (op_id, expected_version, event type, payload) climbing the admission ladder; the performer's session stays with the runtime. What BitSafe maintains as a spawn-time convention ("fresh agent, don't paste round one"), v3 delivers by construction: the only channel between steps is committed state, so "no context from round one" is what you get *unless you work to defeat it*. The capture's own criterion maps field-for-field: round 2 must see "the contract + BitSafe positions + round-1's redline output" = `task` + `context_blocks` + `handoff`; must not see round-1's reasoning = everything the kernel never admitted. v3's handoff discipline **is** BitSafe's independence pattern made structural — this is the section's positive finding.
./v3/design/research/bitsafe-workflow-simulation.md:420:  What *could* leak, honestly enumerated: **(i) the handoff payload itself** — the kernel is de-vocabularized and hands the payload through faithfully, so a drafter whose emit carries its rationale in prose forwards its blind spots; the control is the emit contract (P2 transitions' declared payload schema) constraining round 1's payload to refs — a template-authoring discipline the kernel can enforce at the schema grain but not at the content grain. (The redline document itself necessarily encodes some reasoning; that is inherent to the pattern — BitSafe's round 2 reads the redline too.) **(ii) Same actor session reused** — the built binding is per *role* (`instance.binding[step.role]`), so the template author's first control is two distinct roles ⇒ distinct bindings; but even distinct bindings are only kernel-side intent: session continuity is explicitly adapter-side (primitives memo §8: session continuity is adapter-side (L0c/executor), not kernel), L0c-future §2 requires portable host-owned session handles, and §3 is blunt — `issued_agent_config` is "provenance, not a runtime receipt." A runtime that warm-reuses one legalbot session for both dispatches has silently destroyed the independence, and the transcript cannot see it: issued-fresh vs actually-fresh is GAP-8's issued-vs-applied hole in a new costume. **(iii) Shared memory namespace** — 1675 says personas differ *by* memory namespace, i.e. both rounds run as "legalbot" share one; under planned L11, round 1's `RememberIntent` followed by round 2's recall is a *kernel-adjacent* leak path, and L11's `(observer, observed)` perspective addressing gives the key shape to separate them but no step-scoped access policy. **(iv) Shared runtime workspace** — `runtime_context` is instance-scoped (L0e), so round-1 scratch files would persist into round-2's projection; for legalbot the honest template simply declares no runtime context ("the actor assumes no workspace"), dissolving this vector by design since contract, positions, and redline all live behind provider refs. Net: the template author controls per-step `context_blocks`, distinct role bindings, per-step `agent_config` (fresh-session intent), emit payload schema, and a context-free workflow — everything needed to *declare the intent* of isolation exists; what is missing is any way to declare isolation *as a checkable fact* and any attestation that the runtime honored it (gap below).
./v3/design/research/bitsafe-workflow-simulation.md:434:- **GAP-8 recurs, widened instance:** the issued-vs-applied hole is not model-choice-specific — the kernel issues fresh-session/distinct-binding intent for round 2 (`issued_agent_config`), a runtime warm-reuses round 1's session, and no attestation contract records that the load-bearing independence (1752) was silently violated; L0c §3 names the distinction and defers the receipt.
./v3/design/research/bitsafe-workflow-simulation.md:435:- **Step-level context isolation has no declarable, checkable kernel surface (new).** Isolation between two steps is achievable only as emergent convention — distinct role bindings, per-step `agent_config`, catalog-scoped context blocks, a context-free workflow — never as a declared fact the kernel or runtime must enforce and attest: session continuity is adapter-side with attestation unwritten (L0c §§2–3), planned L11 memory has perspective addressing but no step-scoped access policy (two steps running one persona share one namespace, 1675, and a `RememberIntent`/recall pair is a kernel-adjacent leak), and the L0e runtime context is instance-scoped with no per-step exclusion. Trigger scenario: legalbot's round-2 checker binds the same persona as the drafter; memory recall surfaces round-1's reasoning notes or the adapter resumes the warm session; round 2 inherits round 1's blind spots exactly as capture 1752 warns, the two-round review silently degrades to one round, and the transcript shows two clean dispatches with nothing to detect or record the loss. → **GAP-13**
./v3/implementation/packets/ch11-p2a-gate-admission.md:672:  inventory, annotated or not — deepened TWICE: round-1 lens 1
./v3/implementation/packets/ch11-p2a-gate-admission.md:903:    the round-1 lens-3 catch closed the `ProcessSpec`/`GateRunner`/
./v3/implementation/packets/ch11-p2a-gate-admission.md:948:"fix scoped to the finding" class caught recurring: round 1 fixed
./v3/design/research/symphony-study.md:547:`path_safety.ex:25-45`) and reject `workspace == root`, symlink-escape, outside-root
./plans/archive/plans/2026-05-01-local-plan-watch-plan-v1.md:152:Progress update (2026-05-03): reviewer round 1 for document bubble `plan-watch-observability` identified contract ambiguities in task `6-watch-runner-observability`; the follow-up refinement resolved trigger-kind optionality, timeline `runner_completed` source rules, forbidden stderr/process fallback mirroring, artifact write-failure reason codes, slug/collision/concurrency behavior, timeline schema versioning, call-site evidence mapping, and minimum README/pilot-doc content.
./v3/design/topics/_closed-emit-contract.md:10:The paper test (PASS, §2) → review round 1 (Q1–Q4, §3) → review round 2 (the
./v3/design/topics/_closed-emit-contract.md:83:**New rejection names** (decided in review round 1, Q1): `op_id_collision`
./v3/design/topics/_closed-emit-contract.md:118:## 3. Questions — all four DECIDED in review round 1 (2026-07-07)
./v3/design/topics/_closed-emit-contract.md:140:4. The offer↔gate seam (NEW in round 1) — **DECIDED: close it now, in the
v3/design/topics/_closed-emit-contract.md:10:The paper test (PASS, §2) → review round 1 (Q1–Q4, §3) → review round 2 (the
v3/design/topics/_closed-emit-contract.md:83:**New rejection names** (decided in review round 1, Q1): `op_id_collision`
v3/design/topics/_closed-emit-contract.md:118:## 3. Questions — all four DECIDED in review round 1 (2026-07-07)
v3/design/topics/_closed-emit-contract.md:140:4. The offer↔gate seam (NEW in round 1) — **DECIDED: close it now, in the
v3/design/research/bitsafe-workflow-simulation.md:233:  - *The rework loop and round hygiene.* On `request_rework`, `choice.target` is the draft step; the loop-back trips `advances_round`, so the same commit runs **`clear_stale_decision_context`** — the stale review/approval context is dropped, the fresh rework payload (the instruction + refs) survives as the handoff, and `instance.round` advances in `apply_target_entry_effects`. The re-arrived draft step resumes ACTIVE and the post-commit `DispatchIntent` carries the instruction to the drafter. When round 2's draft re-enters the gate, `park_for_human_decision` mints a **new** `request_ref` — the round-1 ask is dead by correlation, so a late round-1 approve cannot ship round 2's draft. This loop is the exact anchor shape the L3 units were built around; nothing here is planned-level.
v3/design/research/bitsafe-workflow-simulation.md:419:- *(b) The core question — is "no context from round one" natural in v3?* **Yes, and structurally: the isolation is the kernel's default, not a discipline the template must add.** Walk what step 2 actually receives. The `ContextPacket` (l2b `dispatch_intent`) is exactly: `task` (the pinned contract + positions refs — delivered to *every* step), `instruction` (the checker's own step text), `handoff = payload_of_transition_into` — **the committed emit payload of round 1 and nothing else**, `available_ops`, `context_blocks` — rendered *deterministically from the template catalog only* (`assemble_context_blocks`: rendered, ordered, deduped, with provenance; round 2's declared blocks, not round 1's), `effective_agent_config` (L0c resolve: role default + step override + run override), and the runtime-context projection. Round 1's *reasoning* — the session, the conversation, the chain of thought — never appears in this list because **produce-not-perform keeps it out of the kernel entirely**: the kernel sees only the actor envelope (op_id, expected_version, event type, payload) climbing the admission ladder; the performer's session stays with the runtime. What BitSafe maintains as a spawn-time convention ("fresh agent, don't paste round one"), v3 delivers by construction: the only channel between steps is committed state, so "no context from round one" is what you get *unless you work to defeat it*. The capture's own criterion maps field-for-field: round 2 must see "the contract + BitSafe positions + round-1's redline output" = `task` + `context_blocks` + `handoff`; must not see round-1's reasoning = everything the kernel never admitted. v3's handoff discipline **is** BitSafe's independence pattern made structural — this is the section's positive finding.
v3/design/research/bitsafe-workflow-simulation.md:420:  What *could* leak, honestly enumerated: **(i) the handoff payload itself** — the kernel is de-vocabularized and hands the payload through faithfully, so a drafter whose emit carries its rationale in prose forwards its blind spots; the control is the emit contract (P2 transitions' declared payload schema) constraining round 1's payload to refs — a template-authoring discipline the kernel can enforce at the schema grain but not at the content grain. (The redline document itself necessarily encodes some reasoning; that is inherent to the pattern — BitSafe's round 2 reads the redline too.) **(ii) Same actor session reused** — the built binding is per *role* (`instance.binding[step.role]`), so the template author's first control is two distinct roles ⇒ distinct bindings; but even distinct bindings are only kernel-side intent: session continuity is explicitly adapter-side (primitives memo §8: session continuity is adapter-side (L0c/executor), not kernel), L0c-future §2 requires portable host-owned session handles, and §3 is blunt — `issued_agent_config` is "provenance, not a runtime receipt." A runtime that warm-reuses one legalbot session for both dispatches has silently destroyed the independence, and the transcript cannot see it: issued-fresh vs actually-fresh is GAP-8's issued-vs-applied hole in a new costume. **(iii) Shared memory namespace** — 1675 says personas differ *by* memory namespace, i.e. both rounds run as "legalbot" share one; under planned L11, round 1's `RememberIntent` followed by round 2's recall is a *kernel-adjacent* leak path, and L11's `(observer, observed)` perspective addressing gives the key shape to separate them but no step-scoped access policy. **(iv) Shared runtime workspace** — `runtime_context` is instance-scoped (L0e), so round-1 scratch files would persist into round-2's projection; for legalbot the honest template simply declares no runtime context ("the actor assumes no workspace"), dissolving this vector by design since contract, positions, and redline all live behind provider refs. Net: the template author controls per-step `context_blocks`, distinct role bindings, per-step `agent_config` (fresh-session intent), emit payload schema, and a context-free workflow — everything needed to *declare the intent* of isolation exists; what is missing is any way to declare isolation *as a checkable fact* and any attestation that the runtime honored it (gap below).
v3/design/research/bitsafe-workflow-simulation.md:434:- **GAP-8 recurs, widened instance:** the issued-vs-applied hole is not model-choice-specific — the kernel issues fresh-session/distinct-binding intent for round 2 (`issued_agent_config`), a runtime warm-reuses round 1's session, and no attestation contract records that the load-bearing independence (1752) was silently violated; L0c §3 names the distinction and defers the receipt.
v3/design/research/bitsafe-workflow-simulation.md:435:- **Step-level context isolation has no declarable, checkable kernel surface (new).** Isolation between two steps is achievable only as emergent convention — distinct role bindings, per-step `agent_config`, catalog-scoped context blocks, a context-free workflow — never as a declared fact the kernel or runtime must enforce and attest: session continuity is adapter-side with attestation unwritten (L0c §§2–3), planned L11 memory has perspective addressing but no step-scoped access policy (two steps running one persona share one namespace, 1675, and a `RememberIntent`/recall pair is a kernel-adjacent leak), and the L0e runtime context is instance-scoped with no per-step exclusion. Trigger scenario: legalbot's round-2 checker binds the same persona as the drafter; memory recall surfaces round-1's reasoning notes or the adapter resumes the warm session; round 2 inherits round 1's blind spots exactly as capture 1752 warns, the two-round review silently degrades to one round, and the transcript shows two clean dispatches with nothing to detect or record the loss. → **GAP-13**
v3/design/research/symphony-study.md:547:`path_safety.ex:25-45`) and reject `workspace == root`, symlink-escape, outside-root
./plans/archive/tasks/bubble-runtime/ideation-without-explicit-task-bubble-start-phase1.md:285:| T11 | kickoff state transition + marker normalization | ideation bubble RUNNING round=0 | kickoff execute | round 1-re all, implementer aktiv ownership beallitva, `ideation_task_pending=false`, `ideation_mode=true` retained, round_role_history bejegyzes keszul | P1 | required-now | core test |
./plans/archive/tasks/reviewer-gating/reviewer-clean-pass-command-gate-prompt-hardening-bugfix-phase1.md:106:| Reviewer command decision guidance | reszben implicit, PASS-centrikus | explicit clean-path command gate | round 1 rule + round>=2 clean->converged + no `pass --no-findings` clean path + blocker policy unchanged marker | rovid operational pelda | prompt-only, backward-compatible | P1 | required-now |
./plans/archive/tasks/reviewer-gating/reviewer-clean-pass-command-gate-prompt-hardening-bugfix-phase1.md:144:| T2a | Handoff + resume kickoff command gate explicit (round 1 variant) | reviewer handoff + reviewer resume kickoff prompt render round=1 | string generated | mindket prompt tartalmazza `REQ_A/REQ_D`; nem tartalmazza `REQ_B`; nem tartalmazza `FORBID_A/FORBID_B1/FORBID_B2/FORBID_B3/FORBID_C` | P1 | required-now | `tests/core/runtime/tmuxDelivery.test.ts`, `tests/core/bubble/startBubble.test.ts` |
./plans/archive/tasks/reviewer-gating/reviewer-clean-pass-command-gate-prompt-hardening-bugfix-phase1.md:146:| T2c | Handoff + resume kickoff non-clean regression guard (round>=2 + findings) | reviewer handoff + reviewer resume kickoff prompt render round>=2 with findings | string generated | ebben a findings-variantban explicit tartalmazza a kozos gate invariantokat (`REQ_C` es `REQ_D`) es megorzi a findings->`pairflow pass` iranyitast (`REQ_E`); explicit nem tartalmazza `REQ_A/REQ_B` (round-1 es clean-no-findings mondatok hianya), nem jelenik meg konvergenciara terelo tiltott varians (`FORBID_C`), tovabba `FORBID_A/FORBID_B1/FORBID_B2/FORBID_B3` sem. A ket round>=2 varians kozos invarians scope-jat a T2d cross-variant guard rogzi. | P1 | required-now | `tests/core/runtime/tmuxDelivery.test.ts`, `tests/core/bubble/startBubble.test.ts` |
./plans/archive/tasks/reviewer-gating/reviewer-clean-pass-command-gate-prompt-hardening-bugfix-phase1.md:198:2. `AC2`: A gate deterministicen tartalmazza: round 1 `converged` tilos; round>=2 no-findings clean path `converged`; clean pathon `pass --no-findings` tilos (natural wording variansokra is); round>=2 findings eseten `pass` irany megorzott.
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:92:6. Terminologiai igazitas: round-1 esetben a reviewer alternation evidence guardrail precedence marad ervenyben, ezert a converged ut tovabbra is rejectelt; a `severity_gate_round` szabaly ezt nem irja felul.
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:111:3. A round-1 converged guardrail precedence normativ forrasa: `docs/pairflow-initial-design.md` (convergence guardrail szabalyok).
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:112:4. Precedence summary (normativ): round-1/alternation guardrail valtozatlan; a `severity_gate_round` gate csak azon korokben alkalmazando, ahol a converged precondition egyebkent teljesitheto.
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:124:| CS4 | `src/core/convergence/policy.ts` | policy input/evaluator | `validateConvergencePolicy(input: ConvergencePolicyInput) -> ConvergencePolicyResult` | findings evaluation helper + gate messaging | explicit `P0/P1/P2/P3` count + `hasBlocking` + `hasNonBlocking`; structured-first parsing, summary diagnostics fallback; round-1 guardrail policy backref | P1 | required-now | AC7, AC10, AC12, AC14, AC18, AC20, T2, T15, T17 |
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:126:| CS6 | `src/core/agent/converged.ts` | convergence gate wiring | `emitConvergedFromWorkspace(input, deps?) -> Promise<EmitConvergedResult>` | policy invocation input assembly | shared `severity_gate_round` input + canonical redirected metadata source: elozo reviewer PASS `payload.findings` (transcript); round-1 guardrail precedence explicit megtartasa; initial-design eligibility baseline nem gyengul | P1 | required-now | AC14, AC15, AC17, AC18, AC20, T9, T13, T14, T15, T17, T21 |
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:144:| Round-1 guardrail precedence | generic safety note | explicit normative binding | round-1 converged guardrail precedence preserved from `pairflow-initial-design` | explanatory reason context | behavior preservation | P2 | required-now |
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:178:| round-1 convergence guardrail trigger | convergence policy | throw | initial-design guardrail precedence fennmarad | `ROUND1_CONVERGENCE_GUARDRAIL` | info | P2 | required-now |
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:209:| T15 | Round-1 convergence guardrail precedence | reviewer converged intent round-1 helyzetben | convergence policy evaluated | explicit `ROUND1_CONVERGENCE_GUARDRAIL` reject, initial-design precedence megtartva | P2 | required-now | tests/core/agent/converged.test.ts + tests/core/convergence/policy.test.ts |
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:236:18. `AC18`: round-1 converged guardrail precedence explicit megorzott es normativan a `docs/pairflow-initial-design.md`-hez kotott.
./plans/archive/tasks/reviewer-gating/reviewer-convergence-p3-round4-task-2026-03-03.md:311:9. `SL9`: round-1 converged guardrail precedence explicit normativ kotese (`docs/pairflow-initial-design.md`) es teszt-fedezete teljesul.
./plans/archive/tasks/review-policy-reviewer-blocking-threshold-and-shared-ui/review-policy-reviewer-blocking-threshold-routing-consume-phase2a.md:112:   - round 1 reviewer emit semantics
./plans/archive/tasks/review-policy-reviewer-blocking-threshold-and-shared-ui/review-policy-reviewer-blocking-threshold-reviewer-facing-parity-phase2b.md:115:   - round 1 reviewer emit semantics

exec
/bin/zsh -lc "sed -n '330,430p' tools/v3-plan/check_packet.py; sed -n '1800,1948p' tools/v3-plan/check_packet.py; sed -n '2810,2875p' tools/v3-plan/check_packet.py" in /Users/felho/dev/pairflow
 succeeded in 0ms:
# closure must appear in the packet_rows manifest refs; an anchored
# closure's token must additionally appear in the header's
# "`contract:<surface>` rows C…/C…" union when that surface declares
# one. Matched in fence-stripped prose only.
CLOSURE_RE = re.compile(r"\((anchored|derived):\s*([^()]*)\)")
CONTRACT_TOKEN_RE = re.compile(r"contract:([a-z][a-z0-9-]*)#(C[1-9]\d*)")
# The row list may be LINE-WRAPPED (the live ch11-p2a header wraps
# mid-list — the R-INSTRUMENT-PROBE wrapped-form class, caught by this
# check's own first live run): separators admit whitespace/newlines.
HEADER_UNION_RE = re.compile(
    r"`contract:([a-z][a-z0-9-]*)`\s+rows\s+(C[1-9]\d*(?:\s*/\s*C[1-9]\d*)*)"
)
HEADER_UNION_ROW_RE = re.compile(r"C[1-9]\d*")
ROW_CLASSES = ("anchored", "derived", "new-decision")
RATIFICATION_KEYS = {"date", "arms", "commit"}
PROSE_PREFIX = "prose:"

# Line-oriented fence scanning (round 7): a fence opens with >=3
# backticks or tildes and closes ONLY on a line of the SAME character
# with AT LEAST the opener's length (the CommonMark rule) — so a
# ````markdown outer fence QUOTES an inner ```json or ```text block as
# material (the template itself quotes this way). The old
# pair-matching regexes leaked quoted content into the prose scans and
# could read a quoted json block as a machine block.
# Openers and closers may be indented 0-3 spaces (CommonMark); 4+ is
# an indented code block, out of the FENCED-code claim's scope.
FENCE_OPEN_RE = re.compile(r"^ {0,3}(`{3,}|~{3,})(.*)$")
LANE_DEF_RE = re.compile(r"^\|\s*([A-Z]{1,2})(\d+)\s*\|")
# No leading zeros, and ids compare as EXACT strings everywhere —
# int() normalization made O01 == O1, a silent reinterpretation of
# declared data (P5 says "manifest id == table first-cell id").
MANIFEST_ID_RE = re.compile(r"^([A-Z]{1,2})([1-9]\d*)$")
CONTRACT_REF_RE = re.compile(r"^contract:(ch\d+-[a-z0-9-]+)#(C\d+)$")
ADR_STRICT_RE = re.compile(r"^ADR-(\d{3})$")
# The WHOLE [P: family is withdrawn (P6) — not just the three known
# kinds: [P:typo] outside a fence is as much a reappearing carrier as
# [P:anchored …]. "Withdrawn at design time", deliberately not
# "retired": the convention never went live (zero packets used it);
# what this guards against is generation drift re-emitting it from
# the repo's historical texts.
WITHDRAWN_MARK_RE = re.compile(r"\[P:")
# P9: the header tally form the live packets state (template §1's
# Classification line) — matched in fence-stripped prose only.
PROSE_TALLY_RE = re.compile(r"(\d+)\s+anchored\s*/\s*(\d+)\s+derived\s*/\s*(\d+)\s+new-decision")

# P11 (ch13 boundary — the mirror-drift class): the pointer-only
# register. Opt-in BY DECLARATION (the P1 grandfathering pattern at
# the block grain): only a packet carrying the mirror_map block is
# gated; every earlier packet is untouched, stray pointer tokens
# included. The scan verifies DECLARED literals (signature strings,
# pointer ids, anchors) against bytes — free text is never
# interpreted (the P9 precedent).
MIRROR_MAP_KEYS = {"form", "rules"}
MIRROR_RULE_KEYS = {"id", "canonical", "signature", "allow"}
# Strict kebab: no doubled or trailing hyphens (arm finding 8).
MIRROR_RULE_ID_RE = re.compile(r"^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$")
# canonical is a table-defined lane id (the P5 scan's set, reserved
# families excluded) or a section anchor <slug>#<Token>: the slug must
# name a real heading (kebab-prefix match at a hyphen boundary) whose
# section contains the token (arm finding 2), both halves strict-kebab.
MIRROR_ANCHOR_RE = re.compile(
    r"^([a-z][a-z0-9]*(?:-[a-z0-9]+)*)#([A-Za-z][A-Za-z0-9]*(?:-[A-Za-z0-9]+)*)$"
)
POINTER_TOKEN_RE = re.compile(r"→\[([^\[\]\s]+)\]")
# Any visually arrow-like pointer form is scanned; only U+2192 is the
# canonical arrow — a lookalike would be a reader-visible pointer the
# dimension silently ignores (arm finding 9). The lookalike class is
# the Unicode ARROW BLOCKS, not a hand-picked list (arm re-run
# finding 2: a finite set missed U+279D): Arrows, Supplemental
# Arrows-A/-B/-C, Miscellaneous Symbols and Arrows, and the dingbat
# arrow range.
POINTER_ARROWISH_RE = re.compile(
    r"([←-⇿➔-➾⟰-⟿⤀-⥿"
    r"⬀-⯿\U0001f800-\U0001f8ff])\[([^\[\]\s]+)\]"
)
MIRROR_HEADING_RE = re.compile(r"^(#{2,6})\s+(.*)$")


def _mirror_kebab(s: str) -> str:
    out = re.sub(r"[^a-z0-9]+", "-", s.lower()).strip("-")
    return re.sub(r"-{2,}", "-", out)
C_ROW_RE = re.compile(r"^\|\s*(C\d+)\s*\|")
DRAFT_NAME_RE = re.compile(r"^(ch\d+)-([a-z0-9-]+)-contract\.md$")
DATE_RE = re.compile(r"^\d{4}-\d{2}-\d{2}$")


def is_calendar_date(value: object) -> bool:
    """D3/D8.3: a declared date is a REAL day, not merely a
    YYYY-MM-DD-SHAPED string. The regex alone admits 2026-13-40 and
    2026-02-29 — a mistyped month or a copy-pasted leap day is the
    accident this rule exists for, and a date that cannot be reached
    on a calendar is unusable as the audit stamp it claims to be."""
    if not isinstance(value, str) or not DATE_RE.match(value):
        return False
    try:
        datetime.date.fromisoformat(value)
    except ValueError:
        return False
    return True
COMMIT_RE = re.compile(r"^[0-9a-f]{7,40}$")
SHA256_RE = re.compile(r"^[0-9a-f]{64}$")
        checker.error(
            f"--post-build: '{commit}' is a merge commit — audit the packet's "
            f"own single-parent build commit (one-commit rule)"
        )
        return
    # --root: a ROOT commit otherwise yields an EMPTY change list — the
    # same vacuous-audit class as the merge-commit hole above; with
    # --root it diffs against the empty tree and lists every file.
    out = subprocess.run(
        ["git", "-C", str(root), "diff-tree", "--root", "--no-commit-id", "--name-only", "-r", commit],
        capture_output=True,
        text=True,
    )
    if out.returncode != 0:
        checker.error(f"--post-build: git diff-tree failed for '{commit}': {out.stderr.strip()}")
        return
    changed = {line.strip() for line in out.stdout.splitlines() if line.strip()}
    # The vacuous-audit family, closed at the SINK: whatever produced an
    # empty change list (--allow-empty, a wrong sha, a git surprise),
    # a packet build commit lands at least the packet file itself
    # (one-commit rule), so an empty audit proves nothing.
    if not changed:
        checker.error(
            f"--post-build: commit {commit} has an EMPTY change list — a packet "
            f"build commit lands at least the packet file itself, so this audit "
            f"proves nothing (wrong sha or empty commit)"
        )
        return
    # …and the same invariant POSITIVELY: the empty-check rationale
    # already stated it, but only checking emptiness let a code-only or
    # follow-up commit green-light the audit (round 9, reproduced).
    if frame_rel not in changed:
        checker.error(
            f"--post-build: commit {commit} does not change the packet file "
            f"'{frame_rel}' — the build commit lands the packet WITH the build "
            f"(one-commit rule), so this is the wrong commit to audit"
        )
        return
    outside = sorted(changed - allowed)
    if outside:
        checker.error(
            f"{packet_path.name}: commit {commit} touches files OUTSIDE the declared "
            f"mutation boundary: {outside}"
        )


# ---------------------------------------------------------------- runner


def collect_drafts(
    contracts_dir: Path, checker: Checker
) -> tuple[dict[str, dict], list[str], list[str]]:
    """(drafts by slug, reopened names, superseded names) — D6 and D8.9
    both want the NAMES in the summary."""
    drafts: dict[str, dict] = {}
    reopened: list[str] = []
    superseded: list[str] = []
    if not contracts_dir.is_dir():
        return drafts, reopened, superseded
    for path in sorted(contracts_dir.glob("*.md")):
        if path.name == "README.md":
            continue
        info = check_draft(path, checker)
        if info is not None:
            if info["status"] == "reopened":
                reopened.append(path.name)
            elif info["status"] == "superseded":
                superseded.append(path.name)
            m = DRAFT_NAME_RE.match(path.name)
            if m:
                drafts[f"{m.group(1)}-{m.group(2)}"] = info
    return drafts, reopened, superseded


def lint(
    packets_dir: Path,
    contracts_dir: Path,
    adr_dir: Path,
    forbid_reopened: bool = False,
) -> tuple[list[str], dict[str, int]]:
    """The full lint pass as data — the selftest asserts on the error
    LIST (per-dim message substrings), run_lint wraps it for the CLI."""
    checker = Checker()
    # ADR-015 missing-directory guard: a nonexistent directory would
    # lint EMPTY with exit 0 — green while checking nothing (exactly
    # the false-green shape of a checker pointed at a retired home).
    if not packets_dir.is_dir():
        checker.error(
            f"packets directory missing: {packets_dir} — an empty lint proves "
            f"nothing; a nonexistent directory is an error, never a pass"
        )
    if not contracts_dir.is_dir():
        checker.error(
            f"contracts directory missing: {contracts_dir} — an empty lint "
            f"proves nothing; a nonexistent directory is an error, never a pass"
        )
    drafts, reopened, superseded = collect_drafts(contracts_dir, checker)
    # D8: superseded deliberately does NOT join this gate — it is a
    # PERMANENT resting state, so gating on it would red every future
    # approve, close and flip. Its enforcement is anchor-side.
    if forbid_reopened and reopened:
        checker.error(
            f"--forbid-reopened: reopened draft(s) present: {reopened} — the "
            f"zero-reopened gate (approve / chapter close / flip) requires none"
        )
    v2 = grandfathered = 0
    for path in sorted(packets_dir.glob("*.md")):
        if path.name == "README.md":
            continue
        if check_packet(path, drafts, adr_dir, checker):
            v2 += 1
        else:
            grandfathered += 1
    stats = {
        "v2": v2,
        "grandfathered": grandfathered,
        "drafts": len(drafts),
        "reopened": reopened,  # the NAMES — D6 says the summary lists them
        "superseded": superseded,  # …and D8.9 says the same for these
    }
    return checker.errors, stats


def run_lint(
    packets_dir: Path,
    contracts_dir: Path,
    adr_dir: Path,
    forbid_reopened: bool = False,
) -> int:
    errors, stats = lint(packets_dir, contracts_dir, adr_dir, forbid_reopened=forbid_reopened)
    for msg in errors:
        print(f"packet-lint FAIL: {msg}", file=sys.stderr)
    reopened = stats["reopened"]
    reopened_note = f"{len(reopened)} reopened" + (f": {', '.join(reopened)}" if reopened else "")
    superseded = stats["superseded"]
    superseded_note = f"{len(superseded)} superseded" + (
        f": {', '.join(superseded)}" if superseded else ""
    )
    print(
        f"packet-lint: {stats['v2']} v2 packet(s) linted, "
        f"{stats['grandfathered']} pre-v2 grandfathered, "
        f"{stats['drafts']} draft(s) linted "
        f"({reopened_note}, {superseded_note}), "
        f"{len(errors)} error(s)"
    )
    return 1 if errors else 0


# ---------------------------------------------------------------- selftest
        _git_ok(root, "commit", "-q", "-m", "second-block")
        errors, _ = lint(root / "packets", root / "contracts", ADR_DIR)
        assert_red("ratification-dates-decreasing", errors, "dates decrease in document order")
        tmp.cleanup()

    _dates_decreasing_fixture()

    # ---- D3/D5b the schema lock (user-ratified 2026-08-08)
    def _schema_lock_fixture(name: str, schema_json, write_file: bool, substr: str | None) -> None:
        """A green draft whose latest block carries a schema lock over a
        fixture declaration file; `schema_json` is a callable taking the
        file's true sha256 and returning the block's schema object text."""
        fixture = build_green_fixture()
        if fixture is None:
            failures.append(f"selftest fixture setup failed: {name}")
            return
        tmp, root, draft, _packet, green = fixture
        decl = root / "schema-fixture.ts"
        if write_file:
            decl.write_text("export const declaration = { frozen: true };\n", encoding="utf-8")
        true_sha = hashlib.sha256(decl.read_bytes()).hexdigest() if write_file else "0" * 64
        locked = green.replace(
            '"arms": ["a", "b"], "commit"',
            f'"arms": ["a", "b"], "schema": {schema_json(true_sha)}, "commit"',
        )
        draft.write_text(locked, encoding="utf-8")
        errors, _ = lint(root / "packets", root / "contracts", ADR_DIR)
        if substr is None:
            expect_green(name, errors)
        else:
            assert_red(name, errors, substr)
        tmp.cleanup()

    _schema_lock_fixture(
        "schema-lock-bad-keyset",
        lambda sha: '{"path": "schema-fixture.ts"}',
        True,
        "schema must be an object with exactly keys",
    )
    _schema_lock_fixture(
        "schema-lock-bad-sha-shape",
        lambda sha: '{"path": "schema-fixture.ts", "sha256": "xyz"}',
        True,
        "64-character lowercase-hex sha256",
    )
    _schema_lock_fixture(
        "schema-lock-path-missing",
        lambda sha: f'{{"path": "no-such-file.ts", "sha256": "{sha}"}}',
        True,
        "does not exist in the working tree",
    )
    _schema_lock_fixture(
        "schema-lock-hash-mismatch",
        lambda sha: f'{{"path": "schema-fixture.ts", "sha256": "{"f" * 64}"}}',
        True,
        "bytes differ from the recorded sha256",
    )
    _schema_lock_fixture(
        "schema-lock-green",
        lambda sha: f'{{"path": "schema-fixture.ts", "sha256": "{sha}"}}',
        True,
        None,
    )

    def _schema_lock_symlink_escape() -> None:
        # round-1 F2: a symlink inside the root pointing OUTSIDE it must

exec
/bin/zsh -lc "rg -n '"'^class Checker|''^def git_root_and_rel'"' tools/v3-plan/check_packet.py; sed -n '395,420p' tools/v3-plan/check_packet.py; sed -n '620,655p' tools/v3-plan/check_packet.py" in /Users/felho/dev/pairflow
 succeeded in 0ms:
494:class Checker:
596:def git_root_and_rel(path: Path) -> tuple[Path, str] | None:
# canonical arrow — a lookalike would be a reader-visible pointer the
# dimension silently ignores (arm finding 9). The lookalike class is
# the Unicode ARROW BLOCKS, not a hand-picked list (arm re-run
# finding 2: a finite set missed U+279D): Arrows, Supplemental
# Arrows-A/-B/-C, Miscellaneous Symbols and Arrows, and the dingbat
# arrow range.
POINTER_ARROWISH_RE = re.compile(
    r"([←-⇿➔-➾⟰-⟿⤀-⥿"
    r"⬀-⯿\U0001f800-\U0001f8ff])\[([^\[\]\s]+)\]"
)
MIRROR_HEADING_RE = re.compile(r"^(#{2,6})\s+(.*)$")


def _mirror_kebab(s: str) -> str:
    out = re.sub(r"[^a-z0-9]+", "-", s.lower()).strip("-")
    return re.sub(r"-{2,}", "-", out)
C_ROW_RE = re.compile(r"^\|\s*(C\d+)\s*\|")
DRAFT_NAME_RE = re.compile(r"^(ch\d+)-([a-z0-9-]+)-contract\.md$")
DATE_RE = re.compile(r"^\d{4}-\d{2}-\d{2}$")


def is_calendar_date(value: object) -> bool:
    """D3/D8.3: a declared date is a REAL day, not merely a
    YYYY-MM-DD-SHAPED string. The regex alone admits 2026-13-40 and
    2026-02-29 — a mistyped month or a copy-pasted leap day is the
    accident this rule exists for, and a date that cannot be reached
            ["git", "-C", str(root), "merge-base", "--is-ancestor", commit, migration_parent],
            capture_output=True,
        ).returncode
        == 0
    )


def show_at_commit(
    root: Path, commit: str, rel: str, migration_parent: str | None
) -> tuple[str, str] | None:
    """`git show <commit>:<rel>` with the ADR-015 legacy-path alias.
    Returns (frame_rel, text) — frame_rel is whichever rel succeeded
    and becomes the audit frame's coordinate system — or None when
    every permitted path fails."""
    out = subprocess.run(
        ["git", "-C", str(root), "show", f"{commit}:{rel}"],
        capture_output=True,
        text=True,
    )
    if out.returncode == 0:
        return rel, out.stdout
    if rel.startswith(LEGACY_HOME_NEW) and _legacy_alias_permitted(root, commit, migration_parent):
        legacy = LEGACY_HOME_OLD + rel[len(LEGACY_HOME_NEW):]
        out = subprocess.run(
            ["git", "-C", str(root), "show", f"{commit}:{legacy}"],
            capture_output=True,
            text=True,
        )
        if out.returncode == 0:
            return legacy, out.stdout
    return None


# ---------------------------------------------------------------- drafts



exec
/bin/zsh -lc "sed -n '488,510p' tools/v3-plan/check_packet.py; sed -n '585,620p' tools/v3-plan/check_packet.py" in /Users/felho/dev/pairflow
 succeeded in 0ms:
    "arm-approve",
    "arm-build-close",
}
PROVENANCE_KEYS = {"anchored", "derived", "new_decision"}


class Checker:
    def __init__(self) -> None:
        self.errors: list[str] = []
        self.notes: list[str] = []

    def error(self, msg: str) -> None:
        self.errors.append(msg)

    def note(self, msg: str) -> None:
        self.notes.append(msg)


def _reject_duplicate_keys(pairs: list[tuple[str, object]]) -> dict:
    """json.loads silently keeps the LAST duplicate key — in declared
    machine data that is a silent reinterpretation (the reader and the
    tool may disagree on which value holds), so it is a parse error."""
    seen: dict = {}
                checker.error(
                    f"{path_name}: the '{key}' block carries sibling top-level "
                    f"key(s) {siblings} — the record must be the block's ONLY "
                    f"top-level key, or the siblings ride along unread"
                )


def strip_fences(text: str) -> str:
    return _scan_fences(text)[0]


def git_root_and_rel(path: Path) -> tuple[Path, str] | None:
    top = subprocess.run(
        ["git", "-C", str(path.parent), "rev-parse", "--show-toplevel"],
        capture_output=True,
        text=True,
    )
    if top.returncode != 0:
        return None
    root = Path(top.stdout.strip())
    try:
        return root, path.resolve().relative_to(root).as_posix()
    except ValueError:
        return None


def _legacy_alias_permitted(root: Path, commit: str, migration_parent: str | None) -> bool:
    """The ADR-015 ancestry bound: the legacy retry fires ONLY when the
    audited commit is an ancestor of (or equal to) the recorded
    migration parent — true for every commit the history audits can
    legitimately reach, false for every future commit."""
    if migration_parent is None:
        return False
    return (
        subprocess.run(
            ["git", "-C", str(root), "merge-base", "--is-ancestor", commit, migration_parent],

exec
/bin/zsh -lc "python3 - <<'PY'
import hashlib, importlib.util, json, os, subprocess, tempfile
from pathlib import Path

spec = importlib.util.spec_from_file_location('cp', 'tools/v3-plan/check_packet.py')
cp = importlib.util.module_from_spec(spec); spec.loader.exec_module(cp)
valid = {'date':'2026-08-08','arms':['qa'],'commit':'20b73f6a'}
shape_cases = {
  'dotted_filename': 'schema..fixture.ts',
  'head_parent': '../x.ts',
  'middle_parent': 'sub/../x.ts',
  'trailing_parent': 'sub/..',
  'exact_parent': '..',
  'backslash_parent_text': r'sub\\..\\x.ts',
  'double_slash': 'sub//x.ts',
  'unicode_division_slash': 'sub∕..∕x.ts',
}
print('SHAPE')
for name, path in shape_cases.items():
    c=cp.Checker(); block={**valid,'schema':{'path':path,'sha256':'0'*64}}
    cp.check_ratification_shape('fixture.md',0,block,c)
    path_errors=[e for e in c.errors if '.schema.path ' in e]
    print(f'{name}: {\"RED\" if path_errors else \"GREEN\"} | {path_errors[0] if path_errors else \"0 path-shape errors\"}')

with tempfile.TemporaryDirectory(dir='/tmp', prefix='schema-lock-r2-') as td:
    logical_base = Path(td)
    root = logical_base/'repo'; root.mkdir(); (root/'contracts').mkdir()
    subprocess.run(['git','-C',str(root),'init','-q'], check=True)
    draft = root/'contracts'/'ch1-fixture-contract.md'; draft.write_text('fixture\\n')
    outside = logical_base/'outside.ts'; outside.write_text('outside\\n')
    inside = root/'inside.ts'; inside.write_text('inside\\n')
    (root/'dir').mkdir(); (root/'dir'/'nested.ts').write_text('nested\\n')
    (root/'schema..fixture.ts').write_text('dotted\\n')
    (root/r'sub\\..\\x.ts').write_text('backslash\\n')
    (root/'sub∕..∕x.ts').write_text('unicode\\n')
    (root/'link-inside-abs.ts').symlink_to(inside)
    (root/'link-inside-rel.ts').symlink_to('inside.ts')
    (root/'link-outside-abs.ts').symlink_to(outside)
    (root/'link-outside-rel.ts').symlink_to(Path('..')/'outside.ts')
    (root/'escape-dir').symlink_to(logical_base, target_is_directory=True)
    cases = {
      'dotted_regular': ('schema..fixture.ts', root/'schema..fixture.ts', 'GREEN'),
      'inside_symlink_absolute': ('link-inside-abs.ts', inside, 'GREEN'),
      'inside_symlink_relative': ('link-inside-rel.ts', inside, 'GREEN'),
      'outside_symlink_absolute': ('link-outside-abs.ts', outside, 'RED'),
      'outside_symlink_relative': ('link-outside-rel.ts', outside, 'RED'),
      'escaping_intermediate_dir': ('escape-dir/outside.ts', outside, 'RED'),
      'backslash_literal_filename': (r'sub\\..\\x.ts', root/r'sub\\..\\x.ts', 'GREEN'),
      'unicode_slash_literal_filename': ('sub∕..∕x.ts', root/'sub∕..∕x.ts', 'GREEN'),
    }
    print('RESOLUTION')
    for name,(path,target,expected) in cases.items():
        sha=hashlib.sha256(target.read_bytes()).hexdigest()
        c=cp.Checker(); cp.check_schema_lock(draft, {'path':path,'sha256':sha}, c)
        actual='RED' if c.errors else 'GREEN'
        msg=c.errors[0] if c.errors else '0 errors'
        print(f'{name}: {actual} expected={expected} | {msg}')
    located=cp.git_root_and_rel(draft)
    print(f'SYMLINKED_TMP_ROOT: input={root} resolved_input={root.resolve()} git_root={located[0] if located else None} resolved_git_root={located[0].resolve() if located else None}')
PY" in /Users/felho/dev/pairflow
 succeeded in 17ms:
SHAPE
dotted_filename: GREEN | 0 path-shape errors
head_parent: RED | fixture.md: ratification[0].schema.path must be a nonempty repo-relative path
middle_parent: RED | fixture.md: ratification[0].schema.path must be a nonempty repo-relative path
trailing_parent: RED | fixture.md: ratification[0].schema.path must be a nonempty repo-relative path
exact_parent: RED | fixture.md: ratification[0].schema.path must be a nonempty repo-relative path
backslash_parent_text: GREEN | 0 path-shape errors
double_slash: GREEN | 0 path-shape errors
unicode_division_slash: GREEN | 0 path-shape errors
RESOLUTION
dotted_regular: GREEN expected=GREEN | 0 errors
inside_symlink_absolute: GREEN expected=GREEN | 0 errors
inside_symlink_relative: GREEN expected=GREEN | 0 errors
outside_symlink_absolute: RED expected=RED | ch1-fixture-contract.md: schema lock path 'link-outside-abs.ts' resolves outside the draft's repository — the lock binds the draft's own working tree bytes
outside_symlink_relative: RED expected=RED | ch1-fixture-contract.md: schema lock path 'link-outside-rel.ts' resolves outside the draft's repository — the lock binds the draft's own working tree bytes
escaping_intermediate_dir: RED expected=RED | ch1-fixture-contract.md: schema lock path 'escape-dir/outside.ts' resolves outside the draft's repository — the lock binds the draft's own working tree bytes
backslash_literal_filename: GREEN expected=GREEN | 0 errors
unicode_slash_literal_filename: GREEN expected=GREEN | 0 errors
SYMLINKED_TMP_ROOT: input=/tmp/schema-lock-r2-biueotsd/repo resolved_input=/private/tmp/schema-lock-r2-biueotsd/repo git_root=/private/tmp/schema-lock-r2-biueotsd/repo resolved_git_root=/private/tmp/schema-lock-r2-biueotsd/repo

exec
/bin/zsh -lc "node -e \"const p=require('./package.json'); console.log('v3:packet-lint='+p.scripts['v3:packet-lint'])\"; python3 tools/v3-plan/check_packet.py --selftest; python3 tools/v3-plan/check_packet.py" in /Users/felho/dev/pairflow
 succeeded in 10288ms:
selftest: 140 red dims exercised, green fixture + reopen choreography + fenced-noise greens pass, 0 failure(s)
packet-lint: 26 v2 packet(s) linted, 16 pre-v2 grandfathered, 6 draft(s) linted (0 reopened, 1 superseded: ch13-context-block-contract.md), 0 error(s)

codex
A neighbourhood-mátrix minden elvárt ágat hozott: a négy valódi `..` szegmens shape-red; a pontozott fájlnév green; a POSIX-on literális backslash/Unicode elválasztó green; belső abszolút és relatív symlink green; külső abszolút/relatív és symlinkelt köztes könyvtár a kért üzenettel red. A `/tmp`→`/private/tmp` feloldás nem okozott hamis escape-et. Még lefuttatom a kanonikus package parancsot, majd a záró worktree/hash guardot.
exec
/bin/zsh -lc "pnpm v3:packet-lint; date '+END %Y-%m-%d %H:%M:%S %z'; git rev-parse --short=12 HEAD; shasum -a 256 tools/v3-plan/check_packet.py; git status --short --branch; git diff --exit-code 20b73f6a -- tools/v3-plan/check_packet.py; git diff --name-status 20b73f6a"'^ 20b73f6a' in /Users/felho/dev/pairflow
 succeeded in 10680ms:

> @pairflow/cli@0.2.0 v3:packet-lint /Users/felho/dev/pairflow
> python3 ./tools/v3-plan/check_packet.py --selftest && python3 ./tools/v3-plan/check_packet.py

selftest: 140 red dims exercised, green fixture + reopen choreography + fenced-noise greens pass, 0 failure(s)
packet-lint: 26 v2 packet(s) linted, 16 pre-v2 grandfathered, 6 draft(s) linted (0 reopened, 1 superseded: ch13-context-block-contract.md), 0 error(s)
END 2026-08-08 15:54:33 +0200
0d58283eeb1a
367a598eb6cfdb2b7994ae7cab04d9d2b29170f59da0b5a06d9169b9d9bdbad0  tools/v3-plan/check_packet.py
## main...origin/main [ahead 119]
M	tools/v3-plan/check_packet.py

codex
HEAD: `0d58283eeb1a` — lint bytes: `20b73f6a`, hash `367a598eb6cfdb2b`

Counts: IN-SCOPE 0 · CARRIED-SCOPE 0 · UNRUN 0

Neighbourhoods: full

Selftest: 140 red dims + new green, 0 failures

Live lint: 6 drafts, 0 errors

A két fold tart.
tokens used
63 736
HEAD: `0d58283eeb1a` — lint bytes: `20b73f6a`, hash `367a598eb6cfdb2b`

Counts: IN-SCOPE 0 · CARRIED-SCOPE 0 · UNRUN 0

Neighbourhoods: full

Selftest: 140 red dims + new green, 0 failures

Live lint: 6 drafts, 0 errors

A két fold tart.
