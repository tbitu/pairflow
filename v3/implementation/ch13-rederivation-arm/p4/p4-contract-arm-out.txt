OpenAI Codex v0.146.0
--------
workdir: /Users/felho/dev/pairflow
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 019fe240-fa5a-7fc1-a57b-5f318bea0b92
--------
user
# QA review charter — the ch13 contract-v2 draft, on the approve-ready bytes

You are a fresh-context reviewer with full repo access at the repo
root. Read everything yourself; do not trust any summary over the
bytes. This is the external gate before a human ratification act: your
job is FALSIFICATION — find what the three panel rounds and the clean
close missed.

## 0. READ THIS FIRST — the time budget

**A HARD 20-minute wall-clock budget, enforced by an external process
kill; unwritten findings are lost.** Write each finding IN FULL the
moment you confirm it; stop investigating at ~12 minutes;
over-2-minute questions go down as `unrun` with one sentence.

## 1. The review object (verify the hashes FIRST; a mismatch voids your verdict)

| File | sha256 (first 16) |
|---|---|
| `v3/implementation/contracts/ch13-context-block-v2-contract.md` | `81f1daa9ac04a393` |
| `v3/src/definition/schema/templateFormat.ts` | `9368e5253d1163fa` |

HEAD `dc821f5b`. The contract is a DRAFT awaiting human ratification;
the ratifying act will byte-bind BOTH files (the contract-draft
template's schema lock). Decision sources you may consult: the
SUPERSEDED `ch13-context-block-contract.md` (frozen decisions; its
wording is dead), `v3/adr/ADR-019-*.md` (D4/D10/D11),
`v3/implementation/ch13-rederivation-arm/p4/` (probe receipts,
review record), `v3/implementation/plan.md` §13,
`v3/model/` (the model plane).

## 2. The claims this line makes (your falsification targets)

1. **Zero unmeasured claims:** every measurable statement in the rows
   carries an executed receipt (the PROBE-P4-* files) or a DERIVED
   marker with a named measurer. Hunt for a measured-shaped claim with
   neither.
2. **Zero structural restatement:** structural rules live ONLY in the
   declaration; rows cite tags and state decisions. Hunt for a row
   normatively carrying an attribute (grammar, keyset, default,
   message, grain) the declaration owns.
3. **Inheritance without loss:** the superseded contract's 19 rows +
   4 reopen decisions all live on (carried / schema-covered /
   deliberately dropped with reason) per the review record's §9 table.
   Spot-check 5 old rows of your choosing INDEPENDENTLY against the
   table's verdicts.
4. **The declaration is sound:** the ch13v2-rowed nodes compose with
   the engine (probe via `pnpm exec tsx` from /tmp against
   `v3/src/definition/schema/`), and the four DECIDED-HERE markers'
   receipts reproduce.
5. **Channel behaviour:** the stated symmetry scope (authored
   positions identical; `capabilityProfile` and the P5-declared
   produced positions as the named asymmetries).

## 3. Rules

Threat model: OMISSION and INVENTION against the decision sources,
plus any defect in the draft or declaration bytes. Findings outside:
CARRIED-SCOPE, one line. Do not propose fixes. Do NOT modify the
repository — not one byte; scratch under /tmp; a guard runs before and
after. Vocabulary: routine software engineering (conformance,
counterexample, sensitivity check). Run any suite ALONE, never
concurrently.

## 4. Finding format

```
### <CLASS> <n> — <one-line title>
CLASS: IN-SCOPE | CARRIED-SCOPE | UNRUN
WHAT / WHERE / EVIDENCE (exact command + output) / EXPECTED
```

## 5. Closing message

Only: HEAD + both hashes as YOU measured them; counts IN-SCOPE /
CARRIED-SCOPE / UNRUN; which of the five claim targets you exercised
fully/partially; the gates you ran (`pnpm v3:packet-lint`,
`pnpm v3:typecheck`, `pnpm v3:test` — alone); and ONE sentence: is
this draft ready for a human ratification act?

codex
A hash- és HEAD-ellenőrzéssel kezdek; eltérés esetén a charter szerint azonnal érvénytelen a verdict. A repót nem módosítom, minden scratch munka `/tmp` alatt marad.
exec
/bin/zsh -lc 'git rev-parse --short=8 HEAD && shasum -a 256 v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts && git status --short' in /Users/felho/dev/pairflow
 succeeded in 0ms:
dc821f5b
81f1daa9ac04a393e8a7d31d8111778430e3166d89b8e9ee0294c47c6269fe7d  v3/implementation/contracts/ch13-context-block-v2-contract.md
9368e5253d1163fa0f6ffa18290fef5ef4007de4f293862eeee522c1b7ea3c49  v3/src/definition/schema/templateFormat.ts

exec
/bin/zsh -lc 'wc -l v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts v3/implementation/contracts/ch13-context-block-contract.md v3/implementation/plan.md && rg --files v3/adr v3/implementation/ch13-rederivation-arm/p4 v3/model | sort' in /Users/felho/dev/pairflow
 succeeded in 0ms:
     171 v3/implementation/contracts/ch13-context-block-v2-contract.md
     683 v3/src/definition/schema/templateFormat.ts
     380 v3/implementation/contracts/ch13-context-block-contract.md
    2994 v3/implementation/plan.md
    4228 total
v3/adr/ADR-000-record-implementation-decisions-as-adrs.md
v3/adr/ADR-001-code-home-package-topology-module-boundaries.md
v3/adr/ADR-002-language-and-tooling.md
v3/adr/ADR-003-storage-substrate-and-migration-stance.md
v3/adr/ADR-004-op-id-scheme-per-operation-family.md
v3/adr/ADR-005-testkit-module.md
v3/adr/ADR-006-sqlite-driver-node-sqlite.md
v3/adr/ADR-007-drift-test-module.md
v3/adr/ADR-008-transcript-digest-form.md
v3/adr/ADR-009-operator-cli-module-and-dev-boundary.md
v3/adr/ADR-010-diag-store-separate-sqlite-file.md
v3/adr/ADR-011-definition-module.md
v3/adr/ADR-012-yaml-runtime-dependency.md
v3/adr/ADR-013-gates-module-and-registry.md
v3/adr/ADR-014-runtime-core-module-homes-and-provider-seam.md
v3/adr/ADR-015-v3-plane-consolidation.md
v3/adr/ADR-016-delivery-errand-ledger-shape.md
v3/adr/ADR-017-spawn-confinement-boundary.md
v3/adr/ADR-018-sys-reason-token-namespace.md
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md
v3/adr/README.md
v3/adr/_template.md
v3/adr/check.sh
v3/implementation/ch13-rederivation-arm/p4/arm-round1-d11-out.txt
v3/implementation/ch13-rederivation-arm/p4/arm-round1-d11.md
v3/implementation/ch13-rederivation-arm/p4/arm-round1-schemalock-out.txt
v3/implementation/ch13-rederivation-arm/p4/arm-round1-schemalock.md
v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11-out.txt
v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11.md
v3/implementation/ch13-rederivation-arm/p4/arm-round2-schemalock-out.txt
v3/implementation/ch13-rederivation-arm/p4/arm-round2-schemalock.md
v3/implementation/ch13-rederivation-arm/p4/never-copy-sweep-out.txt
v3/implementation/ch13-rederivation-arm/p4/never-copy-sweep.py.txt
v3/implementation/ch13-rederivation-arm/p4/p4-dup-resolution-probe-out.txt
v3/implementation/ch13-rederivation-arm/p4/p4-dup-resolution-probe.ts.txt
v3/implementation/ch13-rederivation-arm/p4/p4-flag1-probe-out.txt
v3/implementation/ch13-rederivation-arm/p4/p4-flag1-probe.ts.txt
v3/implementation/ch13-rederivation-arm/p4/p4-live-declaration-probes-out.txt
v3/implementation/ch13-rederivation-arm/p4/p4-live-declaration-probes.ts.txt
v3/implementation/ch13-rederivation-arm/p4/review-record.md
v3/model/_postlude.html
v3/model/_prelude.html
v3/model/code/action-template-config.new.txt
v3/model/code/auto-action-template-config.new.txt
v3/model/code/emit-contract-template-config.new.txt
v3/model/code/l0a-template-config.new.txt
v3/model/code/l0b-template-config.new.txt
v3/model/code/l0c-template-config.new.txt
v3/model/code/l0d-template-config.new.txt
v3/model/code/l0e-template-config.new.txt
v3/model/code/l0f-mode-template-config.new.txt
v3/model/code/l0f-project-config.new.txt
v3/model/code/l0f-template-config.new.txt
v3/model/code/l2-template-config.new.txt
v3/model/code/l2a-template-config.new.txt
v3/model/code/l2b-template-config.new.txt
v3/model/code/l3-template-config.new.txt
v3/model/code/l4-parent-template.new.txt
v3/model/code/l5-template-config.new.txt
v3/model/code/release-template-config.new.txt
v3/model/core-model-future-topic.md
v3/model/core-model-todo.md
v3/model/core-model.html
v3/model/deltas/action-pseudocode.json
v3/model/deltas/auto-action-pseudocode.json
v3/model/deltas/complete-pseudocode.json
v3/model/deltas/emit-contract-pseudocode.json
v3/model/deltas/l0a-pseudocode.json
v3/model/deltas/l0b-pseudocode.json
v3/model/deltas/l0c-pseudocode.json
v3/model/deltas/l0d-pseudocode.json
v3/model/deltas/l0e-pseudocode.json
v3/model/deltas/l0f-mode-pseudocode.json
v3/model/deltas/l0f-pseudocode.json
v3/model/deltas/l1-pseudocode.json
v3/model/deltas/l2-pseudocode.json
v3/model/deltas/l2a-pseudocode.json
v3/model/deltas/l2b-pseudocode.json
v3/model/deltas/l3-pseudocode.json
v3/model/deltas/l4-pseudocode.json
v3/model/deltas/l5-pseudocode.json
v3/model/deltas/release-pseudocode.json
v3/model/deltas/storage-scope-pseudocode.json
v3/model/ledger.md
v3/model/manifest.json
v3/model/records/absent/archive-purge.json
v3/model/records/absent/auto-workflow-actions.json
v3/model/records/absent/emit-contract.json
v3/model/records/absent/l0a.json
v3/model/records/absent/l0b.json
v3/model/records/absent/l0c.json
v3/model/records/absent/l0d.json
v3/model/records/absent/l0e.json
v3/model/records/absent/l0f-mode.json
v3/model/records/absent/l0f.json
v3/model/records/absent/l1.json
v3/model/records/absent/l2.json
v3/model/records/absent/l2a.json
v3/model/records/absent/l2b.json
v3/model/records/absent/l3.json
v3/model/records/absent/l4-child.json
v3/model/records/absent/l5.json
v3/model/records/absent/runtime-teardown.json
v3/model/records/absent/storage-scope.json
v3/model/records/absent/workflow-actions.json
v3/model/records/invariants/archive-purge.json
v3/model/records/invariants/auto-workflow-actions.json
v3/model/records/invariants/emit-contract.json
v3/model/records/invariants/l0a.json
v3/model/records/invariants/l0b.json
v3/model/records/invariants/l0c.json
v3/model/records/invariants/l0d.json
v3/model/records/invariants/l0e.json
v3/model/records/invariants/l0f-mode.json
v3/model/records/invariants/l0f.json
v3/model/records/invariants/l1.json
v3/model/records/invariants/l2.json
v3/model/records/invariants/l2a.json
v3/model/records/invariants/l2b.json
v3/model/records/invariants/l3.json
v3/model/records/invariants/l4-child.json
v3/model/records/invariants/l5.json
v3/model/records/invariants/runtime-teardown.json
v3/model/records/invariants/storage-scope.json
v3/model/records/invariants/workflow-actions.json
v3/model/sections/01-l0a.html
v3/model/sections/02-l0b.html
v3/model/sections/03-l0c.html
v3/model/sections/04-l0d.html
v3/model/sections/05-l0e.html
v3/model/sections/06-l0f.html
v3/model/sections/07-l1.html
v3/model/sections/08-l2.html
v3/model/sections/09-l2a.html
v3/model/sections/10-l2b.html
v3/model/sections/11-l3.html
v3/model/sections/12-storage-scope.html
v3/model/sections/13-runtime-teardown.html
v3/model/sections/14-workflow-actions.html
v3/model/sections/15-auto-workflow-actions.html
v3/model/sections/16-archive-purge.html
v3/model/sections/17-l0f-mode.html
v3/model/sections/18-l4-child.html
v3/model/sections/19-l5.html
v3/model/sections/20-emit-contract.html
v3/model/units/action-pseudocode/ActionRunner.txt
v3/model/units/action-pseudocode/CREATE_INSTANCE.txt
v3/model/units/action-pseudocode/RECEIVE.txt
v3/model/units/action-pseudocode/RUN_ACTION.txt
v3/model/units/action-pseudocode/action_request.txt
v3/model/units/action-pseudocode/apply_target_entry_effects.txt
v3/model/units/action-pseudocode/boundary_events_of.txt
v3/model/units/action-pseudocode/dispatch_intent.txt
v3/model/units/action-pseudocode/park_for_action.txt
v3/model/units/action-pseudocode/post_commit_output.txt
v3/model/units/action-pseudocode/validate_action_steps.txt
v3/model/units/auto-action-pseudocode/ACTION_RESULT.txt
v3/model/units/auto-action-pseudocode/RECEIVE.txt
v3/model/units/auto-action-pseudocode/action_intent.txt
v3/model/units/auto-action-pseudocode/apply_target_entry_effects.txt
v3/model/units/auto-action-pseudocode/attempt_count_in_episode.txt
v3/model/units/auto-action-pseudocode/dispatch_intent.txt
v3/model/units/auto-action-pseudocode/incoming_recommendation.txt
v3/model/units/auto-action-pseudocode/park_for_action.txt
v3/model/units/auto-action-pseudocode/post_commit_output.txt
v3/model/units/auto-action-pseudocode/validate_action_steps.txt
v3/model/units/auto-action-pseudocode/validate_decision_gates.txt
v3/model/units/complete-pseudocode/DELETE_REQUESTED.txt
v3/model/units/complete-pseudocode/RECEIVE.txt
v3/model/units/complete-pseudocode/archive_or_export.txt
v3/model/units/complete-pseudocode/dispatch_intent.txt
v3/model/units/complete-pseudocode/force_release.txt
v3/model/units/complete-pseudocode/hard_purge.txt
v3/model/units/emit-contract-pseudocode/CREATE_INSTANCE.txt
v3/model/units/emit-contract-pseudocode/HANDLE.txt
v3/model/units/emit-contract-pseudocode/admit_definition.txt
v3/model/units/emit-contract-pseudocode/admit_loaded.txt
v3/model/units/emit-contract-pseudocode/assemble_context_blocks.txt
v3/model/units/emit-contract-pseudocode/dispatch_intent.txt
v3/model/units/emit-contract-pseudocode/emit_contract_of.txt
v3/model/units/emit-contract-pseudocode/handle_help_request.txt
v3/model/units/emit-contract-pseudocode/offerable_ops.txt
v3/model/units/emit-contract-pseudocode/payload_digest.txt
v3/model/units/emit-contract-pseudocode/validate_emit_contract.txt
v3/model/units/emit-contract-pseudocode/validate_emit_contracts.txt
v3/model/units/l0a-pseudocode/HANDLE.txt
v3/model/units/l0b-pseudocode/HANDLE.txt
v3/model/units/l0b-pseudocode/START_INSTANCE.txt
v3/model/units/l0b-pseudocode/dispatch_intent.txt
v3/model/units/l0c-pseudocode/HANDLE.txt
v3/model/units/l0c-pseudocode/START_INSTANCE.txt
v3/model/units/l0c-pseudocode/dispatch_intent.txt
v3/model/units/l0c-pseudocode/resolve_agent_config.txt
v3/model/units/l0d-pseudocode/CANCEL.txt
v3/model/units/l0d-pseudocode/COMPLETE.txt
v3/model/units/l0d-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l0d-pseudocode/FAIL.txt
v3/model/units/l0d-pseudocode/HANDLE.txt
v3/model/units/l0d-pseudocode/KICKOFF.txt
v3/model/units/l0d-pseudocode/RECEIVE.txt
v3/model/units/l0d-pseudocode/RUNTIME_CONTEXT_READY.txt
v3/model/units/l0d-pseudocode/START.txt
v3/model/units/l0d-pseudocode/activate.txt
v3/model/units/l0d-pseudocode/admit_loaded.txt
v3/model/units/l0d-pseudocode/dispatch_intent.txt
v3/model/units/l0e-pseudocode/RUNTIME_CONTEXT_READY.txt
v3/model/units/l0e-pseudocode/RuntimeContextProvider.txt
v3/model/units/l0e-pseudocode/START.txt
v3/model/units/l0e-pseudocode/activate_or_hold.txt
v3/model/units/l0e-pseudocode/dispatch_intent.txt
v3/model/units/l0f-mode-pseudocode/RESOLVE_START.txt
v3/model/units/l0f-mode-pseudocode/apply_mode.txt
v3/model/units/l0f-mode-pseudocode/in_mode.txt
v3/model/units/l0f-mode-pseudocode/resolve_slot.txt
v3/model/units/l0f-mode-pseudocode/valid_mode_declaration.txt
v3/model/units/l0f-pseudocode/RESOLVE_START.txt
v3/model/units/l0f-pseudocode/__preamble__.txt
v3/model/units/l0f-pseudocode/resolve_slot.txt
v3/model/units/l0f-pseudocode/slot_scope.txt
v3/model/units/l1-pseudocode/HANDLE.txt
v3/model/units/l1-pseudocode/RECEIVE.txt
v3/model/units/l1-pseudocode/admit_loaded.txt
v3/model/units/l1-pseudocode/capability.txt
v3/model/units/l1-pseudocode/dispatch_intent.txt
v3/model/units/l1-pseudocode/warrant.txt
v3/model/units/l2-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l2-pseudocode/GateRegistration.txt
v3/model/units/l2-pseudocode/HANDLE.txt
v3/model/units/l2-pseudocode/activate.txt
v3/model/units/l2-pseudocode/admit_definition.txt
v3/model/units/l2-pseudocode/gate_projection.txt
v3/model/units/l2a-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l2a-pseudocode/GateRegistration.txt
v3/model/units/l2a-pseudocode/HANDLE.txt
v3/model/units/l2a-pseudocode/ProcessGateRunner.txt
v3/model/units/l2a-pseudocode/classify_process_result.txt
v3/model/units/l2a-pseudocode/run_process_gate.txt
v3/model/units/l2a-pseudocode/runner_outcome.txt
v3/model/units/l2a-pseudocode/validate_gate_config.txt
v3/model/units/l2b-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l2b-pseudocode/assemble_context_blocks.txt
v3/model/units/l2b-pseudocode/dispatch_intent.txt
v3/model/units/l2b-pseudocode/validate_context_refs.txt
v3/model/units/l3-pseudocode/COMPLETE.txt
v3/model/units/l3-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l3-pseudocode/HANDLE.txt
v3/model/units/l3-pseudocode/RECEIVE.txt
v3/model/units/l3-pseudocode/RESUME_WAIT.txt
v3/model/units/l3-pseudocode/SUBMIT_DECISION.txt
v3/model/units/l3-pseudocode/admit_input.txt
v3/model/units/l3-pseudocode/apply_target_entry_effects.txt
v3/model/units/l3-pseudocode/choice_point.txt
v3/model/units/l3-pseudocode/decision_keys.txt
v3/model/units/l3-pseudocode/directive.txt
v3/model/units/l3-pseudocode/human_decision_request.txt
v3/model/units/l3-pseudocode/incoming_recommendation.txt
v3/model/units/l3-pseudocode/park_for_human_decision.txt
v3/model/units/l3-pseudocode/park_for_wait.txt
v3/model/units/l3-pseudocode/post_commit_output.txt
v3/model/units/l3-pseudocode/required_fields.txt
v3/model/units/l3-pseudocode/validate_decision_gates.txt
v3/model/units/l4-pseudocode/CHILD_LIFECYCLE.txt
v3/model/units/l4-pseudocode/CHILD_SPAWNED.txt
v3/model/units/l4-pseudocode/CHILD_SPAWN_FAILED.txt
v3/model/units/l4-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l4-pseudocode/RECEIVE.txt
v3/model/units/l4-pseudocode/apply_target_entry_effects.txt
v3/model/units/l4-pseudocode/child_intent.txt
v3/model/units/l4-pseudocode/child_lifecycles_of.txt
v3/model/units/l4-pseudocode/dispatch_intent.txt
v3/model/units/l4-pseudocode/emit_child_lifecycle.txt
v3/model/units/l4-pseudocode/is_child_workflow_step.txt
v3/model/units/l4-pseudocode/park_for_child.txt
v3/model/units/l4-pseudocode/post_commit_output.txt
v3/model/units/l4-pseudocode/validate_child_steps.txt
v3/model/units/l5-pseudocode/CREATE_INSTANCE.txt
v3/model/units/l5-pseudocode/HANDLE.txt
v3/model/units/l5-pseudocode/HELP_REPLY.txt
v3/model/units/l5-pseudocode/RECEIVE.txt
v3/model/units/l5-pseudocode/capability.txt
v3/model/units/l5-pseudocode/dispatch_intent.txt
v3/model/units/l5-pseudocode/handle_help_request.txt
v3/model/units/l5-pseudocode/help_exchange_for.txt
v3/model/units/l5-pseudocode/help_request.txt
v3/model/units/l5-pseudocode/post_commit_output.txt
v3/model/units/l5-pseudocode/validate_help_config.txt
v3/model/units/release-pseudocode/CANCEL.txt
v3/model/units/release-pseudocode/COMPLETE.txt
v3/model/units/release-pseudocode/CREATE_INSTANCE.txt
v3/model/units/release-pseudocode/FAIL.txt
v3/model/units/release-pseudocode/RECEIVE.txt
v3/model/units/release-pseudocode/RUNTIME_CONTEXT_RELEASED.txt
v3/model/units/release-pseudocode/RuntimeContextProvider.txt
v3/model/units/release-pseudocode/boundary_events_of.txt
v3/model/units/release-pseudocode/dispatch_intent.txt
v3/model/units/release-pseudocode/errand.txt
v3/model/units/release-pseudocode/initiate_release.txt
v3/model/units/release-pseudocode/release_boundaries.txt
v3/model/units/release-pseudocode/validate_release_policy.txt
v3/model/units/storage-scope-pseudocode/StorageTier.txt
v3/model/units/storage-scope-pseudocode/__preamble__.txt
v3/model/units/storage-scope-pseudocode/evidence_ref_ok.txt
v3/model/units/storage-scope-pseudocode/projection_authoritative.txt
v3/model/units/storage-scope-pseudocode/release_safe.txt

exec
/bin/zsh -lc "sed -n '1,240p' v3/implementation/contracts/ch13-context-block-v2-contract.md
sed -n '1,260p' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md
sed -n '1,300p' v3/implementation/ch13-rederivation-arm/p4/review-record.md
rg -n '"'^## 13|''^### 13|ch13|context block|context-block'"' v3/implementation/plan.md | head -120" in /Users/felho/dev/pairflow
 succeeded in 0ms:
# ch13 — context-block-v2 contract

```json
{"contract_draft": {"chapter": "ch13", "surface": "context-block-v2", "status": "draft"}}
```

## Context (non-normative by declaration)

Successor of `ch13-context-block`, superseded 2026-08-05; inheritance
verified by the P4 completeness pass (review record:
`v3/implementation/ch13-rederivation-arm/p4/`).

**The declaration authority.** This is the first contract authored ON
the ADR-019 schema substrate: every structural rule of this surface
lives as DATA in `v3/src/definition/schema/templateFormat.ts`, and this
contract states DECISIONS and cites those declarations by TAG (ADR-019
D4 — one authority, pointers everywhere else); where a row glosses an
attribute in passing, the gloss is non-binding paraphrase and the
declaration's bytes govern. The ratifying act binds that file's bytes
beside this file's C-rows (the contract-draft template's schema lock)
— and a LATER edit of the declaration (P5's own duties included) turns
the lock red until a re-ratifying act records the new bytes: designed,
not an accident. Citation closure (every tag cited exists; every
ch13v2-rowed node is cited) is checked mechanically at review — no
standing code check exists for it yet; prose restatement is the review
panel's to catch.

**Business invariant.** Context blocks are actor-facing communication:
deterministic prose delivered with the dispatched packet so an actor
hears an operating rule before acting. They influence no verdict and no
kernel decision — enforcement lives elsewhere; this surface only speaks.

**Control model.** A single catalog at template level owns all body text. Three
ref positions point into it by id: role-level and step-level
`promptConcernRefs` inside the agent-config maps, and `contextBlockRefs`
on a gate binding. Authored KEY spellings follow the standing camelCase
rename culture over the model's snake_case names, while model-defined
TOKEN VALUES keep the model's exact spelling — pinned here because the
plan requires this surface to fix the relation explicitly. The declared
schema validates catalog and refs at admission on both channels;
rendering happens at dispatch as a deterministic computation over
admitted inputs.

**Read-path.** Admission reads the authored value — one declaration,
both channels; rendering reads only the admission-produced surfaces
(C13); the run-scope channel is never read (C5).

**Forbidden fallback.** Rendering never drops a ref and never tolerates
a malformed catalog — admission lets neither situation through. No new rejection or issue name: the one issue code is the
ratified `unresolved_context_block_ref`.

**Allowed resolution / missing data.** A template that issues no refs
and declares no catalog is fully legal (every pre-ch13 file). A
template whose refs find no valid entry fails admission with per-site
findings.

**Model-table note (a carried fact; the divergence-stop question is settled).** The model
document's context-block table still shows creation-time rejection on
its ref rows — and the Evidence block itself carries one more
create-mention (its invariant-coverage line's "refs validated at
create" tick) — while the invariant record, the pseudocode reprint and
the same Evidence block's acceptance row all place ref validation at
ADMISSION. Admission governs, as the ch11 sync established.

**Substrate probes (executed 2026-08-08, this authoring; scripts and
outputs preserved in the review record — the P4-1/2/3 sets measured
the pre-adoption substrate through analog surfaces; every claim of
theirs that SURVIVES the adoption was re-executed against the live
ratifying-basis declaration as PROBE-P4-4, the receipt the rows cite
(the pre-D11 halves are inherently pre-adoption measurements and stay
with their own scripts):**
- **PROBE-P4-1** (belt composition): the D10 `validKeysOf` belt at a
  fixed-keyset position reproduces the ratified resolution semantics on
  both channels — a ref to a structurally invalid entry draws the
  entry's own finding AND the per-site coded finding; a wrong-kind or
  absent catalog answers with the empty set, so per-site findings fire
  beside (never suppressed by) the container's own finding; channels
  agree byte-for-byte.
- **PROBE-P4-2** (the plain-map gap → ADR-019 D11): before D11, a ref
  list inside the format-open agent config was unreachable by any lane
  (ghost, numeric and grammar-violating members all passed silently);
  with D11's typed subset the same fixtures produce the declared
  findings, and undeclared sibling keys stay legal open data.
- **PROBE-P4-3** (list-lane interplay + key identity): the engine
  measures every shape-passing occurrence with the membership lane
  (duplicates included), keeps a shape-failing member invisible to
  every list-level lane, and — on the file channel with map-typed keys
  preserved — treats a boolean-keyed entry as unaddressable by the
  string ref of the same spelling (one key-identity class, each lane
  reporting its own finding).
- **PROBE-P4-4** (the live declaration, at the ratifying basis): the
  fail-closed class measured at CLASS WIDTH through `templateFormat`
  itself — present-null, a number, an empty string and a list each
  refuse with the container finding on both channels (present-null
  being the member that drove two of the predecessor line's reopens);
  an absent catalog admits with the empty record materialized; a
  PRESENT-EMPTY catalog with a ref issued fires per site on both
  channels; all three ref positions accumulate in one result; the
  duplicate-ghost and shape-failing-duplicate receipts reproduce; the
  boolean-keyed entry case agrees across channels; the
  channel-GATED `capabilityProfile` key shows its declared asymmetry;
  refs BESIDE a refused catalog draw the container finding AND their
  per-site coded findings, and beside an absent catalog their per-site
  findings, on both channels; an AUTHORED EMPTY ref list admits with
  zero findings; undeclared sibling keys beside the typed field stay
  open data and survive onto the admitted value; a cast-only
  degenerate object admits as the empty record (a function refuses); a grammar-failing key beside a resolving sibling ref
  leaves the ref resolved; the predecessor's key-order measurement
  reproduces on the live engine (quoted integer-like keys hoist and
  re-sort ascending); and the compound CLEAN case — a populated
  catalog, every entry referenced, every ref resolving — admits with
  zero findings (the gate position's clean case needs an injected
  registry and is the P5 fixture family's).

**Decision-home triage (template §1, K0→K4) — v2's new DECIDED-HERE
markers: C1 fail-closed catalog admission, C2 channel-independent key
lane, C7 per-occurrence resolution, C8 shape-failure invisibility.**
All four triage ROW-HOME:
the model shapes none of them (it pins WHEN validation happens —
admission — and says nothing about finding grains or admission-shape
policy), and none carries
beyond-chapter architecture (they are this surface's own lane
semantics, expressed by the one engine's standing behaviour). Every
other row carries decisions already ratified for this surface; their
authority is the predecessor's frozen record, and the old→new mapping
with each divergence's full rationale lives in the review record's
completeness pass — the rows carry the markers and receipts, never the
arguments.

**Carried scope, named:** the realized ch11 contract's prose and map
entries (and one plan line, fixed by this act's aligned edits) still
name the predecessor surface's row id for the binding key — the
pointed-at semantics are carried unchanged by C6, so this is
pointer maintenance, not meaning drift; OWNER: the boundary review
(a route, not a duty of this draft).

**Realization split note.** None exists. Channel scope is an engine
property of the one declaration (source-bearing attributes run where a
source exists); this contract re-ratifies no walk/rung partition.

**Draft metrics (template §5):** rounds to ratify: — · new-decision
rows: 4 DECIDED-HERE markers across 4 rows · post-ratification
reopenings: —

## Contract rows (every normative statement is a C-row)

| ID | Rule |
|---|---|
| C1 | A template's root MAY carry the catalog under the key `contextBlocks` — structural authority `[d-ctxblocks]` (container, key lane, entries) and `[d-root]` (the root keyset's growth by this optional key), with the declared default: an absent catalog admits (the admitted-form surface is C13's). DECIDED HERE (v2): admission of the catalog shape is FAIL-CLOSED — a present value the declared container lane refuses is a container finding and the template does not admit (the authored-value classes measured live: PROBE-P4-4; the accept boundary is the declared lane's own, and a cast-only degenerate object with nothing to enumerate admits as empty — accepted, named, receipt: PROBE-P4-4's degenerate case); the divergence is receipted and argued in the review record's completeness pass. Refs beside a refused or absent catalog still draw their per-site C7 findings (receipt: PROBE-P4-4, both channels) — the belt's broken-operand nature (ADR-019 D10), a construct property no row needs to restate. The root-keyset aggregate-note chain RETIRES at this surface: the declared `[d-root]` fields are the enumerable authority, and the predecessor notes described pre-arrival states — no successor note is owed. |
| C2 | Block ids are ONE namespace with ONE grammar governing the catalog's keys and all ref-list members alike — structural authority `[vc-block-id]` (cited by `[d-block-key]` and `[d-block-ref]`). The namespace is NEW and deliberately distinct from the gate-token and provider-token namespaces; all three ids the model exhibits satisfy it, the digit-bearing one being the grammar's designated positive case (build-driven). A key the grammar refuses draws the key lane's finding at the declared grain — DECIDED HERE (v2): the key lane runs on BOTH channels — strictly fail-louder (receipts: PROBE-P4-4's direct-channel bad-key case, PROBE-P4-3 BAD_KEY_SIBLING; mapping and rationale: the completeness pass). Key-identity on the file channel follows the engine's preserved key types (the boolean-key case: the key lane reports it, and no string ref can address it — each lane reporting its own finding, C9's audit adding its own once realized). |
| C3 | A catalog entry is the body carrier — structural authority `[d-ctx-entry]` / `[d-ctx-body]`. The body is authored static text delivered into the packet without transformation, and no runtime-side or run-supplied channel can write the position (C5 closes the only nearby run-scoped one); its outbound exposure class matches the shipped instruction field's, with prompt shaping owned by the `actor-adapter-rendering` Absent, named not implied. SEMANTIC (no declaration expresses an absence of syntax): the surface defines no interpolation syntax and RESERVES none — brace-styled literal text is ordinary legal prose; any future computed-body syntax and its migration belong to the chapter that takes the `computed-templated-bodies` Absent. |
| C4 | `promptConcernRefs` is the single typed field inside the agent-config value class, which stays format-open in every other key, at both of the template's config positions — the role-level default and the step-level map — structural authority `[d-prompt-refs]` on `[vc-agentconfig]`, value class `[vc-blockidlist]`; the typing basis is ADR-019 D11 (typed subset: every other agent-config key stays legal open data). ch12-C7's delegation clause pre-authorizes this arrival (the refs' value class arriving with its consuming chapter — the aggregate-note convention, so no ch12 row is affected). Absent map, absent key, and a PRESENT EMPTY list: all legal, zero refs (the admitted-form surface is C13's) — the shared value class deliberately declares no nonempty rule, diverging from every other list of the surface (receipt: PROBE-P4-4's empty-authored-list case). ch12-C7's no-field-typed sentence now describes the state before this arrival, per the standing aggregate-note practice — ch12 needs no edit. FORWARD SCOPE: the model also exhibits a MAP-SHAPED, mode-tagged ref member (the mode layer's); today's member class is deliberately string-only, and that member form arrives as D7 format growth with the chapter that takes the mode layer. |
| C5 | Run scope: the `promptConcernRefs` key may appear in a runOverrides entry, but no run-supplied value is checked against the catalog, and rendering never consults it: block emission draws exclusively on the admission-produced surfaces (C13). The packet's block list is therefore the communicated truth, while the merged effective config stays what it always was — recorded run intent in the ch12 cascade. An entry whose only naming mentions arrive at run scope therefore fails C9's audit — dead by design, which C9 and this row jointly guarantee. |
| C6 | The gate-binding ref position: `contextBlockRefs`, legal on ANY binding regardless of evaluator class (carried decision — communication is evaluator-independent) — structural authority `[d-ctx-gate-refs]` on the binding's declaration, value class `[vc-blockidlist]` (identical semantics to C4's position). The binding keyset itself is ch11-C4's realized text; this surface adds no keyset duty (the pointer-maintenance remainder is the Context's named carried scope). |
| C7 | Resolution is ENTRY-BELTED (ADR-019 D10, the construct this surface carried into the vocabulary): every ref member that passed its own shape lane must resolve against the keys of the catalog WHOSE VALUE IS A VALID ENTRY — validity measured by the catalog's own declared entry node, one definition governing both channels; key existence alone is not resolution. The declared lanes run at ADMISSION on both channels (the moment the ch11 sync fixed; the model's invariant record agrees). Structural authority: `[vc-blockidlist]`'s membership rule, carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain, all accumulated. When a ref names an entry the declaration refuses, both facts report — the entry's finding and the coded finding at the ref site — each fixable on its own (PROBE-P4-4). Containment, why C10's dispatch-time miss is integrity drift: nothing the render reads escapes the validated set, and dispatch only ever consumes store-admitted templates. DECIDED HERE (v2): the resolution lane measures EVERY shape-passing occurrence — a duplicated unresolved ref reports per occurrence beside C8's duplicate finding (receipts: PROBE-P4-3 DUP_GHOST, PROBE-P4-4; rationale and old→new mapping: the completeness pass). This row is the refs-validated-at-definition-load invariant's home (disposition test). |
| C8 | Duplicates: a repeated id within one ref list draws the per-occurrence duplicate finding at the declared grain — structural authority `[vc-blockidlist]`'s duplicate rule. DECIDED HERE (v2): a member that failed its OWN shape lane is invisible to every LIST-LEVEL lane — no duplicate finding, no membership finding; its own finding is the trace (receipts: PROBE-P4-3 DUP_BAD_SHAPE, PROBE-P4-4; rationale and mapping: the completeness pass). C9's audit is NOT a list-level lane and is deliberately outside this rule — its reference set reads the raw authored members, as its own row states. The edge family's success negative: the compound CLEAN case — a populated catalog, every entry referenced, every ref resolving — admits with zero findings (receipt: PROBE-P4-4, measured gate-free; the gate position's clean case needs an injected registry, so its member belongs to P5's build-driven fixture family). |
| C9 | Unreferenced-entry hygiene — SEMANTIC (residual R2), realized at P5 as a named lane in code (carrying NO issue-code field — C18's exclusivity governs): a catalog entry no ref names is a validation finding at the entry's path, strict-start (relaxable additively, never tightenable without a breaking change). Carried decisions, all three: WHAT COUNTS AS REFERENCED — every raw member string authored in any ref list whose container held, including members failing the grammar or repeating (a defective mention still names its target; the member's own lanes report the defect). WHAT GETS AUDITED — each key the catalog itself enumerates, unconditionally; no key earns an exemption from the shape of its value (the audit and C7's belt share one enumerable-own-key domain under this engine — the once-accepted asymmetry residual is closed, not carried; the completeness pass records it). WHEN THE CHECK STANDS DOWN — template-wide, whenever any lane that makes a ref list UNREACHABLE fired: among them a ref position's own container lane, the enclosing agent-config container/canonical lanes, and the gate chain's enclosures (binding, pipeline, gates containers) (the audited set is one union over every list, and auditing a partial union would accuse entries whose only mentions sit inside the broken position); a failed CATALOG container needs no stand-down clause — C1 refuses the template outright. |
| C10 | Render order and provenance — SEMANTIC, runtime (realized at the render build): blocks emit by source — the role position first, the step position second, the gate position last — and inside one source the authored list sequence holds. The gate leg walks the admitted gates record in that record's own enumeration, each event's pipeline in its authored sequence, each binding's list likewise, with C11 deciding what may emit. That record enumerates as authored except where the substrate hoists integer-like quoted keys to the front AND re-sorts them ascending among themselves — DERIVED from the recorded substrate measurement PROBE-CB3 (the predecessor's frozen Context holds the record) and RE-EXECUTED on the live engine at this basis (receipt: PROBE-P4-4's key-order case), named measurer: the P5 render build re-executes the probe before pinning order fixtures. An id met a second time emits nothing new — its first position stands — while the provenance list records every emitter as encountered (the dedup-with-retained-provenance invariant's home, disposition test). The admitted-form construction order is thereby a contract-visible property, not a free refactor. Body text comes solely from the admitted catalog; after admission no lookup can fail, so a dispatch-time miss signals kernel-integrity drift and must abort loudly rather than degrade. |
| C11 | The gate-ref predicate — SEMANTIC, runtime: a gate binding may emit its blocks exactly when its transition belongs to both sets: the step's available operations AND the L1 capability narrowing (existing authority logic, nothing minted). The membership half is guaranteed once admission passed — every gates key is one of its step's transitions — so what tests it is the iteration-domain negative: no block may originate outside the dispatched step's own gates. The narrowing half owes its NON-WAIVABLE counterexample: a directly-constructed capability profile that narrows the dispatched role below its transitions must silence that gate's blocks although the event remains available. With default capability derivation the narrowing never bites, which is exactly why this counterexample may not be skipped (the green-but-blind class). The profile's type-level channel is declared at `[d-capability-profile]`. FORWARD SCOPE: the membership half's admission-time guarantee holds only BEFORE the EC layer arrives — that layer changes the operand, and the chapter taking the EC candidate re-examines the half. This row is the authority-scoped-gate-blocks invariant's home (disposition test). |
| C12 | The packet field — SEMANTIC, wire: `ContextPacket.contextBlocks` is ALWAYS present (possibly empty), an ordered list whose members carry the id, the body and the provenance sources, the list sequence being C10's render order, travelling verbatim through the canonical packet channel (object keys canonicalize, array order is semantic). A provenance source member is the model unit's tagged form, its token VALUES model-verbatim snake_case (`role_config` | `step_config` | `gate_binding`) and authored as string literals — this row carries the rename relation's WHOLE normative content: the key half is exhausted by C1/C4/C6 naming their camelCase keys literally plus this row's own packet field name, the token half is this clause; the gate member's location pair is flattened to sibling step/event fields (carried decision). When two bindings of one step and event name the same id, two identical source members appear — provenance is never collapsed. Because the field is unconditional, every full-packet equality fixture in the tree re-pins: one named family, membership enumerated by the owning packet. |
| C13 | The admitted-form surfaces — DERIVED, named measurer: the P5 build's acceptance family plus the parity gate. On admission success the admitted template carries: the catalog record (always present — the declared default covers absence; a present catalog arrives validated, C1 having refused every non-map form); and normalized ref-list fields FOR EVERY ref position (empty list where nothing was authored), produced by the normalizer (ADR-019 D3 — derivation is never validation) as SIBLING fields at the fixed-keyset levels (the roles entry, the step, the admitted binding) — beside, never inside, the raw agent-config maps. TYPE GRAIN (carried decision): the shared raw types mark the normalized fields with `?`, while every admitted VALUE carries them — the standing admitted-form optionality pattern; marking them required was weighed and refused, since it would invalidate each hand-built raw fixture in the tree. The domain type's `contextBlocks?` field already has this shape. CHANNEL GRAIN (the producer-monopoly precedent `advancesRound`, carried WHOLE): the TWO admission-produced positions — the roles entry's and the step's — are DECLARED channel-gated to the direct channel at P5, while the binding's `contextBlockRefs` is C6's authored key and stays channel-both; a caller holding an admitted value thereby re-admits it and a FILE author meets the unknown-key refusal, AND the fill RECOMPUTES each produced position from its authored source, overwriting any caller-supplied value — the monopoly half, which is what keeps C7's containment true on the direct channel. FIELD SPELLINGS, decided here for P5 to declare: the normalized fields carry their authored sources' names — `promptConcernRefs` as the sibling at the roles entry and the step, `contextBlockRefs` on the binding. Realization needs a NEW normalizer hook (D7 format growth landing at P5's own act, never silently), the ref fill running BEFORE the binding rebuild so an unauthored list lands as the empty list, not an undefined carry; and the `[n-effective-config]` hook's carry list must grow `contextBlockRefs`, or the rebuild drops the authored refs — the carry list plus the hook's produced field is the admitted binding's whole keyset. Two named build traps: admission rebuilds only `steps` today (the roles side is new work), and the role-side source is NESTED inside the config map (the first nested-source normalization). P5 also flips the three l2b domain-registry rows (the catalog and ContextBlockRef rows with the definition packet, carrying their realized type names — `BlockId`, the catalog record type; the ContextBlock row with the render side, its witness the packet's rendered block member type) — named here exactly BECAUSE omitting the flip trips no gate; a realized row needs its type name AND the registry's two sync surfaces, and if P5 ships as one packet the assignment collapses onto it unchanged. Rendering consumes these admitted surfaces and nothing rawer, never re-validating at dispatch; the catalog's entry type is the ONLY typed place body text can live (this row homes the single-body-source invariant, disposition type/schema). The raw config maps retain the authored key untouched for the ch12 cascade to read (C5). The ROLE-ENTRY and STEP keysets do not widen for FILE authors — the normalized positions are admission-produced, and a file authoring one meets the unknown-key refusal, while the direct channel admits them under the channel grain above; the binding's `contextBlockRefs` is the stated exclusion: there the AUTHORED key (C6) is itself the normalized position's source and is carried, never refused. |
| C14 | Communication-only, byte-scope precise — SEMANTIC (disposition review): neither the packet field nor the catalog behind it may influence round arithmetic, gate evaluation, transition verdicts or any other decision the kernel takes; the one stated exception is the definition-static family itself, where catalog content decides admission findings and nothing further. The canonical experiment: delete an entry together with its refs in a single edit — every verdict and transition must come out the same, and the differing bytes must be confined to the packet artifact plus, exactly where refs rode the config positions, the committed provenance rows (a ref was always config data there); deleting a gate-sourced ref must leave committed rows byte-identical (the committed surfaces by row id: ch12-C10's provenance rows, ch11-C27's transcript records; the spawn-side closure is ch9-C18's refs-uninterpreted rule). This row is the communication-only invariant's home (disposition review) — the owning packet binds its REV line against THIS byte-precise scope, never a wholesale phrasing. Spawn-side, both carriers are closed: the packet carries refs as data, and the argv-interpretation path is closed by the adapter's refs-uninterpreted rule (ch9). |
| C15 | Determinism — SEMANTIC (disposition test): rendering depends on nothing but its three inputs — instance, admitted template, step; no wall clock, no entropy, no store access outside them — so identical committed state under an identical admitted template must reproduce the block list byte for byte. Carrier: the P5-line golden trace plus a lane that renders twice from one input set and asserts equality. This row is the deterministic-ordered-render invariant's home (disposition test) — C10's construction owns the order half; this row owns reproducibility. |
| C16 | The shipped catalog — SEMANTIC, shipped-artifact duty: the canonical template file gains a catalog carrying the emit-envelope entry and BOTH roles gain a default-config ref to it (carried decision: every actor emits, the block is role-symmetric; this is the first agent-config-family authoring in a shipped template file). The entry's text gets written when the owning packet lands, sourced from the interim knowledge carrier `v1-prompt-parity-audit.md` (§2's Class A envelope content, under §5's interim-carrier ruling); retiring that carrier is bound to the EC chapter's definition of done. Ripple: the named fixture families (canonical-file equality pin moving with the file; effective-config and full-packet equality re-pins (the full-packet member doubling as C12's tree-wide presence ripple); committed-provenance re-pins; the golden-trace fixture disposition — its snake-spelled fixture sites migrate onto the typed key together with a resolving catalog entry, which realizes the L0c slot; absence-consumers swept by key AND value). MEMBERSHIP CRITERION for the re-pin families: a suite belongs iff it CONSUMES the canonical file or the shared fixture — directory location decides nothing, and suites constructing their own templates stay outside. The criterion bounds the CATALOG-CONTENT ripple families only — C12's always-present-field ripple is tree-wide and this criterion never narrows it. Member enumeration stays the owning packet's. |
| C17 | Catalog-authoring caveat — aim, not enforcement: prose describing a gate's configured value carries no freshness guarantee — the config can move under it and nothing notices, because resolution checking establishes only that an id finds an entry, never that the entry's text still tells the truth. Closing the gap needs the two Absents this surface names and does not build (`computed-templated-bodies`, `semantic-parity-check`); the chapter that takes the plan's EC candidate (§1.3's fifth candidate) or a later one owns both. A comment ships beside the catalog in the canonical file saying exactly this, sited where the next entry gets written. Nothing this chapter ships describes gate configuration, so nothing can go stale yet. |
| C18 | The CLI surface — SEMANTIC, machine-shape duty: zero verb growth and zero flag growth; this chapter's lanes ride the standing validate/store/create channels as they are, and C7's code must arrive intact in the CLI document at the far end — build-driven by a lane asserting the actual VALUE of the code field in the CLI JSON (equality, never mere presence) on a ch13-only fixture, and by its negative twin — no other lane of this chapter may carry any code, while the gate schemas' named lanes keep the codes they always had. |
| C19 | The lane inventory: this surface's definition-static lanes are the lanes the ch13v2-rowed VALIDATION nodes introduce — enumerated by the declaration itself — plus C9's one semantic lane. Named exclusions, four: `[d-root]` and `[vc-agentconfig]` are SHARED validation nodes whose pre-existing lanes belong to their own surfaces (only the growth is this chapter's); `[n-effective-config]` is a NORMALIZER cited for C13's duty — derivation introduces no lane (ADR-019 D3); and `[d-capability-profile]` is a raw, lane-less node (its file-channel refusal is `[d-root]`'s own unknown-key lane). Channel scope is an ENGINE property of the one declaration: no walk/rung realization split exists or is re-ratified, and for the catalog and the AUTHORED ref positions (C4/C6) admission answers identically on the two channels — accept or refuse — with only source-bearing attributes differentiating the finding sets (receipt: PROBE-P4-4). Declared, deliberate asymmetries are not defects of that rule: `[d-capability-profile]` is channel-GATED today (receipt: PROBE-P4-4), and the two admission-PRODUCED positions C13 channel-gates join it at P5 — arrivals this row forward-scopes. Named P5 build surfaces: the C9 lane; the C13 normalizer hook WITH its declaration growth (the two channel-gated produced-position nodes under their decided spellings), carry-list growth and registry flips; and the C16/C18 fixture duties. |

## Ratification history (empty at `draft` — blocks are appended by the lifecycle acts)

## Realized map (empty until chapter close)
# ADR-019: structural definition rules become declared schema, validated by one engine on both channels

Status: accepted
Date: 2026-08-05 · amended 2026-08-06 (D1's residual list, D5's corpus
derivation, D9.3's reading) · amended 2026-08-07 (D10 — the entry-belted
membership construct, admitted under D7) · amended 2026-08-08 (D10's
no-reference-ring rule; D11 — the typed-subset `fields` widening on
`map.plain`; user-ratified — each carries a dated marker)
Links: supersedes — · amends — · depends-on ADR-011 · related ADR-003, ADR-012, ADR-013

## Context

The ch13 prose line ended at a MEASURED semantic drift floor: 28
registered rules · 6 literal restatements caught by the pointer-lint ·
9 semantic paraphrases in the first full arm sweep · 8 FRESH ones in
the second sweep after the nine were folded. The second number did not
shrink; it came from new places. The measured cause was the worklist:
the conversion's index was a hand-built mirror table, and the
paraphrases lived exactly where that table never looked (five rules had
zero pointers; four of the table's first nine rows were wrong or
incomplete). The general form — **a lint confines LITERALS, a mirror
list names MEANINGS, and prose is not indexable by meaning.** For
restatement to become structurally impossible, the rules must be DATA.

The user raised the structural form of this at the ch13 boundary
(process-log 2026-08-02, commit `4c1d481f`): every new format surface
(ch8, ch11, ch12, ch13) re-legislates the same failure space in
ratified prose — container preconditions, key/value lanes, unknown
keys, null/scalar/array forgeries, suppression edges, path grammar,
channel symmetry. The direction was ratified 2026-08-03 and this ADR is
its P3 realization.

**The basis is an enumeration, not an impression.**
`v3/implementation/schema-expressiveness-audit.md` — the bytes this ADR
was RATIFIED against carried sha256
`f779855f0a0823020c71951873a3a21244500ecd6644179cb78ac1f3045d8da7`
(classification 59/5/6); the ratification act's own D8 ruling then
moved two rows and resolved R7 empty, so the standing document is
sha256
`bfed821037786fa151f9a9c0f3b25d1142917056f10d52bb1dda5ddb4da6e57d`
(classification 61/3/6). Both pins are recorded because the act changed
its own basis, and only in the direction the act itself ruled — the
audit could not have moved itself.

**A THIRD pin, 2026-08-06.** The user-elected design round (finding F3,
`ch13-rederivation-arm/p3-design/`) showed that the P3 build had filed a
boundary-kept rule under R7, whose definition does not cover it. The
audit gained an EIGHTH residual family and the standing document is now
sha256
`a1a7adc5732bc5de6264de7eeac57b5bfd53b64616bfd67acb15af867d8d0fd2`.
The classification is UNCHANGED at 61/3/6 — R8's members are `S`, because
their obligation is expressible; what is not available is a way to reach
them. Every earlier pin stays recorded, for the same reason the second
one does.

The audit classifies every ratified rule of the four format surfaces
against a proposed declaration form, and was verified by two bounded
fresh-context arm rounds (10 findings then 5, all folded;
`ch13-rederivation-arm/p3/`). Its measured result, at the ratified
basis:

- **125 contract rows** (ch8 38 · ch11 41 · ch12 27 · ch13 19). Of the
  **70** that carry a definition-validation obligation: **59 fully
  declarable · 5 mixed · 6 residual** (**61 · 3 · 6** after D8's
  ruling). The other 55 legislate runtime,
  CLI, store, port or process and are marked non-lane with a per-row
  reason. Zero unclassified rows.
- **112 finding-emit sites** in today's validator and the three
  delegated gate-config schemas — every one mapped to its owning row.
- The residual is **seven named families**, not a vague remainder (D8's
  ruling then resolved one of them empty, leaving six with members).
- The plan's predicted residual was CHECKED: event-grain suppression
  and per-occurrence duplicates REFUTED (declarable); reference
  resolution and unreferenced hygiene PARTIALLY refuted (the
  intra-document forms are declarable with the selector vocabulary
  ch8-C16 already ships). Five families the prediction did not name
  were found, one of which forces D3 below.

## Decision

### D1 — the direction

**Structural rules become per-surface DECLARED SCHEMA — key classes,
containers, grammars, defaults and normalization as DATA — consumed by
ONE engine that validates BOTH the file-walk and direct-construction
channels. Ratified prose remains ONLY for the audited semantic
residual.**

Channel symmetry becomes structural rather than argued: attributes that
read source text (`sourceForm`, key-stringness, the substrate block)
are declared once and marked file-scoped; the engine runs them where a
source exists and skips them where none does. This retires the
hand-partitioned "realization split" that ch11-C40 and ch13-C19 each
spent ratified prose defending.

The residual — the ONLY territory where prose keeps legislating
behaviour — is exactly the audit's families, **EIGHT since 2026-08-06**:
**R1** value-shaped reference resolution · **R2** unreferenced hygiene
with a template-wide skip · **R3** existential cross-rules over resolved
registrations · **R4** admitted-form derivation · **R5** cross-artifact
checks · **R6** substrate-owned wording (a parity residual, no
obligation) · **R7** rows whose only declaration uses a construct this
ADR has not admitted (D8) · **R8** BOUNDARY-KEPT — a rule this vocabulary
CAN express, declared on paper, kept in code because a module or port
boundary puts it out of the engine's reach.

R8 is a different KIND of remainder from R1–R7 and is named rather than
folded into them: R1–R7 record what the vocabulary cannot say, R8 records
what it can say but cannot deliver. Its members stay `S`. The distinction
is load-bearing for D9.3 — a boundary is removable by an act, an
expressiveness limit is not, and a tripwire that cannot tell them apart
watches nothing.

### D2 — scope

**The TEMPLATE surface, now. Nothing else is built.** The other
validated surfaces in the repo are NAMED EXTENSION POINTS and each
needs its own ratified act before any declaration is written for it:
the `GateDecision` stdout contract (ch11-C25), the `GateInvocation`
stdin wire (ch11-C23), the CLI `--run-overrides` input (ch12-C9/C20),
the operator-intent wire (ch12-C13), and the EC emit-contract surface
(plan §1.3's fifth candidate — deliberately excluded from the audit as
having no ratified grammar).

### D3 — the engine owes a SECOND capability, named

The audit's R4 is not a leftover lane: admission both VALIDATES and
PRODUCES the admitted form (`advancesRound` expanded per transition,
the effective gate config materialized, ch13-C17's rebuild). **A
declaration says what is legal; it does not compute a value.** The
build therefore carries a NORMALIZER beside the validator, driven by
declared hooks, and this ADR names it so that "schema" is never read as
covering it. Plain `default:` materialization IS declarable and stays
in the schema; only derivation is the normalizer's.

### D4 — the canonical home: ONE authority, pointers everywhere else

**RECOMMENDED: the declaration lives in the CODE tree as a data-only
module under the definition module (ADR-011) — `v3/src/definition/
schema/templateFormat.ts`, a frozen declaration object with no
executable logic — and the contract carries DECISIONS plus declaration
POINTERS, restating no attribute.** The ratification act names the
schema file's bytes exactly as the contract-draft equality lock names
the C-rows, so the lock is a MECHANISM, not a location.

The weighing, recorded because it is the load-bearing choice here:

- *Ratification-lock pull* — put the declaration in the contract
  markdown. It inherits the ratified-bytes lock for free. Rejected as
  the home: the engine would then parse ratified prose to get its
  rules, which is the exact indexing-prose-by-meaning failure this
  whole line exists to end.
- *Code-consumption pull* — put it in the code tree. The engine
  imports data; a diff is reviewable; the declaration is type-checked.
  Its weakness is that a code file can be edited without a
  ratification act — and that weakness is closed by extending the act
  to the file's bytes, which costs one hash line per act.
- **Two authorities is the outcome neither pull may produce.** The
  contract states no structural attribute; it states decisions and
  cites tags. A rule visible in both places is a defect, and the
  cheapest tripwire is the tag-closure check the audit already ran
  twice by hand (every tag defined is cited, every tag cited is
  defined).

### D5 — the parity gate

**Before the engine replaces any implemented lane, the EXISTING
fixture corpus is replayed against it and must produce identical
verdicts, identical finding PATHS and identical MESSAGES — or an
approved delta list, ratified before the switch, never after.**

**AMENDED 2026-08-06 — the corpus is derived from the CALLERS of the
swapped entry point, never from a file list.** The corpus as originally
named:

The corpus, measured 2026-08-05 by `grep -cE '^\s*(it|test)\('`: **362
cases** — `validate.test.ts` 124 · `load.test.ts` 76 · `admit.test.ts`
59 · `process.test.ts` 59 · `fileDefinitionStore.test.ts` 19 ·
`threshold.test.ts` 13 · `previousReviewerVerdict.test.ts` 12.

Those seven reproduce exactly, and they were the WRONG SET: measured at
the build, `admitTemplate` — the direct-construction channel's entry — is
called from the testkit, kernel, lifecycle and trace suites as well, and
**two thirds of the cases the switch actually affected lived outside the
named files**. A file list is a snapshot of who called the entry point on
the day it was written; the obligation is to replay who calls it NOW.

So the standing rule: enumerate the CALLERS of the entry point being
replaced, and replay every case that reaches them. Where that is the whole
suite, it is the whole suite — the P3 build replayed 1830 executed cases
for a corpus named as 362 source-literal ones, and the difference is
where its delta list came from.

This is a DERIVED claim about behaviour that does not exist yet; its
NAMED MEASURER is the build's parity gate itself. Two delta classes are
pre-named by the audit so they arrive as decisions rather than
surprises: **path grain** (duplicate terminal ids report at `terminal`,
not `terminal[i]`; every key-class lane reports at its containing map —
an engine written naively silently moves these addresses) and
**message wording** (nine measured messages embed rule-specific
rationale; either the declaration carries a literal `message:` or the
delta list authorizes the engine's own wording).

### D6 — retrofit policy

**Existing surfaces stay as built.** Migrating a surface onto the
engine is a deliberate per-surface BOUNDARY act with its own parity
gate and its own ratification — never housekeeping, never folded into a
packet that is doing something else, never a "while we are here". A
surface that is never migrated is a legitimate end state.

### D7 — the format-growth rule

New expressiveness is a RATIFIED AMENDMENT, never silent, in two
flavours that must not be confused:

- **A new NODE declaration** (a key, a grammar, a default) is ordinary
  additive format growth under plan §8.2 — the realizing chapter's
  contract row plus the schema edit, in one act.
- **A new VOCABULARY CONSTRUCT** changes what the engine can express at
  all, and therefore amends THIS ADR. It is admitted only under the
  audit's **≥2 independent ratified rows** test; a construct serving
  exactly one row is refused and that row keeps a prose lane instead.

**Admitted under this rule so far**: one construct, D10 (2026-08-07);
one attribute widening at a new grain, D11 (2026-08-08 — not a new
construct, recorded here so the tally cannot silently drift).

### D8 — the two open constructs — RULED: BOTH ACCEPTED (user, 2026-08-05)

Both were single-use, and the audit deliberately refused to bank their
coverage until this act ruled. **Both are ACCEPTED**, on the ground
that neither is a NEW construct:

- `memberOf: keys(@catalog)` — a selector root that is an INJECTED set
  rather than a document node (ch11-C8's `uses` resolution): admitted
  as a GENERALIZATION of the existing selector root. A set is a set;
  the relation, the finding form and the code carrier are unchanged.
- per-member `code:` inside an enum (ch11-C16's `failInstance` earning
  a distinct issue code): admitted as the existing `code` attribute
  applied at MEMBER grain, not as a new attribute.

**The D9 tripwire was examined at this ruling, not skirted** — recorded
so a later reader can check the reasoning rather than trust it.
Tripwire 1 fires on admitting a single-use CONSTRUCT to make one row
fit. Neither ruling adds a construct: both widen the domain of a
construct that already passes the ≥2-row test (selectors, 7 rows;
`code:`, 7 rows). Had either required a genuinely new construct for its
single row, the correct outcome would have been the prose lane.

Consequence, executed in the audit at this act: residual **R7 is
RESOLVED EMPTY** (kept as a named family with its record, so a future
single-use candidate has a home and the history stays legible), and
ch11-C8 and ch11-C16 move from `H` to `S`. The standing classification
is therefore **61 fully declarable · 3 mixed · 6 residual** of the 70
obligation-bearing rows.

### D9 — the falsifiability criterion

**The direction is declared FAILED, and this ADR deprecated, if
# P4 review record — ch13 contract v2 (context-block-v2)

The committed evidence home for the P4 phase (ch13-rederivation-plan
§3/P4 and the 2026-08-08 inheritance amendment: the old→new mapping and
the phase's verdicts live HERE, beside the panel/arm outputs — the
contract stays clean). Everything below is derived from repo surfaces
at write time; nothing is recalled from conversation.

## 1. STOP 1 rulings (user-ratified 2026-08-08, relayed by the general)

- (a) NAME: `context-block-v2` (option 1 of three presented).
- (b) HOME: extend `v3/src/definition/schema/templateFormat.ts`; no new
  file — the ch13 keys are D7 additive NODE growth of the one template
  surface.
- (c) BYTE-LOCK: the ratification block gains an OPTIONAL `schema` key
  `{path, sha256}`, checked by the draft lint on the LATEST block only;
  two-commit choreography unchanged. **Coordination note, recorded on
  the ruling's instruction:** multiple contracts locking ONE schema
  file will each go red on any edit of it — DESIGNED behaviour (a
  schema edit is an act), and a boundary topic when a second locking
  contract becomes real.
- (d) WORK PLAN + BETS: approved as presented; Flag-candidate #1
  carved out with its executed probe.

## 2. FLAG #1 ruling (user-ratified 2026-08-08)

Option A: the `fields` attribute widens to `map.plain` (ADR-019 D11,
commit `08f99ab9`); build `65b1a9e2`; round-1 fold `14f3ab12`. The P4
declaration adopts the construct only after the arm frame is green.

## 3. The four LOUD-open items (named by the user's STOP-1 ruling)

Awareness only — each fails LOUD at declaration load, so no build is
owed; the P4 authoring is expected to meet them, not to fix them:

1. `^` inside a value-class definition;
2. a delegate reused at a value-class root;
3. duplicate hook tags reporting one path twice;
4. a selector naming a literal key at an open map — refused at load.

(The P4 session could not resolve this list from repo surfaces alone —
it lived only in relay text; recorded here on the ruling's instruction
so it has a repo home.)

## 4. Probe log (executed 2026-08-08; scripts + outputs beside this file)

| Probe | Script | What it measured |
|---|---|---|
| PROBE-P4-1 | `p4-flag1-probe.ts.txt` (P1_*) | the D10 belt at a fixed position: resolving ref clean; `{}` entry → entry finding + coded per-site finding; wrong-kind catalog → container finding AND per-site findings (no suppression); absent catalog → per-site; duplicates per occurrence at index; channels byte-identical |
| PROBE-P4-2 | `p4-flag1-probe.ts.txt` (P2, P3) | pre-D11: a ref list inside `map.plain` unreachable (ghost/numeric/grammar-violating members, zero findings); a runtime `fields` attr on map.plain loaded silently inert |
| PROBE-P4-3 | `p4-dup-resolution-probe.ts.txt` | DUP_GHOST: membership fires per shape-passing occurrence (both indices) beside the dup finding; DUP_BAD_SHAPE: a shape-failing member is invisible to every list-level lane; NONSTRING_KEY_FILE: boolean-keyed entry unaddressable by the string ref of the same spelling (two true findings); BAD_KEY_SIBLING: grammar-failing key draws the key lane, valid sibling ref resolves |
| PROBE-P4-4 | `p4-live-declaration-probes.ts.txt` | the LIVE declaration at the ratifying basis, both channels: the fail-closed class at width (incl. present-null and the boolean members), refs beside refused/absent catalogs, the empty catalog and the EMPTY AUTHORED list, all three positions accumulating, the dup/shape receipts, the boolean key, open siblings, bad-key-with-resolving-sibling, the channel-gated capabilityProfile, the CB3 key-order re-execution (`["1","2","10","ZZZ","AAA"]`), and the compound clean zero. NOTE for auditors: the output's `L_empty_catalog_ref` line reports `agree:false` from the probe's own asymmetric inputs and is superseded by the `_FILE` twin on the next line; the only DESIGNED asymmetry in the file is `L_capability_profile`. |

PROBE-P4-3's first two results are MEASURED DIVERGENCES from the
superseded line's C8(e)/C7 grain decisions; contract v2 rows C7/C8
carry them as DECIDED-HERE (v2) markers with these receipts.

(The divergence RATIONALES live in §9, beside the mapping they belong to.)

## 5. The D11 substrate act (the Flag #1 package)

- `08f99ab9` docs(v3): ADR-019 D11 amendment.
- `65b1a9e2` feat(v3): the build — suite 2044 → 2059 (= exactly the 15
  added tests; zero live delta by arithmetic).
- Arm round 1 (charter/output beside this file; pin gpt-5.6-sol/high,
  guards clean, 415s): **2 IN-SCOPE · 0 CARRIED · 0 UNRUN**, all five
  lenses full. F1 the closure gap (`collectDependsOn` predated the
  widening), F2 the direct-channel raw-Map slip — both build omissions,
  neither a reproduction nor a paraphrase.
- `14f3ab12` fix(v3): the fold, test-first — suite 2059 → 2063 (= the 4
  added tests).
- Arm round 2 (re-check on the folded bytes; pin gpt-5.6-sol/high,
  guards clean, 260s): **0 IN-SCOPE · 0 CARRIED · 0 UNRUN**, both fold
  neighbourhoods exercised in full (value-class-mediated plain fields,
  runtime suppression, non-string and nested Maps, typed-field Maps,
  channel agreement, file-channel anchor/alias), all four gates PASS
  (2063/2063). **The frame closes at round 2 of 3 — the third round is
  unspent reserve.** The construct's verification is green; the P4
  declaration may adopt it (the Flag #1 ruling's condition).

## 5a. The schema-lock guard act (STOP 1 ruling (c), built before the ratifying act)

- `ed86ac24` feat(v3): template §2/§3 + lint D3 extension + D5b + five
  selftest dims (135 → 139).
- Arm round 1 (charter/output beside this file; pin gpt-5.6-sol/high,
  guards clean, 280s): **2 IN-SCOPE · 0 CARRIED · 0 UNRUN**, all four
  lenses full. F1 the `..`-substring false positive (a legal dotted
  filename refused), F2 the symlink escape (an outside target's true
  hash satisfied the lock) — both build omissions in path discipline.
- `20b73f6a` fix(v3): the fold, dims-first — segment-wise `..`,
  resolved-root containment; register 139 → 140 + one new green.
- Arm round 2 (re-check; guards clean, 180s): **0 IN-SCOPE · 0 CARRIED
  · 0 UNRUN**, neighbourhoods full (head/tail/lone `..`, separators,
  inside/relative/intermediate-directory symlinks, the macOS
  `/tmp → /private/tmp` false-escape check), selftest 140 + live lint
  unchanged. **The frame closes at round 2 of 3.**
- `0d58283e` docs(v3): tag closure both directions (C1 ↔ d-root;
  d-capability-profile → ch13v2-C11) — closure re-run CLEAN.

## 6. Experiment §5 running record (P4, per round — derived numbers)

| Event | Yield | Classes | Reopens | Gates | STOPs |
|---|---|---|---|---|---|
| STOP 1 | 4 rulings | — | 0 | 1 (user) | 1 (designed) |
| Flag #1 | 1 ruling | vocabulary gap (measured by probe) | 0 | 1 (user) | 1 (flag) |
| D11 arm r1 | 2 folded | build-omission ×2 | 0 | 0 | 0 |
| D11 arm r2 | 0 findings — frame closes | — | 0 | 0 | 0 |
| Schema-lock arm r1 | 2 folded | build-omission ×2 (path discipline) | 0 | 0 | 0 |
| Schema-lock arm r2 | 0 findings — frame closes | — | 0 | 0 | 0 |
| Tag-closure (authoring-side) | 1 gap folded | citation omission | 0 | 0 | 0 |
| Panel r1 (full, 6 agents) | ~50 folded | 2 P1 (kept-claim defect; inheritance loss) · inheritance/receipt/plan classes | 0 | 0 | 0 |
| Panel r2 (full, 6 agents) | ~35 folded | 1 P1 (precedent contradiction) · fold-propagation classes | 0 | 0 | 1 (bet-boundary STOP, user opened r3) |
| Panel r3 (targeted, 4 agents) | ~20 folded | 0 P1 · 5 P2 (clause scope ×2, ADR-D4 propagation, citation target, receipt gap) · rest hygiene | 0 | 0 | 0 |

## 7. Bet ledger scoring (STOP 1's stated bets)

1. **Authoring** (1 pass + probes, 0 reserve): **HALF** — one pass
   produced the draft, but the panel's rounds found real inheritance
   losses and the first-generation sweep tokenizer masked copied
   sentences; the "0 reserve" half of the bet under-priced the first
   schema-first authoring. Lesson: a first-of-form authoring pass
   prices a loss-hunting review round, not zero reserve.
2. **Flag #1** (1 probe + 1 ruling): **EXACT** — the probe measured the
   gap and the sneak-in in one run; one ruling settled it.
3. **Panel** (2 rounds, 3rd via user): **AS PRICED** — two rounds ran
   autonomously with real yield; the 3rd (targeted) opened through the
   user exactly as the bet reserved it, and closed 0-P1.
4. **Byte-lock guard** (1 build + 1 verification, cap 3): **HALF** —
   the build landed in one pass, but round 1 found two path-discipline
   defects, so the fold forced a second round the bet had not priced
   (the cap absorbed it: closed at 2 of 3). Lesson for the priors:
   filesystem/path semantics are a surprise home of the same kind as
   runtime semantics — a guard build touching them prices 1 build +
   1 round + 1 re-check, not 1+1.
5. **Arm** (1 pointed round + 1 re-check): OPEN for the contract; the
   D11 SUBSTRATE act's own frame ran under the Flag-#1 bet's hedge —
   the hedge ("engine descent touches evaluation core → surprise home,
   reserve through the user") priced the risk correctly in KIND (two
   descent-composition omissions) and the frame absorbed both within
   its cap: 2 findings → fold → 0/0/0, closed at round 2 of 3.

## 7a. The never-copy sweep (authoring-side; METHOD CORRECTED at panel round 1)

Three generations, recorded because the instrument itself was a
finding. (1) The authoring-side sweep split on whitespace with
punctuation attached — 43 runs found, rewritten to 6 "skeleton" runs.
(2) Panel round 1 (lens 2, F2) showed that tokenizer lets a comma swap
mask a copied sentence: WORD-NORMALIZED (case- and
punctuation-insensitive, inline code spans stripped), 12 non-skeleton
runs survived. (3) The round-1 fold rewrote all of them — including
runs the fold itself had re-imported while restoring lost decisions —
and the corrected sweep now returns: 6 runs total, 5 contract-draft
TEMPLATE skeleton + 1 legitimately D10-inherited (the belt sentence
ADR-019 D10 itself quotes), 0 non-skeleton. Script + output beside
this file (`never-copy-sweep-out.txt`); the close re-runs it on the
final bytes. Lesson for the priors: a guard's tokenizer is part of
the guard — the meter must be metered.

## 8. Ergonomics note — what a minimal authoring form should have offered

For the boundary review (carried from the dress rehearsal's
USABILITY-2, confirmed by this phase's authoring):

- A new-surface author must lift the ENTIRE `substrate` block from the
  worked example (round 10 measured this; the P4 probes did the same
  lift three times). A documented minimal substrate constructor — or a
  stated "extend the existing surface" default like the one P4's ruling
  took — would have removed the only scaffolding step the docs do not
  cover.
- The `rows:` attribute forces a citation spelling decision on every
  node before the cited contract exists (P4 chose `ch13v2-C<n>` for
  draft-phase citations, disambiguating from the superseded `ch13-C<n>`
  ids elsewhere in the same file). A stated convention would remove the
  choice.
- The probe scripts' import paths are absolute and repo-specific; a
  documented one-liner ("import from `v3/src/definition/schema`, run
  with `pnpm exec tsx`") is all a first author needs and is nowhere
  written.

## 9. Completeness pass (panel rounds 1–3, 2026-08-08 — old→new over 19 rows + 4 reopens, WITH the divergence rationales)

Run by a dedicated fresh-context Opus agent, direction OLD → NEW, at
round-1 basis old=frozen@superseded, new=1b2b78aa7e9f376a @ 0ad7e5d9;
re-checked at round 2 on c01db023bcd0240c @ 17659831 and spot-checked
at round 3; the FINAL fold's post-state is what the ratifying act
binds. The round folds (§10–§12) restored or closed every item the
pass flagged.

| Old item | Verdict | Where / Why / How |
|---|---|---|
| C1 | carried + schema-covered, MARKED divergence | v2 C1; `[d-ctxblocks]` (map.open + default) in `[d-root]`.fields. Fail-closed replaces normalize-non-record — marked DECIDED HERE (v2), receipted (PROBE-P4-4 class-width incl. present-null). Non-suppression of C7 = D10's broken-operand nature. |
| C2 | carried + schema-covered; walk-side hand-off dropped-vacuous | v2 C2; `[vc-block-id]`/`[d-block-key]`/`[d-block-ref]`. The filter/keep hand-off was a walk/rung artifact; its observable survives (PROBE-P4-3/P4-4 boundary-key cases). Channel-independence of the key lane is the FOURTH marked divergence (post-fold). |
| C3 | carried + schema-covered | v2 C3; `[d-ctx-entry]`/`[d-ctx-body]`; no-reserve stays semantic. |
| C4 | carried + schema-covered | v2 C4; `[d-prompt-refs]` on `[vc-agentconfig]` — the vocabulary was built for it (D11). Fixture disposition → v2 C16. |
| C5 | carried | v2 C5, all three legs. |
| C6 | carried + schema-covered | v2 C6; `[d-ctx-gate-refs]` in `[d-binding]`. |
| C7 | carried + schema-covered, MARKED divergence | v2 C7; the belt became D10's `validKeysOf` with the code on `[vc-blockidlist]`. Per-occurrence resolution marked + receipted (DUP_GHOST, PROBE-P4-4). |
| C8 | compound | (a)(b)(d)(f) structural; (c) → v2 C9 in full; (e) → unique rule + v2 C8; (g) → v2 C8's compound clean negative (restored at the fold, receipted PROBE-P4-4); enumeration guard + primitive rule dropped-vacuous under fail-closed; raw-feed co-fire superseded — marked + receipted (DUP_BAD_SHAPE). Domain asymmetry: CLOSED by the substrate (one enumerable-own-key domain), stated in v2 C9 post-fold. |
| C9 | carried | v2 C10 every leg incl. the hoist+ascending-re-sort basis (restored at the fold) and the DERIVED re-mark with named measurer. |
| C10 | carried + schema-covered; EC scope restored at fold | v2 C11 + `[d-capability-profile]`; the pre-EC boundary re-added. |
| C11 | carried; token grain restored at fold | v2 C12 + the model-verbatim snake token values and literal quoting. |
| C12 | carried | v2 C14. |
| C13 | carried | v2 C15. |
| C14 | carried; membership criterion restored at fold | v2 C16 + consumption-based membership + the named interim carrier. |
| C15 | carried | v2 C17. |
| C16 | dropped-why: executed one-time act | The ch11 reopen act ran 2026-07-26; ch11 is realized with the texts. Pointer maintenance = the Context's named carried scope (owner: boundary review). |
| C17 | compound; two lost legs restored at fold | v2 C13 + default: placement (siblings at fixed-keyset levels), TS-optionality grain, carry-list duty (louder than the original), registry flips + type witnesses, the two build traps; non-record normalization superseded (marked, receipted); single-body-source home restored. |
| C18 | carried | v2 C18. |
| C19 | carried (inventory) + dropped-why (split retired), MARKED | v2 C19: tag-set inventory with the shared nodes as named exclusions; channel scope an engine property; capabilityProfile's declared asymmetry named. |
| Reopen 1 (C1 grain) | carried + partly vacuous | Ref-lane half = D10's nature; whole-result half vacuous (refuse vs admit can no longer be confused). |
| Reopen 2 (direct-channel region) | carried + schema-covered | The belt IS D10; key-exclusion deletion + unconditional domain in v2 C9; the C19 premise dropped-vacuous. |
| Reopen 3 (C17 nullish) | dropped-why: superseded, marked + receipted | v2 C1; absent-half survives as the declared default. |
| Reopen 4 (C17 class width) | dropped-why: superseded, marked + receipted | Same clause; the carrier obligation vacuous when the only non-record outcome is refusal. |

**The divergence RATIONALES (the arguments the rows deliberately do
not carry — the 2026-08-08 inheritance rule's designed home):**
- **C1 fail-closed** replaces the predecessor's normalize-any-
  non-record-to-empty rule. Safe because the defect that rule healed —
  a cast-forged catalog carried onto the admitted value — cannot arise
  under an engine that validates the direct-construction channel: the
  container lane refuses the forgery before any rebuild exists.
- **C2 channel-independent key lane**: the predecessor placed the key
  lane walk-side (file-only) because only the walk saw source keys;
  under one declaration the lane runs wherever a key exists, which is
  strictly fail-louder — no admit/refuse outcome moves, a direct-
  channel authoring defect now gets the same diagnosis a file author
  gets.
- **C7 per-occurrence resolution** replaces first-occurrence-carries:
  that rule made the duplicate lane decide the resolution lane's
  domain — a cross-lane coupling of exactly the class whose deletion
  the predecessor's own 2026-08-01 reopen ratified for the
  key-exclusion case — and per-lane independence is the engine's
  standing shape.
- **C8 shape-failure invisibility** replaces the unconditional
  raw-feed co-fire: a member that failed its own lane already carries
  its trace, and feeding it to sibling lanes multiplied findings for
  one defect while coupling lanes the engine keeps independent.

**Round-2/3 refresh notes on the table above:** old-C17's verdict also
covers the CHANNEL GRAIN and FIELD SPELLINGS decisions C13 gained at
round 2 (the channel narrowing is observably identical to the
predecessor's behaviour — its engine ran no direct-channel keyset lane
— so no divergence marker is owed); old-C8's leg (d) is additionally
homed as C4's authored-empty-list clause; old-C19's verdict reads on
the round-3 form (VALIDATION-node inventory, four named exclusions).

The pass's findings (1 P1, 2 P2, 7 P3) and their dispositions are in
the §10 round record — every lost item was FOLDED back in round 1;
the P1 (registry flip) and both P2s (optionality grain, invariant
homes) are restored in v2 C13 and the invariant homes on C7/C10/C11/
C13 — and, at the round-2 fold, on C14 (communication-only, with the
REV-line byte-scope binding) and C15 (deterministic-ordered-render),
completing all six. Verdict (the agent's, standing): the v2 line carries the old
line's decisions substantially without loss and in places carries
them better; no reopen decision vanished quietly; the divergences are
marked and receipted.

## 10. Panel round 1 (2026-08-08) — full five-lens + completeness, and the fold

Basis: contract 1b2b78aa7e9f376a, declaration 34af910d5cfbd0ba @
0ad7e5d9. Six fresh-context Opus agents (five lenses + the
completeness pass). Yield: ~50 findings — 2 P1 (lens 3's C13
keyset-claim self-contradiction; the completeness pass's registry
flip), ~14 P2, the rest P3/watchpoint/considered. ZERO STOP-class
findings; all three (now four) DECIDED-HERE markers cleared as
row-home by the model-plane audit (lens 2 duty 4).

The CONTENT fold (one batch, this round): supersession arguments moved
out of rows (the 2026-08-08 inheritance rule — the rows keep markers
and receipts); C19's inventory re-parameterized both directions;
C13 restored the placement + optionality + registry-flip + build-trap
decisions and scoped its keyset claim (the P1 fix); C9's stand-down
generalized to reachability and the closed domain-asymmetry stated;
C2 gained the fourth DECIDED-HERE marker; C8 gained the C9 carve-out
and the compound clean negative; C12 gained the wire tokens; C16 the
membership criterion; C7 the admission moment + containment + its
invariant home; C10/C11/C13 their invariant homes + EC scope + the
re-sort clause; the Context gained the rename-culture statement, the
carried-scope pointer note, the lock-consequence sentence, the
narrowed no-restatement claim and the completed model-note
enumeration; the declaration's header lost its stale R1/hash/
collectTags claims and `n-effective-config` gained the C13 row
citation; the domain type's comment re-attributed to C13; five plan
sections gained their aligned-at edits; PROBE-P4-4 (the live
declaration, class-width) landed with script + output; the never-copy
sweep was corrected and re-run to clean (§7a).

Dispositions: folded ~40 · declined 1 (lens 2 F7 — the code literal
in both authorities: defensible via C18's CLI hop, D2 scoping; a
rename would be its own ratified act) · routed 1 (the ch11/plan
pointer maintenance → boundary review, named in Context) · deferred
0 · considered_not_finding per lens reports (in their outputs, this
directory's transcripts are not committed — the reports' substance is
this section). Round 2 (the post-fold re-run) is FULL by the
escalation rule (claim-structure changes + a manifest-class change:
the fourth DECIDED-HERE marker).

## 11. Panel round 2 (2026-08-08) — full six-agent re-run, and its fold

Basis: contract c01db023bcd0240c, declaration a355ada64c35ed7c @
155:candidate (added at ch-11 ratification): the **L2b context-block
224:the ch13 draft ratification round, 2026-07-26): which actor-facing
230:L2b Absent)? The ch13 interim state is honest but seed-dependent: the
2008:- **L2b context blocks** — their own appended chapter (the §1.3 third
2498:- **L2b context blocks** — the named §1.3 candidate, naturally after
2674:candidate — the **L2b context-block surface**, deliberately excluded
2722:### 13.1 Scope and boundaries
2768:   aligned at ch13-p0 pre-approval, 2026-07-26) — handle a closed pipe
2809:### 13.2 Coverage and intake impact
2834:### 13.3 The draft phase
2837:`contracts/ch13-context-block-contract.md` (surface: `context-block`).
2860:packets anchor as `contract:ch13-context-block#Cn`.
2862:**Superseded, and its successor (aligned at the ch13 contract-v2
2863:ratification, 2026-08-08):** the `context-block` draft named above was
2865:`ch13-prose-line`, authorizing plan `ch13-rederivation-plan.md`). The
2866:live surface is `contracts/ch13-context-block-v2-contract.md`
2867:(surface: `context-block-v2`), authored on the ADR-019 declared-schema
2869:and is history. Packets anchor as `contract:ch13-context-block-v2#Cn`
2873:### 13.4 Packets and flow mode
2876:`contracts/ch13-context-block-v2-contract.md` — the successor of
2877:`contracts/ch13-context-block-contract.md`, which was NOT YET RATIFIED
2879:2026-08-05 (aligned at the ch13 contract-v2 ratification, 2026-08-08);
2885:| ch13-P0 | the EPIPE hygiene fix: the shipped CLI entrypoints' output sinks survive a closed pipe (quiet termination instead of a raw stack; scope aligned at ch13-p0 pre-approval, 2026-07-26 — both entrypoints and the shared dispatch shell), its behavior contract decided against the ch6-P4a canonical exit-code matrix, with the claim-derived negative lane; empty ledger slice (a DECLARATION, not an omission — the ch9-P1 precedent) | human approve (the row carries its own new decision — the closed-pipe behavior contract — and §1.3's consistency rule bars an invention-predicted row from flag-free autonomous mode); predicted: invention (memo-born — basis: the ch9 boundary verdict; the enum's closest fit, the basis being a boundary verdict rather than a design memo) |
2886:| ch13-P1 | the definition side: the three context format keys + the catalog on the parsed template, `validate_context_refs` under `admit_definition`, the `unresolved_context_block_ref` issue lane on the established named-lane carrier, the CLI validate extension | flag-free approve → autonomous build (measurement; the §5.5 fallbacks stand); predicted: projection (basis: the chapter draft — pending ratification) |
2887:| ch13-P2 | the dispatch side: `assemble_context_blocks` (order, dedup, retained provenance), the packet's context-blocks field, the gate-ref predicate over ch11's authority logic, the `l2b-pseudocode` golden trace, the shipped template's first catalog entry (the emit-envelope block) + the catalog-authoring caveat comment beside it (§13.1 item 4), the l0c golden-trace re-pin (the C4 fixture disposition), and the journey smoke through the shipped entrypoint | flag-free approve → autonomous build (measurement); predicted: projection (basis: the chapter draft — pending ratification) |
2891:at the ch13 draft round, 2026-07-26) → P0 → P1 → P2 (the README §4 draft-first
2900:**Re-derivation alignment (aligned at the ch13 contract-v2
2905:order from that point is: contract v2 ratification → the ch13-p1 v2
2906:packet (`ch13-rederivation-plan.md` phase P5 — definition side and
2912:rule): **ch13-p1 v2** — content: the definition side and the dispatch
2918:(basis: `contracts/ch13-context-block-v2-contract.md` — the surface
2920:split into ch13-p1a/p1b inherits this row's values unchanged.
2928:### 13.5 Deliverables and DoD
2931:`ch13-context-block-v2` contract-draft (its superseded predecessor
2933:ch13 contract-v2 ratification, 2026-08-08); the EPIPE fix; the
2949:close act; the ch-13 map row flipped; `pnpm v3:deferred --closed ch13`
2961:reopen-vs-aggregate-note criterion (first applied at the ch13 draft —
2964:`ch13-context-block` draft's Context is its only home (the v2 draft
2965:does not carry the criterion — aligned at the ch13 contract-v2
2972:ch8-C10 namespace (owner-raised at the ch13 draft ratification round,
2975:record enumeration re-orders (the ch13 PROBE-CB3 measurement,
2976:recorded in the superseded `ch13-context-block` draft's frozen
2977:Context; the v2 draft's C10 handles the corner honestly, so ch13 does
2978:not need the ban — aligned at the ch13 contract-v2 ratification,
2984:the ch13 contract-v2 draft's named carried scope, 2026-08-08):
2985:`ch11-gate-format-contract.md` names `contract:ch13-context-block#C6`
2989:ch13v2-C6, so this is pointer maintenance, not meaning drift, and no

exec
/bin/zsh -lc "nl -ba v3/implementation/contracts/ch13-context-block-v2-contract.md | sed -n '1,220p'" in /Users/felho/dev/pairflow
 succeeded in 0ms:
     1	# ch13 — context-block-v2 contract
     2	
     3	```json
     4	{"contract_draft": {"chapter": "ch13", "surface": "context-block-v2", "status": "draft"}}
     5	```
     6	
     7	## Context (non-normative by declaration)
     8	
     9	Successor of `ch13-context-block`, superseded 2026-08-05; inheritance
    10	verified by the P4 completeness pass (review record:
    11	`v3/implementation/ch13-rederivation-arm/p4/`).
    12	
    13	**The declaration authority.** This is the first contract authored ON
    14	the ADR-019 schema substrate: every structural rule of this surface
    15	lives as DATA in `v3/src/definition/schema/templateFormat.ts`, and this
    16	contract states DECISIONS and cites those declarations by TAG (ADR-019
    17	D4 — one authority, pointers everywhere else); where a row glosses an
    18	attribute in passing, the gloss is non-binding paraphrase and the
    19	declaration's bytes govern. The ratifying act binds that file's bytes
    20	beside this file's C-rows (the contract-draft template's schema lock)
    21	— and a LATER edit of the declaration (P5's own duties included) turns
    22	the lock red until a re-ratifying act records the new bytes: designed,
    23	not an accident. Citation closure (every tag cited exists; every
    24	ch13v2-rowed node is cited) is checked mechanically at review — no
    25	standing code check exists for it yet; prose restatement is the review
    26	panel's to catch.
    27	
    28	**Business invariant.** Context blocks are actor-facing communication:
    29	deterministic prose delivered with the dispatched packet so an actor
    30	hears an operating rule before acting. They influence no verdict and no
    31	kernel decision — enforcement lives elsewhere; this surface only speaks.
    32	
    33	**Control model.** A single catalog at template level owns all body text. Three
    34	ref positions point into it by id: role-level and step-level
    35	`promptConcernRefs` inside the agent-config maps, and `contextBlockRefs`
    36	on a gate binding. Authored KEY spellings follow the standing camelCase
    37	rename culture over the model's snake_case names, while model-defined
    38	TOKEN VALUES keep the model's exact spelling — pinned here because the
    39	plan requires this surface to fix the relation explicitly. The declared
    40	schema validates catalog and refs at admission on both channels;
    41	rendering happens at dispatch as a deterministic computation over
    42	admitted inputs.
    43	
    44	**Read-path.** Admission reads the authored value — one declaration,
    45	both channels; rendering reads only the admission-produced surfaces
    46	(C13); the run-scope channel is never read (C5).
    47	
    48	**Forbidden fallback.** Rendering never drops a ref and never tolerates
    49	a malformed catalog — admission lets neither situation through. No new rejection or issue name: the one issue code is the
    50	ratified `unresolved_context_block_ref`.
    51	
    52	**Allowed resolution / missing data.** A template that issues no refs
    53	and declares no catalog is fully legal (every pre-ch13 file). A
    54	template whose refs find no valid entry fails admission with per-site
    55	findings.
    56	
    57	**Model-table note (a carried fact; the divergence-stop question is settled).** The model
    58	document's context-block table still shows creation-time rejection on
    59	its ref rows — and the Evidence block itself carries one more
    60	create-mention (its invariant-coverage line's "refs validated at
    61	create" tick) — while the invariant record, the pseudocode reprint and
    62	the same Evidence block's acceptance row all place ref validation at
    63	ADMISSION. Admission governs, as the ch11 sync established.
    64	
    65	**Substrate probes (executed 2026-08-08, this authoring; scripts and
    66	outputs preserved in the review record — the P4-1/2/3 sets measured
    67	the pre-adoption substrate through analog surfaces; every claim of
    68	theirs that SURVIVES the adoption was re-executed against the live
    69	ratifying-basis declaration as PROBE-P4-4, the receipt the rows cite
    70	(the pre-D11 halves are inherently pre-adoption measurements and stay
    71	with their own scripts):**
    72	- **PROBE-P4-1** (belt composition): the D10 `validKeysOf` belt at a
    73	  fixed-keyset position reproduces the ratified resolution semantics on
    74	  both channels — a ref to a structurally invalid entry draws the
    75	  entry's own finding AND the per-site coded finding; a wrong-kind or
    76	  absent catalog answers with the empty set, so per-site findings fire
    77	  beside (never suppressed by) the container's own finding; channels
    78	  agree byte-for-byte.
    79	- **PROBE-P4-2** (the plain-map gap → ADR-019 D11): before D11, a ref
    80	  list inside the format-open agent config was unreachable by any lane
    81	  (ghost, numeric and grammar-violating members all passed silently);
    82	  with D11's typed subset the same fixtures produce the declared
    83	  findings, and undeclared sibling keys stay legal open data.
    84	- **PROBE-P4-3** (list-lane interplay + key identity): the engine
    85	  measures every shape-passing occurrence with the membership lane
    86	  (duplicates included), keeps a shape-failing member invisible to
    87	  every list-level lane, and — on the file channel with map-typed keys
    88	  preserved — treats a boolean-keyed entry as unaddressable by the
    89	  string ref of the same spelling (one key-identity class, each lane
    90	  reporting its own finding).
    91	- **PROBE-P4-4** (the live declaration, at the ratifying basis): the
    92	  fail-closed class measured at CLASS WIDTH through `templateFormat`
    93	  itself — present-null, a number, an empty string and a list each
    94	  refuse with the container finding on both channels (present-null
    95	  being the member that drove two of the predecessor line's reopens);
    96	  an absent catalog admits with the empty record materialized; a
    97	  PRESENT-EMPTY catalog with a ref issued fires per site on both
    98	  channels; all three ref positions accumulate in one result; the
    99	  duplicate-ghost and shape-failing-duplicate receipts reproduce; the
   100	  boolean-keyed entry case agrees across channels; the
   101	  channel-GATED `capabilityProfile` key shows its declared asymmetry;
   102	  refs BESIDE a refused catalog draw the container finding AND their
   103	  per-site coded findings, and beside an absent catalog their per-site
   104	  findings, on both channels; an AUTHORED EMPTY ref list admits with
   105	  zero findings; undeclared sibling keys beside the typed field stay
   106	  open data and survive onto the admitted value; a cast-only
   107	  degenerate object admits as the empty record (a function refuses); a grammar-failing key beside a resolving sibling ref
   108	  leaves the ref resolved; the predecessor's key-order measurement
   109	  reproduces on the live engine (quoted integer-like keys hoist and
   110	  re-sort ascending); and the compound CLEAN case — a populated
   111	  catalog, every entry referenced, every ref resolving — admits with
   112	  zero findings (the gate position's clean case needs an injected
   113	  registry and is the P5 fixture family's).
   114	
   115	**Decision-home triage (template §1, K0→K4) — v2's new DECIDED-HERE
   116	markers: C1 fail-closed catalog admission, C2 channel-independent key
   117	lane, C7 per-occurrence resolution, C8 shape-failure invisibility.**
   118	All four triage ROW-HOME:
   119	the model shapes none of them (it pins WHEN validation happens —
   120	admission — and says nothing about finding grains or admission-shape
   121	policy), and none carries
   122	beyond-chapter architecture (they are this surface's own lane
   123	semantics, expressed by the one engine's standing behaviour). Every
   124	other row carries decisions already ratified for this surface; their
   125	authority is the predecessor's frozen record, and the old→new mapping
   126	with each divergence's full rationale lives in the review record's
   127	completeness pass — the rows carry the markers and receipts, never the
   128	arguments.
   129	
   130	**Carried scope, named:** the realized ch11 contract's prose and map
   131	entries (and one plan line, fixed by this act's aligned edits) still
   132	name the predecessor surface's row id for the binding key — the
   133	pointed-at semantics are carried unchanged by C6, so this is
   134	pointer maintenance, not meaning drift; OWNER: the boundary review
   135	(a route, not a duty of this draft).
   136	
   137	**Realization split note.** None exists. Channel scope is an engine
   138	property of the one declaration (source-bearing attributes run where a
   139	source exists); this contract re-ratifies no walk/rung partition.
   140	
   141	**Draft metrics (template §5):** rounds to ratify: — · new-decision
   142	rows: 4 DECIDED-HERE markers across 4 rows · post-ratification
   143	reopenings: —
   144	
   145	## Contract rows (every normative statement is a C-row)
   146	
   147	| ID | Rule |
   148	|---|---|
   149	| C1 | A template's root MAY carry the catalog under the key `contextBlocks` — structural authority `[d-ctxblocks]` (container, key lane, entries) and `[d-root]` (the root keyset's growth by this optional key), with the declared default: an absent catalog admits (the admitted-form surface is C13's). DECIDED HERE (v2): admission of the catalog shape is FAIL-CLOSED — a present value the declared container lane refuses is a container finding and the template does not admit (the authored-value classes measured live: PROBE-P4-4; the accept boundary is the declared lane's own, and a cast-only degenerate object with nothing to enumerate admits as empty — accepted, named, receipt: PROBE-P4-4's degenerate case); the divergence is receipted and argued in the review record's completeness pass. Refs beside a refused or absent catalog still draw their per-site C7 findings (receipt: PROBE-P4-4, both channels) — the belt's broken-operand nature (ADR-019 D10), a construct property no row needs to restate. The root-keyset aggregate-note chain RETIRES at this surface: the declared `[d-root]` fields are the enumerable authority, and the predecessor notes described pre-arrival states — no successor note is owed. |
   150	| C2 | Block ids are ONE namespace with ONE grammar governing the catalog's keys and all ref-list members alike — structural authority `[vc-block-id]` (cited by `[d-block-key]` and `[d-block-ref]`). The namespace is NEW and deliberately distinct from the gate-token and provider-token namespaces; all three ids the model exhibits satisfy it, the digit-bearing one being the grammar's designated positive case (build-driven). A key the grammar refuses draws the key lane's finding at the declared grain — DECIDED HERE (v2): the key lane runs on BOTH channels — strictly fail-louder (receipts: PROBE-P4-4's direct-channel bad-key case, PROBE-P4-3 BAD_KEY_SIBLING; mapping and rationale: the completeness pass). Key-identity on the file channel follows the engine's preserved key types (the boolean-key case: the key lane reports it, and no string ref can address it — each lane reporting its own finding, C9's audit adding its own once realized). |
   151	| C3 | A catalog entry is the body carrier — structural authority `[d-ctx-entry]` / `[d-ctx-body]`. The body is authored static text delivered into the packet without transformation, and no runtime-side or run-supplied channel can write the position (C5 closes the only nearby run-scoped one); its outbound exposure class matches the shipped instruction field's, with prompt shaping owned by the `actor-adapter-rendering` Absent, named not implied. SEMANTIC (no declaration expresses an absence of syntax): the surface defines no interpolation syntax and RESERVES none — brace-styled literal text is ordinary legal prose; any future computed-body syntax and its migration belong to the chapter that takes the `computed-templated-bodies` Absent. |
   152	| C4 | `promptConcernRefs` is the single typed field inside the agent-config value class, which stays format-open in every other key, at both of the template's config positions — the role-level default and the step-level map — structural authority `[d-prompt-refs]` on `[vc-agentconfig]`, value class `[vc-blockidlist]`; the typing basis is ADR-019 D11 (typed subset: every other agent-config key stays legal open data). ch12-C7's delegation clause pre-authorizes this arrival (the refs' value class arriving with its consuming chapter — the aggregate-note convention, so no ch12 row is affected). Absent map, absent key, and a PRESENT EMPTY list: all legal, zero refs (the admitted-form surface is C13's) — the shared value class deliberately declares no nonempty rule, diverging from every other list of the surface (receipt: PROBE-P4-4's empty-authored-list case). ch12-C7's no-field-typed sentence now describes the state before this arrival, per the standing aggregate-note practice — ch12 needs no edit. FORWARD SCOPE: the model also exhibits a MAP-SHAPED, mode-tagged ref member (the mode layer's); today's member class is deliberately string-only, and that member form arrives as D7 format growth with the chapter that takes the mode layer. |
   153	| C5 | Run scope: the `promptConcernRefs` key may appear in a runOverrides entry, but no run-supplied value is checked against the catalog, and rendering never consults it: block emission draws exclusively on the admission-produced surfaces (C13). The packet's block list is therefore the communicated truth, while the merged effective config stays what it always was — recorded run intent in the ch12 cascade. An entry whose only naming mentions arrive at run scope therefore fails C9's audit — dead by design, which C9 and this row jointly guarantee. |
   154	| C6 | The gate-binding ref position: `contextBlockRefs`, legal on ANY binding regardless of evaluator class (carried decision — communication is evaluator-independent) — structural authority `[d-ctx-gate-refs]` on the binding's declaration, value class `[vc-blockidlist]` (identical semantics to C4's position). The binding keyset itself is ch11-C4's realized text; this surface adds no keyset duty (the pointer-maintenance remainder is the Context's named carried scope). |
   155	| C7 | Resolution is ENTRY-BELTED (ADR-019 D10, the construct this surface carried into the vocabulary): every ref member that passed its own shape lane must resolve against the keys of the catalog WHOSE VALUE IS A VALID ENTRY — validity measured by the catalog's own declared entry node, one definition governing both channels; key existence alone is not resolution. The declared lanes run at ADMISSION on both channels (the moment the ch11 sync fixed; the model's invariant record agrees). Structural authority: `[vc-blockidlist]`'s membership rule, carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain, all accumulated. When a ref names an entry the declaration refuses, both facts report — the entry's finding and the coded finding at the ref site — each fixable on its own (PROBE-P4-4). Containment, why C10's dispatch-time miss is integrity drift: nothing the render reads escapes the validated set, and dispatch only ever consumes store-admitted templates. DECIDED HERE (v2): the resolution lane measures EVERY shape-passing occurrence — a duplicated unresolved ref reports per occurrence beside C8's duplicate finding (receipts: PROBE-P4-3 DUP_GHOST, PROBE-P4-4; rationale and old→new mapping: the completeness pass). This row is the refs-validated-at-definition-load invariant's home (disposition test). |
   156	| C8 | Duplicates: a repeated id within one ref list draws the per-occurrence duplicate finding at the declared grain — structural authority `[vc-blockidlist]`'s duplicate rule. DECIDED HERE (v2): a member that failed its OWN shape lane is invisible to every LIST-LEVEL lane — no duplicate finding, no membership finding; its own finding is the trace (receipts: PROBE-P4-3 DUP_BAD_SHAPE, PROBE-P4-4; rationale and mapping: the completeness pass). C9's audit is NOT a list-level lane and is deliberately outside this rule — its reference set reads the raw authored members, as its own row states. The edge family's success negative: the compound CLEAN case — a populated catalog, every entry referenced, every ref resolving — admits with zero findings (receipt: PROBE-P4-4, measured gate-free; the gate position's clean case needs an injected registry, so its member belongs to P5's build-driven fixture family). |
   157	| C9 | Unreferenced-entry hygiene — SEMANTIC (residual R2), realized at P5 as a named lane in code (carrying NO issue-code field — C18's exclusivity governs): a catalog entry no ref names is a validation finding at the entry's path, strict-start (relaxable additively, never tightenable without a breaking change). Carried decisions, all three: WHAT COUNTS AS REFERENCED — every raw member string authored in any ref list whose container held, including members failing the grammar or repeating (a defective mention still names its target; the member's own lanes report the defect). WHAT GETS AUDITED — each key the catalog itself enumerates, unconditionally; no key earns an exemption from the shape of its value (the audit and C7's belt share one enumerable-own-key domain under this engine — the once-accepted asymmetry residual is closed, not carried; the completeness pass records it). WHEN THE CHECK STANDS DOWN — template-wide, whenever any lane that makes a ref list UNREACHABLE fired: among them a ref position's own container lane, the enclosing agent-config container/canonical lanes, and the gate chain's enclosures (binding, pipeline, gates containers) (the audited set is one union over every list, and auditing a partial union would accuse entries whose only mentions sit inside the broken position); a failed CATALOG container needs no stand-down clause — C1 refuses the template outright. |
   158	| C10 | Render order and provenance — SEMANTIC, runtime (realized at the render build): blocks emit by source — the role position first, the step position second, the gate position last — and inside one source the authored list sequence holds. The gate leg walks the admitted gates record in that record's own enumeration, each event's pipeline in its authored sequence, each binding's list likewise, with C11 deciding what may emit. That record enumerates as authored except where the substrate hoists integer-like quoted keys to the front AND re-sorts them ascending among themselves — DERIVED from the recorded substrate measurement PROBE-CB3 (the predecessor's frozen Context holds the record) and RE-EXECUTED on the live engine at this basis (receipt: PROBE-P4-4's key-order case), named measurer: the P5 render build re-executes the probe before pinning order fixtures. An id met a second time emits nothing new — its first position stands — while the provenance list records every emitter as encountered (the dedup-with-retained-provenance invariant's home, disposition test). The admitted-form construction order is thereby a contract-visible property, not a free refactor. Body text comes solely from the admitted catalog; after admission no lookup can fail, so a dispatch-time miss signals kernel-integrity drift and must abort loudly rather than degrade. |
   159	| C11 | The gate-ref predicate — SEMANTIC, runtime: a gate binding may emit its blocks exactly when its transition belongs to both sets: the step's available operations AND the L1 capability narrowing (existing authority logic, nothing minted). The membership half is guaranteed once admission passed — every gates key is one of its step's transitions — so what tests it is the iteration-domain negative: no block may originate outside the dispatched step's own gates. The narrowing half owes its NON-WAIVABLE counterexample: a directly-constructed capability profile that narrows the dispatched role below its transitions must silence that gate's blocks although the event remains available. With default capability derivation the narrowing never bites, which is exactly why this counterexample may not be skipped (the green-but-blind class). The profile's type-level channel is declared at `[d-capability-profile]`. FORWARD SCOPE: the membership half's admission-time guarantee holds only BEFORE the EC layer arrives — that layer changes the operand, and the chapter taking the EC candidate re-examines the half. This row is the authority-scoped-gate-blocks invariant's home (disposition test). |
   160	| C12 | The packet field — SEMANTIC, wire: `ContextPacket.contextBlocks` is ALWAYS present (possibly empty), an ordered list whose members carry the id, the body and the provenance sources, the list sequence being C10's render order, travelling verbatim through the canonical packet channel (object keys canonicalize, array order is semantic). A provenance source member is the model unit's tagged form, its token VALUES model-verbatim snake_case (`role_config` | `step_config` | `gate_binding`) and authored as string literals — this row carries the rename relation's WHOLE normative content: the key half is exhausted by C1/C4/C6 naming their camelCase keys literally plus this row's own packet field name, the token half is this clause; the gate member's location pair is flattened to sibling step/event fields (carried decision). When two bindings of one step and event name the same id, two identical source members appear — provenance is never collapsed. Because the field is unconditional, every full-packet equality fixture in the tree re-pins: one named family, membership enumerated by the owning packet. |
   161	| C13 | The admitted-form surfaces — DERIVED, named measurer: the P5 build's acceptance family plus the parity gate. On admission success the admitted template carries: the catalog record (always present — the declared default covers absence; a present catalog arrives validated, C1 having refused every non-map form); and normalized ref-list fields FOR EVERY ref position (empty list where nothing was authored), produced by the normalizer (ADR-019 D3 — derivation is never validation) as SIBLING fields at the fixed-keyset levels (the roles entry, the step, the admitted binding) — beside, never inside, the raw agent-config maps. TYPE GRAIN (carried decision): the shared raw types mark the normalized fields with `?`, while every admitted VALUE carries them — the standing admitted-form optionality pattern; marking them required was weighed and refused, since it would invalidate each hand-built raw fixture in the tree. The domain type's `contextBlocks?` field already has this shape. CHANNEL GRAIN (the producer-monopoly precedent `advancesRound`, carried WHOLE): the TWO admission-produced positions — the roles entry's and the step's — are DECLARED channel-gated to the direct channel at P5, while the binding's `contextBlockRefs` is C6's authored key and stays channel-both; a caller holding an admitted value thereby re-admits it and a FILE author meets the unknown-key refusal, AND the fill RECOMPUTES each produced position from its authored source, overwriting any caller-supplied value — the monopoly half, which is what keeps C7's containment true on the direct channel. FIELD SPELLINGS, decided here for P5 to declare: the normalized fields carry their authored sources' names — `promptConcernRefs` as the sibling at the roles entry and the step, `contextBlockRefs` on the binding. Realization needs a NEW normalizer hook (D7 format growth landing at P5's own act, never silently), the ref fill running BEFORE the binding rebuild so an unauthored list lands as the empty list, not an undefined carry; and the `[n-effective-config]` hook's carry list must grow `contextBlockRefs`, or the rebuild drops the authored refs — the carry list plus the hook's produced field is the admitted binding's whole keyset. Two named build traps: admission rebuilds only `steps` today (the roles side is new work), and the role-side source is NESTED inside the config map (the first nested-source normalization). P5 also flips the three l2b domain-registry rows (the catalog and ContextBlockRef rows with the definition packet, carrying their realized type names — `BlockId`, the catalog record type; the ContextBlock row with the render side, its witness the packet's rendered block member type) — named here exactly BECAUSE omitting the flip trips no gate; a realized row needs its type name AND the registry's two sync surfaces, and if P5 ships as one packet the assignment collapses onto it unchanged. Rendering consumes these admitted surfaces and nothing rawer, never re-validating at dispatch; the catalog's entry type is the ONLY typed place body text can live (this row homes the single-body-source invariant, disposition type/schema). The raw config maps retain the authored key untouched for the ch12 cascade to read (C5). The ROLE-ENTRY and STEP keysets do not widen for FILE authors — the normalized positions are admission-produced, and a file authoring one meets the unknown-key refusal, while the direct channel admits them under the channel grain above; the binding's `contextBlockRefs` is the stated exclusion: there the AUTHORED key (C6) is itself the normalized position's source and is carried, never refused. |
   162	| C14 | Communication-only, byte-scope precise — SEMANTIC (disposition review): neither the packet field nor the catalog behind it may influence round arithmetic, gate evaluation, transition verdicts or any other decision the kernel takes; the one stated exception is the definition-static family itself, where catalog content decides admission findings and nothing further. The canonical experiment: delete an entry together with its refs in a single edit — every verdict and transition must come out the same, and the differing bytes must be confined to the packet artifact plus, exactly where refs rode the config positions, the committed provenance rows (a ref was always config data there); deleting a gate-sourced ref must leave committed rows byte-identical (the committed surfaces by row id: ch12-C10's provenance rows, ch11-C27's transcript records; the spawn-side closure is ch9-C18's refs-uninterpreted rule). This row is the communication-only invariant's home (disposition review) — the owning packet binds its REV line against THIS byte-precise scope, never a wholesale phrasing. Spawn-side, both carriers are closed: the packet carries refs as data, and the argv-interpretation path is closed by the adapter's refs-uninterpreted rule (ch9). |
   163	| C15 | Determinism — SEMANTIC (disposition test): rendering depends on nothing but its three inputs — instance, admitted template, step; no wall clock, no entropy, no store access outside them — so identical committed state under an identical admitted template must reproduce the block list byte for byte. Carrier: the P5-line golden trace plus a lane that renders twice from one input set and asserts equality. This row is the deterministic-ordered-render invariant's home (disposition test) — C10's construction owns the order half; this row owns reproducibility. |
   164	| C16 | The shipped catalog — SEMANTIC, shipped-artifact duty: the canonical template file gains a catalog carrying the emit-envelope entry and BOTH roles gain a default-config ref to it (carried decision: every actor emits, the block is role-symmetric; this is the first agent-config-family authoring in a shipped template file). The entry's text gets written when the owning packet lands, sourced from the interim knowledge carrier `v1-prompt-parity-audit.md` (§2's Class A envelope content, under §5's interim-carrier ruling); retiring that carrier is bound to the EC chapter's definition of done. Ripple: the named fixture families (canonical-file equality pin moving with the file; effective-config and full-packet equality re-pins (the full-packet member doubling as C12's tree-wide presence ripple); committed-provenance re-pins; the golden-trace fixture disposition — its snake-spelled fixture sites migrate onto the typed key together with a resolving catalog entry, which realizes the L0c slot; absence-consumers swept by key AND value). MEMBERSHIP CRITERION for the re-pin families: a suite belongs iff it CONSUMES the canonical file or the shared fixture — directory location decides nothing, and suites constructing their own templates stay outside. The criterion bounds the CATALOG-CONTENT ripple families only — C12's always-present-field ripple is tree-wide and this criterion never narrows it. Member enumeration stays the owning packet's. |
   165	| C17 | Catalog-authoring caveat — aim, not enforcement: prose describing a gate's configured value carries no freshness guarantee — the config can move under it and nothing notices, because resolution checking establishes only that an id finds an entry, never that the entry's text still tells the truth. Closing the gap needs the two Absents this surface names and does not build (`computed-templated-bodies`, `semantic-parity-check`); the chapter that takes the plan's EC candidate (§1.3's fifth candidate) or a later one owns both. A comment ships beside the catalog in the canonical file saying exactly this, sited where the next entry gets written. Nothing this chapter ships describes gate configuration, so nothing can go stale yet. |
   166	| C18 | The CLI surface — SEMANTIC, machine-shape duty: zero verb growth and zero flag growth; this chapter's lanes ride the standing validate/store/create channels as they are, and C7's code must arrive intact in the CLI document at the far end — build-driven by a lane asserting the actual VALUE of the code field in the CLI JSON (equality, never mere presence) on a ch13-only fixture, and by its negative twin — no other lane of this chapter may carry any code, while the gate schemas' named lanes keep the codes they always had. |
   167	| C19 | The lane inventory: this surface's definition-static lanes are the lanes the ch13v2-rowed VALIDATION nodes introduce — enumerated by the declaration itself — plus C9's one semantic lane. Named exclusions, four: `[d-root]` and `[vc-agentconfig]` are SHARED validation nodes whose pre-existing lanes belong to their own surfaces (only the growth is this chapter's); `[n-effective-config]` is a NORMALIZER cited for C13's duty — derivation introduces no lane (ADR-019 D3); and `[d-capability-profile]` is a raw, lane-less node (its file-channel refusal is `[d-root]`'s own unknown-key lane). Channel scope is an ENGINE property of the one declaration: no walk/rung realization split exists or is re-ratified, and for the catalog and the AUTHORED ref positions (C4/C6) admission answers identically on the two channels — accept or refuse — with only source-bearing attributes differentiating the finding sets (receipt: PROBE-P4-4). Declared, deliberate asymmetries are not defects of that rule: `[d-capability-profile]` is channel-GATED today (receipt: PROBE-P4-4), and the two admission-PRODUCED positions C13 channel-gates join it at P5 — arrivals this row forward-scopes. Named P5 build surfaces: the C9 lane; the C13 normalizer hook WITH its declaration growth (the two channel-gated produced-position nodes under their decided spellings), carry-list growth and registry flips; and the C16/C18 fixture duties. |
   168	
   169	## Ratification history (empty at `draft` — blocks are appended by the lifecycle acts)
   170	
   171	## Realized map (empty until chapter close)

exec
/bin/zsh -lc "nl -ba v3/src/definition/schema/templateFormat.ts | sed -n '1,760p'" in /Users/felho/dev/pairflow
 succeeded in 0ms:
     1	import { defineSurface } from "./defineSurface.js";
     2	import type { NodeDecl, SurfaceDecl } from "./vocabulary.js";
     3	
     4	/**
     5	 * ADR-019 D4: THE CANONICAL HOME — the template surface's declaration, as
     6	 * DATA. This module contains no executable logic: every export is a frozen
     7	 * declaration object consumed by `engine.ts` (the validator) and
     8	 * `normalizer.ts` (the derivation capability, D3).
     9	 *
    10	 * The two-authority rule (D4): the CONTRACT states decisions and cites the
    11	 * `tag` values below; a row's passing gloss of an attribute is non-binding
    12	 * paraphrase and THESE bytes govern (the contract's Context states the
    13	 * rule). A structural rule carried NORMATIVELY in both places is a defect;
    14	 * citation closure (every tag cited exists; every node rowed to a contract
    15	 * is cited by it) is checked mechanically at review — no standing code
    16	 * check exists for it yet.
    17	 *
    18	 * Every node below realizes the re-expression column of
    19	 * `v3/implementation/schema-expressiveness-audit.md` (the standing pin is
    20	 * recorded in ADR-019's Context — never restated here, so it cannot go
    21	 * stale twice) and carries the ratified rows it realizes in `rows`. The
    22	 * audited residual stays PROSE/CODE and is NOT declared here — R2
    23	 * (unreferenced hygiene), R3 (the existential cross-rule over resolved
    24	 * registrations), R4 (derivation — the normalizer's), R5 (the
    25	 * cross-artifact store check), R6 (substrate wording), R7 (see
    26	 * `d-gate-config`'s note) — with ONE exception since D10: R1's
    27	 * value-shaped reference belt became the `validKeysOf` construct and IS
    28	 * declared below (`vc-blockidlist`).
    29	 */
    30	
    31	// ---------------------------------------------------------------------------
    32	// Value classes (vocabulary #15).
    33	// ---------------------------------------------------------------------------
    34	
    35	/** ch8-C10: ONE grammar for every id class — nonempty, no whitespace, no
    36	 * dot. The citing site supplies `{label}`. */
    37	const idClass: NodeDecl = {
    38	  kind: "string",
    39	  tag: "vc-id-class",
    40	  rows: ["ch8-C10"],
    41	  typeMessage: "{label} must be a nonempty string, got {value}",
    42	  nonempty: { message: "{label} must be a nonempty string, got {value}" },
    43	  grammar: {
    44	    re: "^[^\\s.]+$",
    45	    message: 'invalid {label} {valueJson}: ids contain no whitespace and no "."',
    46	  },
    47	};
    48	
    49	/** ch8-C14 → ch12-C7: a run-profile position is a PLAIN map whose resolved
    50	 * values are canonical-JSON-safe. A non-plain container suppresses the
    51	 * canonical lane (dependent-lane suppression). */
    52	const agentConfigValue: NodeDecl = {
    53	  kind: "map.plain",
    54	  tag: "vc-agentconfig",
    55	  rows: ["ch8-C14", "ch12-C7", "ch13v2-C4"],
    56	  containerMessage: "{label} must be a map; got {value}",
    57	  canonicalJsonSafe: {
    58	    message: "{label} must be canonical-JSON-safe (a map of finite numbers and plain values)",
    59	  },
    60	  // ch13v2-C4 (ADR-019 D11): the ONE typed field of the otherwise
    61	  // format-open agent config. Both template positions (the role-level
    62	  // default and the step-level map) share this class, so one declaration
    63	  // covers both; the runOverrides position never meets this declaration
    64	  // (a CLI input surface — ch13v2-C5's run scope).
    65	  fields: {
    66	    promptConcernRefs: {
    67	      kind: "valueClass",
    68	      tag: "d-prompt-refs",
    69	      rows: ["ch13v2-C4"],
    70	      valueClass: "blockIdList",
    71	      label: "promptConcernRefs",
    72	    },
    73	  },
    74	};
    75	
    76	/** ch13v2-C2: ONE grammar for catalog keys and every ref-list member —
    77	 * the block-id namespace. The citing site supplies `{label}`. */
    78	const blockId: NodeDecl = {
    79	  kind: "string",
    80	  tag: "vc-block-id",
    81	  rows: ["ch13v2-C2"],
    82	  typeMessage: "invalid {label} {valueJson}: block ids are kebab-case strings",
    83	  nonempty: { message: "invalid {label} {valueJson}: block ids are kebab-case strings" },
    84	  grammar: {
    85	    re: "^[a-z][a-z0-9-]*$",
    86	    message: "invalid {label} {valueJson}: block ids match {grammar}",
    87	  },
    88	};
    89	
    90	/** ch13v2-C4/C6/C7/C8: the shared ref-list value class — the entry belt
    91	 * (ADR-019 D10) and the per-occurrence duplicate lane, one definition
    92	 * for all three ref positions. */
    93	const blockIdList: NodeDecl = {
    94	  kind: "list",
    95	  tag: "vc-blockidlist",
    96	  rows: ["ch13v2-C4", "ch13v2-C6", "ch13v2-C7", "ch13v2-C8"],
    97	  containerMessage: "{label} must be a list of context block ids; got {value}",
    98	  memberLaneAt: "index",
    99	  member: {
   100	    kind: "valueClass",
   101	    tag: "d-block-ref",
   102	    rows: ["ch13v2-C4", "ch13v2-C6"],
   103	    valueClass: "blockId",
   104	    label: "context block ref",
   105	  },
   106	  unique: { grain: "perOccurrence", at: "index", message: "duplicate context block ref {valueJson}" },
   107	  memberOf: {
   108	    relation: "memberOf",
   109	    target: { validKeysOf: "$.contextBlocks" },
   110	    code: "unresolved_context_block_ref",
   111	    message: "context block ref {valueJson} does not resolve to an entry",
   112	  },
   113	};
   114	
   115	/**
   116	 * NOT DECLARED HERE, and the reason is a REPO GUARD, not taste: the three
   117	 * delegated gate-config schemas (`declarative.threshold`,
   118	 * `pairflow.previous_reviewer_verdict`, `external.process`) live behind
   119	 * ch11-C5's `delegate: registry(uses)` hand-off, and ch11-P2a G1 —
   120	 * lint-enforced — permits `gates/` to VALUE-import `domain/` and `ports/`
   121	 * only. A registration cannot consume an engine that lives under
   122	 * `definition/`, and moving the engine into `domain/` or `ports/` to make
   123	 * it reachable would be a worse answer than leaving the hand-off alone.
   124	 *
   125	 * Under ADR-019 D2 (the TEMPLATE surface, now; nothing else is built) and
   126	 * D6 (migrating a surface is a deliberate per-surface act, never a "while
   127	 * we are here"), the three schemas stay as built. The audit's paper
   128	 * declarations `[d-gc-threshold]`, `[d-gc-verdict]` and `[d-gc-process]`
   129	 * remain available to the act that migrates them.
   130	 */
   131	
   132	// ---------------------------------------------------------------------------
   133	// The template surface's node tree.
   134	// ---------------------------------------------------------------------------
   135	
   136	const transitionsNode: NodeDecl = {
   137	  kind: "map.open",
   138	  tag: "d-transitions",
   139	  rows: ["ch8-C12", "ch8-C10"],
   140	  presence: { required: true },
   141	  containerMessage: "transitions must be a map of event-type -> target id (it may be empty)",
   142	  keyClass: { kind: "valueClass", tag: "d-event-type", rows: ["ch8-C10"], valueClass: "idClass", label: "event type" },
   143	  keyLaneAt: "container",
   144	  entry: {
   145	    kind: "string",
   146	    tag: "d-target",
   147	    rows: ["ch8-C19"],
   148	    // No `typeMessage`: a non-string target IS the membership fault, and
   149	    // the measured lane emits ONE finding for both.
   150	    memberOf: {
   151	      relation: "memberOf",
   152	      target: { union: [{ keysOf: "$.steps" }, { valuesOf: "$.terminal" }] },
   153	      message: "transition target must name a step or a terminal id; got {value}",
   154	    },
   155	  },
   156	};
   157	
   158	const gateBindingNode: NodeDecl = {
   159	  kind: "map.fixed",
   160	  tag: "d-binding",
   161	  rows: ["ch11-C4"],
   162	  containerMessage: "gate binding must be a map",
   163	  unknownMessage: "unknown gate binding key '{key}' (allowed: {keys})",
   164	  fields: {
   165	    uses: {
   166	      kind: "string",
   167	      tag: "d-uses",
   168	      rows: ["ch11-C6", "ch11-C8", "ch11-C21"],
   169	      presence: { required: true, foldedIntoTypeLane: true },
   170	      typeMessage: "uses must be a non-empty string",
   171	      nonempty: { message: "uses must be a non-empty string" },
   172	      grammar: {
   173	        re: "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$",
   174	        message: "uses must match {grammar} (two or more dot-separated lowercase segments); got {valueJson}",
   175	      },
   176	      // D8's ADMITTED widening: a selector root that is an INJECTED set.
   177	      // The finding reports at the BINDING, not at `…uses` (audit §5 F3).
   178	      memberOf: {
   179	        relation: "memberOf",
   180	        target: { injected: "gateCatalog" },
   181	        at: "container",
   182	        code: "gate_evaluator_unavailable",
   183	        message: "no gate evaluator is registered for '{valueRaw}'",
   184	      },
   185	    },
   186	    config: {
   187	      kind: "delegate",
   188	      tag: "d-gate-config",
   189	      rows: ["ch11-C5"],
   190	      registry: "gateCatalog",
   191	      by: "uses",
   192	      presence: { required: true, foldedIntoTypeLane: true },
   193	      dependsOn: ["d-uses"],
   194	      beltMessage: "gate evaluator '{valueRaw}' reported a config failure without findings",
   195	    },
   196	    // ch13v2-C6: the binding's ref position. The keyset growth itself is
   197	    // ch11-C4's realized text (the executed 2026-07-26 reopen act).
   198	    contextBlockRefs: {
   199	      kind: "valueClass",
   200	      tag: "d-ctx-gate-refs",
   201	      rows: ["ch13v2-C6"],
   202	      valueClass: "blockIdList",
   203	      label: "contextBlockRefs",
   204	    },
   205	  },
   206	};
   207	
   208	const stepNode: NodeDecl = {
   209	  kind: "map.fixed",
   210	  tag: "d-step",
   211	  rows: ["ch8-C9", "ch8-C13", "ch11-C1"],
   212	  containerMessage:
   213	    "a step must be a map with exactly role, instruction, transitions (+ optional agentConfig, gates)",
   214	  unknownMessage: "unknown key {value}",
   215	  laneOrder: "unknownThenMissingThenValues",
   216	  fields: {
   217	    role: {
   218	      kind: "valueClass",
   219	      tag: "d-role-ref",
   220	      rows: ["ch8-C10", "ch8-C16"],
   221	      valueClass: "idClass",
   222	      label: "role name",
   223	      presence: { required: true },
   224	      // A grammar-invalid role makes the USED-role set unreliable, so the
   225	      // role-set equality is suppressed rather than cascaded.
   226	      gating: true,
   227	    },
   228	    instruction: {
   229	      kind: "string",
   230	      tag: "d-instruction",
   231	      rows: ["ch8-C11"],
   232	      presence: { required: true },
   233	      typeMessage: "instruction must be a nonempty string",
   234	      nonempty: { message: "instruction must be a nonempty string" },
   235	    },
   236	    transitions: transitionsNode,
   237	    advancesRound: {
   238	      kind: "raw",
   239	      tag: "d-advances-round",
   240	      rows: ["ch11-C39"],
   241	      // The ADMITTED form's producer-owned field. A direct-construction
   242	      // caller may already hold an admitted value, so the key is legal on
   243	      // that channel and is RECOMPUTED by the normalizer's producer
   244	      // monopoly; it is never authorable in a file.
   245	      channel: "direct",
   246	    },
   247	    agentConfig: {
   248	      kind: "valueClass",
   249	      tag: "d-agentconfig",
   250	      rows: ["ch8-C14", "ch12-C7"],
   251	      valueClass: "agentConfigValue",
   252	      label: "agentConfig",
   253	    },
   254	    gates: {
   255	      kind: "map.open",
   256	      tag: "d-gates",
   257	      rows: ["ch11-C1", "ch11-C2", "ch11-C21"],
   258	      containerMessage: "gates must be a map of event type to gate pipeline",
   259	      keyLaneAt: "container",
   260	      // The gates subtree's file-channel key-STRINGNESS scan: a non-string
   261	      // key cannot become an own property and would blind every own-key
   262	      // scan downstream.
   263	      deepKeyStringness: {
   264	        message: "map keys in the gates subtree must be strings; got {value}",
   265	        channel: "file",
   266	      },
   267	      // ch11-C2: a gates key outside the step's transitions is DEAD config
   268	      // — nothing under it has an operand to be validated against.
   269	      keysSubsetOf: {
   270	        relation: "keysSubsetOf",
   271	        target: { keysOf: "^.transitions" },
   272	        message: "dead gate config: '{key}' is not a transition of step '{ownerKey}'",
   273	      },
   274	      entry: {
   275	        kind: "list",
   276	        tag: "d-pipeline",
   277	        rows: ["ch11-C3"],
   278	        containerMessage: "gate pipeline must be a list",
   279	        nonempty: { message: "gate pipeline must not be empty" },
   280	        memberLaneAt: "index",
   281	        member: gateBindingNode,
   282	      },
   283	    },
   284	  },
   285	};
   286	
   287	const runtimeContextSpecNode: NodeDecl = {
   288	  kind: "map.fixed",
   289	  tag: "d-rtc-spec",
   290	  rows: ["ch12-C3"],
   291	  containerMessage:
   292	    'runtimeContext must be "none" or a spec map { kind, provider, config? } when present; got {value}',
   293	  unknownMessage: "unknown key {value} (a runtimeContext spec map's only keys are kind, provider, config?)",
   294	  fields: {
   295	    kind: {
   296	      kind: "string",
   297	      tag: "d-rtc-kind",
   298	      rows: ["ch12-C3"],
   299	      presence: { required: true },
   300	      typeMessage: "kind must be a string matching {grammar}; got {value}",
   301	      grammar: { re: "^[a-z][a-z0-9_]*$", message: "kind must be a string matching {grammar}; got {value}" },
   302	    },
   303	    provider: {
   304	      kind: "string",
   305	      tag: "d-rtc-provider",
   306	      rows: ["ch12-C3"],
   307	      presence: { required: true },
   308	      typeMessage:
   309	        "provider must be a string matching {grammar} (two or more dot-separated lowercase segments); got {value}",
   310	      grammar: {
   311	        re: "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$",
   312	        message:
   313	          "provider must be a string matching {grammar} (two or more dot-separated lowercase segments); got {value}",
   314	      },
   315	    },
   316	    config: {
   317	      kind: "raw",
   318	      tag: "d-rtc-config",
   319	      rows: ["ch12-C3"],
   320	      containerMessage: "config must be a map when present; got {value}",
   321	    },
   322	  },
   323	};
   324	
   325	const rootNode: NodeDecl = {
   326	  kind: "map.fixed",
   327	  tag: "d-root",
   328	  rows: ["ch8-C7", "ch8-C13", "ch8-C24", "ch8-C25", "ch11-C18", "ch11-C37", "ch12-C1", "ch13v2-C1"],
   329	  containerMessage: "the template root must be a map with exactly ref, start, steps, terminal, roles",
   330	  // ch8-C24's reservation lives in this message and nowhere else: `kind`
   331	  // is illegal today, and the closed keyset below is what makes it so.
   332	  unknownMessage: 'unknown key {value} (fixed keysets grow only by ratified additive keys — V16 reserves "kind")',
   333	  // ch8-C25: the removed/renamed key registry, EMPTY at v0. The lane is
   334	  // live; the registry has no members yet.
   335	  removedKeys: {},
   336	  laneOrder: "missingThenUnknown",
   337	  fields: {
   338	    ref: {
   339	      kind: "map.fixed",
   340	      tag: "d-ref",
   341	      rows: ["ch8-C8"],
   342	      presence: { required: true },
   343	      containerMessage: "ref must be a map with exactly id and version",
   344	      unknownMessage: "unknown key {value}",
   345	      fields: {
   346	        id: {
   347	          kind: "string",
   348	          tag: "d-ref-id",
   349	          rows: ["ch8-C8"],
   350	          presence: { required: true },
   351	          typeMessage: "id must be a string matching {grammar} (filename-safe); got {value}",
   352	          grammar: {
   353	            re: "^[a-z0-9][a-z0-9-]*$",
   354	            message: "id must be a string matching {grammar} (filename-safe); got {value}",
   355	          },
   356	        },
   357	        version: {
   358	          kind: "integer",
   359	          tag: "d-ref-version",
   360	          rows: ["ch8-C8"],
   361	          presence: { required: true },
   362	          sourceForm: "plainDecimalInteger",
   363	          resolvedForm: { safeInteger: true, min: 1, message: "{path} must resolve to a safe integer >= 1" },
   364	        },
   365	      },
   366	    },
   367	    start: {
   368	      kind: "string",
   369	      tag: "d-start",
   370	      rows: ["ch8-C18"],
   371	      presence: { required: true },
   372	      memberOf: {
   373	        relation: "memberOf",
   374	        target: { keysOf: "$.steps" },
   375	        message: "start must name an existing step; got {value}",
   376	      },
   377	    },
   378	    steps: {
   379	      kind: "map.open",
   380	      tag: "d-steps",
   381	      rows: ["ch8-C9", "ch8-C10"],
   382	      presence: { required: true },
   383	      containerMessage: "steps must be a NONEMPTY map of step-id -> step",
   384	      // NOT gating: ch8-C21's container precondition binds MISSING or
   385	      // WRONG-KIND containers only. An empty map is a valid map OF ITS
   386	      // KIND, so `keys($.steps)` = the empty set EXISTS and every rule
   387	      // selecting over it runs normally (ch11-P4 F7's `(b′)` entry).
   388	      nonempty: { message: "steps must be a NONEMPTY map" },
   389	      keyClass: { kind: "valueClass", tag: "d-step-id", rows: ["ch8-C10"], valueClass: "idClass", label: "step id" },
   390	      keyLaneAt: "container",
   391	      entry: stepNode,
   392	    },
   393	    terminal: {
   394	      kind: "list",
   395	      tag: "d-terminal",
   396	      rows: ["ch8-C17", "ch8-C10"],
   397	      presence: { required: true },
   398	      containerMessage: "terminal must be a nonempty list of unique ids",
   399	      nonempty: { message: "terminal must be a NONEMPTY list" },
   400	      memberLaneAt: "container",
   401	      member: {
   402	        kind: "valueClass",
   403	        tag: "d-terminal-id",
   404	        rows: ["ch8-C10"],
   405	        valueClass: "idClass",
   406	        label: "terminal id",
   407	      },
   408	      unique: { grain: "perOccurrence", at: "container", message: "duplicate terminal id {valueJson}" },
   409	      disjointFrom: {
   410	        relation: "disjointFrom",
   411	        target: { keysOf: "$.steps" },
   412	        message: "terminal id {valueJson} collides with a step id (terminal is disjoint from keys(steps))",
   413	      },
   414	    },
   415	    roles: {
   416	      kind: "map.open",
   417	      tag: "d-roles",
   418	      rows: ["ch8-C15", "ch8-C10", "ch8-C16"],
   419	      presence: { required: true },
   420	      containerMessage: "roles must be a map of role-name -> { defaultActor?, defaultAgentConfig? }",
   421	      keyClass: {
   422	        kind: "valueClass",
   423	        tag: "d-role-name",
   424	        rows: ["ch8-C10"],
   425	        valueClass: "idClass",
   426	        label: "role name",
   427	        // The declared-side twin of `d-role-ref`: a grammar-invalid
   428	        // declared name suppresses the role-set equality.
   429	        gating: true,
   430	      },
   431	      keyLaneAt: "container",
   432	      entry: {
   433	        kind: "map.fixed",
   434	        tag: "d-roles-entry",
   435	        rows: ["ch8-C15", "ch12-C6"],
   436	        containerMessage:
   437	          "a roles entry must be a map whose legal keys are the optional defaultActor and defaultAgentConfig",
   438	        unknownMessage: "unknown key {value}",
   439	        fields: {
   440	          defaultActor: {
   441	            kind: "string",
   442	            tag: "d-defaultactor",
   443	            rows: ["ch8-C15"],
   444	            typeMessage: "defaultActor must be a nonempty string when present",
   445	            nonempty: { message: "defaultActor must be a nonempty string when present" },
   446	          },
   447	          defaultAgentConfig: {
   448	            kind: "valueClass",
   449	            tag: "d-defaultagent",
   450	            rows: ["ch12-C6", "ch12-C7"],
   451	            valueClass: "agentConfigValue",
   452	            label: "defaultAgentConfig",
   453	          },
   454	        },
   455	      },
   456	    },
   457	    capabilityProfile: {
   458	      kind: "raw",
   459	      tag: "d-capability-profile",
   460	      rows: ["ch13v2-C11"],
   461	      // The L1 authorization profile: a TYPE-LEVEL channel only. Authored
   462	      // restrictions are a deferred Absent, so the key stays an
   463	      // unknown-key rejection in a template FILE and is legal only on the
   464	      // direct-construction channel.
   465	      channel: "direct",
   466	    },
   467	    round: {
   468	      kind: "map.fixed",
   469	      tag: "d-round",
   470	      rows: ["ch11-C37", "ch11-C38", "ch11-C40"],
   471	      containerMessage: "round must be a map with the single key advanceOnArrivalAt",
   472	      unknownMessage: "unknown key {value} (round's only key is advanceOnArrivalAt)",
   473	      fields: {
   474	        advanceOnArrivalAt: {
   475	          kind: "list",
   476	          tag: "d-round-list",
   477	          rows: ["ch11-C40"],
   478	          presence: { required: true },
   479	          containerMessage: "advanceOnArrivalAt must be a nonempty list of step ids",
   480	          nonempty: { message: "round.advanceOnArrivalAt must not be empty" },
   481	          memberLaneAt: "index",
   482	          member: {
   483	            kind: "string",
   484	            tag: "d-round-member",
   485	            rows: ["ch11-C40"],
   486	            typeMessage: "advanceOnArrivalAt member must be a step id string; got {value}",
   487	          },
   488	          memberOf: {
   489	            relation: "memberOf",
   490	            target: { keysOf: "$.steps" },
   491	            message: "round.advanceOnArrivalAt member '{valueRaw}' is not a step",
   492	          },
   493	          unique: {
   494	            grain: "perOccurrence",
   495	            at: "index",
   496	            message: "round.advanceOnArrivalAt member '{valueRaw}' is duplicated",
   497	          },
   498	        },
   499	      },
   500	    },
   501	    runtimeContext: {
   502	      kind: "union",
   503	      tag: "d-rtc",
   504	      rows: ["ch11-C18", "ch12-C2", "ch12-C4"],
   505	      default: "none",
   506	      literals: ["none"],
   507	      mapCase: runtimeContextSpecNode,
   508	      removedValues: {
   509	        required:
   510	          'runtimeContext: "required" is retired — author the spec map { kind, provider, config? } (a directly-constructed RuntimeContextSpec)',
   511	      },
   512	      message:
   513	        'runtimeContext must be "none" or a spec map { kind, provider, config? } when present; got {value}',
   514	    },
   515	    activation: {
   516	      kind: "map.fixed",
   517	      tag: "d-activation",
   518	      rows: ["ch12-C1"],
   519	      default: { mode: "immediate" },
   520	      containerMessage: "activation must be a map with the single key mode",
   521	      unknownMessage: "unknown key {value} (activation's only key is mode)",
   522	      fields: {
   523	        mode: {
   524	          kind: "enum",
   525	          tag: "d-act-mode",
   526	          rows: ["ch12-C1"],
   527	          presence: { required: true },
   528	          // The authored↔stored token map (ch12-C1's C1/C11 mapping),
   529	          // declared so neither side silently forks.
   530	          members: [
   531	            { value: "immediate", store: "immediate" },
   532	            { value: "deferredKickoff", store: "deferred_kickoff", channel: "file" },
   533	            // The direct channel's input IS the domain type, whose token
   534	            // domain is the STORED form (ch12-C1's authored↔stored map).
   535	            { value: "deferred_kickoff", channel: "direct" },
   536	          ],
   537	          message: "mode must be one of {members}; got {value}",
   538	        },
   539	      },
   540	    },
   541	    contextBlocks: {
   542	      kind: "map.open",
   543	      tag: "d-ctxblocks",
   544	      rows: ["ch13v2-C1"],
   545	      // ch13v2-C1: absent-catalog normalization — the admitted form always
   546	      // carries a catalog record. The declared default realizes the absent
   547	      // half; a PRESENT non-map value fails the container lane, so
   548	      // fail-closed replaces the superseded normalize-non-record class.
   549	      default: {},
   550	      containerMessage: "contextBlocks must be a map of block-id -> { body }; got {value}",
   551	      keyClass: {
   552	        kind: "valueClass",
   553	        tag: "d-block-key",
   554	        rows: ["ch13v2-C2"],
   555	        valueClass: "blockId",
   556	        label: "context block id",
   557	      },
   558	      keyLaneAt: "container",
   559	      entry: {
   560	        kind: "map.fixed",
   561	        tag: "d-ctx-entry",
   562	        rows: ["ch13v2-C3"],
   563	        containerMessage: "a context block entry must be a map with exactly body; got {value}",
   564	        unknownMessage: "unknown key {value} (a context block entry's only key is body)",
   565	        missingMessage: 'missing required key "{key}"',
   566	        fields: {
   567	          body: {
   568	            kind: "string",
   569	            tag: "d-ctx-body",
   570	            rows: ["ch13v2-C3"],
   571	            presence: { required: true },
   572	            typeMessage: "body must be a nonempty string; got {value}",
   573	            nonempty: { message: "body must be a nonempty string" },
   574	          },
   575	        },
   576	      },
   577	    },
   578	  },
   579	};
   580	
   581	// ---------------------------------------------------------------------------
   582	// The surface.
   583	// ---------------------------------------------------------------------------
   584	
   585	export const templateFormat: SurfaceDecl = defineSurface({
   586	  // The SUBSTRATE block carries only what is consumed or asserted, and
   587	  // every branch is TAG-ADDRESSABLE so a ratified row can cite it by
   588	  // pointer instead of repeating its text (design review F6). The
   589	  // rules it used to state about the yaml library's own behaviour — the
   590	  // core schema, one document per file, warning promotion, alias
   591	  // resolution — are real ratified rules (ch8-C1/C2/C3/C5) but they are
   592	  // the LIBRARY's behaviour, not a knob anything here reads; stating them
   593	  // as declaration made them read as authority they were not (arm round
   594	  // 1, F5). Their test carriers are `load.test.ts`'s substrate lanes.
   595	  substrate: {
   596	    read: {
   597	      tag: "d-read",
   598	      rows: ["ch8-C6"],
   599	      message: "invalid UTF-8 byte sequence (templates are strict UTF-8)",
   600	    },
   601	    parse: {
   602	      directive: {
   603	        tag: "d-directive",
   604	        rows: ["ch8-C34"],
   605	        only: "1.2",
   606	        message: "%YAML {key} directive: only YAML 1.2 is supported",
   607	      },
   608	      duplicateKeys: {
   609	        tag: "d-dupkeys",
   610	        rows: ["ch8-C4"],
   611	        message: "Map keys must be unique",
   612	      },
   613	    },
   614	    resolve: {
   615	      graph: {
   616	        tag: "d-graph",
   617	        rows: ["ch8-C5"],
   618	        message: "cyclic value structure: the resolved template graph must be acyclic",
   619	      },
   620	    },
   621	    // ch8-C23: the CLOSED issue-code namespace, disjoint from registry
   622	    // names. `unresolved_context_block_ref` rides the declared resolution
   623	    // lane below since the ch13v2 adoption; its end-to-end CLI travel is
   624	    // P5's (ch13v2-C18).
   625	    codes: {
   626	      tag: "d-codes",
   627	      rows: ["ch8-C23"],
   628	      values: [
   629	        "gate_evaluator_unavailable",
   630	        "runtime_context_required_for_process_gate",
   631	        "invalid_process_gate_config",
   632	        "gate_config_not_supported",
   633	        "unresolved_context_block_ref",
   634	      ],
   635	    },
   636	    internalFailure: {
   637	      tag: "d-internal-failure",
   638	      rows: ["ch8-C22", "ch8-C36"],
   639	      path: "$",
   640	      message: "internal validator failure: {valueRaw}",
   641	    },
   642	  },
   643	  root: rootNode,
   644	  valueClasses: { idClass, agentConfigValue, blockId, blockIdList },
   645	  crossRules: [
   646	    {
   647	      tag: "d-roleset",
   648	      rows: ["ch8-C16"],
   649	      relation: "equals",
   650	      left: { keysOf: "$.roles" },
   651	      right: { collect: "$.steps.*.role" },
   652	      missingFromLeft: {
   653	        at: "roles",
   654	        message: "role {valueJson} is used by steps but not declared",
   655	      },
   656	      missingFromRight: {
   657	        at: "roles.{valueRaw}",
   658	        message: "role {valueJson} is declared but not used by any step",
   659	      },
   660	    },
   661	  ],
   662	  normalizers: [
   663	    {
   664	      tag: "n-advances-round",
   665	      rows: ["ch11-C39", "ch11-C38"],
   666	      hook: "expandAdvancesRound",
   667	      over: "$.steps",
   668	      edges: "transitions",
   669	      advanceSet: "$.round.advanceOnArrivalAt",
   670	      into: "advancesRound",
   671	    },
   672	    {
   673	      tag: "n-effective-config",
   674	      // ch13v2-C13 legislates this hook's carry-list growth at P5 — the
   675	      // row citation keeps the coupling inside the closure check.
   676	      rows: ["ch11-C20", "ch11-C5", "ch13v2-C13"],
   677	      hook: "materializeEffectiveConfigs",
   678	      over: "$.steps.*.gates",
   679	      carry: ["uses"],
   680	      into: "config",
   681	    },
   682	  ],
   683	} satisfies SurfaceDecl);

codex
A review object byte-for-byte egyezik a charterrel. Most a contract–declaration authority splitet, a négy DECIDED-HERE viselkedést és az öröklési táblát ellenőrzöm; a hosszú suite-okat egymás után, önállóan futtatom.
exec
/bin/zsh -lc 'for f in v3/implementation/ch13-rederivation-arm/p4/*probe*.txt; do echo "FILE $f"; sed -n '"'1,260p' \""'$f"; done' in /Users/felho/dev/pairflow
 succeeded in 0ms:
FILE v3/implementation/ch13-rederivation-arm/p4/p4-dup-resolution-probe-out.txt
# Re-executed 2026-08-08 at HEAD 14f3ab12 (POST-D11 substrate).
# NOTE for p4-flag1-probe: the ORIGINAL Flag #1 measurement ran PRE-D11
# (at 7ebbe6b7): P2 and P3 then both yielded ZERO findings (the gap and
# the silently-inert sneak-in). At this HEAD, P3 yields the declared
# coded finding instead — the same probe, flipped by the build, which is
# the widening's acceptance evidence; P2 still yields zero because its
# declaration states no typed subset (the DISCRIMINATING twin).
DUP_GHOST [{"path":"refs[1]","message":"duplicate context block ref \"ghost\""},{"path":"refs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"refs[1]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}]
DUP_OK [{"path":"refs[1]","message":"duplicate context block ref \"alpha\""}]
DUP_BAD_SHAPE [{"path":"refs[0]","message":"invalid context block id \"Bad\""},{"path":"refs[1]","message":"invalid context block id \"Bad\""}]
NONSTRING_KEY_FILE [{"path":"contextBlocks","message":"invalid context block id true"},{"path":"refs[0]","message":"context block ref \"true\" does not resolve to an entry","code":"unresolved_context_block_ref"}]
BAD_KEY_SIBLING [{"path":"contextBlocks","message":"invalid context block id \"BAD\""}]
FILE v3/implementation/ch13-rederivation-arm/p4/p4-dup-resolution-probe.ts.txt
/**
 * P4 draft probe — duplicate x resolution interplay (old ch13-C8(e)+C7:
 * a repeat occurrence draws the duplicate finding and is EXCLUDED from
 * resolution; the FIRST occurrence carries resolution).
 * Also: non-string catalog key on the file channel (old C7's PROBE-CB2
 * discriminating case) under the new engine + belt.
 */
import { parseDocument } from "/Users/felho/dev/pairflow/node_modules/yaml/dist/index.js";
import { runSurface } from "/Users/felho/dev/pairflow/v3/src/definition/schema/engine.ts";
import type { EngineChannel } from "/Users/felho/dev/pairflow/v3/src/definition/schema/engine.ts";
import { defineSurface } from "/Users/felho/dev/pairflow/v3/src/definition/schema/defineSurface.ts";
import { templateFormat } from "/Users/felho/dev/pairflow/v3/src/definition/schema/templateFormat.ts";
import type { NodeDecl, SurfaceDecl } from "/Users/felho/dev/pairflow/v3/src/definition/schema/vocabulary.ts";

const rows = ["probe"] as const;
const noCatalog = { resolve: () => null };

const decl: SurfaceDecl = defineSurface({
  substrate: templateFormat.substrate,
  root: {
    kind: "map.fixed",
    tag: "root",
    rows,
    containerMessage: "root map",
    unknownMessage: "unknown {value}",
    fields: {
      contextBlocks: {
        kind: "map.open",
        tag: "cat",
        rows,
        containerMessage: "contextBlocks must be a map; got {value}",
        keyClass: {
          kind: "string",
          tag: "cat-key",
          rows,
          typeMessage: "invalid context block id {valueJson}",
          grammar: { re: "^[a-z][a-z0-9-]*$", message: "invalid context block id {valueJson}" },
        },
        keyLaneAt: "container",
        entry: {
          kind: "map.fixed",
          tag: "entry",
          rows,
          containerMessage: "entry must be a map; got {value}",
          unknownMessage: "unknown key {value}",
          missingMessage: 'missing required key "{key}"',
          fields: {
            body: {
              kind: "string",
              tag: "body",
              rows,
              presence: { required: true },
              typeMessage: "body must be a nonempty string",
              nonempty: { message: "body must be a nonempty string" },
            },
          },
        },
      },
      refs: {
        kind: "list",
        tag: "refs",
        rows,
        containerMessage: "refs must be a list",
        memberLaneAt: "index",
        member: {
          kind: "string",
          tag: "ref",
          rows,
          typeMessage: "invalid context block id {valueJson}",
          grammar: { re: "^[a-z][a-z0-9-]*$", message: "invalid context block id {valueJson}" },
        },
        unique: { grain: "perOccurrence", at: "index", message: "duplicate context block ref {valueJson}" },
        memberOf: {
          relation: "memberOf",
          target: { validKeysOf: "$.contextBlocks" },
          code: "unresolved_context_block_ref",
          message: "context block ref {valueJson} does not resolve to an entry",
        },
      },
    },
  },
  valueClasses: {},
  crossRules: [],
  normalizers: [],
});

function run(value: unknown, channel: EngineChannel = { kind: "direct" }) {
  return runSurface(decl, value, { channel, catalog: noCatalog }).findings;
}

// 1) duplicated GHOST ref: does the unresolved lane fire per occurrence or
//    only at the first (old C8(e): repeats excluded from resolution)?
console.log("DUP_GHOST", JSON.stringify(run({ refs: ["ghost", "ghost"] })));
// 2) duplicated RESOLVING ref: dup findings only.
console.log("DUP_OK", JSON.stringify(run({ contextBlocks: { alpha: { body: "x" } }, refs: ["alpha", "alpha"] })));
// 3) shape-failing duplicate: co-fires shape + dup on the same index
//    (old C8(e): per-defect-class findings)?
console.log("DUP_BAD_SHAPE", JSON.stringify(run({ refs: ["Bad", "Bad"] })));
// 4) PROBE-CB2 class on the FILE channel: a bare `true:` key beside the
//    grammar-valid ref "true" — non-string key preserved (mapAsMap), belt
//    must treat "true" as unresolved (SameValueZero: "true" != true).
const yaml = 'contextBlocks:\n  true:\n    body: x\nrefs:\n  - "true"\n';
const doc = parseDocument(yaml);
console.log(
  "NONSTRING_KEY_FILE",
  JSON.stringify(run(doc.toJS({ mapAsMap: true }), { kind: "file", doc, source: yaml })),
);
// 5) grammar-failing STRING key with no ref naming it: key lane fires;
//    a shape-passing ref cannot address it (one shared grammar) — vacuous
//    leg; and a ref naming a VALID sibling still resolves.
console.log(
  "BAD_KEY_SIBLING",
  JSON.stringify(run({ contextBlocks: { BAD: { body: "x" }, ok: { body: "y" } }, refs: ["ok"] })),
);
FILE v3/implementation/ch13-rederivation-arm/p4/p4-flag1-probe-out.txt
# Re-executed 2026-08-08 at HEAD 14f3ab12 (POST-D11 substrate).
# NOTE for p4-flag1-probe: the ORIGINAL Flag #1 measurement ran PRE-D11
# (at 7ebbe6b7): P2 and P3 then both yielded ZERO findings (the gap and
# the silently-inert sneak-in). At this HEAD, P3 yields the declared
# coded finding instead — the same probe, flipped by the build, which is
# the widening's acceptance evidence; P2 still yields zero because its
# declaration states no typed subset (the DISCRIMINATING twin).
P1_resolving {"direct":[],"file":[],"agree":true}
P1_belted_empty_entry {"direct":[{"path":"contextBlocks.alpha","message":"missing required key \"body\""},{"path":"binding.contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"file":[{"path":"contextBlocks.alpha","message":"missing required key \"body\""},{"path":"binding.contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"agree":true}
P1_wrong_kind_catalog {"direct":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> entry; got a list"},{"path":"binding.contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"file":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> entry; got a list"},{"path":"binding.contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"agree":true}
P1_absent_catalog {"direct":[{"path":"binding.contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"file":[{"path":"binding.contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"agree":true}
P1_duplicates {"direct":[{"path":"binding.contextBlockRefs[1]","message":"duplicate context block ref \"alpha\""},{"path":"binding.contextBlockRefs[2]","message":"duplicate context block ref \"alpha\""}],"file":[{"path":"binding.contextBlockRefs[1]","message":"duplicate context block ref \"alpha\""},{"path":"binding.contextBlockRefs[2]","message":"duplicate context block ref \"alpha\""}],"agree":true}
P2_ghost_ref_inside_plain {"direct":[],"file":[],"agree":true}
P3_sneaked_fields {"direct":[{"path":"agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"file":[{"path":"agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"agree":true}
FILE v3/implementation/ch13-rederivation-arm/p4/p4-flag1-probe.ts.txt
/**
 * P4 Flag-candidate #1 — executed probe (2026-08-08).
 *
 * Question: can the CURRENT vocabulary express the superseded ch13-C4
 * position — a typed `promptConcernRefs` list INSIDE the format-open
 * agentConfig (`map.plain`) — and what happens when an author tries?
 *
 * P1: the belt composition at a map.fixed position (the ch13-C6 analog)
 *     — expected to WORK with the existing vocabulary incl. validKeysOf.
 * P2: the same ref list nested inside a map.plain position (the ch13-C4
 *     shape) — expected: NO lane reaches it; zero findings from the
 *     position on both channels.
 * P3: a map.plain node carrying a runtime `fields` attribute (illegal at
 *     the type level, present at runtime) — measures whether defineSurface
 *     refuses it or it loads and silently validates nothing.
 */
import { parseDocument } from "/Users/felho/dev/pairflow/node_modules/yaml/dist/index.js";
import { runSurface } from "/Users/felho/dev/pairflow/v3/src/definition/schema/engine.ts";
import type { EngineChannel } from "/Users/felho/dev/pairflow/v3/src/definition/schema/engine.ts";
import { defineSurface } from "/Users/felho/dev/pairflow/v3/src/definition/schema/defineSurface.ts";
import { templateFormat } from "/Users/felho/dev/pairflow/v3/src/definition/schema/templateFormat.ts";
import type { NodeDecl, SurfaceDecl } from "/Users/felho/dev/pairflow/v3/src/definition/schema/vocabulary.ts";

const rows = ["probe"] as const;
const noCatalog = { resolve: () => null };

const catalogNode: NodeDecl = {
  kind: "map.open",
  tag: "d-ctxblocks",
  rows,
  containerMessage: "contextBlocks must be a map of block-id -> entry; got {value}",
  keyClass: {
    kind: "string",
    tag: "d-block-id",
    rows,
    typeMessage: "invalid context block id {valueJson}",
    grammar: { re: "^[a-z][a-z0-9-]*$", message: "invalid context block id {valueJson}; expected {grammar}" },
  },
  keyLaneAt: "container",
  entry: {
    kind: "map.fixed",
    tag: "d-ctx-entry",
    rows,
    containerMessage: "a context block entry must be a map with exactly body; got {value}",
    unknownMessage: "unknown key {value}",
    fields: {
      body: {
        kind: "string",
        tag: "d-ctx-body",
        rows,
        presence: { required: true },
        typeMessage: "body must be a nonempty string; got {value}",
        nonempty: { message: "body must be a nonempty string" },
      },
    },
  },
};

const blockRefList: NodeDecl = {
  kind: "list",
  tag: "d-ctx-refs",
  rows,
  containerMessage: "contextBlockRefs must be a list of block ids; got {value}",
  memberLaneAt: "index",
  member: {
    kind: "string",
    tag: "d-ctx-ref-member",
    rows,
    typeMessage: "invalid context block id {valueJson}",
    grammar: { re: "^[a-z][a-z0-9-]*$", message: "invalid context block id {valueJson}; expected {grammar}" },
  },
  unique: { grain: "perOccurrence", at: "index", message: "duplicate context block ref {valueJson}" },
  memberOf: {
    relation: "memberOf",
    target: { validKeysOf: "$.contextBlocks" },
    code: "unresolved_context_block_ref",
    message: "context block ref {valueJson} does not resolve to an entry",
  },
};

function surfaceWith(fields: Record<string, NodeDecl>): SurfaceDecl {
  return {
    substrate: templateFormat.substrate, // lifted whole — the round-10 USABILITY-2 pattern
    root: {
      kind: "map.fixed",
      tag: "d-probe-root",
      rows,
      containerMessage: "root must be a map",
      unknownMessage: "unknown key {value}",
      fields,
    },
    valueClasses: {},
    crossRules: [],
    normalizers: [],
  };
}

function run(surface: SurfaceDecl, value: unknown, channel: EngineChannel = { kind: "direct" }) {
  return runSurface(surface, value, { channel, catalog: noCatalog }).findings;
}

function both(surface: SurfaceDecl, label: string, value: unknown, yamlText: string) {
  const direct = run(surface, value);
  const doc = parseDocument(yamlText);
  const file = run(surface, doc.toJS({ mapAsMap: true }), { kind: "file", doc, source: yamlText });
  console.log(
    label,
    JSON.stringify({ direct, file, agree: JSON.stringify(direct) === JSON.stringify(file) }),
  );
}

// ---------------------------------------------------------------------------
// P1 — the ch13-C6 analog: refs at a FIXED-map position, belt via validKeysOf.
// ---------------------------------------------------------------------------
const p1 = defineSurface(
  surfaceWith({
    contextBlocks: catalogNode,
    binding: {
      kind: "map.fixed",
      tag: "d-probe-binding",
      rows,
      containerMessage: "binding must be a map",
      unknownMessage: "unknown key {value}",
      fields: { contextBlockRefs: blockRefList },
    },
  }),
);

both(p1, "P1_resolving", { contextBlocks: { alpha: { body: "text" } }, binding: { contextBlockRefs: ["alpha"] } },
  'contextBlocks:\n  alpha:\n    body: text\nbinding:\n  contextBlockRefs:\n    - alpha\n');
both(p1, "P1_belted_empty_entry", { contextBlocks: { alpha: {} }, binding: { contextBlockRefs: ["alpha"] } },
  "contextBlocks:\n  alpha: {}\nbinding:\n  contextBlockRefs:\n    - alpha\n");
both(p1, "P1_wrong_kind_catalog", { contextBlocks: ["alpha"], binding: { contextBlockRefs: ["alpha"] } },
  "contextBlocks:\n  - alpha\nbinding:\n  contextBlockRefs:\n    - alpha\n");
both(p1, "P1_absent_catalog", { binding: { contextBlockRefs: ["alpha"] } },
  "binding:\n  contextBlockRefs:\n    - alpha\n");
both(p1, "P1_duplicates", { contextBlocks: { alpha: { body: "text" } }, binding: { contextBlockRefs: ["alpha", "alpha", "alpha"] } },
  'contextBlocks:\n  alpha:\n    body: text\nbinding:\n  contextBlockRefs:\n    - alpha\n    - alpha\n    - alpha\n');

// ---------------------------------------------------------------------------
// P2 — the ch13-C4 shape: the SAME ref list nested inside map.plain.
// ---------------------------------------------------------------------------
const p2 = defineSurface(
  surfaceWith({
    contextBlocks: catalogNode,
    agentConfig: {
      kind: "map.plain",
      tag: "d-probe-agentconfig",
      rows,
      containerMessage: "agentConfig must be a map; got {value}",
      canonicalJsonSafe: { message: "agentConfig must be canonical-JSON-safe" },
    },
  }),
);

both(p2, "P2_ghost_ref_inside_plain",
  { contextBlocks: { alpha: { body: "text" } }, agentConfig: { promptConcernRefs: ["ghost", 123, "Bad Id"] } },
  'contextBlocks:\n  alpha:\n    body: text\nagentConfig:\n  promptConcernRefs:\n    - ghost\n    - 123\n    - Bad Id\n');

// ---------------------------------------------------------------------------
// P3 — the sneak-in: a runtime `fields` attribute on map.plain. Type-illegal
// (cast to bypass), measured at runtime: refused, or loaded-and-inert?
// ---------------------------------------------------------------------------
const plainWithFields = {
  kind: "map.plain",
  tag: "d-probe-agentconfig",
  rows,
  containerMessage: "agentConfig must be a map; got {value}",
  canonicalJsonSafe: { message: "agentConfig must be canonical-JSON-safe" },
  fields: { promptConcernRefs: blockRefList },
} as unknown as NodeDecl;

try {
  const p3 = defineSurface(surfaceWith({ contextBlocks: catalogNode, agentConfig: plainWithFields }));
  both(p3, "P3_sneaked_fields",
    { contextBlocks: { alpha: { body: "text" } }, agentConfig: { promptConcernRefs: ["ghost"] } },
    'contextBlocks:\n  alpha:\n    body: text\nagentConfig:\n  promptConcernRefs:\n    - ghost\n');
} catch (error) {
  console.log("P3_sneaked_fields", "REFUSED AT LOAD:", (error as Error).message.split("\n")[0]);
}
FILE v3/implementation/ch13-rederivation-arm/p4/p4-live-declaration-probes-out.txt
# PROBE-P4-4 — executed against the LIVE declaration; grown across panel rounds 1-3.
# AUDITOR NOTES: (1) L_empty_catalog_ref's agree:false is the probe's own asymmetric
# inputs, superseded by the _FILE twin on the next line; the only DESIGNED channel
# asymmetry is L_capability_profile. (2) run()'s findings are FILTERED to the ch13
# surfaces; the *_TOTAL lines carry the UNFILTERED zeros for the absolute-zero rows.
L_null_direct {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got null"}]}
L_null {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got null"}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got null"}]},"agree":true}
L_42 {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"}]}
L_emptystring {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got \"\""}]}
L_list {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got a list"}]}
L_absent {"findings":[],"ctx":{}}
L_empty_catalog_ref {"direct":{"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}},"file":{"findings":[],"ctx":{}},"agree":false}
L_empty_catalog_ref_FILE {"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_all_positions {"findings":[{"path":"steps.s1.gates.GO[0]","message":"no gate evaluator is registered for 'a.b'","code":"gate_evaluator_unavailable"},{"path":"contextBlocks.alpha","message":"missing required key \"body\""},{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"steps.s1.gates.GO[0].contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{"alpha":{}}}
L_dup_ghost {"findings":[{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[1]","message":"duplicate context block ref \"ghost\""},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[1]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_dup_badshape {"findings":[{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"invalid context block ref \"Bad\": block ids match ^[a-z][a-z0-9-]*$"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[1]","message":"invalid context block ref \"Bad\": block ids match ^[a-z][a-z0-9-]*$"}],"ctx":{}}
L_bool_key {"direct":{"findings":[{"path":"contextBlocks","message":"invalid context block id true: block ids are kebab-case strings"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"true\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}},"file":{"findings":[{"path":"contextBlocks","message":"invalid context block id true: block ids are kebab-case strings"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"true\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}},"agree":true}
L_bad_key_direct {"findings":[{"path":"contextBlocks","message":"invalid context block id \"BAD\": block ids match ^[a-z][a-z0-9-]*$"}],"ctx":{"BAD":{"body":"x"}}}
L_capability_profile {"direct":{"findings":[],"ctx":{}},"file":{"findings":[{"path":"capabilityProfile","message":"unknown key \"capabilityProfile\" (fixed keysets grow only by ratified additive keys — V16 reserves \"kind\")"}],"ctx":{}},"agree":false}
L_refused_catalog_with_ref {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"},{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}]}
L_refused_catalog_with_ref_FILE {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"},{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}]}
L_absent_catalog_with_ref_FILE {"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_absent_catalog_with_ref {"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_42_both {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"}]},"agree":true}
L_emptystring_both {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got \"\""}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got \"\""}]},"agree":true}
L_list_both {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got a list"}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got a list"}]},"agree":true}
L_empty_authored_list {"findings":[],"ctx":{}}
L_open_sibling {"findings":[],"ctx":{"alpha":{"body":"x"}}}
L_bad_key_valid_sibling {"findings":[{"path":"contextBlocks","message":"invalid context block id \"BAD\": block ids match ^[a-z][a-z0-9-]*$"}],"ctx":{"BAD":{"body":"x"},"ok":{"body":"y"}}}
L_cb3_reexec {"findings":0,"keyOrder":["1","2","10","ZZZ","AAA"]}
L_degenerate_date {"findings":[],"ctx":{}}
L_degenerate_class {"findings":[],"ctx":{}}
L_degenerate_fn_refused {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got ()=>1"}]}
L_gates_record_keyorder {"keyOrder":["1","2","10","ZZZ","AAA"]}
L_open_sibling_retained {"totalFindings":0,"admittedConfig":{"promptConcernRefs":["alpha"],"skill_refs":["free"],"nested":{"any":1}}}
L_clean_TOTAL 0 L_empty_list_TOTAL 0
L_clean_compound {"findings":[],"ctx":{"emit-envelope":{"body":"text"},"no-converged-before-round-2":{"body":"t2"}}}
FILE v3/implementation/ch13-rederivation-arm/p4/p4-live-declaration-probes.ts.txt
/**
 * PROBE-P4-4 — probe receipts against the LIVE ratifying-basis
 * declaration; grown across panel rounds 1-3 (round-2 and round-3
 * additions are labelled inline).
 * INSTRUMENT NOTE: run() FILTERS findings to the ch13 surfaces (paths
 * containing contextBlocks/ConcernRefs/capabilityProfile, or any coded
 * finding); the *_TOTAL cases assert UNFILTERED zeros where a row
 * claims an absolute zero.
 * Every case runs through templateFormat itself on both channels.
 * Covers: the fail-closed class incl. PRESENT-NULL (lens-2 F3, the
 * member that drove two of the old line's reopens), the EMPTY catalog
 * (lens-3 F5), the belt cases, the dup/shape interplay, the boolean
 * key, the three ref positions, the channel-gated capabilityProfile
 * (lens-1 F1's declared asymmetry), and the compound CLEAN case
 * (lens-3 F3's success negative).
 */
import { parseDocument } from "/Users/felho/dev/pairflow/node_modules/yaml/dist/index.js";
import { runSurface } from "/Users/felho/dev/pairflow/v3/src/definition/schema/engine.ts";
import type { EngineChannel } from "/Users/felho/dev/pairflow/v3/src/definition/schema/engine.ts";
import { templateFormat } from "/Users/felho/dev/pairflow/v3/src/definition/schema/templateFormat.ts";

const noCatalog = { resolve: () => null };

const base = {
  ref: { id: "probe", version: 1 },
  start: "s1",
  steps: { s1: { role: "r1", instruction: "do", transitions: {} } },
  terminal: ["done"],
  roles: { r1: {} },
};

function run(value: unknown, channel: EngineChannel = { kind: "direct" }) {
  const r = runSurface(templateFormat, value, { channel, catalog: noCatalog });
  return { findings: r.findings.filter((f) => String(f.path).includes("contextBlocks") || String(f.path).includes("ConcernRefs") || String(f.path).includes("capabilityProfile") || f.code !== undefined), ctx: (r.normalized as Record<string, unknown> | undefined)?.["contextBlocks"] };
}

function both(label: string, extra: Record<string, unknown>, yamlExtra: string) {
  const direct = run({ ...base, ...extra });
  const yaml =
    "ref: {id: probe, version: 1}\nstart: s1\nsteps:\n  s1: {role: r1, instruction: do, transitions: {}}\nterminal: [done]\nroles:\n  r1: {}\n" + yamlExtra;
  const doc = parseDocument(yaml);
  const file = run(doc.toJS({ mapAsMap: true }), { kind: "file", doc, source: yaml });
  console.log(label, JSON.stringify({ direct, file, agree: JSON.stringify(direct.findings) === JSON.stringify(file.findings) }));
}

// C1 fail-closed, CLASS WIDTH incl. the reopen-driving members:
console.log("L_null_direct", JSON.stringify(run({ ...base, contextBlocks: null })));
both("L_null", { contextBlocks: null }, "contextBlocks: null\n");
console.log("L_42", JSON.stringify(run({ ...base, contextBlocks: 42 })));
console.log("L_emptystring", JSON.stringify(run({ ...base, contextBlocks: "" })));
console.log("L_list", JSON.stringify(run({ ...base, contextBlocks: ["x"] })));
// C1 default: absent admits, admitted form carries {}:
console.log("L_absent", JSON.stringify(run(base)));
// EMPTY catalog + a ref (lens-3 F5's missing receipt):
both("L_empty_catalog_ref", { contextBlocks: {}, steps: { s1: { role: "r1", instruction: "do", transitions: {}, agentConfig: { promptConcernRefs: ["ghost"] } } } },
  "contextBlocks: {}\n");
// (the file twin with the SAME logical value — the earlier run's disagreement
// was the probe's own asymmetric inputs, corrected here)
{
  const yaml = "ref: {id: probe, version: 1}\nstart: s1\nsteps:\n  s1:\n    role: r1\n    instruction: do\n    transitions: {}\n    agentConfig:\n      promptConcernRefs: [ghost]\nterminal: [done]\nroles:\n  r1: {}\ncontextBlocks: {}\n";
  const doc = parseDocument(yaml);
  console.log("L_empty_catalog_ref_FILE", JSON.stringify(run(doc.toJS({ mapAsMap: true }), { kind: "file", doc, source: yaml })));
}
// belt: ref to malformed entry (two findings), all three positions accumulate:
console.log("L_all_positions", JSON.stringify(run({
  ...base,
  contextBlocks: { alpha: {} },
  steps: { s1: { role: "r1", instruction: "do", transitions: { GO: "done" },
    agentConfig: { promptConcernRefs: ["alpha"] },
    gates: { GO: [{ uses: "a.b", config: {}, contextBlockRefs: ["alpha"] }] } } },
  roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["alpha"] } } },
})));
// dup ghost / shape-failing dup (the two DECIDED-HERE receipts, live):
console.log("L_dup_ghost", JSON.stringify(run({ ...base, roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["ghost", "ghost"] } } } })));
console.log("L_dup_badshape", JSON.stringify(run({ ...base, roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["Bad", "Bad"] } } } })));
// boolean key on the file channel + grammar-failing key on DIRECT (C2's
// channel-independent key lane — the fourth DECIDED-HERE receipt):
both("L_bool_key", { contextBlocks: new Map<unknown, unknown>([[true, { body: "x" }]]), roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["true"] } } } },
  'contextBlocks:\n  true:\n    body: x\nroles:\n  r1:\n    defaultAgentConfig:\n      promptConcernRefs: ["true"]\n');
console.log("L_bad_key_direct", JSON.stringify(run({ ...base, contextBlocks: { BAD: { body: "x" } } })));
// capabilityProfile: channel-GATED by declaration (file refuses, direct legal):
both("L_capability_profile", { capabilityProfile: {} }, "capabilityProfile: {}\n");
// R2 additions — the receipt gaps the round-2 lenses named:
// (a) refs BESIDE a refused catalog and BESIDE an absent catalog (C1's
//     non-suppression clause, live), both channels;
console.log("L_refused_catalog_with_ref", JSON.stringify(run({ ...base, contextBlocks: 42, steps: { s1: { role: "r1", instruction: "do", transitions: {}, agentConfig: { promptConcernRefs: ["ghost"] } } } })));
{
  const mk = (cb: string) => "ref: {id: probe, version: 1}\nstart: s1\nsteps:\n  s1:\n    role: r1\n    instruction: do\n    transitions: {}\n    agentConfig:\n      promptConcernRefs: [ghost]\nterminal: [done]\nroles:\n  r1: {}\n" + cb;
  for (const [label, extra, yaml] of [
    ["L_refused_catalog_with_ref_FILE", null, mk("contextBlocks: 42\n")],
    ["L_absent_catalog_with_ref_FILE", null, mk("")],
  ] as const) {
    const doc = parseDocument(yaml);
    console.log(label, JSON.stringify(run(doc.toJS({ mapAsMap: true }), { kind: "file", doc, source: yaml })));
  }
  console.log("L_absent_catalog_with_ref", JSON.stringify(run({ ...base, steps: { s1: { role: "r1", instruction: "do", transitions: {}, agentConfig: { promptConcernRefs: ["ghost"] } } } })));
}
// (b) the fail-closed class members routed through BOTH channels;
both("L_42_both", { contextBlocks: 42 }, "contextBlocks: 42\n");
both("L_emptystring_both", { contextBlocks: "" }, 'contextBlocks: ""\n');
both("L_list_both", { contextBlocks: ["x"] }, "contextBlocks: [x]\n");
// (c) an AUTHORED EMPTY ref list is legal and contributes zero refs;
console.log("L_empty_authored_list", JSON.stringify(run({ ...base, roles: { r1: { defaultAgentConfig: { promptConcernRefs: [] } } } })));
// (d) an undeclared sibling key beside the typed field stays open data;
console.log("L_open_sibling", JSON.stringify(run({ ...base, contextBlocks: { alpha: { body: "x" } }, roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["alpha"], skill_refs: ["free"], nested: { any: 1 } } } } })));
// (e) a grammar-failing key beside a VALID sibling ref, live;
console.log("L_bad_key_valid_sibling", JSON.stringify(run({ ...base, contextBlocks: { BAD: { body: "x" }, ok: { body: "y" } }, roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["ok"] } } } })));
// (f) PROBE-CB3 re-executed on the LIVE engine: quoted integer-like keys
//     hoist to the front AND re-sort ascending among themselves;
{
  const yaml = 'ref: {id: probe, version: 1}\nstart: s1\nsteps:\n  s1:\n    role: r1\n    instruction: do\n    transitions: {ZZZ: done, "10": done, "2": done, AAA: done, "1": done}\nterminal: [done]\nroles:\n  r1: {}\n';
  const doc = parseDocument(yaml);
  const r = runSurface(templateFormat, doc.toJS({ mapAsMap: true }), { channel: { kind: "file", doc, source: yaml }, catalog: noCatalog });
  const steps = (r.normalized as Record<string, unknown>)["steps"] as Record<string, { transitions: Record<string, string> }>;
  console.log("L_cb3_reexec", JSON.stringify({ findings: r.findings.length, keyOrder: Object.keys(steps["s1"].transitions) }));
}
// R3 additions:
// (g) C1's degenerate accept boundary: an object-like with nothing to
//     enumerate admits as the empty record; a function refuses;
class Degenerate { }
console.log("L_degenerate_date", JSON.stringify(run({ ...base, contextBlocks: new Date(0) })));
console.log("L_degenerate_class", JSON.stringify(run({ ...base, contextBlocks: new Degenerate() })));
console.log("L_degenerate_fn_refused", JSON.stringify(run({ ...base, contextBlocks: () => 1 })));
// (h) C10's operand measured directly: the admitted GATES record's key
//     order under quoted integer-like event keys (hoist + ascending);
{
  const yaml = 'ref: {id: probe, version: 1}\nstart: s1\nsteps:\n  s1:\n    role: r1\n    instruction: do\n    transitions: {ZZZ: done, "10": done, "2": done, AAA: done, "1": done}\n    gates:\n      ZZZ: [{uses: a.b, config: {}}]\n      "10": [{uses: a.b, config: {}}]\n      "2": [{uses: a.b, config: {}}]\n      AAA: [{uses: a.b, config: {}}]\n      "1": [{uses: a.b, config: {}}]\nterminal: [done]\nroles:\n  r1: {}\n';
  const doc = parseDocument(yaml);
  const r = runSurface(templateFormat, doc.toJS({ mapAsMap: true }), { channel: { kind: "file", doc, source: yaml }, catalog: noCatalog });
  const steps = (r.normalized as Record<string, unknown>)["steps"] as Record<string, { gates?: Record<string, unknown> }>;
  console.log("L_gates_record_keyorder", JSON.stringify({ keyOrder: Object.keys(steps["s1"].gates ?? {}) }));
}
// (i) open siblings RETAINED on the admitted value (unfiltered view);
{
  const r = runSurface(templateFormat, { ...base, contextBlocks: { alpha: { body: "x" } }, roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["alpha"], skill_refs: ["free"], nested: { any: 1 } } } } }, { channel: { kind: "direct" }, catalog: noCatalog });
  const roles = (r.normalized as Record<string, unknown>)["roles"] as Record<string, { defaultAgentConfig?: unknown }>;
  console.log("L_open_sibling_retained", JSON.stringify({ totalFindings: r.findings.length, admittedConfig: roles["r1"].defaultAgentConfig }));
}
// (j) UNFILTERED zeros for the rows claiming absolute zeros;
{
  const clean = runSurface(templateFormat, { ...base, contextBlocks: { "emit-envelope": { body: "text" } }, roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["emit-envelope"] } } } }, { channel: { kind: "direct" }, catalog: noCatalog });
  const empty = runSurface(templateFormat, { ...base, roles: { r1: { defaultAgentConfig: { promptConcernRefs: [] } } } }, { channel: { kind: "direct" }, catalog: noCatalog });
  console.log("L_clean_TOTAL", clean.findings.length, "L_empty_list_TOTAL", empty.findings.length);
}
// the compound CLEAN success case (old C8(g)): populated catalog, every
// entry referenced, every ref resolving -> zero findings:
// (gate-free so the zero is a TRUE zero — a gate binding needs an injected
// registry, which is the P5 fixture's concern, not this receipt's)
console.log("L_clean_compound", JSON.stringify(run({
  ...base,
  contextBlocks: { "emit-envelope": { body: "text" }, "no-converged-before-round-2": { body: "t2" } },
  steps: { s1: { role: "r1", instruction: "do", transitions: {},
    agentConfig: { promptConcernRefs: ["no-converged-before-round-2"] } } },
  roles: { r1: { defaultAgentConfig: { promptConcernRefs: ["emit-envelope"] } } },
})));

exec
/bin/zsh -lc 'cp v3/implementation/ch13-rederivation-arm/p4/p4-live-declaration-probes.ts.txt /tmp/ch13-qa-live-probe.ts && pnpm exec tsx /tmp/ch13-qa-live-probe.ts' in /Users/felho/dev/pairflow
 succeeded in 373ms:
L_null_direct {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got null"}]}
L_null {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got null"}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got null"}]},"agree":true}
L_42 {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"}]}
L_emptystring {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got \"\""}]}
L_list {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got a list"}]}
L_absent {"findings":[],"ctx":{}}
L_empty_catalog_ref {"direct":{"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}},"file":{"findings":[],"ctx":{}},"agree":false}
L_empty_catalog_ref_FILE {"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_all_positions {"findings":[{"path":"steps.s1.gates.GO[0]","message":"no gate evaluator is registered for 'a.b'","code":"gate_evaluator_unavailable"},{"path":"contextBlocks.alpha","message":"missing required key \"body\""},{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"steps.s1.gates.GO[0].contextBlockRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"alpha\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{"alpha":{}}}
L_dup_ghost {"findings":[{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[1]","message":"duplicate context block ref \"ghost\""},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[1]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_dup_badshape {"findings":[{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"invalid context block ref \"Bad\": block ids match ^[a-z][a-z0-9-]*$"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[1]","message":"invalid context block ref \"Bad\": block ids match ^[a-z][a-z0-9-]*$"}],"ctx":{}}
L_bool_key {"direct":{"findings":[{"path":"contextBlocks","message":"invalid context block id true: block ids are kebab-case strings"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"true\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}},"file":{"findings":[{"path":"contextBlocks","message":"invalid context block id true: block ids are kebab-case strings"},{"path":"roles.r1.defaultAgentConfig.promptConcernRefs[0]","message":"context block ref \"true\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}},"agree":true}
L_bad_key_direct {"findings":[{"path":"contextBlocks","message":"invalid context block id \"BAD\": block ids match ^[a-z][a-z0-9-]*$"}],"ctx":{"BAD":{"body":"x"}}}
L_capability_profile {"direct":{"findings":[],"ctx":{}},"file":{"findings":[{"path":"capabilityProfile","message":"unknown key \"capabilityProfile\" (fixed keysets grow only by ratified additive keys — V16 reserves \"kind\")"}],"ctx":{}},"agree":false}
L_refused_catalog_with_ref {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"},{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}]}
L_refused_catalog_with_ref_FILE {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"},{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}]}
L_absent_catalog_with_ref_FILE {"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_absent_catalog_with_ref {"findings":[{"path":"steps.s1.agentConfig.promptConcernRefs[0]","message":"context block ref \"ghost\" does not resolve to an entry","code":"unresolved_context_block_ref"}],"ctx":{}}
L_42_both {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got 42"}]},"agree":true}
L_emptystring_both {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got \"\""}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got \"\""}]},"agree":true}
L_list_both {"direct":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got a list"}]},"file":{"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got a list"}]},"agree":true}
L_empty_authored_list {"findings":[],"ctx":{}}
L_open_sibling {"findings":[],"ctx":{"alpha":{"body":"x"}}}
L_bad_key_valid_sibling {"findings":[{"path":"contextBlocks","message":"invalid context block id \"BAD\": block ids match ^[a-z][a-z0-9-]*$"}],"ctx":{"BAD":{"body":"x"},"ok":{"body":"y"}}}
L_cb3_reexec {"findings":0,"keyOrder":["1","2","10","ZZZ","AAA"]}
L_degenerate_date {"findings":[],"ctx":{}}
L_degenerate_class {"findings":[],"ctx":{}}
L_degenerate_fn_refused {"findings":[{"path":"contextBlocks","message":"contextBlocks must be a map of block-id -> { body }; got ()=>1"}]}
L_gates_record_keyorder {"keyOrder":["1","2","10","ZZZ","AAA"]}
L_open_sibling_retained {"totalFindings":0,"admittedConfig":{"promptConcernRefs":["alpha"],"skill_refs":["free"],"nested":{"any":1}}}
L_clean_TOTAL 0 L_empty_list_TOTAL 0
L_clean_compound {"findings":[],"ctx":{"emit-envelope":{"body":"text"},"no-converged-before-round-2":{"body":"t2"}}}

exec
/bin/zsh -lc "nl -ba v3/implementation/contracts/ch13-context-block-contract.md | sed -n '1,430p'" in /Users/felho/dev/pairflow
 succeeded in 0ms:
     1	# ch13 — context-block contract
     2	
     3	```json
     4	{"contract_draft": {"chapter": "ch13", "surface": "context-block", "status": "superseded"}}
     5	```
     6	
     7	## Context (non-normative by declaration)
     8	
     9	**Business invariant.** L2 enforces; L2b communicates. A context block
    10	is deterministic, actor-facing prose rendered into the dispatched
    11	ContextPacket so the actor is told an operating rule before acting —
    12	it never enters a verdict, a gate decision, or any kernel decision
    13	surface (`l2b/communication-only`, disposition `review`; the precise
    14	byte-scope is C12's).
    15	
    16	**Control model.** One render mechanism, two ref sources, one body
    17	source: the template-level catalog holds every body; role/step
    18	prompt-concern refs (the L0c slot this chapter first resolves) and
    19	gate/policy block refs are id lists into it. Refs are validated at
    20	ADMISSION-time definition-static checking (the definition-issue
    21	channel, static analog of binding coverage; the walk/rung realization
    22	split is C19's); the render is a pure function at dispatch.
    23	
    24	**Read-path, staged.** The definition-static lanes read the SOURCE
    25	form (the walk) and the built template (the admission rung), per
    26	C19's split; `assemble_context_blocks(instance, template, step)`
    27	reads the admitted template's NORMALIZED fields and catalog (C17) —
    28	never the merged effective agent config (C5).
    29	
    30	**Forbidden fallback.** No render-time drop of an unresolved ref and
    31	no render-time skip of a malformed catalog (neither state exists
    32	post-admission — C7/C9/C17); no code-invented rejection name (the
    33	issue code is the EXISTING `unresolved_context_block_ref` from the
    34	ch11 model-sync 31-name definition-issue family).
    35	
    36	**Allowed resolution.** An absent catalog with zero refs issued is
    37	the every-existing-file state — fully legal, zero-impact.
    38	
    39	**Missing-data.** A template with refs but no resolving catalog
    40	entries fails at definition-static checking (per-site findings, C7).
    41	
    42	**Model-table note (superseded cells, recorded — no divergence stop
    43	owed):** the model's Canonical Context Block Contract table still
    44	says "reject at create" on its two ref rows; the same section's
    45	invariant record (`refs-validated-at-definition-load`: "runs under
    46	ADMISSION"), the `CREATE_INSTANCE` reprint's own comment, and the
    47	Evidence block all say ADMISSION — the table cells are pre-model-fix
    48	residue of the ch11 sync (definition-static validation moved to the
    49	admission channel). This draft follows ADMISSION.
    50	
    51	**Substrate probes (recorded per the ch11 probe-record form —
    52	yaml@2.9.0, the v3 package's pinned dependency; YAML 1.2 semantics
    53	via `YAML.parseDocument`):**
    54	- **PROBE-CB1 (2026-07-26; re-executed at the round-2 and round-3
    55	  panels):** kebab-case catalog keys with digit-bearing segments
    56	  (`no-converged-before-round-2`) are plain STRINGS at node level; a
    57	  bare digit-leading key (`2`) is a NUMBER at node level — the GP2
    58	  identity class.
    59	- **PROBE-CB2 (2026-07-26; re-executed at the round-2 and round-3
    60	  panels):** a bare `true:` key is a BOOLEAN at node level; beside
    61	  the string key `"true":` the pair passes the duplicate-key gate
    62	  (the loader's `yamlNodeEqual` is SameValueZero on scalar values —
    63	  `true ≠ "true"`). Under a NAKED default `toJS` the pair would
    64	  collapse to one key (last wins) — but the v3 loader resolves with
    65	  `toJS({ mapAsMap: true })` EXACTLY to prevent that collapse (its
    66	  own comment says so), so non-string keys ARRIVE PRESERVED at the
    67	  walk, and every sibling open-key surface (`steps`, `roles`,
    68	  `transitions`, the gates subtree) already carries a WALK-side
    69	  non-string-key validation lane reporting at the nearest
    70	  addressable path. The consequence for this chapter is C2's key
    71	  lane in the walk — the sibling pattern applied to the new open-key
    72	  map.
    73	- **PROBE-CB3 (2026-07-26, executed at the round-2 panel through the
    74	  real `loadTemplate`; EXTENDED at the round-3 panel):** integer-like
    75	  keys authored in QUOTED form (`"2":`) admit as record keys and
    76	  HOIST to the front of the record's enumeration order (`transitions`
    77	  authored `ZZZ, "2", AAA` → admitted keys `["2","ZZZ","AAA"]`, and
    78	  the rebuilt gates record mirrors it); MULTIPLE integer-like keys
    79	  additionally re-sort ASCENDING among themselves (authored
    80	  `ZZZ,"10","2",AAA,"1"` → `["1","2","10","ZZZ","AAA"]` — the
    81	  round-3 measurement); an UNQUOTED `2:` is rejected upstream as a
    82	  non-string key. C9's order basis rests on this measured behavior.
    83	
    84	**Prepared reopen texts (ch11-C4 + ch11-C30 + the ch11-C41
    85	comparative parenthetical — ONE post-close reopen act over three
    86	named sites, riding this draft's ratification; the act's carrier row
    87	is C16; the realized→reopened→realized mechanics per the
    88	contract-draft template §4):**
    89	- C4 becomes: *"A gate map is FIXED-KEYSET: `uses` (required) +
    90	  `config` (optional map) + `contextBlockRefs` (optional — value
    91	  class and semantics owned by `contract:ch13-context-block#C6`). NO
    92	  `implementation` / `execution` / `id` keys exist — the
    93	  implementation and execution axes are evaluator-INTRINSIC
    94	  (registry-resolved from `uses`), never authored; unknown keys are
    95	  ADMISSION issues per the ch8-C13 fail-closed culture."*
    96	- C30's trailing `context_block_refs` clause (semicolon-joined — the
    97	  edit also closes the preceding clause with a period) becomes:
    98	  *"The authored gate-binding key `contextBlockRefs` (the model's
    99	  `context_block_refs`) joined the binding keyset at the ch13
   100	  ratification act — value class and semantics:
   101	  `contract:ch13-context-block#C6`; before ch13 it was explicitly
   102	  not a key of this surface."* (Only that clause changes; the row's
   103	  remaining text is untouched.)
   104	- C41's comparative parenthetical is edited IN FULL (both mentions —
   105	  the half-fix would leave the antecedent naming a pattern the same
   106	  act retires): *"deliberately NOT the then-standing C30
   107	  `context_block_refs` non-surface pattern: `context_block_refs` was
   108	  another chapter's semantics (landed at ch13 as the authored key
   109	  `contextBlockRefs` — `contract:ch13-context-block#C6`), while the override is ratified
   110	  model capability on THIS surface"*.
   111	- Map-entry duties, ITEMIZED (the token-blind `v3:realized-map` lint
   112	  cannot catch these — the ch9-C27 lesson): C4's entry updates its
   113	  keyset parenthetical — *"(uses + optional config)"* becomes
   114	  *"(uses + optional config; + contextBlockRefs at ch13 —
   115	  delegation: ch13#C6)"* — beside its historical landing; C30's
   116	  entry RETIRES the code witness *"context_block_refs is NOT a key
   117	  (validate.ts unknown-key)"* and records the delegation (the
   118	  witness's intent is falsified by ch13-P1 by design — and the
   119	  witness was mis-sited anyway: the binding unknown-key lane lives
   120	  in the admission rung, not validate.ts); C41's entry is
   121	  annotation-only (the clause fix). All three realize VACUOUSLY
   122	  (this chapter's close owns the new key's realization).
   123	
   124	**Reopen-vs-aggregate-note criterion (applied here; promotion is a
   125	NAMED boundary-review item — plan §13.5(e) is the carrier):** a row
   126	is REOPENED when its operative text is a hard promise about THIS
   127	surface (ch11-C4's closed keyset; ch11-C30's "explicitly not a key";
   128	C41's present-tense comparative); it is AGGREGATE-NOTED when its own
   129	text pre-authorizes or forward-scopes the arrival (ch12-C7's
   130	delegation clause names this chapter; ch12-C1's aggregate-note chain;
   131	ch9-C18's "the later ContextAssembly surface (L2b, the named §1.3
   132	candidate)" — aggregate-noted here: its "later" clause resolves to
   133	this chapter, recorded without a reopen). Tie-break, decided by the
   134	live case: a row carrying BOTH a hard promise AND a delegation clause
   135	(ch12-C7) follows the DELEGATION — the promise is self-scoped to the
   136	pre-arrival state by the row's own next clause. Fourth application,
   137	recorded: ch11-C21's lane matrix is SELF-SCOPING ("the containers
   138	this surface introduces"; its checklist derivation is its own P4's) —
   139	its text stays true after the act (the `gate-map unknown key` lane is
   140	parametric on C4's keyset), the ch13 binding-level lanes are C19's
   141	inventory, and the reopened C4's delegation pointer carries
   142	discoverability: aggregate-noted, no reopen.
   143	
   144	**Seed-row disposition (every §13.3 item landed — the ch8–ch12 form;
   145	no seed item is undisposed):** the three keys' camelCase spelling +
   146	attachment points + keyset rules → C1/C3/C4/C6; the catalog entry's
   147	fixed keyset + unknown-key disposition → C3; the packet TS shape +
   148	the three provenance source spellings incl. the gate-binding
   149	step+event pair → C11; the admission finding's lane (path form,
   150	`code` carrier, accumulate-vs-short-circuit vs ch8-C21/C36) →
   151	C7/C19; the empty/absent matrix (incl. the same-id-from-role-AND-step
   152	case) → C8 + C9; the render when a gate ref's binding is not
   153	offerable → C10; the shipped catalog entry's PRESENCE rule → C14;
   154	the REQUIRED caveat row → C15. Rows beyond the seed (C2, C5, C12's byte-scope precision, C13,
   155	C16, C17, C18, C19) per §13.3's "the draft decides".
   156	
   157	**Decision-home triage (template §1, the K0→K4 sequence — outcomes
   158	for ALL TWELVE DECIDED-HERE markers: C2 key lane + grammar, C3
   159	no-reserve, C6 any-binding, C7 entry-belted resolution grain (added
   160	at the 2026-08-01 reopen), C8(c) strict flip, C8(c) no-exclusion +
   161	unconditional domain (added at the same reopen), C8(e) hygiene lane,
   162	C9 record-order, C11 pair-flattening + quoting, C14 both-roles, C16
   163	act composition, C17 optional-on-raw shape):** all twelve triage
   164	ROW-HOME. K0: none is model-shaped — the model's own text leaves
   165	each surface open: set-iteration order (the unit iterates a set); a
   166	new id namespace's grammar; reserved syntax; intra-list repetition;
   167	catalog-entry hygiene; the evaluator-class scope of a communication
   168	key; the JSON grain of a pseudocode tuple (C11: the unit itself
   169	emits the TAGGED `{ source, at }` member — the tagged shape is the
   170	unit's own form, NOT a ch13 invention; what is decided here is only
   171	the `at`-pair flattening and the token quoting — the ch11-C13/C16/
   172	C37 rename culture governs the byte grain, so no model wave); the
   173	shipped file's authoring position; an act's composition; a TS
   174	optionality grain; and the two 2026-08-01 additions are likewise
   175	open in the model — its `refs-validated-at-definition-load` text
   176	fixes WHEN refs are validated, never what "resolved" requires
   177	structurally, and the unreferenced lane is itself a hygiene lane
   178	above the model's text (C8(c)'s own strict-start marker), so neither
   179	the belt nor the domain rule is model-shaped. K1–K4: no
   180	beyond-chapter architecture shape —
   181	C2's namespace follows the ch8-C10 row-home precedent (id grammars
   182	live in format rows; no ADR exists for any of them); no safety
   183	class, no innocent-diff risk beyond the rows' own text.
   184	
   185	**Consumed authorities (context pointers, not rules):** ch8-C7 root
   186	additive growth; ch8-C10 id-grammar rationale; ch8-C13 fixed/open map
   187	classification; ch8-C15 roles-entry keyset (as grown by ch12-C6 —
   188	the authority that makes C4's role position exist); ch8-C21
   189	accumulated findings + container suppression; ch8-C31 dev-validate
   190	machine shape; ch8-C36 staging + the standing cross-rung accumulation
   191	(the walk hands a best-effort template; the admission rung's findings
   192	JOIN the walk's in ONE validate-stage result — the realized ch11-P4
   193	F7 form C19 relies on); ch11-C1 step-keyset growth precedent; ch11-C2
   194	(gates keys ⊆ transitions keys — the reason C10's membership leg is
   195	by-construction); ch11-C3 (gate pipelines are lists; authored order
   196	is pipeline order); ch11-C4 + ch11-C30 + the ch11-C41 parenthetical
   197	(the named reopen act); ch11-C7 (the LIST-INDEX `[<i>]` path
   198	grammar); ch11-C9 (the three-class registry); ch11-C40 (the
   199	per-occurrence duplicate-lane precedent, unconditional `seen` feed —
   200	AND the REALIZATION-SPLIT precedent C19 applies: source-form lanes
   201	in the walk, value-level lanes in the admission rung); ch11-P2a A9
   202	named-lane `code` carrier; ch12-C6 (roles-entry keyset growth);
   203	ch12-C7 (the agentConfig refs-field delegation — its exact clause:
   204	the refs' value classes "arrive ADDITIVELY with their consuming
   205	chapter (the ch-9 ActorAdapter / L2b ContextAssembly)": this
   206	chapter); ch12-C8/C10 (the cascade + the commit-time
   207	`issued_agent_config` provenance C12's byte-scope names); ch12-C1
   208	(root-aggregate note convention); ch9-C18 (`*_refs` uninterpreted at
   209	the adapter — aggregate-noted above); the model's Canonical Context
   210	Block Contract table (two cells superseded — the note above) + the
   211	four `l2b-pseudocode` units + the six l2b invariants; plan §13.
   212	
   213	**Draft metrics (template §5):** rounds to ratify: 5 panel rounds
   214	(4 full + 1 targeted) + 1 top-level close, CLEAN; new-decision
   215	content: 10 DECIDED-HERE markers across 9 rows; post-ratification
   216	reopenings: 0.
   217	
   218	**Reopen record (2026-08-01, the ch13-P1 gate-1 arm re-check #3).**
   219	Reopened C1's closing clause alone: the row called the
   220	present-but-malformed catalog's result "exactly the
   221	absent-catalog-with-refs shape", an identity that holds at the
   222	REF-LANE grain (the same per-site C7 findings, nothing resolving) and
   223	fails at the whole-result grain (an absent key runs no container lane,
   224	so it cannot produce the container finding the malformed case does).
   225	The clause now states both grains. Three consecutive review rounds
   226	contested the packet's projection of this clause from ALTERNATING
   227	directions — a packet could satisfy the row's letter or the code,
   228	never both — which located the defect in the contract bytes rather
   229	than in the projection. Reopen-delta new-decision rows: 0 (a fact
   230	correction; no decision moves, and the C-row's normative content —
   231	one container finding, the named suppressed dependents, C7 still
   232	firing — is unchanged).
   233	
   234	**Dated update (2026-08-01, the C1 grain reopen):** post-ratification
   235	reopenings 0 → 1 (closed by re-ratification within the same act).
   236	
   237	**Second reopen record (2026-08-01, the ch13-P1 panel round 7 — the
   238	direct-construction region).** Reopened C7, C8(c), C2's far-side edge
   239	and C19's CHANNEL SCOPE sentence. The rows had declined to specify
   240	what the admission rung does with a catalog on the
   241	direct-construction channel: C19 handed it to "the testkit author's
   242	discipline" on a type-foreclosure premise that measurement falsified
   243	(`tsc --strict` rejects an array, a wrong-valued entry object and a
   244	class instance alike, while an ungrammatical KEY needs no cast at
   245	all), and C8(c) conditioned its key exclusion on a C2 finding that
   246	never exists there. Three consecutive review rounds each found P1
   247	defects in that one region, every one traceable to a packet having to
   248	invent policy the rows left open. Decided: C7 resolution is
   249	ENTRY-BELTED against C3's definition BY REFERENCE; C8(c)'s
   250	C2-failed-key exclusion is DELETED on both sides and its domain is
   251	ALL own enumerable keys with no value condition; C19's realization
   252	split is UNTOUCHED (the key lane stays walk-side) with only its
   253	CHANNEL SCOPE rationale corrected to the measured facts. Outcome: the
   254	admit/refuse result is channel-symmetric for every catalog and entry
   255	shape, the finding SET differing only by the lanes C19 places in the
   256	walk. Reopen-delta new-decision rows: 2 (C7's grain, C8(c)'s domain —
   257	the triage roster carries both, ROW-HOME, neither model-shaped).
   258	
   259	**Dated update (2026-08-01, the direct-channel region reopen):**
   260	post-ratification reopenings 1 → 2 (closed by re-ratification within
   261	the same act).
   262	
   263	**Third reopen record (2026-08-02, the ch13-P1 arm gate-1 on the
   264	approve-ready bytes).** Reopened C17's catalog normalization clause
   265	and C8(c)'s nullish residual sentence — two rows, nothing else. The
   266	arm found C8(c) asserting an identity the rows could not deliver: a
   267	cast-forged `contextBlocks: null` with zero refs was called
   268	"observably identical to the legal absent-catalog state", while
   269	measurement showed `admitTemplate` admitting it and carrying the
   270	`null` onto the admitted value against C17's own typed surface, so
   271	the render would meet a non-record it may neither revalidate nor dig
   272	through. Decided: C17 normalizes absent, present-null and
   273	present-undefined alike to `{}`, the nullish idiom the sibling
   274	`activation` rebuild already applies (`admit.ts:444`), which makes
   275	C8(c)'s identity true BY CONSTRUCTION. Reopen-delta new-decision
   276	rows: 1 (the nullish grain, marked DECIDED HERE in C17).
   277	
   278	TWO FORM PRECEDENTS this act sets, recorded because later drafts
   279	should copy them. FIRST — the DERIVED marking: C8(c)'s identity
   280	cannot be measured until the rebuild exists, so the row SAYS it is
   281	derived instead of wearing a false "measured", and NAMES its
   282	measuring carrier (the normalization family's empty-direction
   283	full-value equality member, audited by the build-close sensitivity
   284	pass). A derived claim without a named verifier is a deferred
   285	assertion, not a contract row. SECOND — the provenance of the defect:
   286	the false sentence entered from the RATIFYING side's own drafting
   287	without a probe behind it, which is why the executed-probe rule is
   288	recorded in the process log as binding on reviewer-proposed and
   289	author-proposed sentences alike.
   290	
   291	**Dated update (2026-08-02, the nullish-normalization reopen):**
   292	post-ratification reopenings 2 → 3 (closed by re-ratification within
   293	the same act).
   294	
   295	**Fourth reopen record (2026-08-02, the ch13-P1 arm gate-1 re-check —
   296	the SAME DAY as the third, and that is the point).** Reopened C17's
   297	normalization clause and C8(c)'s measuring-carrier sentence — two
   298	rows, nothing else. The third reopen had been scoped to the NULLISH
   299	catalog; the arm's re-check showed the defect was the CLASS "an
   300	admitted catalog may be a non-record", so four of five members
   301	survived the fix. RECEIPTS (live `admitTemplate`, zero refs,
   302	2026-08-02):
   303	
   304	    contextBlocks: null  → ok:true  carried: null  (object)
   305	    contextBlocks: 42    → ok:true  carried: 42    (number)
   306	    contextBlocks: true  → ok:true  carried: true  (boolean)
   307	    contextBlocks: ""    → ok:true  carried: ""    (string)
   308	    contextBlocks: []    → ok:true  carried: []    (object)
   309	
   310	Decided: the authored value is carried in place IFF it is a RECORD
   311	(a non-null, non-array object — the predicate defined in C17 for the
   312	rebuild rule) and normalizes to `{}` otherwise, so C17's typed
   313	surface is true without exception. C8(c)'s carrier grew to match: the
   314	absent fixture and EVERY non-record fixture must yield byte-identical
   315	admitted values. Reopen-delta new-decision rows: 1 (the class-width
   316	grain). TWO CORRECTIONS the ratifying side caught before GO, recorded
   317	because both are rule-instances: the draft first attributed the
   318	record predicate to C8(c), which does not state it (an UNVERIFIED
   319	CITATION — the citation-shaped form of an unrun "measured"); and the
   320	empty-array receipt was demanded in the record rather than in
   321	conversation, since a MEASURED label travels with its receipt.
   322	
   323	**Dated update (2026-08-02, the class-width normalization reopen):**
   324	post-ratification reopenings 3 → 4 (closed by re-ratification within
   325	the same act).
   326	
   327	## Contract rows (every normative statement is a C-row)
   328	
   329	| ID | Rule |
   330	|---|---|
   331	| C1 | The template top level gains the OPTIONAL `contextBlocks` key (ch8-C7 additive growth; the root keyset becomes the ch8 five + the ratified optional keys `runtimeContext`, `round`, `activation`, and this — ch12-C1's aggregate note described the pre-ch13 state; no earlier row is modified). camelCase realization of the model's `context_blocks` (the ch11-C13/C16/C37 rename culture — stated so neither side silently forks). Its value is an OPEN-KEY map (the ch8-C13 classification: its KEYS are data — block ids, C2; its VALUES are C3's). A PRESENT value that is not a map — present-null, scalar, list — is a container-precondition finding at path `contextBlocks` (ONE finding, dependent lanes suppressed — the ch8-C21 container rule; the suppressed dependents are the CATALOG-side lanes: C2's key lane, C3's entry lanes, and C8(c)'s reference check — NOT the ref-list member lanes, which presuppose their own containers, and NOT C7, whose per-site findings still fire because no entry can resolve; AT THE REF-LANE GRAIN the resulting shape is exactly the absent-catalog-with-refs shape — the same per-site C7 findings, nothing resolving — while THIS case adds its OWN one container finding beside them, which an absent key cannot produce because no container lane runs on it; stated at both grains, not accidental). An ABSENT key with zero refs issued is legal (every pre-ch13 file). |
   332	| C2 | Block-id KEY LANE and grammar (a NEW namespace — catalog keys and every ref-list member). ONE key lane, the sibling helper shape (the realized `idGrammarError` form rejects non-strings and grammar violations in one lane — a split identity-vs-grammar precedence would be unobservable at build): a catalog key that is not a node-level STRING (number, boolean, null-form — PROBE-CB1/CB2's identity classes; the walk sees them because `mapAsMap: true` preserves key types) OR that fails the grammar below yields ONE walk-side validation finding at the CONTAINING path `contextBlocks` — UNCONDITIONALLY, string and non-string alike (the realized sibling form: the steps/roles/transitions/gates key lanes all report at the bare containing path; the key-segment conditional belongs to the unknown-key/container lane class, not to key lanes — stated so the build copies the right idiom, and the C3 entry-lane paths (`contextBlocks.<id>`) stay DISTINCT from this lane's by construction). Hand-off: the walk FILTERS NON-STRING keys out of the built catalog (a typed Record cannot carry them); a grammar-failing STRING key STAYS in the built catalog (the realized non-cascade culture — the sibling walk keeps grammar-failing string ids), its presence INERT AT THE RESOLUTION LANE ONLY: no shape-passing ref can address it (one shared grammar). It is NOT inert at C8(c) — since the 2026-08-01 reopen no key is excluded from the unreferenced check, so such a key draws that finding too unless a raw ref member names it. N failing keys yield N findings at the one bare path, separated by message (the sibling multi-key behavior). Grammar (string keys and members): nonempty, matching `^[a-z][a-z0-9-]*$`. DECIDED HERE, strict start: letter-start excludes the numeric string forms; the charset bans whitespace and `.` by construction (block ids appear as ch8-C21 path segments); the `-` separator deliberately diverges from the ch11-C6/ch12-C3 underscore token grammars (the model's exhibited ids are kebab — stated so the namespaces do not blur). The model's exhibited ids (`reviewer-severity-ontology`, `no-converged-before-round-2`) are members — and the digit-bearing kebab STRING key is the lane's named SUCCESS member (PROBE-CB1's discriminating case; a digit-rejecting implementation must red — P1-driven). Two-sided edges, stated here as on their owner rows: this lane is suppressed under C1's catalog-container failure; a C2-failed key is NOT excluded from C8(c) (the carve-out was deleted at the 2026-08-01 reopen — the two lanes state two independent facts, and on the direct-construction channel this lane does not run at all). A ref-list member violating the grammar fails the C4/C6 member-shape lane and is EXCLUDED from C7's resolution set (C7 states the same carve-out from its side). |
   333	| C3 | A catalog ENTRY is a FIXED-KEYSET map: `{ body }`, `body` REQUIRED, a nonempty string — authored static text rendered VERBATIM, and template-authored DEFINITION data by construction: no runtime, actor, or run-supplied channel reaches this position (the catalog is admitted-template content; C5 closes the only run-scoped channel near the field family). Outbound, the same bytes travel verbatim into the actor's `packet.json` with no length or content bound — the same exposure class as the shipped `instruction` field; prompt shaping is the `actor-adapter-rendering` Absent, named, not implied. No interpolation syntax exists, and none is reserved: a body containing `{{…}}`-like text is legal literal prose (DECIDED HERE — rejecting would speculatively enforce the computed-bodies Absent; that later chapter owns any syntax and its migration, including the consequence that today's literal `{{…}}` prose would need migration then). Unknown keys in an entry are definition-static findings (the ch8-C13 fail-closed culture; walk-side per C19's split); an entry value that is not a map — present-null, scalar, list — is its container-precondition finding at `contextBlocks.<id>` (suppressing the entry's body lane). Two-sided suppression edges, stated here as on their source rows: this row's entry lanes are suppressed under C1's catalog-container failure and under a C2 key failure on the same entry. |
   334	| C4 | The role/step ref positions: `promptConcernRefs` INSIDE the two template agentConfig positions (`roles.<r>.defaultAgentConfig`, `steps.<s>.agentConfig` — the positions exist per ch8-C15 as grown by ch12-C6) — camelCase of the model's `prompt_concern_refs`; the ch12-C7-DELEGATED additive arrival (ch12-C7 pre-authorizes the refs' value classes arriving with "their consuming chapter (the ch-9 ActorAdapter / L2b ContextAssembly)" — no ch12 reopen; this is the FIRST field of the otherwise format-OPEN agentConfig map that admission types, and ch12-C7's "NO field of it is format-enforced or kernel-interpreted" sentence thereby described the pre-ch13 state — the aggregate-note convention, no ch12 row modified; the GP2-scan exemption on agentConfig stays safe beside this: any non-string-keyed map nested in agentConfig already fails the canonical-JSON value lane as a non-plain object). Value class: a list (possibly empty) of C2-grammar strings. ABSENT states are legal and contribute ZERO refs: an absent `defaultAgentConfig`/`agentConfig` map, or a present map without the key (C17 materializes the normalized form to `[]` — the render never dereferences an absent path). A present value that is not a list — present-null, scalar, map — is a container-precondition finding at the field's path (dependent member lanes suppressed, and C8(c)'s check is SKIPPED for the whole template — the soundness reason: the reference set is a template-wide UNION, and a per-list skip would emit PHANTOM unreferenced findings for entries referenced only from the broken list); a non-string, empty-string, or grammar-violating member is a finding at `…promptConcernRefs[<i>]` (the ch11-C7 list-index path grammar, 0-based). Every OTHER agentConfig field stays exactly as raw and format-open as ch12-C7 states. The tree's one live snake-spelled FIXTURE (`prompt_concern_refs` in the l0c golden-trace file — TWO sites: the authored step key and the `RESOLVE_REVIEW` expected-cascade echo; a TS fixture, not an authored template file) is DISPOSED at P2: BOTH sites move to the typed key WITH a catalog entry, realizing the L0c slot's resolution (this chapter's acceptance evidence); the post-move fixture carries the typed key beside untyped snake siblings at those sites (`skill_refs`; the implementer-side map carries `prompt_profile_refs`) — the CORRECT end state (one typed field, open raw vocabulary), stated so it does not read as an inconsistency; the sweep searches the VALUE (`reviewer-severity-ontology`) too, per R-ABSENCE-CONSUMERS. |
   335	| C5 | A `runOverrides` entry MAY carry `promptConcernRefs` (ch12-C7: the kernel merges and records runOverrides opaquely — that surface is untouched, the merged value flows into `effectiveAgentConfig` as DATA), but it is NEVER a render source: the render reads the admitted template's normalized declarations (C17), never the merged effective config, and the provenance source enum has no run-level member (model-verbatim — the unit's read-path plus the three-member source set). Consequences, all DECLARED observables with a named owner (the P2 packet's test families): (a) run-supplied ref ids are NOT validated against the catalog and never resolve to blocks — and a catalog entry serving ONLY run-supplied refs is rejected by C8(c) as unreferenced (dead by design, the two rows compose); (b) because the ch12-C8 cascade merges top-level keys wholesale (arrays REPLACE), a run override or step override CAN make `effectiveAgentConfig.promptConcernRefs` differ from the rendered set — the packet's `contextBlocks` field IS the rendered truth (communication), `effectiveAgentConfig` is recorded run intent (data); an adapter renders neither, it passes the packet through (ch9). Stated so the two read-paths never silently fork. |
   336	| C6 | The gate-binding keyset gains OPTIONAL `contextBlockRefs` — VIA the named post-close reopen act over ch11-C4, ch11-C30, and the ch11-C41 parenthetical, riding this draft's ratification (C16 is the act's carrier; prepared texts in Context; ch11-C30's realized text otherwise promise-binds the key's absence, with a code witness in its map entry the act retires). camelCase of the model's `context_block_refs`. Value class: identical to C4's (a possibly-empty list of C2-grammar strings; same container/member lanes incl. present-null — a container failure likewise skips C8(c) — paths `steps.<s>.gates.<event>[<i>].contextBlockRefs[<j>]`, the ch11-C7 list-index grammar). Unknown-key fail-closed on the binding is UNCHANGED. The key is legal on ANY binding — declarative, packaged, and process alike (the ch11-C9 three-class registry; communication is evaluator-independent, DECIDED HERE — the restrict-to-declarative alternative was rejected: the model's config instance exhibits declarative only because that is the trace's gate, not a scoping rule). |
   337	| C7 | The resolution lane: `validate_context_refs` runs at ADMISSION-time definition-static checking (`admit_definition`; realization per C19's split), its lookup domain the BUILT catalog Record (no stringify-first lookup). One OBSERVABLE lookup consequence, stated with its vacuous twin: keys and members share ONE grammar, so no shape-passing ref can address a grammar-failing STRING key — that leg is VACUOUS by construction (stated, never testable); the observable case is the FILTERED NON-STRING key, whose refs are unresolved and fire HERE (the typed catalog factually cannot carry the key — two true findings for the one authoring defect, deliberately; the discriminating fixture is PROBE-CB2's class: a bare `true:` entry beside the grammar-valid ref `"true"`): EVERY issued ref — each role/step `promptConcernRefs` member and each gate `contextBlockRefs` member that PASSED its C4/C6 member-shape lane (a shape-failing member is excluded here; a beyond-first duplicate occurrence fails C8(e) and is excluded here — the FIRST occurrence carries resolution) — must RESOLVE TO AN ENTRY (DECIDED HERE at the 2026-08-01 reopen, the ENTRY-BELTED grain): the ref must be an OWN key of the catalog AND the value at that key must BE AN ENTRY AS C3 DEFINES ONE — by REFERENCE, never restated here, so ONE definition of "entry" governs both channels (a fixed-keyset `{ body }` map whose `body` is a nonempty string; an empty body, an extra key, or a non-map value all fail the belt exactly as they fail C3). Key existence alone is not resolution — a key whose value is not a C3 entry leaves its refs UNRESOLVED and fires this lane per site. What the belt makes unnecessary, stated so no reader reintroduces it: NO catalog-container predicate and NO array special case is owed beside it — an array's own grammar-passing `length` (whose value is a NUMBER) and a wrong-valued `{ <id>: 42 }` alike fail the belt — only the NULL/UNDEFINED guard the own-property read itself requires (`Object.hasOwn(null, …)` and `Object.hasOwn(undefined, …)` throw; a PRIMITIVE catalog does not throw — `Object.hasOwn("abc", "length")` is true — and needs no guard because the belt refuses its values anyway). CHANNEL CONSEQUENCE, both directions: on the DIRECT-CONSTRUCTION channel this is the whole of the catalog's shape discipline at resolution time (no walk lane runs there); on the FILE channel the belt runs just the same (the walk's build slot materializes entry values whatever their shape — the non-cascade culture), so a malformed entry NAMED BY A REF draws BOTH C3's entry finding and this lane's per-site finding — two independent, separately-fixable facts, the multi-true-finding pattern this chapter already ratifies at C2's filtered non-string key. The admit/refuse OUTCOME is therefore channel-symmetric for every entry shape; only the finding SET differs, and only by the lanes C19 places in the walk. An unresolved ref is the `unresolved_context_block_ref` DEFINITION ISSUE (the EXISTING ch11-model-sync name; no registry rejection name is touched), carried on the named-lane form: `code: "unresolved_context_block_ref"` present on exactly this lane (the ch11-P2a A9 carrier), path = the REF SITE (`…promptConcernRefs[<i>]` / `…contextBlockRefs[<j>]`), one finding PER unresolved site over the resolution set, ALL sites ACCUMULATED in one result — a projection of the standing ch8-C21 validate-stage culture (the model unit's first-issue return is the semantic floor). Refs issued with NO RESOLVING ENTRY fire this lane per site — an absent catalog, a present-but-empty catalog, and a malformed catalog (C1's single container finding beside these) alike. This row is the `l2b/refs-validated-at-definition-load` invariant's home (disposition `test`). Containment, both legs stated: the validated ref set ⊇ the render's read set (equality reachable on a single-step template; C9 reads the dispatched step's role, its own step, and its authorized gates), and every dispatch site reads a store-admitted template — together these are why C9's render-time miss is integrity drift, not a business state. |
   338	| C8 | The empty/absent/edge matrix: (a) absent catalog + zero refs → legal, zero-impact; (b) catalog present + zero refs → legal; (c) an UNREFERENCED catalog entry is a validation finding at its path (DECIDED HERE, strict start; like (e), a hygiene lane above the model's text — the unit checks ref→catalog only — the ch8-C16 declared==used shape: strict-start is the ONE direction later relaxable additively, while permissive-start could be tightened only as a breaking format change; the shipped C14 entry is referenced by construction). The REFERENCE SET is the RAW authored member strings across all well-formed ref lists — grammar-violating and duplicate members COUNT as references (they evidence intent; their own lanes carry the defect), and the check is SKIPPED entirely when any ref-position container lane fired or the catalog container itself failed (the template-wide grain is a SOUNDNESS rule, not merely anti-cascade: the reference set is a template-wide union, so a narrower skip would emit phantom findings); NO key is excluded from this check (DECIDED HERE at the 2026-08-01 reopen — the former C2-failed-key carve-out is DELETED on both of its sides, here and at C2's edge): the check runs on EVERY channel, and its DOMAIN is the catalog's own ENUMERABLE keys — ALL of them, with NO value condition (DECIDED HERE at the same reopen): a key whose value is not a C3 entry is still a declared-and-unused key and draws this finding, because a value condition would reinstate an unstated exception of exactly the kind deleted above, and its justification fails on the direct-construction channel for the same reason. ENUMERATION GUARD: a cast-forged null/undefined catalog is not enumerated (`Object.keys(null)` throws) and yields nothing of this lane — residual named rather than left implicit: such a catalog carrying ZERO refs ADMITS, and its admitted value is then IDENTICAL to the absent-catalog state of (a) — not merely similar: C17's rebuild normalizes a nullish catalog to `{}` exactly as it normalizes an absent one (reopened 2026-08-02). That identity is DERIVED from C17's rule and is NOT measurable until the rebuild exists; its NAMED MEASURING CARRIER is the normalization acceptance family's EMPTY-DIRECTION member, which proves the admitted form by FULL-VALUE equality — the ABSENT fixture and EVERY non-record fixture (present-null, a scalar, an empty string, an empty array) must yield BYTE-IDENTICAL admitted values — audited by the build-close arm's mandatory sensitivity pass against the built test body. What IS measured, 2026-08-02, is the defect the normalization closes: today's `admitTemplate` admits `contextBlocks: null` and carries the `null` onto the admitted value — `{"ok":true,"contextBlocks":null}` — which this row's own C17 typed surface forbids. A PRIMITIVE catalog enumerates by its own rules, NOT to nothing — MEASURED 2026-08-01: `Object.keys(42)`, `Object.keys(true)` and `Object.keys("")` are empty while `Object.keys("abc")` is `["0", "1", "2"]` — so a nonempty-string forgery draws this lane at each index key and REFUSES, the fail-closed direction, needing no rule of its own. DOMAIN ASYMMETRY, named once here because it is load-bearing: C7 resolves over OWN keys at any enumerability while this lane enumerates own ENUMERABLE keys, so a cast-forged NON-ENUMERABLE own entry can resolve a ref yet is never audited here — a residual bounded to that cast class, accepted rather than hidden. Rationale, recorded because the carve-out had one: the exception did not SAVE the second complaint, it deferred it by one run (fix the key, and the unreferenced finding appears next time), while costing a cross-lane coupling — the unreferenced check had to know whether the key lane had fired, which is unanswerable on the direct-construction channel where that lane does not run. CONSEQUENCE MATRIX, stated so the two channels are read correctly: FILE channel, ungrammatical key, no ref naming it → TWO findings (C2's grammar finding + this one); DIRECT channel, same catalog → ONE finding (this one — C2 is file-channel per C19); EITHER channel, ungrammatical key WITH a matching RAW ref member → NO unreferenced finding, because the reference set is raw member strings and a grammar-violating member still COUNTS as a reference (that member carries its own C4/C6 finding); EITHER channel, a key whose VALUE is not a C3 entry and which nothing references → this lane fires on it like any other (the domain has no value condition), and where a ref DOES name it C7's belt fires instead. The admit/refuse OUTCOME is channel-symmetric throughout; only the finding SET differs, and only by the lanes C19 places in the walk. (d) an EMPTY ref list → legal, renders nothing; (e) a DUPLICATE id within ONE ref list is a validation finding PER repeat occurrence — `[a, a, a]` yields findings at index 1 AND index 2 (the ch11-C40 per-occurrence precedent; the `seen` set is fed UNCONDITIONALLY by raw members under SameValueZero — the realized Set semantics — so a shape-failing duplicate co-fires its shape lane and this lane on the same index, per-defect-class findings), each repeat excluded from C7's resolution set (DECIDED HERE — an admission hygiene lane above the model's text, the ch11-C15 hardening precedent; the DESIGNED repeat is the cross-source one, C9's dedup); (f) `contextBlocks: {}` (present-empty map) and an ABSENT catalog behave identically under refs: legal with zero refs; with refs issued, C7 fires per site (no resolving entry); (g) the fully-valid populated case — catalog + resolving refs — admits CLEAN with zero findings: the family's success negative, P1-driven, its member set including the digit-bearing kebab id (C2's named success member). |
   339	| C9 | The render (`assemble_context_blocks`, model-verbatim with ONE order decision and ONE iteration-domain restatement — the unit loops `authorized_ops` then `gates_for`; this row loops the gates record filtered by C10, membership-equivalent via ch11-C2, the argument carried at C10): source order IS render order — role refs, then step refs, then gate refs; within each source, list order; the gate leg iterates the admitted step's `gates` RECORD in its OWN key order (the surface the refs live on — the invariant's "declaration order within each" read on the gate source itself), each event's pipeline in binding-list order (ch11-C3: authored order IS pipeline order), each binding's `contextBlockRefs` in list order, filtered by the C10 predicate. RECORD KEY ORDER, not "authored declaration order", deliberately: the admitted form is a JS record; integer-like event keys authored in QUOTED form hoist to the front AND re-sort ascending among themselves (PROBE-CB3; an unquoted `2:` is rejected upstream as a non-string key); record enumeration order is the deterministic, substrate-true basis. Two alternatives considered and REJECTED (DECIDED HERE): the `transitions`-record order (a different surface that does not carry the refs; `availableOps` continues to publish ITS order independently) and a canonical SORTED key order (deterministic and hoist-immune, but it would divorce render order from authored reading sequence — the invariant's own gloss is "order is reading sequence"). Construction-order dependency DECLARED: the admitted record's key order is the authored order modulo quoted-integer hoisting because admission REBUILDS the gates record over the authored keys — an admission refactor that changes that construction order is a C13-visible contract change, not a free refactor. Dedup: a ref reached again renders ONCE, first-seen position; `provenance.sources[]` appends EVERY emitter in encounter order — this row is the `l2b/dedup-with-retained-provenance` invariant's home (disposition `test`). Bodies resolve from the admitted catalog ONLY (post-admission a miss cannot exist — C7's two containment legs; a miss at render time is kernel-integrity drift, throw, never a skip — the standing dispatch integrity culture). `HANDLE`'s verdict path is untouched. |
   340	| C10 | The gate-ref predicate: a binding's blocks render iff its transition ∈ available_ops ∩ L1 capability (model-verbatim; the `l2b/authority-scoped-gate-blocks` invariant, disposition `test`; the ch11 authority logic REUSED — no fresh authority logic). Honest per-leg accounting: the MEMBERSHIP leg (∈ available_ops) holds BY ADMISSION on every admitted template — ch11-C2 makes every gates key a transition of its step, so the leg can never exclude anything post-admission; its test carrier is therefore the render's ITERATION-DOMAIN negative (blocks come only from the DISPATCHED step's gates — the golden trace's cross-role leg: the reviewer's rule is absent from the implementer's packet). The L1 NARROWING leg carries the MANDATORY discriminating negative via a directly-constructed `capabilityProfile` — the live type-level channel (narrowing profiles are already test-constructed on the kernel seam today; only FILE authoring of profiles is the deferred Absent): a profile narrowing the dispatched role's current step below its transitions must NOT render that gate's block even though the event ∈ availableOps — the lane that reds an implementation dropping the `∩ capability` term. Under the DEFAULT derivation the intersection is a no-op — which is exactly why the explicit-profile negative is mandatory, never waived (the green-but-blind class). FORWARD SCOPE: the EC layer's reprint replaces `available_ops` with `offerable_ops` — the membership leg's by-admission status is PRE-EC, and re-examining it belongs to the §1.3 EC candidate's chapter. |
   341	| C11 | The packet field: `ContextPacket` gains `contextBlocks` — camelCase of the model's packet-level `context_blocks` (the same rename culture, stated) — ALWAYS present (possibly empty; model-verbatim — the `dispatch_intent` reprint carries the field unconditionally — with the ch12-P2 E1 always-present precedent behind it), an ORDERED list of `{ id, body, provenance: { sources } }`. A source member is `{ source: "role_config" }` \| `{ source: "step_config" }` \| `{ source: "gate_binding", step, event }` — the TAGGED shape and the `source` field are the MODEL UNIT'S OWN form (the unit's members are tagged `{ source }` maps, the gate member additionally carrying `at`; the domain view's bare-token spelling is its compressed notation); the token VALUES are model-verbatim (the realized discriminant-value culture keeps model snake_case tokens: `runner_error`, `kickoff_pending`); DECIDED HERE is only the `at: (step, event)` pair's flattening to sibling `step`/`event` fields and the string-literal quoting (a nested `at: {step, event}` map was the alternative — rejected for canonical-JSON flatness; stated so neither side silently forks). Two bindings on one (step, event) each carrying the same ref yield two IDENTICAL source members (no source-level dedup — model-verbatim append; stated as the decided observable, P2-owned). The list order is the C9 render order and travels VERBATIM through the existing canonical `packet.json` channel (the ch9 H2 surface — object keys canonicalize sorted, array order is semantic and preserved). The always-present field ripples every full-packet deep-equal fixture in the suite — a NAMED family spanning the kernel/testkit/runner/CLI suites, member enumeration P2's (R-ALTITUDE-LINE). |
   342	| C12 | Communication-only boundary, byte-scope precise: the packet FIELD `contextBlocks` (and the catalog behind it) never enters gate evaluation, transition verdicts, round arithmetic, or any kernel DECISION surface; the definition-static lanes (C1–C8) are the stated exception — catalog content decides ADMISSION findings, never a runtime verdict. The canonical deletion experiment: removing a catalog entry WITH its refs in the same edit leaves every verdict, round, and transition outcome identical; the bytes that change are the packet artifact (`packet.json` / the `contextBlocks` field) AND — exactly where the removed refs rode the two agentConfig positions — the commit-time `issued_agent_config` provenance rows (ch12-C10's surface: refs are config DATA and were always recorded there; blocks themselves never enter any committed row). The GATE-source variant is committed-row byte-identical (ch11-C27 records `{uses, verdict, reason, message, evidence_refs}` only). Spawn-side, TWO carriers reach a real process and both are closed: the packet artifact carries the refs as data (this row's packet-byte scope), and the argv-INTERPRETATION path over `effectiveAgentConfig` is closed by ch9-C18 (`*_refs` left uninterpreted — aggregate-noted in Context). This row is the `l2b/communication-only` invariant's home (disposition `review` — the owning packet carries its `REV-*` line against THIS precise scope, never the wholesale form). |
   343	| C13 | Determinism: the render is a PURE function of (instance, template, step) — no clock, no randomness, no store read beyond its inputs; same committed instance state + same admitted template ⇒ byte-identical `contextBlocks` (the `l2b/deterministic-ordered-render` invariant, disposition `test`; carrier: the P2 golden trace plus a dedicated same-inputs double-render equality lane, P2-owned). |
   344	| C14 | The shipped-catalog presence rule: the canonical `v3/templates/local-pair-v0@1.yaml` gains THREE insertions — a root `contextBlocks` catalog carrying the entry id `emit-envelope`, and a NEW `defaultAgentConfig: { promptConcernRefs: [emit-envelope] }` subtree under EACH of the two roles (an untruncated draft-time sweep found the file authoring neither key; this makes the canonical file the first agentConfig-family authoring in an authored template FILE — the l0c TS fixture authors the family in-test today — DECIDED HERE: role-level on BOTH roles, because every actor emits and the block is role-symmetric). The entry's PROSE is packet-time authored (the interim carrier of the emit-envelope knowledge — `v1-prompt-parity-audit.md` §5; its retirement is bound into the DoD of the chapter that takes the §1.3 EC candidate). Ripple, the named families (member enumeration the owning packet's, R-ALTITUDE-LINE): (i) the ch8-P2 equality pin FOLLOWS the file in the same commit (fixture + file move together, the pin test unchanged — the ch11-P4 Y1/Y2 precedent; both pin sides pass through admission, so C17's normalized fields appear symmetrically); (ii) the `effectiveAgentConfig` assertions and full-packet deep-equals — spanning the CLI/journey suites AND the kernel/testkit deep-equal fixtures (C11's family, one sweep) — re-pin, P2; (iii) the COMMITTED-provenance family: `issued_agent_config` assertions re-pin to the new resolved values (the C12-named byte surface — the shared fixture flows through the ch12-C8 cascade into every committed transition); MEMBERSHIP is by `fixtureTemplate`/canonical-file CONSUMPTION, not by directory (the floor and testkit suites, the runner delivery-loop suite, and the root CT files are in; a suite building its own template is out) — P2; (iv) the l0c golden-trace re-pin (a ch12 trace: BOTH fixture sites move per C4's disposition and gain the catalog entry) — P2; (v) consumers of the keys' former ABSENCE are swept by VALUE and by key (R-ABSENCE-CONSUMERS). |
   345	| C15 | Catalog-authoring caveat (the plan §13.3 REQUIRED row; aim, not enforcement): a block body that states a gate's configured VALUE can go stale against that gate, and NOTHING detects it — C7 proves a ref RESOLVES, never that prose matches config; the missing capabilities are the two named L2b Absents (`computed-templated-bodies`, `semantic-parity-check`), owned by a later chapter. The shipped file carries a sibling COMMENT beside the catalog stating exactly this (plan §13.1 item 4 — placed where the risk is CREATED: the author of the NEXT entry). This chapter ships no gate-describing body, so the risk is latent here by construction. |
   346	| C16 | The reopen-act carrier (the ch9-C27/ch12-C26 precedent — the ratification act's own cross-contract duties, normative; one GO, TWO named acts: this draft's ratification + ONE ch11 reopen act over three named sites — DECIDED HERE, the act's composition): the act executes the ch11-C4 + ch11-C30 + C41-parenthetical reopens per the Context's prepared texts and itemized map duties (C4's keyset-parenthetical update included). EXECUTABLE ORDER, four commits (the ch9 act's git-verified precedent): this draft's CONTENT commit first (carrying the final rows AND all SIX draft-round plan edits — the §13.4 order line, the §13.4 P2-row l0c re-pin clause, the §13.5 registry-flip DoD clause, the §13.5(e) item with its fallback, the §13.5(f) integer-key-ban boundary item, and the §1.3 carried-item (3) system-vs-workflow sub-question (the last two the owner's ratification-round additions) — R-ALIGNED-UP; plus the FH-3 future-hardening record (the ratification round's parse-don't-validate question, `future-hardening.md`)) → ch11 reopen commit 1 (the three site edits + status `reopened` + `realized_map` removed) → ch11 reopen commit 2 (the updated map restored per the ITEMIZED duties — C41's entry gaining the file's minimal reopen annotation, the :252/:264 convention; a FOURTH reopen-record prose BLOCK in ch11's Context — the file's own convention, INCLUDING its `Reopen-delta new-decision rows: 0` line; the close-metrics increment recorded as a NEW dated update line "3 → 4" — the existing dated 2026-07-23 line stays untouched, the count is cumulative with dated increments, never a silent edit of a dated line; the new ratification block whose recorded `commit` is reopen commit 1's hash, FULL 40-hex (the file's current block form); status back to `realized`) → the ch13 draft's own ratifying commit LAST, closing the act (its ratification block records the CONTENT commit's hash — template §4). Re-ratification is permanently human (one GO covers the act's whole payload — named bytes). Every commit stays green on the draft-FORM rules; the packet-anchor surface into ch11 is loud-red for the window BY DESIGN, and the zero-reopened gate's THREE gate points (packet approve, chapter close, process flips — README §5.5's canonical inventory; the check-docs wrapper binds the flag on its approve/close modes) all sit OUTSIDE the act, so none may land between the ch11 pair. Plan §13.4's order line carries the act's annotation (aligned at the ch13 draft round). |
   347	| C17 | The admitted-form normalization (the ch11-C38/C39 admission-normalized precedent) — the `type/schema` carriers: on all-or-nothing admission success, the rebuild MATERIALIZES the validated context surfaces into dedicated normalized fields on the admitted value — the catalog as `contextBlocks: Readonly<Record<BlockId, { body: string }>>` in place IFF THE AUTHORED VALUE IS A RECORD — a non-null, non-array object, the predicate DEFINED HERE for this rebuild rule — and `{}` OTHERWISE: absent, present-null, present-undefined, a scalar, an empty string, an empty array, ANY non-record value that reached the rebuild normalizes to `{}` (DECIDED HERE at the 2026-08-02 CLASS-WIDTH reopen, which GENERALIZES the nullish clause it replaces — that instance-width form left the rest of its own class carried, MEASURED 2026-08-02 through the live `admitTemplate`: `null`, `42`, `true`, `""` and `[]` each admit with zero refs and are carried onto the admitted value unchanged, receipts in the reopen record). NO TENSION WITH C7, stated so the two rows are never read against each other: C7's "no catalog-container predicate is owed" governs the RESOLUTION lane, where the entry belt makes such a predicate unnecessary — the predicate defined here serves ONLY this rebuild's normalize-or-carry decision, creates no lane, emits no finding, and moves no template's admit/refuse outcome. Stated at class width deliberately: this row's typed surface is then true WITHOUT exception on every admitted template, and the render — which C9/C17 forbid to revalidate or dig through `unknown` — never meets a non-record. The sibling precedent is the one the nullish form already cited (`activation`, `admit.ts:444`), widened from `??` to the predicate. A NONEMPTY array or string never reaches this rule: A4's domain fires at its index keys and the template is refused before any rebuild.), each role entry's and each step's validated ref list as a SIBLING field `promptConcernRefs: readonly BlockId[]` beside (never inside) the raw agentConfig map (absent map, absent key, or empty list ⇒ `[]`), and each admitted gate binding's `contextBlockRefs: readonly BlockId[]` likewise (⇒ `[]`). TYPE SHAPE per the standing dead-belt precedent (`advancesRound?`): the fields are OPTIONAL (`?`) on the shared raw types — `AdmittedTemplate` is a brand, not a structural type — with the always-present guarantee holding at the VALUE level on every admitted value; required fields would break every hand-built raw fixture and are REJECTED (DECIDED HERE — the optionality grain). The AUTHORED keysets do NOT grow: `ROLES_ENTRY_KEYS` stays `{defaultActor, defaultAgentConfig}` and the step keyset is untouched — the normalized fields are admission-PRODUCED, never authorable (an authored `promptConcernRefs` at the roles-entry or step level is an unknown-key finding, unchanged; the authoring position is C4's, inside agentConfig). THREE named consequences, all new P1 work: (1) admission REBUILDS `roles` (today only `steps` is rebuilt); (2) the role-side normalized list is the first normalization whose SOURCE is nested (inside `defaultAgentConfig`) rather than in place; (3) the admitted gate binding is constructed FRESH with exactly `{uses, config}` today — the rebuild gains the `contextBlockRefs` field, or every gate-sourced block would silently vanish on a fully-admitted template. The RAW agentConfig maps keep the authored key as cascade-read DATA (C5 reads THAT); at the two template positions the same key is admission-VALIDATED (C4) — "opaque" describes only the runOverrides position; the RENDER reads ONLY the normalized fields (no `unknown`-digging, no re-validation at dispatch — the ch11-C22 class); the definition-static lanes read the source form and the built template (C19), never these fields (they do not exist yet at checking time). The `l2b/single-body-source` invariant's `type/schema` disposition is carried HERE: body text has exactly one typed home (the catalog's `{ body }`), and every ref position's type is id-only. RIPPLE, two named families (owner P1): the admitted-form shape-delta lane (the "what admission adds" deep-equal gains the new materialized fields beside `activation`/`runtimeContext`/`advancesRound`), and the THREE `l2b/*` domain-registry rows flip `pending` → realized WITH THEIR REALIZING PACKETS — the catalog and `ContextBlockRef` rows at P1 (their types land here; `BlockId` and the catalog record type are the realized-form names), the `ContextBlock` row at P2 (the packet-rendered value) — the registry test asserts key sets, not dispositions, so the flip is a NAMED duty precisely because no gate reds on its omission (the C16 token-blind-lint lesson applied to our own manifest). |
   348	| C18 | The CLI surface: NO new verbs and NO new flags — the chapter's definition-static laneS (C19's inventory) ride the existing channels unchanged: dev `validate` surfaces them through the ch8-C31 `{stage, findings}` machine shape (stage `validate`; C7's lane carries its `code` end-to-end into the CLI doc — the one cross-boundary hop, DRIVEN by a P1 lane asserting the `code` field's VALUE (equality with `unresolved_context_block_ref`, never presence alone) in the CLI JSON, plus the exclusivity negative (no other CH13 lane carries a `code`; ch11's A9 named lanes keep theirs — the doc-level assert binds a ch13-only fixture)), and the store/create paths through their standing typed-error docs (the ch11-C28 analog, stated so P1 neither mints a surface nor re-derives its absence). |
   349	| C19 | The lane inventory + realization split (the ch11-C40 REALIZATION-SPLIT precedent, applied at chapter grain): the chapter's definition-static lanes are exactly — C1's container lane, C2's key lane, C3's three entry lanes (keyset / body / container), C4's and C6's container + member-shape lanes, C7's resolution lane (the ONE code-carrying lane), C8(c)'s unreferenced-entry lane, and C8(e)'s duplicate lane. SPLIT: the CATALOG-SIDE lanes (C1 container, C2 key, C3's three) live in the WALK — the Map-form source traversal where non-string keys are visible and the four sibling key lanes already live; the REF-SIDE and VALUE-LEVEL lanes (C4/C6 containers + member shapes, C7 resolution, C8(c), C8(e)) live in the ADMISSION RUNG over the built template. The ref-side containers/members sit rung-side DELIBERATELY, diverging from C40's source-form classification for its typed input (stated, two reasons): the refs live inside the format-OPEN agentConfig, where member primitiveness is NOT type-foreclosed on the direct-construction channel — rung placement covers that channel too; and C8(c)'s skip-on-container-failure signal becomes INTRA-RUNG (a walk→rung findings signal does not exist in the pipeline interface and is not minted). CHANNEL SCOPE, stated symmetrically: the walk lanes are FILE-CHANNEL. On the direct-construction channel the ratified catalog type forecloses NOTHING that a cast does not reach — MEASURED 2026-08-01: `tsc --strict` rejects an array, a wrong-valued entry object, and a class instance alike against C17's `Readonly<Record<BlockId, { body: string }>>`, so no forgery class is type-endorsed and none is type-prevented — while an ungrammatical KEY needs no cast at all (`BlockId` is an UNBRANDED string alias, so any string is a legal key). Entry-shape discipline there is therefore NOT the testkit author's: C7's belt checks it at resolution on BOTH channels (reopened 2026-08-01). What remains the testkit author's is key GRAMMAR alone — and an ungrammatical key still FAILS there, by C8(c)'s unreferenced check when nothing names it, or by C4/C6's member-shape lane when a raw ref does; only the DIAGNOSIS is weaker there, never the outcome (step-id grammar is walk-only the same way). The OBSERVABLE is unchanged either way: both rungs' findings JOIN in the ONE `{stage: "validate", findings}` result (the standing cross-rung accumulation — the realized ch11-P4 F7 form), so C7's all-sites-accumulated promise holds across the split. Named build surfaces (P1): the walk's legal-root-keys set gains `contextBlocks`; the walk gains a `contextBlocks` build slot (the C2 hand-off: non-string keys filtered, grammar-failing string keys kept); and the admission rung's gate-binding key allowlist (today hard-coded `uses`/`config`) grows `contextBlockRefs`. |
   350	
   351	
   352	## Ratification history (empty at `draft` — blocks are appended by the lifecycle acts)
   353	
   354	```json
   355	{"ratification": {"date": "2026-07-26", "arms": ["none external — the internal five-lens Opus panel (4 full + 1 targeted round + top-level close CLEAN @ 20a3c3d2); the codex arm consciously waived by the owner at GO (2026-07-26)"], "commit": "16a668b08dfe6e9e36076573c226503113ea8a16"}}
   356	```
   357	
   358	```json
   359	{"ratification": {"date": "2026-08-01", "arms": ["agent-invoked codex arm (pin gpt-5.6-sol/high), ch13-P1 gate-1 re-check #3 finding 4 — the arm read the PACKET's bytes and located the defect in C1; no arm reviewed this reopen's own bytes"], "commit": "616f4aa6299d8ece3dd1c20e169c83960540246b"}}
   360	```
   361	
   362	```json
   363	{"ratification": {"date": "2026-08-01", "arms": ["ch13-P1 panel round 7 — five fresh-context Opus lenses on the packet, whose lens-1/lens-2 executed probes located the region and falsified C19's type-foreclosure premise; agent-invoked codex arm gate-1 re-check #4 (pin gpt-5.6-sol/high) on the same packet bytes; a draft-scoped adversarial pass on the DRAFTED rows returned 3 P1 + 4 lesser findings, all folded before this act. No arm reviewed the FINAL reopen bytes: the last defect in them — a false measured claim, `Object.keys(\"abc\")` is [\"0\",\"1\",\"2\"] and not empty — was caught by the human at a NO-GO and corrected before the GO"], "commit": "3c6ed1815efe4266f71f0a6e6ae4a34028f8fcee"}}
   364	```
   365	
   366	```json
   367	{"ratification": {"date": "2026-08-02", "arms": ["agent-invoked codex arm, ch13-P1 gate-1 on the approve-ready bytes (pin gpt-5.6-sol/high, 880s, guards clean) — it re-executed all seven declared premises, the untruncated ripple sweeps, 378 tests and v3:typecheck, and its P1 located THIS defect by probe; no arm reviewed this reopen's own bytes, and the DERIVED identity it introduces is by construction unverifiable until the rebuild exists (its measuring carrier is named in C8(c))"], "commit": "a0cf73b51bee8a65d6c4155d0689ec4f9a68e340"}}
   368	```
   369	
   370	```json
   371	{"ratification": {"date": "2026-08-02", "arms": ["agent-invoked codex arm, ch13-P1 gate-1 re-check on the prior approve-ready bytes (pin gpt-5.6-sol/high, 550s, guards clean) — it re-executed every declared premise, the untruncated ripple sweeps, v3:typecheck, v3:adr-check and the full v3:test (71 files, 1830 tests green), and its P1 measured the surviving class members this act normalizes; no arm reviewed this reopen's own bytes, and the post-normalization identity stays DERIVED with its measuring carrier named in C8(c)"], "commit": "8360510eab21093f13e6cba471c814a9052ba077"}}
   372	```
   373	
   374	## Realized map (empty until chapter close)
   375	
   376	## Supersede record
   377	
   378	```json
   379	{"superseded": {"date": "2026-08-05", "oracle_branch": "ch13-prose-line", "oracle_tip": "bb313036c7e50ad2625f0669f76a90bf317255e3", "plan": "v3/implementation/ch13-rederivation-plan.md"}}
   380	```

exec
/bin/zsh -lc "nl -ba v3/implementation/contracts/ch13-context-block-contract.md | sed -n '315,352p'" in /Users/felho/dev/pairflow
 succeeded in 0ms:
   315	admitted values. Reopen-delta new-decision rows: 1 (the class-width
   316	grain). TWO CORRECTIONS the ratifying side caught before GO, recorded
   317	because both are rule-instances: the draft first attributed the
   318	record predicate to C8(c), which does not state it (an UNVERIFIED
   319	CITATION — the citation-shaped form of an unrun "measured"); and the
   320	empty-array receipt was demanded in the record rather than in
   321	conversation, since a MEASURED label travels with its receipt.
   322	
   323	**Dated update (2026-08-02, the class-width normalization reopen):**
   324	post-ratification reopenings 3 → 4 (closed by re-ratification within
   325	the same act).
   326	
   327	## Contract rows (every normative statement is a C-row)
   328	
   329	| ID | Rule |
   330	|---|---|
   331	| C1 | The template top level gains the OPTIONAL `contextBlocks` key (ch8-C7 additive growth; the root keyset becomes the ch8 five + the ratified optional keys `runtimeContext`, `round`, `activation`, and this — ch12-C1's aggregate note described the pre-ch13 state; no earlier row is modified). camelCase realization of the model's `context_blocks` (the ch11-C13/C16/C37 rename culture — stated so neither side silently forks). Its value is an OPEN-KEY map (the ch8-C13 classification: its KEYS are data — block ids, C2; its VALUES are C3's). A PRESENT value that is not a map — present-null, scalar, list — is a container-precondition finding at path `contextBlocks` (ONE finding, dependent lanes suppressed — the ch8-C21 container rule; the suppressed dependents are the CATALOG-side lanes: C2's key lane, C3's entry lanes, and C8(c)'s reference check — NOT the ref-list member lanes, which presuppose their own containers, and NOT C7, whose per-site findings still fire because no entry can resolve; AT THE REF-LANE GRAIN the resulting shape is exactly the absent-catalog-with-refs shape — the same per-site C7 findings, nothing resolving — while THIS case adds its OWN one container finding beside them, which an absent key cannot produce because no container lane runs on it; stated at both grains, not accidental). An ABSENT key with zero refs issued is legal (every pre-ch13 file). |
   332	| C2 | Block-id KEY LANE and grammar (a NEW namespace — catalog keys and every ref-list member). ONE key lane, the sibling helper shape (the realized `idGrammarError` form rejects non-strings and grammar violations in one lane — a split identity-vs-grammar precedence would be unobservable at build): a catalog key that is not a node-level STRING (number, boolean, null-form — PROBE-CB1/CB2's identity classes; the walk sees them because `mapAsMap: true` preserves key types) OR that fails the grammar below yields ONE walk-side validation finding at the CONTAINING path `contextBlocks` — UNCONDITIONALLY, string and non-string alike (the realized sibling form: the steps/roles/transitions/gates key lanes all report at the bare containing path; the key-segment conditional belongs to the unknown-key/container lane class, not to key lanes — stated so the build copies the right idiom, and the C3 entry-lane paths (`contextBlocks.<id>`) stay DISTINCT from this lane's by construction). Hand-off: the walk FILTERS NON-STRING keys out of the built catalog (a typed Record cannot carry them); a grammar-failing STRING key STAYS in the built catalog (the realized non-cascade culture — the sibling walk keeps grammar-failing string ids), its presence INERT AT THE RESOLUTION LANE ONLY: no shape-passing ref can address it (one shared grammar). It is NOT inert at C8(c) — since the 2026-08-01 reopen no key is excluded from the unreferenced check, so such a key draws that finding too unless a raw ref member names it. N failing keys yield N findings at the one bare path, separated by message (the sibling multi-key behavior). Grammar (string keys and members): nonempty, matching `^[a-z][a-z0-9-]*$`. DECIDED HERE, strict start: letter-start excludes the numeric string forms; the charset bans whitespace and `.` by construction (block ids appear as ch8-C21 path segments); the `-` separator deliberately diverges from the ch11-C6/ch12-C3 underscore token grammars (the model's exhibited ids are kebab — stated so the namespaces do not blur). The model's exhibited ids (`reviewer-severity-ontology`, `no-converged-before-round-2`) are members — and the digit-bearing kebab STRING key is the lane's named SUCCESS member (PROBE-CB1's discriminating case; a digit-rejecting implementation must red — P1-driven). Two-sided edges, stated here as on their owner rows: this lane is suppressed under C1's catalog-container failure; a C2-failed key is NOT excluded from C8(c) (the carve-out was deleted at the 2026-08-01 reopen — the two lanes state two independent facts, and on the direct-construction channel this lane does not run at all). A ref-list member violating the grammar fails the C4/C6 member-shape lane and is EXCLUDED from C7's resolution set (C7 states the same carve-out from its side). |
   333	| C3 | A catalog ENTRY is a FIXED-KEYSET map: `{ body }`, `body` REQUIRED, a nonempty string — authored static text rendered VERBATIM, and template-authored DEFINITION data by construction: no runtime, actor, or run-supplied channel reaches this position (the catalog is admitted-template content; C5 closes the only run-scoped channel near the field family). Outbound, the same bytes travel verbatim into the actor's `packet.json` with no length or content bound — the same exposure class as the shipped `instruction` field; prompt shaping is the `actor-adapter-rendering` Absent, named, not implied. No interpolation syntax exists, and none is reserved: a body containing `{{…}}`-like text is legal literal prose (DECIDED HERE — rejecting would speculatively enforce the computed-bodies Absent; that later chapter owns any syntax and its migration, including the consequence that today's literal `{{…}}` prose would need migration then). Unknown keys in an entry are definition-static findings (the ch8-C13 fail-closed culture; walk-side per C19's split); an entry value that is not a map — present-null, scalar, list — is its container-precondition finding at `contextBlocks.<id>` (suppressing the entry's body lane). Two-sided suppression edges, stated here as on their source rows: this row's entry lanes are suppressed under C1's catalog-container failure and under a C2 key failure on the same entry. |
   334	| C4 | The role/step ref positions: `promptConcernRefs` INSIDE the two template agentConfig positions (`roles.<r>.defaultAgentConfig`, `steps.<s>.agentConfig` — the positions exist per ch8-C15 as grown by ch12-C6) — camelCase of the model's `prompt_concern_refs`; the ch12-C7-DELEGATED additive arrival (ch12-C7 pre-authorizes the refs' value classes arriving with "their consuming chapter (the ch-9 ActorAdapter / L2b ContextAssembly)" — no ch12 reopen; this is the FIRST field of the otherwise format-OPEN agentConfig map that admission types, and ch12-C7's "NO field of it is format-enforced or kernel-interpreted" sentence thereby described the pre-ch13 state — the aggregate-note convention, no ch12 row modified; the GP2-scan exemption on agentConfig stays safe beside this: any non-string-keyed map nested in agentConfig already fails the canonical-JSON value lane as a non-plain object). Value class: a list (possibly empty) of C2-grammar strings. ABSENT states are legal and contribute ZERO refs: an absent `defaultAgentConfig`/`agentConfig` map, or a present map without the key (C17 materializes the normalized form to `[]` — the render never dereferences an absent path). A present value that is not a list — present-null, scalar, map — is a container-precondition finding at the field's path (dependent member lanes suppressed, and C8(c)'s check is SKIPPED for the whole template — the soundness reason: the reference set is a template-wide UNION, and a per-list skip would emit PHANTOM unreferenced findings for entries referenced only from the broken list); a non-string, empty-string, or grammar-violating member is a finding at `…promptConcernRefs[<i>]` (the ch11-C7 list-index path grammar, 0-based). Every OTHER agentConfig field stays exactly as raw and format-open as ch12-C7 states. The tree's one live snake-spelled FIXTURE (`prompt_concern_refs` in the l0c golden-trace file — TWO sites: the authored step key and the `RESOLVE_REVIEW` expected-cascade echo; a TS fixture, not an authored template file) is DISPOSED at P2: BOTH sites move to the typed key WITH a catalog entry, realizing the L0c slot's resolution (this chapter's acceptance evidence); the post-move fixture carries the typed key beside untyped snake siblings at those sites (`skill_refs`; the implementer-side map carries `prompt_profile_refs`) — the CORRECT end state (one typed field, open raw vocabulary), stated so it does not read as an inconsistency; the sweep searches the VALUE (`reviewer-severity-ontology`) too, per R-ABSENCE-CONSUMERS. |
   335	| C5 | A `runOverrides` entry MAY carry `promptConcernRefs` (ch12-C7: the kernel merges and records runOverrides opaquely — that surface is untouched, the merged value flows into `effectiveAgentConfig` as DATA), but it is NEVER a render source: the render reads the admitted template's normalized declarations (C17), never the merged effective config, and the provenance source enum has no run-level member (model-verbatim — the unit's read-path plus the three-member source set). Consequences, all DECLARED observables with a named owner (the P2 packet's test families): (a) run-supplied ref ids are NOT validated against the catalog and never resolve to blocks — and a catalog entry serving ONLY run-supplied refs is rejected by C8(c) as unreferenced (dead by design, the two rows compose); (b) because the ch12-C8 cascade merges top-level keys wholesale (arrays REPLACE), a run override or step override CAN make `effectiveAgentConfig.promptConcernRefs` differ from the rendered set — the packet's `contextBlocks` field IS the rendered truth (communication), `effectiveAgentConfig` is recorded run intent (data); an adapter renders neither, it passes the packet through (ch9). Stated so the two read-paths never silently fork. |
   336	| C6 | The gate-binding keyset gains OPTIONAL `contextBlockRefs` — VIA the named post-close reopen act over ch11-C4, ch11-C30, and the ch11-C41 parenthetical, riding this draft's ratification (C16 is the act's carrier; prepared texts in Context; ch11-C30's realized text otherwise promise-binds the key's absence, with a code witness in its map entry the act retires). camelCase of the model's `context_block_refs`. Value class: identical to C4's (a possibly-empty list of C2-grammar strings; same container/member lanes incl. present-null — a container failure likewise skips C8(c) — paths `steps.<s>.gates.<event>[<i>].contextBlockRefs[<j>]`, the ch11-C7 list-index grammar). Unknown-key fail-closed on the binding is UNCHANGED. The key is legal on ANY binding — declarative, packaged, and process alike (the ch11-C9 three-class registry; communication is evaluator-independent, DECIDED HERE — the restrict-to-declarative alternative was rejected: the model's config instance exhibits declarative only because that is the trace's gate, not a scoping rule). |
   337	| C7 | The resolution lane: `validate_context_refs` runs at ADMISSION-time definition-static checking (`admit_definition`; realization per C19's split), its lookup domain the BUILT catalog Record (no stringify-first lookup). One OBSERVABLE lookup consequence, stated with its vacuous twin: keys and members share ONE grammar, so no shape-passing ref can address a grammar-failing STRING key — that leg is VACUOUS by construction (stated, never testable); the observable case is the FILTERED NON-STRING key, whose refs are unresolved and fire HERE (the typed catalog factually cannot carry the key — two true findings for the one authoring defect, deliberately; the discriminating fixture is PROBE-CB2's class: a bare `true:` entry beside the grammar-valid ref `"true"`): EVERY issued ref — each role/step `promptConcernRefs` member and each gate `contextBlockRefs` member that PASSED its C4/C6 member-shape lane (a shape-failing member is excluded here; a beyond-first duplicate occurrence fails C8(e) and is excluded here — the FIRST occurrence carries resolution) — must RESOLVE TO AN ENTRY (DECIDED HERE at the 2026-08-01 reopen, the ENTRY-BELTED grain): the ref must be an OWN key of the catalog AND the value at that key must BE AN ENTRY AS C3 DEFINES ONE — by REFERENCE, never restated here, so ONE definition of "entry" governs both channels (a fixed-keyset `{ body }` map whose `body` is a nonempty string; an empty body, an extra key, or a non-map value all fail the belt exactly as they fail C3). Key existence alone is not resolution — a key whose value is not a C3 entry leaves its refs UNRESOLVED and fires this lane per site. What the belt makes unnecessary, stated so no reader reintroduces it: NO catalog-container predicate and NO array special case is owed beside it — an array's own grammar-passing `length` (whose value is a NUMBER) and a wrong-valued `{ <id>: 42 }` alike fail the belt — only the NULL/UNDEFINED guard the own-property read itself requires (`Object.hasOwn(null, …)` and `Object.hasOwn(undefined, …)` throw; a PRIMITIVE catalog does not throw — `Object.hasOwn("abc", "length")` is true — and needs no guard because the belt refuses its values anyway). CHANNEL CONSEQUENCE, both directions: on the DIRECT-CONSTRUCTION channel this is the whole of the catalog's shape discipline at resolution time (no walk lane runs there); on the FILE channel the belt runs just the same (the walk's build slot materializes entry values whatever their shape — the non-cascade culture), so a malformed entry NAMED BY A REF draws BOTH C3's entry finding and this lane's per-site finding — two independent, separately-fixable facts, the multi-true-finding pattern this chapter already ratifies at C2's filtered non-string key. The admit/refuse OUTCOME is therefore channel-symmetric for every entry shape; only the finding SET differs, and only by the lanes C19 places in the walk. An unresolved ref is the `unresolved_context_block_ref` DEFINITION ISSUE (the EXISTING ch11-model-sync name; no registry rejection name is touched), carried on the named-lane form: `code: "unresolved_context_block_ref"` present on exactly this lane (the ch11-P2a A9 carrier), path = the REF SITE (`…promptConcernRefs[<i>]` / `…contextBlockRefs[<j>]`), one finding PER unresolved site over the resolution set, ALL sites ACCUMULATED in one result — a projection of the standing ch8-C21 validate-stage culture (the model unit's first-issue return is the semantic floor). Refs issued with NO RESOLVING ENTRY fire this lane per site — an absent catalog, a present-but-empty catalog, and a malformed catalog (C1's single container finding beside these) alike. This row is the `l2b/refs-validated-at-definition-load` invariant's home (disposition `test`). Containment, both legs stated: the validated ref set ⊇ the render's read set (equality reachable on a single-step template; C9 reads the dispatched step's role, its own step, and its authorized gates), and every dispatch site reads a store-admitted template — together these are why C9's render-time miss is integrity drift, not a business state. |
   338	| C8 | The empty/absent/edge matrix: (a) absent catalog + zero refs → legal, zero-impact; (b) catalog present + zero refs → legal; (c) an UNREFERENCED catalog entry is a validation finding at its path (DECIDED HERE, strict start; like (e), a hygiene lane above the model's text — the unit checks ref→catalog only — the ch8-C16 declared==used shape: strict-start is the ONE direction later relaxable additively, while permissive-start could be tightened only as a breaking format change; the shipped C14 entry is referenced by construction). The REFERENCE SET is the RAW authored member strings across all well-formed ref lists — grammar-violating and duplicate members COUNT as references (they evidence intent; their own lanes carry the defect), and the check is SKIPPED entirely when any ref-position container lane fired or the catalog container itself failed (the template-wide grain is a SOUNDNESS rule, not merely anti-cascade: the reference set is a template-wide union, so a narrower skip would emit phantom findings); NO key is excluded from this check (DECIDED HERE at the 2026-08-01 reopen — the former C2-failed-key carve-out is DELETED on both of its sides, here and at C2's edge): the check runs on EVERY channel, and its DOMAIN is the catalog's own ENUMERABLE keys — ALL of them, with NO value condition (DECIDED HERE at the same reopen): a key whose value is not a C3 entry is still a declared-and-unused key and draws this finding, because a value condition would reinstate an unstated exception of exactly the kind deleted above, and its justification fails on the direct-construction channel for the same reason. ENUMERATION GUARD: a cast-forged null/undefined catalog is not enumerated (`Object.keys(null)` throws) and yields nothing of this lane — residual named rather than left implicit: such a catalog carrying ZERO refs ADMITS, and its admitted value is then IDENTICAL to the absent-catalog state of (a) — not merely similar: C17's rebuild normalizes a nullish catalog to `{}` exactly as it normalizes an absent one (reopened 2026-08-02). That identity is DERIVED from C17's rule and is NOT measurable until the rebuild exists; its NAMED MEASURING CARRIER is the normalization acceptance family's EMPTY-DIRECTION member, which proves the admitted form by FULL-VALUE equality — the ABSENT fixture and EVERY non-record fixture (present-null, a scalar, an empty string, an empty array) must yield BYTE-IDENTICAL admitted values — audited by the build-close arm's mandatory sensitivity pass against the built test body. What IS measured, 2026-08-02, is the defect the normalization closes: today's `admitTemplate` admits `contextBlocks: null` and carries the `null` onto the admitted value — `{"ok":true,"contextBlocks":null}` — which this row's own C17 typed surface forbids. A PRIMITIVE catalog enumerates by its own rules, NOT to nothing — MEASURED 2026-08-01: `Object.keys(42)`, `Object.keys(true)` and `Object.keys("")` are empty while `Object.keys("abc")` is `["0", "1", "2"]` — so a nonempty-string forgery draws this lane at each index key and REFUSES, the fail-closed direction, needing no rule of its own. DOMAIN ASYMMETRY, named once here because it is load-bearing: C7 resolves over OWN keys at any enumerability while this lane enumerates own ENUMERABLE keys, so a cast-forged NON-ENUMERABLE own entry can resolve a ref yet is never audited here — a residual bounded to that cast class, accepted rather than hidden. Rationale, recorded because the carve-out had one: the exception did not SAVE the second complaint, it deferred it by one run (fix the key, and the unreferenced finding appears next time), while costing a cross-lane coupling — the unreferenced check had to know whether the key lane had fired, which is unanswerable on the direct-construction channel where that lane does not run. CONSEQUENCE MATRIX, stated so the two channels are read correctly: FILE channel, ungrammatical key, no ref naming it → TWO findings (C2's grammar finding + this one); DIRECT channel, same catalog → ONE finding (this one — C2 is file-channel per C19); EITHER channel, ungrammatical key WITH a matching RAW ref member → NO unreferenced finding, because the reference set is raw member strings and a grammar-violating member still COUNTS as a reference (that member carries its own C4/C6 finding); EITHER channel, a key whose VALUE is not a C3 entry and which nothing references → this lane fires on it like any other (the domain has no value condition), and where a ref DOES name it C7's belt fires instead. The admit/refuse OUTCOME is channel-symmetric throughout; only the finding SET differs, and only by the lanes C19 places in the walk. (d) an EMPTY ref list → legal, renders nothing; (e) a DUPLICATE id within ONE ref list is a validation finding PER repeat occurrence — `[a, a, a]` yields findings at index 1 AND index 2 (the ch11-C40 per-occurrence precedent; the `seen` set is fed UNCONDITIONALLY by raw members under SameValueZero — the realized Set semantics — so a shape-failing duplicate co-fires its shape lane and this lane on the same index, per-defect-class findings), each repeat excluded from C7's resolution set (DECIDED HERE — an admission hygiene lane above the model's text, the ch11-C15 hardening precedent; the DESIGNED repeat is the cross-source one, C9's dedup); (f) `contextBlocks: {}` (present-empty map) and an ABSENT catalog behave identically under refs: legal with zero refs; with refs issued, C7 fires per site (no resolving entry); (g) the fully-valid populated case — catalog + resolving refs — admits CLEAN with zero findings: the family's success negative, P1-driven, its member set including the digit-bearing kebab id (C2's named success member). |
   339	| C9 | The render (`assemble_context_blocks`, model-verbatim with ONE order decision and ONE iteration-domain restatement — the unit loops `authorized_ops` then `gates_for`; this row loops the gates record filtered by C10, membership-equivalent via ch11-C2, the argument carried at C10): source order IS render order — role refs, then step refs, then gate refs; within each source, list order; the gate leg iterates the admitted step's `gates` RECORD in its OWN key order (the surface the refs live on — the invariant's "declaration order within each" read on the gate source itself), each event's pipeline in binding-list order (ch11-C3: authored order IS pipeline order), each binding's `contextBlockRefs` in list order, filtered by the C10 predicate. RECORD KEY ORDER, not "authored declaration order", deliberately: the admitted form is a JS record; integer-like event keys authored in QUOTED form hoist to the front AND re-sort ascending among themselves (PROBE-CB3; an unquoted `2:` is rejected upstream as a non-string key); record enumeration order is the deterministic, substrate-true basis. Two alternatives considered and REJECTED (DECIDED HERE): the `transitions`-record order (a different surface that does not carry the refs; `availableOps` continues to publish ITS order independently) and a canonical SORTED key order (deterministic and hoist-immune, but it would divorce render order from authored reading sequence — the invariant's own gloss is "order is reading sequence"). Construction-order dependency DECLARED: the admitted record's key order is the authored order modulo quoted-integer hoisting because admission REBUILDS the gates record over the authored keys — an admission refactor that changes that construction order is a C13-visible contract change, not a free refactor. Dedup: a ref reached again renders ONCE, first-seen position; `provenance.sources[]` appends EVERY emitter in encounter order — this row is the `l2b/dedup-with-retained-provenance` invariant's home (disposition `test`). Bodies resolve from the admitted catalog ONLY (post-admission a miss cannot exist — C7's two containment legs; a miss at render time is kernel-integrity drift, throw, never a skip — the standing dispatch integrity culture). `HANDLE`'s verdict path is untouched. |
   340	| C10 | The gate-ref predicate: a binding's blocks render iff its transition ∈ available_ops ∩ L1 capability (model-verbatim; the `l2b/authority-scoped-gate-blocks` invariant, disposition `test`; the ch11 authority logic REUSED — no fresh authority logic). Honest per-leg accounting: the MEMBERSHIP leg (∈ available_ops) holds BY ADMISSION on every admitted template — ch11-C2 makes every gates key a transition of its step, so the leg can never exclude anything post-admission; its test carrier is therefore the render's ITERATION-DOMAIN negative (blocks come only from the DISPATCHED step's gates — the golden trace's cross-role leg: the reviewer's rule is absent from the implementer's packet). The L1 NARROWING leg carries the MANDATORY discriminating negative via a directly-constructed `capabilityProfile` — the live type-level channel (narrowing profiles are already test-constructed on the kernel seam today; only FILE authoring of profiles is the deferred Absent): a profile narrowing the dispatched role's current step below its transitions must NOT render that gate's block even though the event ∈ availableOps — the lane that reds an implementation dropping the `∩ capability` term. Under the DEFAULT derivation the intersection is a no-op — which is exactly why the explicit-profile negative is mandatory, never waived (the green-but-blind class). FORWARD SCOPE: the EC layer's reprint replaces `available_ops` with `offerable_ops` — the membership leg's by-admission status is PRE-EC, and re-examining it belongs to the §1.3 EC candidate's chapter. |
   341	| C11 | The packet field: `ContextPacket` gains `contextBlocks` — camelCase of the model's packet-level `context_blocks` (the same rename culture, stated) — ALWAYS present (possibly empty; model-verbatim — the `dispatch_intent` reprint carries the field unconditionally — with the ch12-P2 E1 always-present precedent behind it), an ORDERED list of `{ id, body, provenance: { sources } }`. A source member is `{ source: "role_config" }` \| `{ source: "step_config" }` \| `{ source: "gate_binding", step, event }` — the TAGGED shape and the `source` field are the MODEL UNIT'S OWN form (the unit's members are tagged `{ source }` maps, the gate member additionally carrying `at`; the domain view's bare-token spelling is its compressed notation); the token VALUES are model-verbatim (the realized discriminant-value culture keeps model snake_case tokens: `runner_error`, `kickoff_pending`); DECIDED HERE is only the `at: (step, event)` pair's flattening to sibling `step`/`event` fields and the string-literal quoting (a nested `at: {step, event}` map was the alternative — rejected for canonical-JSON flatness; stated so neither side silently forks). Two bindings on one (step, event) each carrying the same ref yield two IDENTICAL source members (no source-level dedup — model-verbatim append; stated as the decided observable, P2-owned). The list order is the C9 render order and travels VERBATIM through the existing canonical `packet.json` channel (the ch9 H2 surface — object keys canonicalize sorted, array order is semantic and preserved). The always-present field ripples every full-packet deep-equal fixture in the suite — a NAMED family spanning the kernel/testkit/runner/CLI suites, member enumeration P2's (R-ALTITUDE-LINE). |
   342	| C12 | Communication-only boundary, byte-scope precise: the packet FIELD `contextBlocks` (and the catalog behind it) never enters gate evaluation, transition verdicts, round arithmetic, or any kernel DECISION surface; the definition-static lanes (C1–C8) are the stated exception — catalog content decides ADMISSION findings, never a runtime verdict. The canonical deletion experiment: removing a catalog entry WITH its refs in the same edit leaves every verdict, round, and transition outcome identical; the bytes that change are the packet artifact (`packet.json` / the `contextBlocks` field) AND — exactly where the removed refs rode the two agentConfig positions — the commit-time `issued_agent_config` provenance rows (ch12-C10's surface: refs are config DATA and were always recorded there; blocks themselves never enter any committed row). The GATE-source variant is committed-row byte-identical (ch11-C27 records `{uses, verdict, reason, message, evidence_refs}` only). Spawn-side, TWO carriers reach a real process and both are closed: the packet artifact carries the refs as data (this row's packet-byte scope), and the argv-INTERPRETATION path over `effectiveAgentConfig` is closed by ch9-C18 (`*_refs` left uninterpreted — aggregate-noted in Context). This row is the `l2b/communication-only` invariant's home (disposition `review` — the owning packet carries its `REV-*` line against THIS precise scope, never the wholesale form). |
   343	| C13 | Determinism: the render is a PURE function of (instance, template, step) — no clock, no randomness, no store read beyond its inputs; same committed instance state + same admitted template ⇒ byte-identical `contextBlocks` (the `l2b/deterministic-ordered-render` invariant, disposition `test`; carrier: the P2 golden trace plus a dedicated same-inputs double-render equality lane, P2-owned). |
   344	| C14 | The shipped-catalog presence rule: the canonical `v3/templates/local-pair-v0@1.yaml` gains THREE insertions — a root `contextBlocks` catalog carrying the entry id `emit-envelope`, and a NEW `defaultAgentConfig: { promptConcernRefs: [emit-envelope] }` subtree under EACH of the two roles (an untruncated draft-time sweep found the file authoring neither key; this makes the canonical file the first agentConfig-family authoring in an authored template FILE — the l0c TS fixture authors the family in-test today — DECIDED HERE: role-level on BOTH roles, because every actor emits and the block is role-symmetric). The entry's PROSE is packet-time authored (the interim carrier of the emit-envelope knowledge — `v1-prompt-parity-audit.md` §5; its retirement is bound into the DoD of the chapter that takes the §1.3 EC candidate). Ripple, the named families (member enumeration the owning packet's, R-ALTITUDE-LINE): (i) the ch8-P2 equality pin FOLLOWS the file in the same commit (fixture + file move together, the pin test unchanged — the ch11-P4 Y1/Y2 precedent; both pin sides pass through admission, so C17's normalized fields appear symmetrically); (ii) the `effectiveAgentConfig` assertions and full-packet deep-equals — spanning the CLI/journey suites AND the kernel/testkit deep-equal fixtures (C11's family, one sweep) — re-pin, P2; (iii) the COMMITTED-provenance family: `issued_agent_config` assertions re-pin to the new resolved values (the C12-named byte surface — the shared fixture flows through the ch12-C8 cascade into every committed transition); MEMBERSHIP is by `fixtureTemplate`/canonical-file CONSUMPTION, not by directory (the floor and testkit suites, the runner delivery-loop suite, and the root CT files are in; a suite building its own template is out) — P2; (iv) the l0c golden-trace re-pin (a ch12 trace: BOTH fixture sites move per C4's disposition and gain the catalog entry) — P2; (v) consumers of the keys' former ABSENCE are swept by VALUE and by key (R-ABSENCE-CONSUMERS). |
   345	| C15 | Catalog-authoring caveat (the plan §13.3 REQUIRED row; aim, not enforcement): a block body that states a gate's configured VALUE can go stale against that gate, and NOTHING detects it — C7 proves a ref RESOLVES, never that prose matches config; the missing capabilities are the two named L2b Absents (`computed-templated-bodies`, `semantic-parity-check`), owned by a later chapter. The shipped file carries a sibling COMMENT beside the catalog stating exactly this (plan §13.1 item 4 — placed where the risk is CREATED: the author of the NEXT entry). This chapter ships no gate-describing body, so the risk is latent here by construction. |
   346	| C16 | The reopen-act carrier (the ch9-C27/ch12-C26 precedent — the ratification act's own cross-contract duties, normative; one GO, TWO named acts: this draft's ratification + ONE ch11 reopen act over three named sites — DECIDED HERE, the act's composition): the act executes the ch11-C4 + ch11-C30 + C41-parenthetical reopens per the Context's prepared texts and itemized map duties (C4's keyset-parenthetical update included). EXECUTABLE ORDER, four commits (the ch9 act's git-verified precedent): this draft's CONTENT commit first (carrying the final rows AND all SIX draft-round plan edits — the §13.4 order line, the §13.4 P2-row l0c re-pin clause, the §13.5 registry-flip DoD clause, the §13.5(e) item with its fallback, the §13.5(f) integer-key-ban boundary item, and the §1.3 carried-item (3) system-vs-workflow sub-question (the last two the owner's ratification-round additions) — R-ALIGNED-UP; plus the FH-3 future-hardening record (the ratification round's parse-don't-validate question, `future-hardening.md`)) → ch11 reopen commit 1 (the three site edits + status `reopened` + `realized_map` removed) → ch11 reopen commit 2 (the updated map restored per the ITEMIZED duties — C41's entry gaining the file's minimal reopen annotation, the :252/:264 convention; a FOURTH reopen-record prose BLOCK in ch11's Context — the file's own convention, INCLUDING its `Reopen-delta new-decision rows: 0` line; the close-metrics increment recorded as a NEW dated update line "3 → 4" — the existing dated 2026-07-23 line stays untouched, the count is cumulative with dated increments, never a silent edit of a dated line; the new ratification block whose recorded `commit` is reopen commit 1's hash, FULL 40-hex (the file's current block form); status back to `realized`) → the ch13 draft's own ratifying commit LAST, closing the act (its ratification block records the CONTENT commit's hash — template §4). Re-ratification is permanently human (one GO covers the act's whole payload — named bytes). Every commit stays green on the draft-FORM rules; the packet-anchor surface into ch11 is loud-red for the window BY DESIGN, and the zero-reopened gate's THREE gate points (packet approve, chapter close, process flips — README §5.5's canonical inventory; the check-docs wrapper binds the flag on its approve/close modes) all sit OUTSIDE the act, so none may land between the ch11 pair. Plan §13.4's order line carries the act's annotation (aligned at the ch13 draft round). |
   347	| C17 | The admitted-form normalization (the ch11-C38/C39 admission-normalized precedent) — the `type/schema` carriers: on all-or-nothing admission success, the rebuild MATERIALIZES the validated context surfaces into dedicated normalized fields on the admitted value — the catalog as `contextBlocks: Readonly<Record<BlockId, { body: string }>>` in place IFF THE AUTHORED VALUE IS A RECORD — a non-null, non-array object, the predicate DEFINED HERE for this rebuild rule — and `{}` OTHERWISE: absent, present-null, present-undefined, a scalar, an empty string, an empty array, ANY non-record value that reached the rebuild normalizes to `{}` (DECIDED HERE at the 2026-08-02 CLASS-WIDTH reopen, which GENERALIZES the nullish clause it replaces — that instance-width form left the rest of its own class carried, MEASURED 2026-08-02 through the live `admitTemplate`: `null`, `42`, `true`, `""` and `[]` each admit with zero refs and are carried onto the admitted value unchanged, receipts in the reopen record). NO TENSION WITH C7, stated so the two rows are never read against each other: C7's "no catalog-container predicate is owed" governs the RESOLUTION lane, where the entry belt makes such a predicate unnecessary — the predicate defined here serves ONLY this rebuild's normalize-or-carry decision, creates no lane, emits no finding, and moves no template's admit/refuse outcome. Stated at class width deliberately: this row's typed surface is then true WITHOUT exception on every admitted template, and the render — which C9/C17 forbid to revalidate or dig through `unknown` — never meets a non-record. The sibling precedent is the one the nullish form already cited (`activation`, `admit.ts:444`), widened from `??` to the predicate. A NONEMPTY array or string never reaches this rule: A4's domain fires at its index keys and the template is refused before any rebuild.), each role entry's and each step's validated ref list as a SIBLING field `promptConcernRefs: readonly BlockId[]` beside (never inside) the raw agentConfig map (absent map, absent key, or empty list ⇒ `[]`), and each admitted gate binding's `contextBlockRefs: readonly BlockId[]` likewise (⇒ `[]`). TYPE SHAPE per the standing dead-belt precedent (`advancesRound?`): the fields are OPTIONAL (`?`) on the shared raw types — `AdmittedTemplate` is a brand, not a structural type — with the always-present guarantee holding at the VALUE level on every admitted value; required fields would break every hand-built raw fixture and are REJECTED (DECIDED HERE — the optionality grain). The AUTHORED keysets do NOT grow: `ROLES_ENTRY_KEYS` stays `{defaultActor, defaultAgentConfig}` and the step keyset is untouched — the normalized fields are admission-PRODUCED, never authorable (an authored `promptConcernRefs` at the roles-entry or step level is an unknown-key finding, unchanged; the authoring position is C4's, inside agentConfig). THREE named consequences, all new P1 work: (1) admission REBUILDS `roles` (today only `steps` is rebuilt); (2) the role-side normalized list is the first normalization whose SOURCE is nested (inside `defaultAgentConfig`) rather than in place; (3) the admitted gate binding is constructed FRESH with exactly `{uses, config}` today — the rebuild gains the `contextBlockRefs` field, or every gate-sourced block would silently vanish on a fully-admitted template. The RAW agentConfig maps keep the authored key as cascade-read DATA (C5 reads THAT); at the two template positions the same key is admission-VALIDATED (C4) — "opaque" describes only the runOverrides position; the RENDER reads ONLY the normalized fields (no `unknown`-digging, no re-validation at dispatch — the ch11-C22 class); the definition-static lanes read the source form and the built template (C19), never these fields (they do not exist yet at checking time). The `l2b/single-body-source` invariant's `type/schema` disposition is carried HERE: body text has exactly one typed home (the catalog's `{ body }`), and every ref position's type is id-only. RIPPLE, two named families (owner P1): the admitted-form shape-delta lane (the "what admission adds" deep-equal gains the new materialized fields beside `activation`/`runtimeContext`/`advancesRound`), and the THREE `l2b/*` domain-registry rows flip `pending` → realized WITH THEIR REALIZING PACKETS — the catalog and `ContextBlockRef` rows at P1 (their types land here; `BlockId` and the catalog record type are the realized-form names), the `ContextBlock` row at P2 (the packet-rendered value) — the registry test asserts key sets, not dispositions, so the flip is a NAMED duty precisely because no gate reds on its omission (the C16 token-blind-lint lesson applied to our own manifest). |
   348	| C18 | The CLI surface: NO new verbs and NO new flags — the chapter's definition-static laneS (C19's inventory) ride the existing channels unchanged: dev `validate` surfaces them through the ch8-C31 `{stage, findings}` machine shape (stage `validate`; C7's lane carries its `code` end-to-end into the CLI doc — the one cross-boundary hop, DRIVEN by a P1 lane asserting the `code` field's VALUE (equality with `unresolved_context_block_ref`, never presence alone) in the CLI JSON, plus the exclusivity negative (no other CH13 lane carries a `code`; ch11's A9 named lanes keep theirs — the doc-level assert binds a ch13-only fixture)), and the store/create paths through their standing typed-error docs (the ch11-C28 analog, stated so P1 neither mints a surface nor re-derives its absence). |
   349	| C19 | The lane inventory + realization split (the ch11-C40 REALIZATION-SPLIT precedent, applied at chapter grain): the chapter's definition-static lanes are exactly — C1's container lane, C2's key lane, C3's three entry lanes (keyset / body / container), C4's and C6's container + member-shape lanes, C7's resolution lane (the ONE code-carrying lane), C8(c)'s unreferenced-entry lane, and C8(e)'s duplicate lane. SPLIT: the CATALOG-SIDE lanes (C1 container, C2 key, C3's three) live in the WALK — the Map-form source traversal where non-string keys are visible and the four sibling key lanes already live; the REF-SIDE and VALUE-LEVEL lanes (C4/C6 containers + member shapes, C7 resolution, C8(c), C8(e)) live in the ADMISSION RUNG over the built template. The ref-side containers/members sit rung-side DELIBERATELY, diverging from C40's source-form classification for its typed input (stated, two reasons): the refs live inside the format-OPEN agentConfig, where member primitiveness is NOT type-foreclosed on the direct-construction channel — rung placement covers that channel too; and C8(c)'s skip-on-container-failure signal becomes INTRA-RUNG (a walk→rung findings signal does not exist in the pipeline interface and is not minted). CHANNEL SCOPE, stated symmetrically: the walk lanes are FILE-CHANNEL. On the direct-construction channel the ratified catalog type forecloses NOTHING that a cast does not reach — MEASURED 2026-08-01: `tsc --strict` rejects an array, a wrong-valued entry object, and a class instance alike against C17's `Readonly<Record<BlockId, { body: string }>>`, so no forgery class is type-endorsed and none is type-prevented — while an ungrammatical KEY needs no cast at all (`BlockId` is an UNBRANDED string alias, so any string is a legal key). Entry-shape discipline there is therefore NOT the testkit author's: C7's belt checks it at resolution on BOTH channels (reopened 2026-08-01). What remains the testkit author's is key GRAMMAR alone — and an ungrammatical key still FAILS there, by C8(c)'s unreferenced check when nothing names it, or by C4/C6's member-shape lane when a raw ref does; only the DIAGNOSIS is weaker there, never the outcome (step-id grammar is walk-only the same way). The OBSERVABLE is unchanged either way: both rungs' findings JOIN in the ONE `{stage: "validate", findings}` result (the standing cross-rung accumulation — the realized ch11-P4 F7 form), so C7's all-sites-accumulated promise holds across the split. Named build surfaces (P1): the walk's legal-root-keys set gains `contextBlocks`; the walk gains a `contextBlocks` build slot (the C2 hand-off: non-string keys filtered, grammar-failing string keys kept); and the admission rung's gate-binding key allowlist (today hard-coded `uses`/`config`) grows `contextBlockRefs`. |
   350	
   351	
   352	## Ratification history (empty at `draft` — blocks are appended by the lifecycle acts)

exec
/bin/zsh -lc 'pnpm v3:packet-lint' in /Users/felho/dev/pairflow
 succeeded in 9587ms:

> @pairflow/cli@0.2.0 v3:packet-lint /Users/felho/dev/pairflow
> python3 ./tools/v3-plan/check_packet.py --selftest && python3 ./tools/v3-plan/check_packet.py

selftest: 140 red dims exercised, green fixture + reopen choreography + fenced-noise greens pass, 0 failure(s)
packet-lint: 26 v2 packet(s) linted, 16 pre-v2 grandfathered, 6 draft(s) linted (0 reopened, 1 superseded: ch13-context-block-contract.md), 0 error(s)

exec
/bin/zsh -lc "rg -n '"'^##|''^###|'"\\*\\*D4|\\*\\*D10|\\*\\*D11|D4|D10|D11' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md" in /Users/felho/dev/pairflow
 succeeded in 0ms:
5:derivation, D9.3's reading) · amended 2026-08-07 (D10 — the entry-belted
6:membership construct, admitted under D7) · amended 2026-08-08 (D10's
7:no-reference-ring rule; D11 — the typed-subset `fields` widening on
11:## Context
79:## Decision
81:### D1 — the direction
114:### D2 — scope
125:### D3 — the engine owes a SECOND capability, named
136:### D4 — the canonical home: ONE authority, pointers everywhere else
165:### D5 — the parity gate
204:### D6 — retrofit policy
212:### D7 — the format-growth rule
225:**Admitted under this rule so far**: one construct, D10 (2026-08-07);
226:one attribute widening at a new grain, D11 (2026-08-08 — not a new
229:### D8 — the two open constructs — RULED: BOTH ACCEPTED (user, 2026-08-05)
258:### D9 — the falsifiability criterion
284:### D10 — the ENTRY-BELTED membership construct (amendment, user-ratified 2026-08-07)
373:### D11 — the TYPED-SUBSET `fields` widening on `map.plain` (amendment, user-ratified 2026-08-08)
398:the two-position reading D10 ratified. D9's tripwire 1 was examined at
426:## Alternatives Considered
442:- **Put the declaration in the contract markdown (D4's rejected pull).**
451:## IC-N Screen (mandatory)
462:## Consequences
487:## Verification
491:- **The tag-closure check (D4)**: every declaration tag cited by a
503:## Related
510:the ratified decision record) · ADR-011 (the definition module, D4's

exec
/bin/zsh -lc "sed -n '125,205p' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md
sed -n '275,430p' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md
sed -n '480,505p' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md" in /Users/felho/dev/pairflow
 succeeded in 0ms:
### D3 — the engine owes a SECOND capability, named

The audit's R4 is not a leftover lane: admission both VALIDATES and
PRODUCES the admitted form (`advancesRound` expanded per transition,
the effective gate config materialized, ch13-C17's rebuild). **A
declaration says what is legal; it does not compute a value.** The
build therefore carries a NORMALIZER beside the validator, driven by
declared hooks, and this ADR names it so that "schema" is never read as
covering it. Plain `default:` materialization IS declarable and stays
in the schema; only derivation is the normalizer's.

### D4 — the canonical home: ONE authority, pointers everywhere else

**RECOMMENDED: the declaration lives in the CODE tree as a data-only
module under the definition module (ADR-011) — `v3/src/definition/
schema/templateFormat.ts`, a frozen declaration object with no
executable logic — and the contract carries DECISIONS plus declaration
POINTERS, restating no attribute.** The ratification act names the
schema file's bytes exactly as the contract-draft equality lock names
the C-rows, so the lock is a MECHANISM, not a location.

The weighing, recorded because it is the load-bearing choice here:

- *Ratification-lock pull* — put the declaration in the contract
  markdown. It inherits the ratified-bytes lock for free. Rejected as
  the home: the engine would then parse ratified prose to get its
  rules, which is the exact indexing-prose-by-meaning failure this
  whole line exists to end.
- *Code-consumption pull* — put it in the code tree. The engine
  imports data; a diff is reviewable; the declaration is type-checked.
  Its weakness is that a code file can be edited without a
  ratification act — and that weakness is closed by extending the act
  to the file's bytes, which costs one hash line per act.
- **Two authorities is the outcome neither pull may produce.** The
  contract states no structural attribute; it states decisions and
  cites tags. A rule visible in both places is a defect, and the
  cheapest tripwire is the tag-closure check the audit already ran
  twice by hand (every tag defined is cited, every tag cited is
  defined).

### D5 — the parity gate

**Before the engine replaces any implemented lane, the EXISTING
fixture corpus is replayed against it and must produce identical
verdicts, identical finding PATHS and identical MESSAGES — or an
approved delta list, ratified before the switch, never after.**

**AMENDED 2026-08-06 — the corpus is derived from the CALLERS of the
swapped entry point, never from a file list.** The corpus as originally
named:

The corpus, measured 2026-08-05 by `grep -cE '^\s*(it|test)\('`: **362
cases** — `validate.test.ts` 124 · `load.test.ts` 76 · `admit.test.ts`
59 · `process.test.ts` 59 · `fileDefinitionStore.test.ts` 19 ·
`threshold.test.ts` 13 · `previousReviewerVerdict.test.ts` 12.

Those seven reproduce exactly, and they were the WRONG SET: measured at
the build, `admitTemplate` — the direct-construction channel's entry — is
called from the testkit, kernel, lifecycle and trace suites as well, and
**two thirds of the cases the switch actually affected lived outside the
named files**. A file list is a snapshot of who called the entry point on
the day it was written; the obligation is to replay who calls it NOW.

So the standing rule: enumerate the CALLERS of the entry point being
replaced, and replay every case that reaches them. Where that is the whole
suite, it is the whole suite — the P3 build replayed 1830 executed cases
for a corpus named as 362 source-literal ones, and the difference is
where its delta list came from.

This is a DERIVED claim about behaviour that does not exist yet; its
NAMED MEASURER is the build's parity gate itself. Two delta classes are
pre-named by the audit so they arrive as decisions rather than
surprises: **path grain** (duplicate terminal ids report at `terminal`,
not `terminal[i]`; every key-class lane reports at its containing map —
an engine written naively silently moves these addresses) and
**message wording** (nine measured messages embed rule-specific
rationale; either the declaration carries a literal `message:` or the
delta list authorizes the engine's own wording).

### D6 — retrofit policy

   **How this tripwire was defeated once, recorded so it is not defeated
   the same way twice** (design review F3): the P3 build kept a rule in
   code and filed it under an EXISTING family id whose definition did not
   cover it. The family count stayed at seven, the tripwire read green,
   and a new kind of remainder had appeared unseen. Renaming is not a
   ratified amendment. The tripwire fires on a new KIND of residual, not
   only on a new id — and a rule kept in code for a reason no existing
   family states IS a new kind, whatever it is called.

### D10 — the ENTRY-BELTED membership construct (amendment, user-ratified 2026-08-07)

**ADMITTED**: a membership construct that measures against the keys of an
open map whose VALUE IS A VALID ENTRY — "resolve against valid-entry
keys" — rather than against key existence. Spelled `validKeysOf` as a
selector root beside `keysOf`.

**Why it was needed, and how it was found.** Not by inspection. The
2026-08-07 dress-rehearsal round put an outside author in front of this
substrate for the first time and asked them to declare the ch13
context-block surface. They reached for the only available form,
`memberOf` over `keysOf`, and it loaded — then treated a reference to a
key whose entry was `{}` as RESOLVED. The superseded ch13-C7 says the
opposite in as many words: *"Key existence alone is not resolution — a
key whose value is not a C3 entry leaves its refs UNRESOLVED and fires
this lane per site."* Ten rounds of examining this substrate could not
have produced that, because nobody had tried to SAY it.

**ITS SEMANTICS ON A BROKEN OPERAND — part of the construct, not a
separate feature.** When the catalog operand is wrong-kind, absent, or
otherwise unresolvable, NO membership query against it can succeed. The
construct therefore answers with the EMPTY SET, never with "unreliable":
every referencing site gets its own per-site unresolved finding, and the
container's failure NEVER suppresses them.

This is the one place the ratified corpus already said so — the
superseded ch13-C1's *"NOT C7, whose per-site findings still fire because
no entry can resolve"* — turned from an EXCEPTION a contract had to spell
out into the NATURE of a construct. An exception that must be restated at
every site is the drift this ADR exists to end; a nature is stated once
and cannot be forgotten.

**DERIVATION DUTY, executed rather than assumed.** The whole ratified
corpus was swept for other rows carrying a suppression exemption — every
C-row in every contract, matched on container-precondition language
against exemption language. **Exactly one exists: ch13-C1's exemption of
C7.** ch13-C2 mentions suppression as a lane that IS suppressed, and
ch13-C6's match is about registry classes, not suppression. No ch8, ch11
or ch12 row carries one. So this construct absorbs the corpus's only
suppression exemption, and creates no general exception mechanism.

**THE ADMISSION-TEST READING, recorded because it is the close call.**
The audit's D7 test admits a construct on ≥2 INDEPENDENT ratified rows.
This construct is carried by ONE row — the superseded ch13-C7 — which
governs TWO ref positions, C4's `promptConcernRefs` and C6's
`contextBlockRefs`. **The user ratified the two-position reading.** The
recorded alternative: refusing it would not have avoided an amendment,
because the rule would then have kept a prose lane and grown the
residual family — a residual-family amendment instead of a vocabulary
one. Between two amendments, keeping a STRUCTURAL rule DECLARATIVE wins.
D9's tripwire 1 was examined at this ruling and not skirted: it fires on
admitting a single-use construct to make one row fit, and the judgement
here is that two independent ref positions under one row is not the
single-use case that tripwire names.

**ONE DEFINITION OF "ENTRY", by reference.** The construct does NOT take
a separately named entry shape. Validity is the catalog's own declared
`entry:` node, measured by whether that entry's evaluation produced any
finding. A separately named shape would be a SECOND definition of
"entry" — precisely what ch13-C7 forbade when it defined the belt "by
REFERENCE, never restated here, so ONE definition of 'entry' governs both
channels", and precisely the drift class the P3 aftermath spent five
rounds eliminating. The load-time protection the amendment asked for
lands on the only name that exists: a mistyped OPERAND PATH fails the
load, closure-checked through the one resolver like every other
reference.

**The live declaration does not adopt it.** Its first user is P4's
contract; the expected live behavioral delta is zero.

**NO REFERENCE RING (amended 2026-08-08, user-ratified).** A belt's
operands may not form a cycle — catalog A's entries belting on B while
B's entries belt on A, directly or transitively. Such a declaration is
REFUSED AT LOAD, once per ring, naming every belt in it.

The reason is that the question has no answer rather than a hard one:
neither belt can say whether its own entries are valid until the other
already has, so what the walk reports depends on which catalog was
declared first. Round 11 measured exactly that — the same document, the
same rules, one finding in one order and two in the other. Refusing at
load is the sibling of the value-class ring guard and the same shape: a
declaration that cannot be decided is refused before any document meets
it, with no runtime machinery, and the deferred drain's ordering becomes
unreachable by construction rather than by care.

A future surface with a GENUINE mutual-belt need amends this section with
itself as the named user — WATCH-first, never a loosening in advance of
one.

### D11 — the TYPED-SUBSET `fields` widening on `map.plain` (amendment, user-ratified 2026-08-08)

**ADMITTED**: the `fields` attribute — until this act the fixed map's —
is legal on `map.plain`, with OPEN-KEYSET semantics that are the point:
the listed fields are typed and validated WHEN PRESENT; every other key
stays legal, uninterpreted open data; NO unknown-key lane exists. A
plain map remains format-open — the widening types a SUBSET, it closes
nothing.

**Why it was needed, and how it was found.** Not by inspection — by the
P4 authoring probe (executed 2026-08-08, both channels). The superseded
ch13-C4 puts `promptConcernRefs` INSIDE the two format-open agentConfig
positions, and the vocabulary had no way to say it: a ghost ref, a
numeric member and a grammar-violating member inside the plain map all
passed with ZERO findings. The same probe measured the sneak-in: a
runtime `fields` attribute forced onto a `map.plain` node loaded and
was silently inert — the loads-cleanly-validates-nothing class the
design review named as this substrate's root risk.

**THE ADMISSION-TEST READING, recorded because it is the close call.**
This is an existing attribute at a NEW GRAIN — the D8 pattern (the
selector-root and `code` widenings), not a new construct: `fields` on
`map.fixed` carries the template's whole fixed-keyset row population.
The widening's carrier is the superseded ch13-C4's TWO positions
(`roles.*.defaultAgentConfig`, `steps.*.agentConfig`) under one row —
the two-position reading D10 ratified. D9's tripwire 1 was examined at
this ruling and not skirted: it fires on a single-use CONSTRUCT
admitted to make one row fit; this ruling admits no construct, and the
two-position reading has a standing ratified precedent.

**SEMANTICS, exact.** A typed field is validated when its key is
PRESENT (own-property); an absent field is legal and produces nothing —
absence normalization is the ADMITTED form's business (R4, the
normalizer), never this lane's. Validation transforms NOTHING: a plain
map's value is authored data and passes through unchanged. The
container lanes are unchanged and still gate: a non-plain or
non-canonical map suppresses the typed-field lanes under the implicit
container rule. There is no `presence`, no `default` and no `channel`
on a plain-map field — see the applicability rule below.

**ATTRIBUTE APPLICABILITY IS LOUD at the new grain.** An attribute the
engine does not read on a plain-map field is REFUSED at declaration
load, never accepted-and-ignored: `presence` (a plain map has no
missing-key lane), `default` (nothing materializes into authored open
data) and `channel` (read only on a field of a `map.fixed`) — the
latter two by the standing load guards, the first by this amendment.
This kills the measured silently-inert class for the widened grain;
grains this act does not touch keep their standing guards and nothing
else, so the rule's scope is exactly the widening's.

**The live declaration does not adopt it in this act.** Its first user
is P4's contract; the expected live behavioral delta is zero.

## Alternatives Considered

- **Keep ratified prose plus a hand-written validator (the status
  quo).** Rejected on measurement, not taste: the drift floor above is
  what this shape produces, and its second sweep did not shrink.
  lands on whoever migrates a surface. None of the benefit is realized
  at this ADR — it is realized at P4/P5.
- **Neutral.** Today's validator keeps working unchanged under D6; this
  ADR authorizes a direction and a scope, not a rewrite. The issue-code
  namespace, the finding forms and the CLI machine shape are unchanged
  by design — they are inputs to the parity gate, not outputs of it.

## Verification

- **The parity gate (D5)** is the primary proof: 362 existing cases
  replayed, verdict/path/message identical or an approved delta list.
- **The tag-closure check (D4)**: every declaration tag cited by a
  contract row exists in the schema file and vice versa — run twice by
  hand during the audit, both difference lists empty at 51/51.
- **The ≥2-row admission test (D7)** is recorded per construct in the
  audit's §2.2, each cell enumerating its rows; a construct that cannot
  produce its second row is refused.
- **The falsifiability tripwires (D9)** are checked at the P4 contract
  authoring and at every migration act; firing one opens a scope
  review, not a work-around.
- The audit itself is the standing basis document, pinned by hash in
  Context.

## Related

`v3/implementation/schema-expressiveness-audit.md` (the enumeration

exec
/bin/zsh -lc "rg -n 'measur|receipt|unrun|structural|restat|channel|symmetr|claim|close|CARRIED' v3/implementation/ch13-rederivation-arm/p4/review-record.md | tail -180" in /Users/felho/dev/pairflow
 succeeded in 0ms:
47:| Probe | Script | What it measured |
49:| PROBE-P4-1 | `p4-flag1-probe.ts.txt` (P1_*) | the D10 belt at a fixed position: resolving ref clean; `{}` entry → entry finding + coded per-site finding; wrong-kind catalog → container finding AND per-site findings (no suppression); absent catalog → per-site; duplicates per occurrence at index; channels byte-identical |
52:| PROBE-P4-4 | `p4-live-declaration-probes.ts.txt` | the LIVE declaration at the ratifying basis, both channels: the fail-closed class at width (incl. present-null and the boolean members), refs beside refused/absent catalogs, the empty catalog and the EMPTY AUTHORED list, all three positions accumulating, the dup/shape receipts, the boolean key, open siblings, bad-key-with-resolving-sibling, the channel-gated capabilityProfile, the CB3 key-order re-execution (`["1","2","10","ZZZ","AAA"]`), and the compound clean zero. NOTE for auditors: the output's `L_empty_catalog_ref` line reports `agree:false` from the probe's own asymmetric inputs and is superseded by the `_FILE` twin on the next line; the only DESIGNED asymmetry in the file is `L_capability_profile`. |
56:carry them as DECIDED-HERE (v2) markers with these receipts.
66:  guards clean, 415s): **2 IN-SCOPE · 0 CARRIED · 0 UNRUN**, all five
68:  widening), F2 the direct-channel raw-Map slip — both build omissions,
73:  guards clean, 260s): **0 IN-SCOPE · 0 CARRIED · 0 UNRUN**, both fold
76:  channel agreement, file-channel anchor/alias), all four gates PASS
77:  (2063/2063). **The frame closes at round 2 of 3 — the third round is
86:  guards clean, 280s): **2 IN-SCOPE · 0 CARRIED · 0 UNRUN**, all four
92:- Arm round 2 (re-check; guards clean, 180s): **0 IN-SCOPE · 0 CARRIED
96:  unchanged. **The frame closes at round 2 of 3.**
105:| Flag #1 | 1 ruling | vocabulary gap (measured by probe) | 0 | 1 (user) | 1 (flag) |
107:| D11 arm r2 | 0 findings — frame closes | — | 0 | 0 | 0 |
109:| Schema-lock arm r2 | 0 findings — frame closes | — | 0 | 0 | 0 |
111:| Panel r1 (full, 6 agents) | ~50 folded | 2 P1 (kept-claim defect; inheritance loss) · inheritance/receipt/plan classes | 0 | 0 | 0 |
113:| Panel r3 (targeted, 4 agents) | ~20 folded | 0 P1 · 5 P2 (clause scope ×2, ADR-D4 propagation, citation target, receipt gap) · rest hygiene | 0 | 0 | 0 |
123:2. **Flag #1** (1 probe + 1 ruling): **EXACT** — the probe measured the
127:   user exactly as the bet reserved it, and closed 0-P1.
131:   (the cap absorbed it: closed at 2 of 3). Lesson for the priors:
140:   its cap: 2 findings → fold → 0/0/0, closed at round 2 of 3.
155:this file (`never-copy-sweep-out.txt`); the close re-runs it on the
165:  worked example (round 10 measured this; the P4 probes did the same
186:binds. The round folds (§10–§12) restored or closed every item the
191:| C1 | carried + schema-covered, MARKED divergence | v2 C1; `[d-ctxblocks]` (map.open + default) in `[d-root]`.fields. Fail-closed replaces normalize-non-record — marked DECIDED HERE (v2), receipted (PROBE-P4-4 class-width incl. present-null). Non-suppression of C7 = D10's broken-operand nature. |
197:| C7 | carried + schema-covered, MARKED divergence | v2 C7; the belt became D10's `validKeysOf` with the code on `[vc-blockidlist]`. Per-occurrence resolution marked + receipted (DUP_GHOST, PROBE-P4-4). |
198:| C8 | compound | (a)(b)(d)(f) structural; (c) → v2 C9 in full; (e) → unique rule + v2 C8; (g) → v2 C8's compound clean negative (restored at the fold, receipted PROBE-P4-4); enumeration guard + primitive rule dropped-vacuous under fail-closed; raw-feed co-fire superseded — marked + receipted (DUP_BAD_SHAPE). Domain asymmetry: CLOSED by the substrate (one enumerable-own-key domain), stated in v2 C9 post-fold. |
199:| C9 | carried | v2 C10 every leg incl. the hoist+ascending-re-sort basis (restored at the fold) and the DERIVED re-mark with named measurer. |
207:| C17 | compound; two lost legs restored at fold | v2 C13 + default: placement (siblings at fixed-keyset levels), TS-optionality grain, carry-list duty (louder than the original), registry flips + type witnesses, the two build traps; non-record normalization superseded (marked, receipted); single-body-source home restored. |
209:| C19 | carried (inventory) + dropped-why (split retired), MARKED | v2 C19: tag-set inventory with the shared nodes as named exclusions; channel scope an engine property; capabilityProfile's declared asymmetry named. |
211:| Reopen 2 (direct-channel region) | carried + schema-covered | The belt IS D10; key-exclusion deletion + unconditional domain in v2 C9; the C19 premise dropped-vacuous. |
212:| Reopen 3 (C17 nullish) | dropped-why: superseded, marked + receipted | v2 C1; absent-half survives as the declared default. |
213:| Reopen 4 (C17 class width) | dropped-why: superseded, marked + receipted | Same clause; the carrier obligation vacuous when the only non-record outcome is refusal. |
217:- **C1 fail-closed** replaces the predecessor's normalize-any-
220:  under an engine that validates the direct-construction channel: the
222:- **C2 channel-independent key lane**: the predecessor placed the key
226:  channel authoring defect now gets the same diagnosis a file author
241:round 2 (the channel narrowing is observably identical to the
242:predecessor's behaviour — its engine ran no direct-channel keyset lane
256:marked and receipted.
263:keyset-claim self-contradiction; the completeness pass's registry
270:and receipts); C19's inventory re-parameterized both directions;
272:decisions and scoped its keyset claim (the P1 fix); C9's stand-down
273:generalized to reachability and the closed domain-asymmetry stated;
280:narrowed no-restatement claim and the completed model-note
282:collectTags claims and `n-effective-config` gained the C13 row
295:escalation rule (claim-structure changes + a manifest-class change:
301:17659831. Yield: 1 P1 (the C13 channel question — resolved by carrying
303:positions channel-gated to direct at P5, the `advancesRound` pattern's
306:arithmetic; two on the C13 channel seam; the completeness re-check
318:grain gloss pointered; C8 scopes its clean receipt gate-free; C9 names
319:the gate chain's enclosures; C10's re-execution claim gains its live
320:receipt (the CB3 case joined PROBE-P4-4); C12 carries the rename
326:The probe suite grew the round's receipt gaps (refused/absent catalog
327:WITH refs both channels, the class members routed through both channels, the
350:positions with the binding named channel-both AT the claim, and the
353:exclusion, the authored-position scoping of the symmetry sentence, the
354:two forward-scoped asymmetry arrivals, and the declaration growth in
357:accept gains its receipt; the Context gains the Read-path answer.
359:measurement (the operand itself, no longer the transitions analog),
361:instrument note now discloses the finding filter. The record: the
369:CARRIED TO THE FLAGS ROUND, not folded: ADR-019 D4's original absolute
370:("the contract states no structural attribute; a rule visible in both

exec
/bin/zsh -lc "sed -n '286,395p' v3/implementation/ch13-rederivation-arm/p4/review-record.md" in /Users/felho/dev/pairflow
 succeeded in 0ms:
sweep was corrected and re-run to clean (§7a).

Dispositions: folded ~40 · declined 1 (lens 2 F7 — the code literal
in both authorities: defensible via C18's CLI hop, D2 scoping; a
rename would be its own ratified act) · routed 1 (the ch11/plan
pointer maintenance → boundary review, named in Context) · deferred
0 · considered_not_finding per lens reports (in their outputs, this
directory's transcripts are not committed — the reports' substance is
this section). Round 2 (the post-fold re-run) is FULL by the
escalation rule (claim-structure changes + a manifest-class change:
the fourth DECIDED-HERE marker).

## 11. Panel round 2 (2026-08-08) — full six-agent re-run, and its fold

Basis: contract c01db023bcd0240c, declaration a355ada64c35ed7c @
17659831. Yield: 1 P1 (the C13 channel question — resolved by carrying
the producer-monopoly precedent's declaration half: normalized
positions channel-gated to direct at P5, the `advancesRound` pattern's
own stated reason) · ~13 P2 · ~20 P3/watchpoint. ZERO STOP-class.
Notable: three lenses independently converged on C19's exclusion
arithmetic; two on the C13 channel seam; the completeness re-check
verified every round-1 restoration ON THE BYTES and caught the fold's
own transit loss (the evacuated rationales — written into the record
at round 2 and MOVED under §9 at round 3, which is what makes the
contract's six citations true).

The round-2 fold: C13 gains the CHANNEL GRAIN and FIELD SPELLINGS
clauses and the carry∪into gloss fix; C4 homes the authored EMPTY
list, the mode-tagged member's forward scope, and the aggregate-note
retirement (with C1's chain-retirement clause); C1's class-width gloss
re-worded to the declared lane's own boundary (the degenerate-object
accept named); C2/C1/C9 lose their predecessor-rule descriptions; C7's
grain gloss pointered; C8 scopes its clean receipt gate-free; C9 names
the gate chain's enclosures; C10's re-execution claim gains its live
receipt (the CB3 case joined PROBE-P4-4); C12 carries the rename
relation's whole normative content; C14/C15 gain the last two
invariant homes (C14 with the REV byte-scope binding and its ch12-C10/
ch11-C27/ch9-C18 row ids); C16's carrier pointer corrected to §2-under-
§5 and its criterion scoped against C12's tree-wide ripple; C19
re-parameterized over VALIDATION nodes with three named exclusions.
The probe suite grew the round's receipt gaps (refused/absent catalog
WITH refs both channels, the class members routed through both channels, the
empty authored list, open siblings, bad-key-with-resolving-sibling,
the CB3 re-execution); the sweep script is committed beside its
output; the templateFormat header and the domain comment aligned; the
plan gained §13.5(g) (the pointer-maintenance carrier), the live P5
packet's mode+prediction row, and the Shipped line's stamp.

Dispositions: folded ~30 · declined 0 · routed 0 · the rest
considered/cleared in the lens outputs. The panel bet (2 rounds, the
3rd through the user) is now at its boundary — the 3rd (targeted)
round after this content fold opens through the user, per the ratified
STOP-1 work plan.

## 12. Panel round 3 (2026-08-08, targeted — opened by the user at the bet boundary) and its fold

Basis: contract ddc5820089b58a1b, declaration 9368e5253d1163fa @
b5537ce8. Four agents: lenses 1/3 (delta-scoped), the mandatory lens-4
reconciliation, the completeness spot-check. Yield: 0 P1 · 5 P2 · ~20
P3/watchpoint, zero STOP-class. The completeness spot-check's verdict:
the inheritance guarantee holds, rationales included; the residue was
record hygiene, not decision loss.

The fold: C13's CHANNEL GRAIN qualified to the two admission-produced
positions with the binding named channel-both AT the claim, and the
producer-monopoly's RECOMPUTE half carried explicitly (two lenses
converged with executed evidence); C19 gains its fourth named
exclusion, the authored-position scoping of the symmetry sentence, the
two forward-scoped asymmetry arrivals, and the declaration growth in
its P5 list; C12's key enumeration gains its own field name; C16 tags
the full-packet member as C12's tree-wide ripple; C1's degenerate
accept gains its receipt; the Context gains the Read-path answer.
PROBE-P4-4 grew the degenerate cases, the gates-record key-order
measurement (the operand itself, no longer the transitions analog),
the unfiltered-retention case and the unfiltered true zeros, and its
instrument note now discloses the finding filter. The record: the
rationales moved under §9 (the six contract citations now point true),
§9's heading/basis repaired and its table refreshed for the round-2/3
rows, §6 gained the panel rounds, bets 1 and 3 scored. The plan:
§13.5(g)'s site count corrected to six (two realized_map entries carry
the pointer, the third mentions the act without it), and §13.4's row
basis marker reworded to "the surface this act ratifies".

CARRIED TO THE FLAGS ROUND, not folded: ADR-019 D4's original absolute
("the contract states no structural attribute; a rule visible in both
places is a defect") vs the refined gloss/normative form now on the
contract and the declaration — a D-section edit is a user-ratified
amendment, so the prepared one-clause D4 clarification rides the
ratification GO, never a silent edit (lens-4 R3 F1).

exec
/bin/zsh -lc "sed -n '2715,2998p' v3/implementation/plan.md" in /Users/felho/dev/pairflow
 succeeded in 0ms:
   clean first calibration point for the Opus arm.
3. **One contract-draft, for the L2b surface only.** The EPIPE
   closed-pipe behavior contract stays a PACKET-time decision (the
   decision-home triage: a single-packet decision has no cross-packet
   drift to prevent).
4. **Arm slicing: none** — see the autonomy-stage note above.

### 13.1 Scope and boundaries

**In scope:**

1. **The dispatch-time render.** `assemble_context_blocks`:
   deterministic, bodies ONLY from the catalog, source order = render
   order (role → step → gate, declaration order within each), a
   repeated id renders once while `provenance.sources[]` accumulates
   every emitter. The gate ref's predicate — it renders iff its
   transition is in `available_ops ∩ L1 capability` — REUSES the
   transition-existence + L1 authority logic ch 11 already defines;
   no fresh authority logic is written here. The packet gains one
   ordered field of `{ id, body, provenance }`. `HANDLE`'s verdict
   path is untouched.
2. **The definition-load ref check.** `validate_context_refs` runs
   under ADMISSION (`admit_definition`), beside the rest of the
   definition-static family; an unresolved ref is the
   `unresolved_context_block_ref` DEFINITION ISSUE — an admission
   failure, never a render-time drop. The code is an EXISTING name
   (the ch11 model fix's 31-name definition-issue family, see
   `ch11-model-sync-delta.md`); it joins the named-lane carrier the
   ch11 gate work established (`code` on the validate-stage finding).
3. **The format's three context keys.** Per the §8.2 stance (a
   capability and its format surface land in the same chapter): the
   template-level catalog (single body source — bodies never live
   inline at a ref site), the role/step prompt-concern refs (the L0c
   slot that has had no resolver until now), and the gate/policy
   block refs. camelCase realization of the model's snake_case names
   per the established rename culture (ch11-C13/C16/C37, ch12) —
   stated so neither side silently forks; unknown keys are ADMISSION
   issues per the ch8-C13 fail-closed culture.
4. **The shipped template's first catalog entry + the journey
   smoke.** Per opening disposition 1. The catalog additionally
   carries an AUTHORING COMMENT beside it (user-ratified
   2026-07-25): a block body stating a gate's configured value can go
   stale against that gate with nothing detecting it. It sits in the
   shipped file because the reader who needs it is whoever opens that
   file to add the NEXT entry — the person who turns the latent risk
   live. This chapter ships no such body (the only entry describes
   the emit envelope, not a gate), so the risk is latent here by
   construction; the comment and its draft sibling (§13.3) are aim,
   not enforcement.
5. **The EPIPE fix (the chapter's hygiene packet).** The SHIPPED CLI
   entrypoints' output sinks — the operator CLI's and the dev CLI's,
   whose entrypoints carry the byte-identical bare sink and share the
   `dispatch` shell that recognizes the quiet-termination path (scope
   aligned at ch13-p0 pre-approval, 2026-07-26) — handle a closed pipe
   quietly instead of crashing with a raw stack (repro: `pnpm v3:cli
   detail … | head -1`; also reached via a `| jq` that parse-fails
   mid-stream). Today those sinks are bare `process.stdout.write` /
   `process.stderr.write` calls with no EPIPE handling — note the
   contrast with the gate lane, which already treats an EPIPE against
   a fast-exiting child as best-effort (`runner/spawn.ts` GR2). The
   closed-pipe behavior contract is decided in the packet,
   against the ch6-P4a canonical exit-code matrix.

**Out of scope (deliberate):**

- **Computed / templated bodies** — a body is authored static text;
  interpolation like `{{ gate.config.value }}` is a declared Absent.
  The consequence is stated, not hidden: bump a gate's threshold and
  the prose that describes it silently lies until someone edits it.
- **Semantic parity check** — the ref check proves a ref RESOLVES,
  never that its prose matches the gate config it describes. Drift is
  not detected (declared Absent).
- **Conditional block bodies** — selection is by the authority
  predicate only; round- or state-dependent body variants are
  computed-body work (declared Absent).
- **Rich context assembly** — semantic retrieval, memory, skill-doc
  expansion, model-specific prompt shaping (declared Absent).
- **Actor-adapter prompt shaping** — the shipped adapter materializes
  the packet verbatim; how a model-specific adapter folds blocks into
  a prompt is adapter work (declared Absent).
- **A phase axis** (separate ref lists for a fresh start vs a
  continuation) — a v1 mechanism with NO L2b counterpart
  (`v1-prompt-parity-audit.md` §1). No speculative key: it enters
  with its semantics or not at all.
- **EC's `op_contracts`** — the MECHANIZED emit contract (per
  offerable op: required fields, domains, assertions, evidence
  obligations) stays EC's, after the MVP cut. This chapter's
  emit-envelope catalog entry is an INTERIM prose carrier and says so.
- **The `round ≥ 2` converge gate in the shipped template** — the
  model's config instance pairs the block with a real gate; adding
  that gate would change how OUR pair-workflow runs (the reviewer
  could no longer converge in round 1). That is a product decision,
  deliberately not made as an L2b side effect (user, 2026-07-25).

### 13.2 Coverage and intake impact

Unit ownership: **4 ids** (all `l2b-pseudocode`) — two new-name units
(`assemble_context_blocks`, `validate_context_refs`) and two reprints
(`CREATE_INSTANCE`, `dispatch_intent`) whose dispositions the owning
packets declare (the authoring-time discovery is the authority).
Rejections: **ZERO new behavioral** — the 54-name registry is
byte-untouched, because `unresolved_context_block_ref` is a
definition ISSUE code, not a registry rejection name; the drift lanes
must be green before AND after. Invariants: **6**, dispositions
already fixed by the ch-5 map (4 `test` / 1 `type/schema` /
1 `review`) — no checker, so `storeCheckers` is untouched. Chapter
traces: **1 golden trace** (`l2b-pseudocode`) — the rule renders for
the actor who could emit the gated transition, is ABSENT for one who
could not, and a multi-source id renders once with both sources
retained. No IC/PI intake rows flip: map-extension surface.

Carried IN from ch 9 (§1.3 carried items): the EPIPE product item
(realized at P0) and the DETACHED realized-map arm audit of
`ch9-runner-contract.md`, whose deadline is THIS chapter's close —
inherited as a close obligation (§13.5). The §1.3 carried item (3),
the MVP-cut vs v1-parity re-read, is due at this chapter's BOUNDARY,
before the plan sequences what follows — a boundary-review duty, not
a build blocker.

### 13.3 The draft phase

Before ANY packet (README §4):
`contracts/ch13-context-block-contract.md` (surface: `context-block`).
Indicative C-row set — the draft decides, this list only scopes it:
the three YAML keys' camelCase spelling, their attachment points, and
their keyset rules; the catalog entry's fixed keyset and its
unknown-key disposition; the packet field's TS shape and how the
three provenance source kinds are spelled (including the
gate-binding pair of step + event); the admission finding's lane
(path form, the `code` carrier, accumulate-vs-short-circuit against
ch8-C21/C36); the empty/absent matrix (no catalog and no refs; a
catalog present but unreferenced; an empty ref list; the same id
reached from a role AND its step); the render's behavior when a gate
ref's binding is not offerable; and the shipped catalog entry's
PRESENCE rule (its id and that it exists — its prose is packet-time
authoring, not a contract row). REQUIRED row (user-ratified
2026-07-25): a catalog-authoring CAVEAT — a block body that states a
gate's configured VALUE can go stale against that gate, and nothing
detects it (the two declared L2b Absents, `computed-templated-bodies`
and `semantic-parity-check`, are the missing capability; the ref
check proves resolution, never parity). The caveat is placed where
the risk is CREATED, not where it is suffered: this row plus the
§13.1 item 4 template comment. Neither enforces — they aim, and the
enforcing form is deliberately out of chapter (§13.1). Ratification
is permanently human;
packets anchor as `contract:ch13-context-block#Cn`.

**Superseded, and its successor (aligned at the ch13 contract-v2
ratification, 2026-08-08):** the `context-block` draft named above was
superseded 2026-08-05 (record in its own file; oracle branch
`ch13-prose-line`, authorizing plan `ch13-rederivation-plan.md`). The
live surface is `contracts/ch13-context-block-v2-contract.md`
(surface: `context-block-v2`), authored on the ADR-019 declared-schema
substrate; the indicative C-row list above described the prose line
and is history. Packets anchor as `contract:ch13-context-block-v2#Cn`
— an anchor into the superseded draft is lint-red. Ratification is
permanently human, unchanged.

### 13.4 Packets and flow mode

Draft reference (§1.3 convention):
`contracts/ch13-context-block-v2-contract.md` — the successor of
`contracts/ch13-context-block-contract.md`, which was NOT YET RATIFIED
at this chapter ratification, was ratified 2026-07-26 and superseded
2026-08-05 (aligned at the ch13 contract-v2 ratification, 2026-08-08);
the draft round runs FIRST, before any packet, so the prediction bases
below are visibly CONDITIONAL per the §1.3 convention.

| Packet | Content | Mode |
|---|---|---|
| ch13-P0 | the EPIPE hygiene fix: the shipped CLI entrypoints' output sinks survive a closed pipe (quiet termination instead of a raw stack; scope aligned at ch13-p0 pre-approval, 2026-07-26 — both entrypoints and the shared dispatch shell), its behavior contract decided against the ch6-P4a canonical exit-code matrix, with the claim-derived negative lane; empty ledger slice (a DECLARATION, not an omission — the ch9-P1 precedent) | human approve (the row carries its own new decision — the closed-pipe behavior contract — and §1.3's consistency rule bars an invention-predicted row from flag-free autonomous mode); predicted: invention (memo-born — basis: the ch9 boundary verdict; the enum's closest fit, the basis being a boundary verdict rather than a design memo) |
| ch13-P1 | the definition side: the three context format keys + the catalog on the parsed template, `validate_context_refs` under `admit_definition`, the `unresolved_context_block_ref` issue lane on the established named-lane carrier, the CLI validate extension | flag-free approve → autonomous build (measurement; the §5.5 fallbacks stand); predicted: projection (basis: the chapter draft — pending ratification) |
| ch13-P2 | the dispatch side: `assemble_context_blocks` (order, dedup, retained provenance), the packet's context-blocks field, the gate-ref predicate over ch11's authority logic, the `l2b-pseudocode` golden trace, the shipped template's first catalog entry (the emit-envelope block) + the catalog-authoring caveat comment beside it (§13.1 item 4), the l0c golden-trace re-pin (the C4 fixture disposition), and the journey smoke through the shipped entrypoint | flag-free approve → autonomous build (measurement); predicted: projection (basis: the chapter draft — pending ratification) |

Order: draft ratification (carrying the ch11 reopen act — C4 + C30
+ the C41 comparative clause; the draft's C16 carrier row; aligned
at the ch13 draft round, 2026-07-26) → P0 → P1 → P2 (the README §4 draft-first
rule binds without exception — P0 anchors no draft row but still
follows the ratification, the ch12-P0 precedent). P1 before P2 is the
definition → dispatch dependency: the render reads the parsed
catalog. The §8.2 no-speculative-keys stance binds at CHAPTER grain —
a key never ships without its semantics in the same chapter — so P1's
keys being consumed only at P2 is inside the stance, not an exception
to it. One packet = packet file + code + tests in ONE commit.

**Re-derivation alignment (aligned at the ch13 contract-v2
ratification, 2026-08-08):** the order above is the PROSE line's and
stands as history — its draft ratification and the ch11 reopen act it
carried (C4 + C30 + the C41 comparative clause, the SUPERSEDED draft's
C16 carrier row) both EXECUTED on 2026-07-26, and P0 landed. The live
order from that point is: contract v2 ratification → the ch13-p1 v2
packet (`ch13-rederivation-plan.md` phase P5 — definition side and
dispatch side; the P1/P2 split of the table above is the prose line's
and does not bind the re-derivation). The v2 draft carries NO
cross-contract reopen act: the ch11 binding keyset is already
ch11-C4's realized text. The live packet's row values, recorded HERE
at the v2 ratification (the §1.3 convention's recorded-at-ratification
rule): **ch13-p1 v2** — content: the definition side and the dispatch
side in one packet (declaration growth + the C13 normalizer hook and
carry-list growth, the C9 lane, the render and the packet field, the
registry flips, the C16/C18 fixture duties); mode: flag-free approve →
autonomous build (measurement — the §5.5 fallbacks stand, the two
transitional external-arm gates unchanged); predicted: projection
(basis: `contracts/ch13-context-block-v2-contract.md` — the surface
this act ratifies). A
split into ch13-p1a/p1b inherits this row's values unchanged.

Mutation-pilot flow note: the pilot DUAL-RUNS beside arm gate-2 on
every packet of this chapter, catches labeled code-mutation vs
input-domain. This is the pilot's SECOND data chapter — its
pre-declared two-chapter window CLOSES at this boundary, which
decides keep or stop.

### 13.5 Deliverables and DoD

Shipped: this section; the ratified-then-realized
`ch13-context-block-v2` contract-draft (its superseded predecessor
stays in the tree, frozen, as the decision record — aligned at the
ch13 contract-v2 ratification, 2026-08-08); the EPIPE fix; the
definition-load ref check + its issue lane; the three context format
keys; the dispatch-time render + the packet field; the shipped
template's first catalog entry; the journey smoke.

DoD: the packets' contract tests green with claim-derived negatives
EXECUTED; the `l2b` golden trace green; the drift suite green (the
unit-map lock extends with the 4 ids; the 54-name registry
byte-untouched — zero new behavioral rejections, asserted before AND
after; the three l2b entity rows of the domain registry flipped
realized by their owning packets — the registry test pins key sets,
not dispositions, so the flip is a named duty, never gate-caught);
invariant dispositions realized per the ch-5 map (no checker
added); coverage validation green; all v3 bridges + the FULL
`pnpm ci:local` gate green; the draft flipped `realized`-in-place
with its `realized_map` and `pnpm v3:realized-map` GREEN inside the
close act; the ch-13 map row flipped; `pnpm v3:deferred --closed ch13`
clean; the dogfooding checkpoint run-or-waived (a hand-driven run
whose actor's `packet.json` carries the catalog block — the
acceptance floor's live half); process-log review held at the
boundary, including (a) the `model-tier-experiment-2.md` §7
arm-comparison entry — the FIRST Opus-arm chapter, read against ch9's
Fable data on same-band packet pairs; (b) the mutation-pilot
second-chapter yield read and its keep/stop verdict; (c) the
INHERITED detached realized-map arm audit of `ch9-runner-contract.md`
(§1.3 carried item 2 — its deadline is this close); (d) the §1.3
carried item (3) MVP-cut vs v1-parity re-read, due before the plan
sequences the chapters that follow; and (e) the
reopen-vs-aggregate-note criterion (first applied at the ch13 draft —
its Context carries the applied form): decide its promotion to a
standing README rule or its retirement — until then the SUPERSEDED
`ch13-context-block` draft's Context is its only home (the v2 draft
does not carry the criterion — aligned at the ch13 contract-v2
ratification, 2026-08-08; that file is frozen and never-copy, so a
promotion decision must lift the criterion OUT of it rather than
leave it sitting there), and this line is the carrier that keeps
that decision from silently lapsing; a DEFERRED decision enters §1.3
as a carried item (the standing cross-chapter carry form), never a
silent lapse; and (f) the integer-like key-ban candidate on the
ch8-C10 namespace (owner-raised at the ch13 draft ratification round,
2026-07-26): canonical integer-form string keys (`"2"`, `"10"`) are
legal event-type/step-id spellings today and are the exact class JS
record enumeration re-orders (the ch13 PROBE-CB3 measurement,
recorded in the superseded `ch13-context-block` draft's frozen
Context; the v2 draft's C10 handles the corner honestly, so ch13 does
not need the ban — aligned at the ch13 contract-v2 ratification,
2026-08-08) — the boundary decides ADOPT (a ch8-C10 reopen in its own act,
with migration text; an untruncated draft-time sweep found ZERO
affected files, so the ban is free now and only gets more expensive
with time) or WATCH; a deferred adoption likewise enters §1.3 as a
carried item; and (g) the ch11 POINTER MAINTENANCE (routed here by
the ch13 contract-v2 draft's named carried scope, 2026-08-08):
`ch11-gate-format-contract.md` names `contract:ch13-context-block#C6`
in C4, C30, C41, the fourth reopen record and two `realized_map`
entries (a third map entry mentions the reopen act without carrying
the pointer) — the pointed-at SEMANTICS are carried unchanged by
ch13v2-C6, so this is pointer maintenance, not meaning drift, and no
lint catches it (the realized-map scanner matches same-contract refs
only; the packet-anchor rule is manifest-scoped). The boundary
decides REPOINT (a ch11 annotation act, tense/pointer conversions
only) or LEAVE-WITH-RECORD; either way the decision is recorded,
never a silent lapse.

exec
/bin/zsh -lc "rg -n 'D4 clarification|gloss.*non-binding|one-clause|structural attribute|flags round|CARRIED TO THE FLAGS' v3/implementation v3/adr | head -100" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:159:  contract states no structural attribute; it states decisions and
v3/implementation/ch13-rederivation-arm/p3-build/arm-round1-out.txt:636:  contract states no structural attribute; it states decisions and
v3/implementation/process-log.md:1262:  re-run" rule interacting with fix-all: one-clause mirror-sync
v3/implementation/contracts/ch13-context-block-v2-contract.md:18:attribute in passing, the gloss is non-binding paraphrase and the
v3/implementation/ch13-rederivation-arm/p3-design/design-review-out.txt:639:   134	  contract states no structural attribute; it states decisions and
v3/implementation/ch13-rederivation-arm/p4/review-record.md:369:CARRIED TO THE FLAGS ROUND, not folded: ADR-019 D4's original absolute
v3/implementation/ch13-rederivation-arm/p4/review-record.md:370:("the contract states no structural attribute; a rule visible in both
v3/implementation/ch13-rederivation-arm/p4/review-record.md:373:amendment, so the prepared one-clause D4 clarification rides the
v3/implementation/ch13-rederivation-arm/p3-aftermath/arm-round10-out.txt:1633:  contract states no structural attribute; it states decisions and

exec
/bin/zsh -lc "rg -n -C 5 'context_blocks|prompt_concern_refs|context_block_refs|refs-validated-at-definition-load|l2b/' v3/model | head -500" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/model/ledger.md-312-- `l2a` · **evidence-on-every-run** — evidence on every run
v3/model/ledger.md-313-- `l2a` · **explicit-output-mode** — explicit output mode
v3/model/ledger.md-314-- `l2a` · **runner-error-business-block** — runner error ≠ business block
v3/model/ledger.md-315-- `l2a` · **still-inline-only** — still inline only
v3/model/ledger.md-316-- `l2b` · **single-body-source** — single body source
v3/model/ledger.md:317:- `l2b` · **refs-validated-at-definition-load** — refs validated at definition load
v3/model/ledger.md-318-- `l2b` · **deterministic-ordered-render** — deterministic ordered render
v3/model/ledger.md-319-- `l2b` · **authority-scoped-gate-blocks** — authority-scoped gate blocks
v3/model/ledger.md-320-- `l2b` · **dedup-with-retained-provenance** — dedup with retained provenance
v3/model/ledger.md-321-- `l2b` · **communication-only** — communication only
v3/model/ledger.md-322-- `l3` · **park-is-one-visible-transition** — park is one visible transition
--
v3/model/ledger.md-499-- **Execution contract — a named runner and its values** — ProcessGateRunner · GateInvocation (value) · ProcessResult (value)
v3/model/ledger.md-500-- **Canonical Process Gate Contract — the single source of truth for external.process** — (no entities)
v3/model/ledger.md-501-
v3/model/ledger.md-502-### `l2b` (2 blocks · 3 entities)
v3/model/ledger.md-503-
v3/model/ledger.md:504:- **Context blocks — a catalog and its rendered values** — context_blocks catalog (template) · ContextBlockRef · ContextBlock (value)
v3/model/ledger.md-505-- **Canonical Context Block Contract — the single source of truth for the render** — (no entities)
v3/model/ledger.md-506-
v3/model/ledger.md-507-### `l3` (3 blocks · 5 entities)
v3/model/ledger.md-508-
v3/model/ledger.md-509-- **Human decision — a wait, an Ask, and a transcript pair** — wait step + RESUME_WAIT (minimal shape at L3) · human_gate (step type) · apply_target_entry_effects(...) (shared rule) · HumanDecisionRequest (value) · DECISION_REQUEST / DECISION_MADE (transcript)
--
v3/model/deltas/action-pseudocode.json-44-    "activate",
v3/model/deltas/action-pseudocode.json-45-    "CANCEL",
v3/model/deltas/action-pseudocode.json-46-    "COMPLETE",
v3/model/deltas/action-pseudocode.json-47-    "FAIL",
v3/model/deltas/action-pseudocode.json-48-    "HANDLE",
v3/model/deltas/action-pseudocode.json:49:    "assemble_context_blocks",
v3/model/deltas/action-pseudocode.json-50-    "dispatch_intent"
v3/model/deltas/action-pseudocode.json-51-  ]
v3/model/deltas/action-pseudocode.json-52-}
--
v3/model/deltas/release-pseudocode.json-40-    "activate",
v3/model/deltas/release-pseudocode.json-41-    "CANCEL",
v3/model/deltas/release-pseudocode.json-42-    "COMPLETE",
v3/model/deltas/release-pseudocode.json-43-    "FAIL",
v3/model/deltas/release-pseudocode.json-44-    "HANDLE",
v3/model/deltas/release-pseudocode.json:45:    "assemble_context_blocks",
v3/model/deltas/release-pseudocode.json-46-    "dispatch_intent"
v3/model/deltas/release-pseudocode.json-47-  ]
v3/model/deltas/release-pseudocode.json-48-}
--
v3/model/deltas/emit-contract-pseudocode.json-58-    "handle_help_request",
v3/model/deltas/emit-contract-pseudocode.json-59-    "help_request",
v3/model/deltas/emit-contract-pseudocode.json-60-    "HELP_REPLY",
v3/model/deltas/emit-contract-pseudocode.json-61-    "help_exchange_for",
v3/model/deltas/emit-contract-pseudocode.json-62-    "validate_help_config",
v3/model/deltas/emit-contract-pseudocode.json:63:    "assemble_context_blocks",
v3/model/deltas/emit-contract-pseudocode.json-64-    "dispatch_intent",
v3/model/deltas/emit-contract-pseudocode.json-65-    "is_child_workflow_step",
v3/model/deltas/emit-contract-pseudocode.json-66-    "validate_child_steps",
v3/model/deltas/emit-contract-pseudocode.json-67-    "park_for_child",
v3/model/deltas/emit-contract-pseudocode.json-68-    "child_intent",
--
v3/model/deltas/complete-pseudocode.json-47-    "activate",
v3/model/deltas/complete-pseudocode.json-48-    "CANCEL",
v3/model/deltas/complete-pseudocode.json-49-    "COMPLETE",
v3/model/deltas/complete-pseudocode.json-50-    "FAIL",
v3/model/deltas/complete-pseudocode.json-51-    "HANDLE",
v3/model/deltas/complete-pseudocode.json:52:    "assemble_context_blocks",
v3/model/deltas/complete-pseudocode.json-53-    "dispatch_intent",
v3/model/deltas/complete-pseudocode.json-54-    "DELETE_REQUESTED",
v3/model/deltas/complete-pseudocode.json-55-    "force_release",
v3/model/deltas/complete-pseudocode.json-56-    "hard_purge",
v3/model/deltas/complete-pseudocode.json-57-    "archive_or_export"
--
v3/model/deltas/l3-pseudocode.json-37-    "activate",
v3/model/deltas/l3-pseudocode.json-38-    "CANCEL",
v3/model/deltas/l3-pseudocode.json-39-    "COMPLETE",
v3/model/deltas/l3-pseudocode.json-40-    "FAIL",
v3/model/deltas/l3-pseudocode.json-41-    "HANDLE",
v3/model/deltas/l3-pseudocode.json:42:    "assemble_context_blocks",
v3/model/deltas/l3-pseudocode.json-43-    "dispatch_intent"
v3/model/deltas/l3-pseudocode.json-44-  ]
v3/model/deltas/l3-pseudocode.json-45-}
--
v3/model/deltas/l4-pseudocode.json-47-    "activate",
v3/model/deltas/l4-pseudocode.json-48-    "CANCEL",
v3/model/deltas/l4-pseudocode.json-49-    "COMPLETE",
v3/model/deltas/l4-pseudocode.json-50-    "FAIL",
v3/model/deltas/l4-pseudocode.json-51-    "HANDLE",
v3/model/deltas/l4-pseudocode.json:52:    "assemble_context_blocks",
v3/model/deltas/l4-pseudocode.json-53-    "dispatch_intent",
v3/model/deltas/l4-pseudocode.json-54-    "is_child_workflow_step",
v3/model/deltas/l4-pseudocode.json-55-    "validate_child_steps",
v3/model/deltas/l4-pseudocode.json-56-    "park_for_child",
v3/model/deltas/l4-pseudocode.json-57-    "child_intent",
--
v3/model/deltas/l5-pseudocode.json-52-    "handle_help_request",
v3/model/deltas/l5-pseudocode.json-53-    "help_request",
v3/model/deltas/l5-pseudocode.json-54-    "HELP_REPLY",
v3/model/deltas/l5-pseudocode.json-55-    "help_exchange_for",
v3/model/deltas/l5-pseudocode.json-56-    "validate_help_config",
v3/model/deltas/l5-pseudocode.json:57:    "assemble_context_blocks",
v3/model/deltas/l5-pseudocode.json-58-    "dispatch_intent",
v3/model/deltas/l5-pseudocode.json-59-    "is_child_workflow_step",
v3/model/deltas/l5-pseudocode.json-60-    "validate_child_steps",
v3/model/deltas/l5-pseudocode.json-61-    "park_for_child",
v3/model/deltas/l5-pseudocode.json-62-    "child_intent",
--
v3/model/deltas/l2b-pseudocode.json-23-    "activate",
v3/model/deltas/l2b-pseudocode.json-24-    "CANCEL",
v3/model/deltas/l2b-pseudocode.json-25-    "COMPLETE",
v3/model/deltas/l2b-pseudocode.json-26-    "FAIL",
v3/model/deltas/l2b-pseudocode.json-27-    "HANDLE",
v3/model/deltas/l2b-pseudocode.json:28:    "assemble_context_blocks",
v3/model/deltas/l2b-pseudocode.json-29-    "dispatch_intent"
v3/model/deltas/l2b-pseudocode.json-30-  ]
v3/model/deltas/l2b-pseudocode.json-31-}
--
v3/model/deltas/auto-action-pseudocode.json-47-    "activate",
v3/model/deltas/auto-action-pseudocode.json-48-    "CANCEL",
v3/model/deltas/auto-action-pseudocode.json-49-    "COMPLETE",
v3/model/deltas/auto-action-pseudocode.json-50-    "FAIL",
v3/model/deltas/auto-action-pseudocode.json-51-    "HANDLE",
v3/model/deltas/auto-action-pseudocode.json:52:    "assemble_context_blocks",
v3/model/deltas/auto-action-pseudocode.json-53-    "dispatch_intent"
v3/model/deltas/auto-action-pseudocode.json-54-  ]
v3/model/deltas/auto-action-pseudocode.json-55-}
--
v3/model/core-model-future-topic.md-107-  or gate pipeline enforces.
v3/model/core-model-future-topic.md-108-
v3/model/core-model-future-topic.md-109-#### 4. Emit affordances should become structured, not only text blocks
v3/model/core-model-future-topic.md-110-
v3/model/core-model-future-topic.md-111-`ContextPacket.available_ops` is currently a small navigation surface and L2b
v3/model/core-model-future-topic.md:112:adds deterministic `context_blocks` as communication-only prose. A later
v3/model/core-model-future-topic.md-113-context-packet slice should make the actor-facing "what can I emit now?" surface
v3/model/core-model-future-topic.md-114-structured enough for adapters and agents to reason over directly.
v3/model/core-model-future-topic.md-115-
v3/model/core-model-future-topic.md-116-- Represent each reachable emit as an affordance with event type, target step
v3/model/core-model-future-topic.md-117-  where known, availability status, gate summaries, required payload schema,
--
v3/model/sections/05-l0e.html-90-            <div class="ent"><div class="en">Rejected(runtime_context_provider_unavailable)</div><div class="fields">the spec names a provider the registry cannot resolve</div></div>
v3/model/sections/05-l0e.html-91-          </div>
v3/model/sections/05-l0e.html-92-        </div>
v3/model/sections/05-l0e.html-93-        <div class="note domain">
v3/model/sections/05-l0e.html-94-          This is the third member of the resolver family — <code>AgentConfig</code>→ActorAdapter,
v3/model/sections/05-l0e.html:95:          <code>prompt_concern_refs</code>→ContextAssembly, <code>RuntimeContextSpec</code>→RuntimeContextProvider.
v3/model/sections/05-l0e.html-96-          All share one shape: a portable intent in the definition, a named fulfiller, and a projection into
v3/model/sections/05-l0e.html-97-          the packet — none of them L8. <b>Naming:</b> <code>template.runtime_context</code> is the declared
v3/model/sections/05-l0e.html-98-          <em>requirement</em> (<code>none | required(spec)</code>); <code>instance.runtime_context</code> is the L0d
v3/model/sections/05-l0e.html-99-          lifecycle <em>state</em> (<code>none | requested | ready</code>) — same name, different aggregate
v3/model/sections/05-l0e.html-100-          (definition vs run).
--
v3/model/code/l0d-template-config.new.txt-17-      agent_config: { approach: systematic }   # overrides/extends the role default
v3/model/code/l0d-template-config.new.txt-18-      on_pass: review            # PASS → review
v3/model/code/l0d-template-config.new.txt-19-    - id: review
v3/model/code/l0d-template-config.new.txt-20-      role: reviewer
v3/model/code/l0d-template-config.new.txt-21-      instruction: "Review the diff; pass back or converge."
v3/model/code/l0d-template-config.new.txt:22:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/code/l0d-template-config.new.txt-23-      on_pass:      implement     # PASS → implement
v3/model/code/l0d-template-config.new.txt-24-      on_converged: done          # CONVERGED → done
v3/model/code/l0d-template-config.new.txt-25-          
--
v3/model/units/l5-pseudocode/dispatch_intent.txt-14-    instance_id, expected_version: instance.version, task: instance.task,
v3/model/units/l5-pseudocode/dispatch_intent.txt-15-    role:          step.role,                                      # dispatched-as role → echoed back as expected_role
v3/model/units/l5-pseudocode/dispatch_intent.txt-16-    instruction:   step.instruction,
v3/model/units/l5-pseudocode/dispatch_intent.txt-17-    handoff:       payload_of_transition_into(instance, step_id) ⊕ help_exchange_for(instance, step_id),  # L5 — the stay enrichment: the envelope that brought us here ⊕ this step/round's answered asks (transcript-derived)
v3/model/units/l5-pseudocode/dispatch_intent.txt-18-    available_ops: event_types_of(step.transitions) ∪ ({ HELP_REQUEST } IF step.help is declared),   # navigation affordances + the L5 ask affordance; L1 enforces capability in HANDLE. Offer and gate agree on the DEFAULT-derived path (same condition); an explicit authored profile may exclude the op and the offer does not yet consult it — a seam CLOSED at the emit-contract slice (offerable_ops: the offer calls capability itself)
v3/model/units/l5-pseudocode/dispatch_intent.txt:19:    context_blocks: assemble_context_blocks(instance, template, step),   # L2b — rendered, ordered, deduped, with provenance; communication only
v3/model/units/l5-pseudocode/dispatch_intent.txt-20-    effective_agent_config: resolve_agent_config(template, step, instance),   # resolved portable run intent (was raw agent_config at L0b)
v3/model/units/l5-pseudocode/dispatch_intent.txt-21-    runtime_context: runtime_view }                              # the projection, or none for a context-free workflow
v3/model/units/l5-pseudocode/dispatch_intent.txt-22-  RETURN DispatchIntent { actor, packet }            # a local/manual driver delivers; durable channel is L8
v3/model/units/l5-pseudocode/dispatch_intent.txt-23-
v3/model/units/l5-pseudocode/dispatch_intent.txt-24-
--
v3/model/code/auto-action-template-config.new.txt-28-      # worktree path defaults to <repo-parent>/.pairflow-worktrees/<repo>/<instance_id>
v3/model/code/auto-action-template-config.new.txt-29-    release:                      # LC2 — worktree is teardown-managed, so a release policy is MANDATORY (validate_release_policy at load)
v3/model/code/auto-action-template-config.new.txt-30-      policy: required            # required | retained | external — retained/external carry NO kernel release obligation
v3/model/code/auto-action-template-config.new.txt-31-      boundaries: [merge_completed]   # LC3 the merge_action's `merged` outcome emits this; LC2 releases the worktree when the merge completes
v3/model/code/auto-action-template-config.new.txt-32-      #   (was [terminal] at the L3 anchor; now a post-commit action exists, so the boundary moves past commit/merge — exactly the LC2 deferral)
v3/model/code/auto-action-template-config.new.txt:33:  context_blocks:               # L2b — the catalog: id → { body }; the single body source for rendered packet blocks
v3/model/code/auto-action-template-config.new.txt-34-    reviewer-severity-ontology:
v3/model/code/auto-action-template-config.new.txt-35-      body: "Rate findings blocker / major / minor; converge only when no blocker or major finding remains."
v3/model/code/auto-action-template-config.new.txt-36-    no-converged-before-round-2:
v3/model/code/auto-action-template-config.new.txt-37-      body: "Do not emit CONVERGED before round 2 — the convergence gate rejects an earlier attempt and the round is not burned."
v3/model/code/auto-action-template-config.new.txt-38-  steps:
--
v3/model/code/auto-action-template-config.new.txt-62-          #     on_runner_error: block_transition
v3/model/code/auto-action-template-config.new.txt-63-          #     on_timeout: block_transition
v3/model/code/auto-action-template-config.new.txt-64-    - id: review
v3/model/code/auto-action-template-config.new.txt-65-      role: reviewer
v3/model/code/auto-action-template-config.new.txt-66-      instruction: "Review the diff; pass back or converge."
v3/model/code/auto-action-template-config.new.txt:67:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/code/auto-action-template-config.new.txt-68-      on_pass:      implement     # PASS → implement
v3/model/code/auto-action-template-config.new.txt-69-      on_converged:               # CONVERGED → perf_test (LC3b): an AUTO action runs before the human gate; the L2 converge gates still guard this transition. recommends moves to the edge that now enters the human gate (perf_test.pass)
v3/model/code/auto-action-template-config.new.txt-70-        target: perf_test
v3/model/code/auto-action-template-config.new.txt-71-        gates:                    # ordered inline pipeline; first block stops, round not burned
v3/model/code/auto-action-template-config.new.txt-72-          - uses: declarative.threshold              # declarative DSL: path / op / value over allowed gate_projection fields
v3/model/code/auto-action-template-config.new.txt-73-            config: { metric: round, op: ">=", value: 2 }
v3/model/code/auto-action-template-config.new.txt:74:            context_block_refs: [no-converged-before-round-2]   # L2b — communicate the rule this gate enforces
v3/model/code/auto-action-template-config.new.txt-75-          - uses: pairflow.previous_reviewer_verdict # packaged pure pairflow policy
v3/model/code/auto-action-template-config.new.txt-76-            config: { required: true }
v3/model/code/auto-action-template-config.new.txt-77-    - id: perf_test
v3/model/code/auto-action-template-config.new.txt-78-      type: action               # LC3b — an AUTO action: runs on arrival, classifies the result, routes 3 ways (why it is an action, not a gate — a gate only allows/blocks the PASS it is given)
v3/model/code/auto-action-template-config.new.txt-79-      trigger: auto              # no operator: arrival claims WAITING(action_running) + emits an ActionIntent; the runner runs post-commit; the ACTION_RESULT event routes
--
v3/model/records/invariants/l2b.json-5-   "indent": "            ",
v3/model/records/invariants/l2b.json-6-   "items": [
v3/model/records/invariants/l2b.json-7-    {
v3/model/records/invariants/l2b.json-8-     "id": "single-body-source",
v3/model/records/invariants/l2b.json-9-     "name_html": "single body source",
v3/model/records/invariants/l2b.json:10:     "body_html": "a context block’s body lives only in <code>template.context_blocks</code>; refs are ids, never inline prose — one place to author, dedup, and validate"
v3/model/records/invariants/l2b.json-11-    },
v3/model/records/invariants/l2b.json-12-    {
v3/model/records/invariants/l2b.json:13:     "id": "refs-validated-at-definition-load",
v3/model/records/invariants/l2b.json-14-     "name_html": "refs validated at definition load",
v3/model/records/invariants/l2b.json-15-     "body_html": "<code>validate_context_refs(template)</code> runs under ADMISSION (<code>admit_definition</code>) beside the rest of the definition-static family; an unresolved ref is the <code>unresolved_context_block_ref</code> definition issue — an admission failure, not a render-time drop"
v3/model/records/invariants/l2b.json-16-    },
v3/model/records/invariants/l2b.json-17-    {
v3/model/records/invariants/l2b.json-18-     "id": "deterministic-ordered-render",
v3/model/records/invariants/l2b.json-19-     "name_html": "deterministic ordered render",
v3/model/records/invariants/l2b.json:20:     "body_html": "role → step → gate, declaration order within each; same instance state + config ⇒ byte-identical <code>context_blocks</code>. Order is reading sequence, not precedence/override"
v3/model/records/invariants/l2b.json-21-    },
v3/model/records/invariants/l2b.json-22-    {
v3/model/records/invariants/l2b.json-23-     "id": "authority-scoped-gate-blocks",
v3/model/records/invariants/l2b.json-24-     "name_html": "authority-scoped gate blocks",
v3/model/records/invariants/l2b.json-25-     "body_html": "a gate’s block renders only for a transition in <code>available_ops ∩ L1 capability</code> — transition existence plus the L1 role/<code>current_step</code> authority check; never blind step membership"
--
v3/model/records/invariants/l2b.json-30-     "body_html": "an id reached from several sources renders once; <code>provenance.sources[]</code> keeps every emitter, so the packet stays terse but auditable"
v3/model/records/invariants/l2b.json-31-    },
v3/model/records/invariants/l2b.json-32-    {
v3/model/records/invariants/l2b.json-33-     "id": "communication-only",
v3/model/records/invariants/l2b.json-34-     "name_html": "communication only",
v3/model/records/invariants/l2b.json:35:     "body_html": "<code>context_blocks</code> never enter a verdict or the commit; removing a block cannot change what L2 enforces (correctness is L2’s job, legibility is L2b’s)"
v3/model/records/invariants/l2b.json-36-    }
v3/model/records/invariants/l2b.json-37-   ]
v3/model/records/invariants/l2b.json-38-  }
v3/model/records/invariants/l2b.json-39- ]
v3/model/records/invariants/l2b.json-40-}
--
v3/model/core-model.html-900-        <div class="agg">
v3/model/core-model.html-901-          <div class="agg-label">Template aggregate — gains the run profile</div>
v3/model/core-model.html-902-          <div class="ents">
v3/model/core-model.html-903-            <div class="ent"><div class="en">Role</div><div class="fields">… + <b>default_agent_config</b> (the base run profile)</div></div>
v3/model/core-model.html-904-            <div class="ent"><div class="en">Step</div><div class="fields"><b>agent_config</b> now an override input (no longer raw pass-through)</div></div>
v3/model/core-model.html:905:            <div class="ent"><div class="en">AgentConfig <span style="font-size:10px;color:var(--muted)">(value)</span></div><div class="fields">inline: mode, approach, persona/profile, execution_hints; refs: model_ref/model_hint, prompt_profile_refs, prompt_concern_refs, skill_refs, tool_refs, tool_policy_ref (e.g. MCP) — all portable run intent</div></div>
v3/model/core-model.html-906-          </div>
v3/model/core-model.html-907-        </div>
v3/model/core-model.html-908-        <div class="agg">
v3/model/core-model.html-909-          <div class="agg-label">Instance aggregate — gains run overrides</div>
v3/model/core-model.html-910-          <div class="ents">
--
v3/model/core-model.html-953-      agent_config: { approach: systematic }   # overrides/extends the role default
v3/model/core-model.html-954-      on_pass: review            # PASS → review
v3/model/core-model.html-955-    - id: review
v3/model/core-model.html-956-      role: reviewer
v3/model/core-model.html-957-      instruction: "Review the diff; pass back or converge."
v3/model/core-model.html:958:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/core-model.html-959-      on_pass:      implement     # PASS → implement
v3/model/core-model.html-960-      on_converged: done          # CONVERGED → done
v3/model/core-model.html-961-          </script>
v3/model/core-model.html-962-        </div>
v3/model/core-model.html-963-        <div class="note config">
--
v3/model/core-model.html-1327-      agent_config: { approach: systematic }   # overrides/extends the role default
v3/model/core-model.html-1328-      on_pass: review            # PASS → review
v3/model/core-model.html-1329-    - id: review
v3/model/core-model.html-1330-      role: reviewer
v3/model/core-model.html-1331-      instruction: "Review the diff; pass back or converge."
v3/model/core-model.html:1332:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/core-model.html-1333-      on_pass:      implement     # PASS → implement
v3/model/core-model.html-1334-      on_converged: done          # CONVERGED → done
v3/model/core-model.html-1335-          </script>
v3/model/core-model.html-1336-        </div>
v3/model/core-model.html-1337-        <div class="note config">
--
v3/model/core-model.html-1652-            <div class="ent"><div class="en">Rejected(runtime_context_provider_unavailable)</div><div class="fields">the spec names a provider the registry cannot resolve</div></div>
v3/model/core-model.html-1653-          </div>
v3/model/core-model.html-1654-        </div>
v3/model/core-model.html-1655-        <div class="note domain">
v3/model/core-model.html-1656-          This is the third member of the resolver family — <code>AgentConfig</code>→ActorAdapter,
v3/model/core-model.html:1657:          <code>prompt_concern_refs</code>→ContextAssembly, <code>RuntimeContextSpec</code>→RuntimeContextProvider.
v3/model/core-model.html-1658-          All share one shape: a portable intent in the definition, a named fulfiller, and a projection into
v3/model/core-model.html-1659-          the packet — none of them L8. <b>Naming:</b> <code>template.runtime_context</code> is the declared
v3/model/core-model.html-1660-          <em>requirement</em> (<code>none | required(spec)</code>); <code>instance.runtime_context</code> is the L0d
v3/model/core-model.html-1661-          lifecycle <em>state</em> (<code>none | requested | ready</code>) — same name, different aggregate
v3/model/core-model.html-1662-          (definition vs run).
--
v3/model/core-model.html-1706-      agent_config: { approach: systematic }   # overrides/extends the role default
v3/model/core-model.html-1707-      on_pass: review            # PASS → review
v3/model/core-model.html-1708-    - id: review
v3/model/core-model.html-1709-      role: reviewer
v3/model/core-model.html-1710-      instruction: "Review the diff; pass back or converge."
v3/model/core-model.html:1711:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/core-model.html-1712-      on_pass:      implement     # PASS → implement
v3/model/core-model.html-1713-      on_converged: done          # CONVERGED → done
v3/model/core-model.html-1714-          </script>
v3/model/core-model.html-1715-        </div>
v3/model/core-model.html-1716-        <div class="note config">
--
v3/model/core-model.html-1729-          <div class="absent-item">No <b>provider health / availability</b> validation beyond name resolution <span class="at">→ later</span></div>
v3/model/core-model.html-1730-          <div class="absent-item">No <b>provisioning-failure handling</b> — a provider that accepts the name but fails to provision (or fails later) surfaces as a kernel <code>FAIL</code> event (L0d); retry / compensation is later <span class="at">→ later / §18</span></div>
v3/model/core-model.html-1731-          <div class="absent-item">No <b>run-override cascade</b> of the spec — template-only at L0e <span class="at">→ later</span></div>
v3/model/core-model.html-1732-          <div class="absent-item">No <b>durable delivery</b> (L8), <b>actor process launch</b> (tmux), or <b>credential / grant</b> (L7) <span class="at">→ L8 / L7</span></div>
v3/model/core-model.html-1733-          <div class="absent-item">No <b>conditional / per-step runtime context</b> — the requirement is whole-run (<code>none | required</code>); a step or child acquiring context later is category 3 <span class="at">→ later</span></div>
v3/model/core-model.html:1734:          <div class="absent-item">No <b>full ContextAssembly</b> — L0e adds only the runtime/workspace projection; general <code>context_blocks</code> rendering <span class="at">→ L2b</span></div>
v3/model/core-model.html-1735-        </div>
v3/model/core-model.html-1736-
v3/model/core-model.html-1737-        <h3 class="view-h"><span class="view-tag evidence">Evidence</span> Why we accept L0e for now</h3>
v3/model/core-model.html-1738-        <div class="note evidence">
v3/model/core-model.html-1739-          <ul style="margin:0">
--
v3/model/core-model.html-1992-      agent_config: { approach: systematic }   # overrides/extends the role default
v3/model/core-model.html-1993-      on_pass: review            # PASS → review
v3/model/core-model.html-1994-    - id: review
v3/model/core-model.html-1995-      role: reviewer
v3/model/core-model.html-1996-      instruction: "Review the diff; pass back or converge."
v3/model/core-model.html:1997:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/core-model.html-1998-      on_pass:      implement     # PASS → implement
v3/model/core-model.html-1999-      on_converged: done          # CONVERGED → done
v3/model/core-model.html-2000-          </script>
v3/model/core-model.html-2001-        </div>
v3/model/core-model.html-2002-        <p>Then the repo-local <b>ProjectConfig</b> (a new file — empty baseline) fills those slots with the
--
v3/model/core-model.html-2831-      agent_config: { approach: systematic }   # overrides/extends the role default
v3/model/core-model.html-2832-      on_pass: review            # PASS → review
v3/model/core-model.html-2833-    - id: review
v3/model/core-model.html-2834-      role: reviewer
v3/model/core-model.html-2835-      instruction: "Review the diff; pass back or converge."
v3/model/core-model.html:2836:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/core-model.html-2837-      on_pass:      implement     # PASS → implement
v3/model/core-model.html-2838-      on_converged:               # CONVERGED → done, now a gated transition (L2)
v3/model/core-model.html-2839-        target: done
v3/model/core-model.html-2840-        gates:                    # ordered inline pipeline; first block stops, round not burned
v3/model/core-model.html-2841-          - uses: declarative.threshold              # declarative DSL: path / op / value over allowed gate_projection fields
--
v3/model/core-model.html-3349-          #     on_runner_error: block_transition
v3/model/core-model.html-3350-          #     on_timeout: block_transition
v3/model/core-model.html-3351-    - id: review
v3/model/core-model.html-3352-      role: reviewer
v3/model/core-model.html-3353-      instruction: "Review the diff; pass back or converge."
v3/model/core-model.html:3354:      agent_config: { approach: thorough, prompt_concern_refs: [reviewer-severity-ontology], skill_refs: [test-runner] }
v3/model/core-model.html-3355-      on_pass:      implement     # PASS → implement
v3/model/core-model.html-3356-      on_converged:               # CONVERGED → done, now a gated transition (L2)
v3/model/core-model.html-3357-        target: done
v3/model/core-model.html-3358-        gates:                    # ordered inline pipeline; first block stops, round not burned
v3/model/core-model.html-3359-          - uses: declarative.threshold              # declarative DSL: path / op / value over allowed gate_projection fields
--
v3/model/core-model.html-3409-        <h2 class="level-h"><span class="level-badge">L2b</span> Policy/gate context contribution</h2>
v3/model/core-model.html-3410-        <p class="level-sub">L2 <b>enforces</b> a rule — an early <code>CONVERGED</code> is rejected. L2b
v3/model/core-model.html-3411-        <b>communicates</b> it: the kernel renders a deterministic, actor-facing <code>context_block</code> into
v3/model/core-model.html-3412-        the dispatched packet, so the reviewer is told the rule before it acts and never burns a round on an emit
v3/model/core-model.html-3413-        that would be blocked. This is the first <b>ContextAssembly</b> slice — and where L0c’s
v3/model/core-model.html:3414:        <code>prompt_concern_refs</code> finally resolve.</p>
v3/model/core-model.html-3415-
v3/model/core-model.html:3416:        <p><b>One render mechanism, two sources.</b> A template-level <code>context_blocks</code> catalog
v3/model/core-model.html:3417:        (<code>id → { body }</code>) is the single body source; role/step <code>prompt_concern_refs</code> (from
v3/model/core-model.html:3418:        L0c) and gate/policy <code>context_block_refs</code> are just id lists into it. The kernel resolves the
v3/model/core-model.html:3419:        issued refs, orders and dedups them, and renders them into <code>ContextPacket.context_blocks</code> with
v3/model/core-model.html-3420-        provenance. Nothing here changes a verdict: blocks are <b>communication only</b> — deleting one cannot
v3/model/core-model.html-3421-        change what L2 enforces.</p>
v3/model/core-model.html-3422-
v3/model/core-model.html-3423-        <h3 class="view-h"><span class="view-tag runtime">Runtime</span> A concrete trace</h3>
v3/model/core-model.html-3424-        <p>The reviewer is dispatched at <code>review</code>; the converge transition carries a round-threshold
v3/model/core-model.html-3425-        gate (<code>round ≥ 2</code>) and a context block that states the rule:</p>
v3/model/core-model.html-3426-        <div class="trace">
v3/model/core-model.html-3427-          <div class="trace-row"><div class="trace-n">1</div><div class="trace-c">
v3/model/core-model.html-3428-            <div class="ev">kernel dispatches the <code>reviewer</code> at <code>review</code> (round 1)</div>
v3/model/core-model.html:3429:            <div class="meta"><code>assemble_context_blocks</code> renders two blocks into the packet: <code>reviewer-severity-ontology</code> (provenance <code>source: step_config</code>) and <code>no-converged-before-round-2</code> (provenance <code>source: gate_binding(review, CONVERGED)</code>) — the latter because <code>CONVERGED ∈ available_ops ∩ L1 capability</code> for the reviewer here</div>
v3/model/core-model.html-3430-          </div></div>
v3/model/core-model.html-3431-          <div class="trace-row"><div class="trace-n">2</div><div class="trace-c">
v3/model/core-model.html-3432-            <div class="ev">the reviewer reads the packet and, at round 1, emits <code>PASS</code> (not <code>CONVERGED</code>)</div>
v3/model/core-model.html-3433-            <div class="meta">it was <em>told</em> “no CONVERGED before round 2,” so it does not spend an emit on a doomed converge — contrast L2, where an uninformed reviewer emitted <code>CONVERGED</code> at round 1 and got <code>gate_blocked(round_below_min)</code>. Same enforcement; L2b removed the wasted attempt</div>
v3/model/core-model.html-3434-          </div></div>
--
v3/model/core-model.html-3443-          <code>no-converged-before-round-2</code> from the catalog changes what the actor is <em>told</em>, never
v3/model/core-model.html-3444-          what the kernel <em>enforces</em>.
v3/model/core-model.html-3445-        </div>
v3/model/core-model.html-3446-
v3/model/core-model.html-3447-        <h3 class="view-h"><span class="view-tag pseudo">Pseudocode</span> The rule behind the trace</h3>
v3/model/core-model.html:3448:        <p>Two helpers are added — <code>assemble_context_blocks</code> (dispatch-time render) and
v3/model/core-model.html-3449-        <code>validate_context_refs</code> (definition-load check) — and the packet gains one field. The kernel
v3/model/core-model.html-3450-        handlers are otherwise unchanged:</p>
v3/model/core-model.html-3451-        <div class="code-diff" aria-label="L2b pseudocode" data-old-label="L2a baseline" data-new-label="L2b current">
v3/model/core-model.html-3452-          <script type="text/plain" class="diff-source" data-code-old-ref="l2a-pseudocode"></script>
v3/model/core-model.html-3453-          <script type="text/plain" class="diff-source" data-code-new data-code-id="l2b-pseudocode">
--
v3/model/core-model.html-3559-    IF raw.on_exit["0"] NOT IN { allow, warn, block } OR raw.on_exit[nonzero] NOT IN { allow, warn, block }
v3/model/core-model.html-3560-       THEN issue(invalid_process_gate_config)                        # buckets map only to realized verdicts — no route smuggled in
v3/model/core-model.html-3561-  RETURN effective(raw with defaults materialized)                    # output.mode, dispositions resolved — the ONE config form downstream
v3/model/core-model.html-3562-
v3/model/core-model.html-3563-validate_context_refs(template) → ok | issues                       # ADMISSION (admit_definition) at definition load — the definition-issue channel; the static analog of binding coverage / validate_gate_config
v3/model/core-model.html:3564:  catalog ← template.context_blocks                                   # id → { body }; the single body source
v3/model/core-model.html:3565:  FOR ref IN all_issued_context_block_refs(template):                 # every role/step prompt_concern_ref + gate context_block_ref
v3/model/core-model.html-3566-    IF ref NOT IN catalog THEN RETURN issue(unresolved_context_block_ref)
v3/model/core-model.html-3567-  RETURN ok                                                           # existence only — NOT semantic parity between a block’s prose and the gate config it describes
v3/model/core-model.html-3568-
v3/model/core-model.html-3569-RECEIVE(input) → Outcome                                               # one kernel entry; route by input source class
v3/model/core-model.html-3570-  MATCH input.source:
--
v3/model/core-model.html-3721-  #   never re-commit a target computed from stale state
v3/model/core-model.html-3722-
v3/model/core-model.html-3723-  intent ← (instance.kernel_status = TERMINAL) ? none : dispatch_intent(instance, template, instance.current_step)  # derive after commit
v3/model/core-model.html-3724-  RETURN Committed(instance.version, intent)
v3/model/core-model.html-3725-
v3/model/core-model.html:3726:assemble_context_blocks(instance, template, step) → [ContextBlock]    # L2b — deterministic render; bodies resolved from the catalog only
v3/model/core-model.html:3727:  catalog  ← template.context_blocks                                  # id → { body }; the single body source
v3/model/core-model.html-3728-  rendered ← ordered_map()                                            # first-seen order; dedups by id, accumulates provenance.sources[]
v3/model/core-model.html-3729-  emit(ref, source):                                                  # local closure — resolve once; on a repeat only append the provenance source
v3/model/core-model.html-3730-    IF rendered.has(ref) THEN rendered[ref].provenance.sources.append(source) ; RETURN
v3/model/core-model.html-3731-    rendered.put(ref, { id: ref, body: catalog[ref].body, provenance: { sources: [source] } })
v3/model/core-model.html-3732-  # render order IS source order: role refs → step refs → gate/policy refs, declaration order within each — render-order, NOT precedence/override
v3/model/core-model.html:3733:  FOR ref IN template.role(step.role).default_agent_config.prompt_concern_refs:   # 1) role identity / operating rules
v3/model/core-model.html-3734-    emit(ref, { source: role_config })
v3/model/core-model.html:3735:  FOR ref IN step.agent_config.prompt_concern_refs:                               # 2) the current step
v3/model/core-model.html-3736-    emit(ref, { source: step_config })
v3/model/core-model.html-3737-  authorized_ops ← capability(template, step.role, step.id) ∩ event_types_of(step.transitions)   # L1 authority ∩ transition existence — the ops this actor may legally emit
v3/model/core-model.html-3738-  FOR event_type IN authorized_ops:                                              # 3) gates on transitions the actor may emit — not blind step membership
v3/model/core-model.html-3739-    FOR gate IN template.gates_for(step.id, event_type):
v3/model/core-model.html:3740:      FOR ref IN gate.context_block_refs:
v3/model/core-model.html-3741-        emit(ref, { source: gate_binding, at: (step.id, event_type) })
v3/model/core-model.html-3742-  RETURN rendered.values()                                            # ordered [{ id, body, provenance: { sources } }]
v3/model/core-model.html-3743-
v3/model/core-model.html-3744-dispatch_intent(instance, template, step_id) → DispatchIntent
v3/model/core-model.html-3745-  step  ← template.step(step_id)
--
v3/model/core-model.html-3756-    instance_id, expected_version: instance.version, task: instance.task,
v3/model/core-model.html-3757-    role:          step.role,                                      # dispatched-as role → echoed back as expected_role
v3/model/core-model.html-3758-    instruction:   step.instruction,
v3/model/core-model.html-3759-    handoff:       payload_of_transition_into(instance, step_id),  # the envelope that brought us here; absent at start
v3/model/core-model.html-3760-    available_ops: event_types_of(step.transitions),              # navigation affordances; L1 enforces capability in HANDLE
v3/model/core-model.html:3761:    context_blocks: assemble_context_blocks(instance, template, step),   # L2b — rendered, ordered, deduped, with provenance; communication only
v3/model/core-model.html-3762-    effective_agent_config: resolve_agent_config(template, step, instance),   # resolved portable run intent (was raw agent_config at L0b)
v3/model/core-model.html-3763-    runtime_context: runtime_view }                              # the projection, or none for a context-free workflow
v3/model/core-model.html-3764-  RETURN DispatchIntent { actor, packet }            # a local/manual driver delivers; durable channel is L8
v3/model/core-model.html-3765-          </script>
v3/model/core-model.html-3766-        </div>
v3/model/core-model.html-3767-        <div class="note pseudo">
v3/model/core-model.html:3768:          <code>assemble_context_blocks</code> is deterministic: bodies come only from the catalog, the source
v3/model/core-model.html-3769-          order <em>is</em> the render order (role → step → gate), and a repeated id renders once while
v3/model/core-model.html-3770-          <code>provenance.sources[]</code> accumulates every emitter. The gate branch reuses the same
v3/model/core-model.html-3771-          transition-existence + L1 <code>capability</code> logic the kernel already defines — no fresh authority
v3/model/core-model.html-3772-          logic. None of this touches <code>HANDLE</code>’s verdict path.
v3/model/core-model.html-3773-        </div>
v3/model/core-model.html-3774-
v3/model/core-model.html-3775-        <h3 class="view-h"><span class="view-tag domain">Domain</span> What L2b adds</h3>
v3/model/core-model.html-3776-        <div class="agg">
v3/model/core-model.html-3777-          <div class="agg-label">Context blocks — a catalog and its rendered values</div>
v3/model/core-model.html-3778-          <div class="ents">
v3/model/core-model.html:3779:            <div class="ent"><div class="en">context_blocks catalog <span style="font-size:10px;color:var(--muted)">(template)</span></div><div class="fields">template-level <code>id → { body }</code> map; the <b>single body source</b>. Both ref kinds point into it; bodies never live inline at a ref site</div></div>
v3/model/core-model.html:3780:            <div class="ent"><div class="en">ContextBlockRef</div><div class="fields">two sources, one vocabulary: role/step <code>prompt_concern_refs</code> (L0c) and gate/policy <code>context_block_refs</code> — id lists, resolved against the catalog</div></div>
v3/model/core-model.html:3781:            <div class="ent"><div class="en">ContextBlock <span style="font-size:10px;color:var(--muted)">(value)</span></div><div class="fields"><code>{ id, body, provenance: { sources[] } }</code> rendered into <code>ContextPacket.context_blocks</code>; <code>sources[]</code> = <code>role_config | step_config | gate_binding(step, event)</code></div></div>
v3/model/core-model.html-3782-          </div>
v3/model/core-model.html-3783-        </div>
v3/model/core-model.html-3784-        <div class="agg">
v3/model/core-model.html-3785-          <div class="agg-label">Canonical Context Block Contract — the single source of truth for the render</div>
v3/model/core-model.html-3786-          <table class="contract">
v3/model/core-model.html-3787-            <thead><tr><th>Field / case</th><th>Required?</th><th>Valid values / behavior</th><th>Invalid / absent behavior</th></tr></thead>
v3/model/core-model.html-3788-            <tbody>
v3/model/core-model.html:3789:              <tr><td>template.context_blocks</td><td>yes, if any ref issued</td><td>catalog <code>id → { body }</code>; single body source</td><td>a ref with no entry ⇒ <code>unresolved_context_block_ref</code></td></tr>
v3/model/core-model.html:3790:              <tr><td>context_blocks[id].body</td><td>yes</td><td>authored static text (no interpolation)</td><td>absent entry ⇒ <code>unresolved_context_block_ref</code></td></tr>
v3/model/core-model.html:3791:              <tr><td>prompt_concern_refs (role/step)</td><td>no</td><td>id list into the catalog</td><td>unresolved ⇒ reject at create (<code>validate_context_refs</code>)</td></tr>
v3/model/core-model.html:3792:              <tr><td>context_block_refs (gate/policy)</td><td>no</td><td>id list into the catalog</td><td>unresolved ⇒ reject at create (<code>validate_context_refs</code>)</td></tr>
v3/model/core-model.html:3793:              <tr class="grp"><td colspan="4">Render — assemble_context_blocks</td></tr>
v3/model/core-model.html-3794-              <tr><td>render order</td><td>runtime</td><td>role → step → gate; declaration order within each</td><td>render-order only, <b>not</b> precedence/override</td></tr>
v3/model/core-model.html-3795-              <tr><td>gate ref predicate</td><td>runtime</td><td>renders iff its transition ∈ <code>available_ops ∩ L1 capability</code></td><td>otherwise not rendered (not this actor’s op)</td></tr>
v3/model/core-model.html-3796-              <tr><td>dedup</td><td>runtime</td><td>same id ⇒ one rendered block</td><td>—</td></tr>
v3/model/core-model.html-3797-              <tr><td>provenance.sources[]</td><td>runtime</td><td>every emitter retained (<code>role_config | step_config | gate_binding</code>)</td><td>—</td></tr>
v3/model/core-model.html:3798:              <tr><td>ContextPacket.context_blocks</td><td>runtime</td><td>ordered <code>[{ id, body, provenance }]</code></td><td>—</td></tr>
v3/model/core-model.html-3799-              <tr class="grp"><td colspan="4">Scope brake</td></tr>
v3/model/core-model.html-3800-              <tr><td>semantic parity (prose ↔ gate config)</td><td>—</td><td><b>not</b> checked — existence only</td><td>drift (gate <code>value</code> vs block prose) not caught</td></tr>
v3/model/core-model.html-3801-              <tr><td>computed / templated body</td><td>—</td><td>out of scope</td><td>interpolation like <code>{{ gate.config.value }}</code> is later</td></tr>
v3/model/core-model.html-3802-            </tbody>
v3/model/core-model.html-3803-          </table>
v3/model/core-model.html:3804:          <div class="fields" style="margin-top:8px">This table is the contract; <code>assemble_context_blocks</code> is its executable sketch, <code>validate_context_refs</code> enforces the catalog rows at definition load, and the config below is one instance of it.</div>
v3/model/core-model.html-3805-        </div>
v3/model/core-model.html-3806-        <div class="note domain">
v3/model/core-model.html-3807-          The boundary with L2 is <b>enforcement vs communication</b>. L2’s gate keeps the system correct; L2b’s
v3/model/core-model.html-3808-          block makes the same rule legible from configuration — paying off v1, which baked these operating rules
v3/model/core-model.html-3809-          into prompt prose. L2b proves a ref <em>resolves</em>, not that its prose <em>matches</em> the gate it
--
v3/model/core-model.html-3812-
v3/model/core-model.html-3813-        <h3 class="view-h"><span class="view-tag invariant">Invariant</span> New at L2b</h3>
v3/model/core-model.html-3814-        <div class="agg invariant">
v3/model/core-model.html-3815-          <div class="agg-label">Added rules</div>
v3/model/core-model.html-3816-          <div class="ents">
v3/model/core-model.html:3817:            <div class="ent"><div class="en">single body source</div><div class="fields">a context block’s body lives only in <code>template.context_blocks</code>; refs are ids, never inline prose — one place to author, dedup, and validate</div></div>
v3/model/core-model.html-3818-            <div class="ent"><div class="en">refs validated at definition load</div><div class="fields"><code>validate_context_refs(template)</code> runs under ADMISSION (<code>admit_definition</code>) beside the rest of the definition-static family; an unresolved ref is the <code>unresolved_context_block_ref</code> definition issue — an admission failure, not a render-time drop</div></div>
v3/model/core-model.html:3819:            <div class="ent"><div class="en">deterministic ordered render</div><div class="fields">role → step → gate, declaration order within each; same instance state + config ⇒ byte-identical <code>context_blocks</code>. Order is reading sequence, not precedence/override</div></div>
v3/model/core-model.html-3820-            <div class="ent"><div class="en">authority-scoped gate blocks</div><div class="fields">a gate’s block renders only for a transition in <code>available_ops ∩ L1 capability</code> — transition existence plus the L1 role/<code>current_step</code> authority check; never blind step membership</div></div>
v3/model/core-model.html-3821-            <div class="ent"><div class="en">dedup with retained provenance</div><div class="fields">an id reached from several sources renders once; <code>provenance.sources[]</code> keeps every emitter, so the packet stays terse but auditable</div></div>
v3/model/core-model.html:3822:            <div class="ent"><div class="en">communication only</div><div class="fields"><code>context_blocks</code> never enter a verdict or the commit; removing a block cannot change what L2 enforces (correctness is L2’s job, legibility is L2b’s)</div></div>
v3/model/core-model.html-3823-          </div>
v3/model/core-model.html-3824-        </div>
v3/model/core-model.html-3825-
v3/model/core-model.html-3826-        <h3 class="view-h"><span class="view-tag config">Config</span> The template — a catalog, and the converge gate communicates its rule</h3>
v3/model/core-model.html-3827-        <p>The pre-existing <code>reviewer-severity-ontology</code> ref (declared at L0c) now resolves against a
v3/model/core-model.html:3828:        new <code>context_blocks</code> catalog, and the L2 converge threshold gate gains a
v3/model/core-model.html:3829:        <code>context_block_refs</code> that states the rule it enforces:</p>
v3/model/core-model.html-3830-        <div class="code-diff" aria-label="L2b template config" data-old-label="L2a baseline" data-new-label="L2b current">
v3/model/core-model.html-3831-          <script type="text/plain" class="diff-source" data-code-old-ref="l2a-template-config"></script>
v3/model/core-model.html-3832-          <script type="text/plain" class="diff-source" data-code-new data-code-id="l2b-template-config">
v3/model/core-model.html-3833-template:
v3/model/core-model.html-3834-  id: local-pair-v0
--
v3/model/core-model.html-3854-      repo: current                                            # literal — not repo-overridable here
v3/model/core-model.html-3855-      base_branch:    ${slot:runtime.worktree.base_branch}     # was literal main; default now lives in slots, repo may override

exec
/bin/zsh -lc "rg -n 'collectTags|tag closure|tag-closure|ch13v2' v3/src v3/test tools | head -200" in /Users/felho/dev/pairflow
 succeeded in 0ms:
rg: v3/test: No such file or directory (os error 2)
v3/src/domain/template.ts:187:   * ch13v2-C1 + ch13v2-C13 (the context-block-v2 draft): the
v3/src/domain/template.ts:189:   * `{ body }`. OPTIONAL on the shared raw type per ch13v2-C13's
v3/src/domain/template.ts:194:   * value (ch13v2-C1). A PRESENT value the declared container lane
v3/src/domain/template.ts:195:   * refuses fails admission (fail-closed, ch13v2-C1), so no such form
v3/src/definition/admit.test.ts:206:    // ch11-P2c A1 + ch12-p1b G3 + ch12-p3 R1 + ch13v2-C1: the shape deltas
v3/src/definition/schema/templateFormat.ts:55:  rows: ["ch8-C14", "ch12-C7", "ch13v2-C4"],
v3/src/definition/schema/templateFormat.ts:60:  // ch13v2-C4 (ADR-019 D11): the ONE typed field of the otherwise
v3/src/definition/schema/templateFormat.ts:64:  // (a CLI input surface — ch13v2-C5's run scope).
v3/src/definition/schema/templateFormat.ts:69:      rows: ["ch13v2-C4"],
v3/src/definition/schema/templateFormat.ts:76:/** ch13v2-C2: ONE grammar for catalog keys and every ref-list member —
v3/src/definition/schema/templateFormat.ts:81:  rows: ["ch13v2-C2"],
v3/src/definition/schema/templateFormat.ts:90:/** ch13v2-C4/C6/C7/C8: the shared ref-list value class — the entry belt
v3/src/definition/schema/templateFormat.ts:96:  rows: ["ch13v2-C4", "ch13v2-C6", "ch13v2-C7", "ch13v2-C8"],
v3/src/definition/schema/templateFormat.ts:102:    rows: ["ch13v2-C4", "ch13v2-C6"],
v3/src/definition/schema/templateFormat.ts:196:    // ch13v2-C6: the binding's ref position. The keyset growth itself is
v3/src/definition/schema/templateFormat.ts:201:      rows: ["ch13v2-C6"],
v3/src/definition/schema/templateFormat.ts:328:  rows: ["ch8-C7", "ch8-C13", "ch8-C24", "ch8-C25", "ch11-C18", "ch11-C37", "ch12-C1", "ch13v2-C1"],
v3/src/definition/schema/templateFormat.ts:460:      rows: ["ch13v2-C11"],
v3/src/definition/schema/templateFormat.ts:544:      rows: ["ch13v2-C1"],
v3/src/definition/schema/templateFormat.ts:545:      // ch13v2-C1: absent-catalog normalization — the admitted form always
v3/src/definition/schema/templateFormat.ts:554:        rows: ["ch13v2-C2"],
v3/src/definition/schema/templateFormat.ts:562:        rows: ["ch13v2-C3"],
v3/src/definition/schema/templateFormat.ts:570:            rows: ["ch13v2-C3"],
v3/src/definition/schema/templateFormat.ts:623:    // lane below since the ch13v2 adoption; its end-to-end CLI travel is
v3/src/definition/schema/templateFormat.ts:624:    // P5's (ch13v2-C18).
v3/src/definition/schema/templateFormat.ts:674:      // ch13v2-C13 legislates this hook's carry-list growth at P5 — the
v3/src/definition/schema/templateFormat.ts:676:      rows: ["ch11-C20", "ch11-C5", "ch13v2-C13"],
v3/src/definition/schema/vocabulary.ts:164: * D4) and the ratified rows it realizes (the tag-closure check's other
v3/src/definition/validate.test.ts:794:      // ch13v2-C1: admission materializes the absent catalog to the empty

exec
/bin/zsh -lc 'pnpm v3:typecheck' in /Users/felho/dev/pairflow
 succeeded in 1347ms:

> @pairflow/cli@0.2.0 v3:typecheck /Users/felho/dev/pairflow
> pnpm --dir v3 typecheck


> @pairflow/v3@0.0.0 typecheck /Users/felho/dev/pairflow/v3
> tsc --noEmit


exec
/bin/zsh -lc 'pnpm v3:test' in /Users/felho/dev/pairflow
codex
A három kötelező gate közül a packet-lint és a typecheck zöld; a teljes v3 teszt most egyedül fut. Az élő P4-4 próba a `/tmp`-ből reprodukálta a rögzített kimenetet, beleértve mind a négy DECIDED-HERE esetet.
 succeeded in 25072ms:

> @pairflow/cli@0.2.0 v3:test /Users/felho/dev/pairflow
> pnpm --dir v3 test


> @pairflow/v3@0.0.0 test /Users/felho/dev/pairflow/v3
> vitest run


 RUN  v4.1.10 /Users/felho/dev/pairflow/v3


 Test Files  72 passed (72)
      Tests  2063 passed (2063)
   Start at  18:46:11
   Duration  24.64s (transform 2.97s, setup 0ms, import 5.87s, tests 79.22s, environment 4ms)


exec
/bin/zsh -lc "rg -n 'contract states no structural attribute|rule visible in both places is a defect|with the declared default|per-site findings at the declared grain|per-occurrence duplicate finding|default: \\{|unique: \\{ grain:|code: \"unresolved_context_block_ref\"' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/src/definition/schema/templateFormat.ts:106:  unique: { grain: "perOccurrence", at: "index", message: "duplicate context block ref {valueJson}" },
v3/src/definition/schema/templateFormat.ts:110:    code: "unresolved_context_block_ref",
v3/src/definition/schema/templateFormat.ts:408:      unique: { grain: "perOccurrence", at: "container", message: "duplicate terminal id {valueJson}" },
v3/src/definition/schema/templateFormat.ts:519:      default: { mode: "immediate" },
v3/src/definition/schema/templateFormat.ts:549:      default: {},
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:159:  contract states no structural attribute; it states decisions and
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:160:  cites tags. A rule visible in both places is a defect, and the
v3/implementation/contracts/ch13-context-block-v2-contract.md:149:| C1 | A template's root MAY carry the catalog under the key `contextBlocks` — structural authority `[d-ctxblocks]` (container, key lane, entries) and `[d-root]` (the root keyset's growth by this optional key), with the declared default: an absent catalog admits (the admitted-form surface is C13's). DECIDED HERE (v2): admission of the catalog shape is FAIL-CLOSED — a present value the declared container lane refuses is a container finding and the template does not admit (the authored-value classes measured live: PROBE-P4-4; the accept boundary is the declared lane's own, and a cast-only degenerate object with nothing to enumerate admits as empty — accepted, named, receipt: PROBE-P4-4's degenerate case); the divergence is receipted and argued in the review record's completeness pass. Refs beside a refused or absent catalog still draw their per-site C7 findings (receipt: PROBE-P4-4, both channels) — the belt's broken-operand nature (ADR-019 D10), a construct property no row needs to restate. The root-keyset aggregate-note chain RETIRES at this surface: the declared `[d-root]` fields are the enumerable authority, and the predecessor notes described pre-arrival states — no successor note is owed. |
v3/implementation/contracts/ch13-context-block-v2-contract.md:155:| C7 | Resolution is ENTRY-BELTED (ADR-019 D10, the construct this surface carried into the vocabulary): every ref member that passed its own shape lane must resolve against the keys of the catalog WHOSE VALUE IS A VALID ENTRY — validity measured by the catalog's own declared entry node, one definition governing both channels; key existence alone is not resolution. The declared lanes run at ADMISSION on both channels (the moment the ch11 sync fixed; the model's invariant record agrees). Structural authority: `[vc-blockidlist]`'s membership rule, carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain, all accumulated. When a ref names an entry the declaration refuses, both facts report — the entry's finding and the coded finding at the ref site — each fixable on its own (PROBE-P4-4). Containment, why C10's dispatch-time miss is integrity drift: nothing the render reads escapes the validated set, and dispatch only ever consumes store-admitted templates. DECIDED HERE (v2): the resolution lane measures EVERY shape-passing occurrence — a duplicated unresolved ref reports per occurrence beside C8's duplicate finding (receipts: PROBE-P4-3 DUP_GHOST, PROBE-P4-4; rationale and old→new mapping: the completeness pass). This row is the refs-validated-at-definition-load invariant's home (disposition test). |
v3/implementation/contracts/ch13-context-block-v2-contract.md:156:| C8 | Duplicates: a repeated id within one ref list draws the per-occurrence duplicate finding at the declared grain — structural authority `[vc-blockidlist]`'s duplicate rule. DECIDED HERE (v2): a member that failed its OWN shape lane is invisible to every LIST-LEVEL lane — no duplicate finding, no membership finding; its own finding is the trace (receipts: PROBE-P4-3 DUP_BAD_SHAPE, PROBE-P4-4; rationale and mapping: the completeness pass). C9's audit is NOT a list-level lane and is deliberately outside this rule — its reference set reads the raw authored members, as its own row states. The edge family's success negative: the compound CLEAN case — a populated catalog, every entry referenced, every ref resolving — admits with zero findings (receipt: PROBE-P4-4, measured gate-free; the gate position's clean case needs an injected registry, so its member belongs to P5's build-driven fixture family). |

exec
/bin/zsh -lc "rg -n 'first agent-config-family authoring|MEASURED|DERIVED|PROBE' v3/implementation/contracts/ch13-context-block-v2-contract.md | tail -30
rg -n 'agentConfig|defaultAgentConfig' v3/templates || true" in /Users/felho/dev/pairflow
 succeeded in 0ms:
69:ratifying-basis declaration as PROBE-P4-4, the receipt the rows cite
72:- **PROBE-P4-1** (belt composition): the D10 `validKeysOf` belt at a
79:- **PROBE-P4-2** (the plain-map gap → ADR-019 D11): before D11, a ref
84:- **PROBE-P4-3** (list-lane interplay + key identity): the engine
91:- **PROBE-P4-4** (the live declaration, at the ratifying basis): the
149:| C1 | A template's root MAY carry the catalog under the key `contextBlocks` — structural authority `[d-ctxblocks]` (container, key lane, entries) and `[d-root]` (the root keyset's growth by this optional key), with the declared default: an absent catalog admits (the admitted-form surface is C13's). DECIDED HERE (v2): admission of the catalog shape is FAIL-CLOSED — a present value the declared container lane refuses is a container finding and the template does not admit (the authored-value classes measured live: PROBE-P4-4; the accept boundary is the declared lane's own, and a cast-only degenerate object with nothing to enumerate admits as empty — accepted, named, receipt: PROBE-P4-4's degenerate case); the divergence is receipted and argued in the review record's completeness pass. Refs beside a refused or absent catalog still draw their per-site C7 findings (receipt: PROBE-P4-4, both channels) — the belt's broken-operand nature (ADR-019 D10), a construct property no row needs to restate. The root-keyset aggregate-note chain RETIRES at this surface: the declared `[d-root]` fields are the enumerable authority, and the predecessor notes described pre-arrival states — no successor note is owed. |
150:| C2 | Block ids are ONE namespace with ONE grammar governing the catalog's keys and all ref-list members alike — structural authority `[vc-block-id]` (cited by `[d-block-key]` and `[d-block-ref]`). The namespace is NEW and deliberately distinct from the gate-token and provider-token namespaces; all three ids the model exhibits satisfy it, the digit-bearing one being the grammar's designated positive case (build-driven). A key the grammar refuses draws the key lane's finding at the declared grain — DECIDED HERE (v2): the key lane runs on BOTH channels — strictly fail-louder (receipts: PROBE-P4-4's direct-channel bad-key case, PROBE-P4-3 BAD_KEY_SIBLING; mapping and rationale: the completeness pass). Key-identity on the file channel follows the engine's preserved key types (the boolean-key case: the key lane reports it, and no string ref can address it — each lane reporting its own finding, C9's audit adding its own once realized). |
152:| C4 | `promptConcernRefs` is the single typed field inside the agent-config value class, which stays format-open in every other key, at both of the template's config positions — the role-level default and the step-level map — structural authority `[d-prompt-refs]` on `[vc-agentconfig]`, value class `[vc-blockidlist]`; the typing basis is ADR-019 D11 (typed subset: every other agent-config key stays legal open data). ch12-C7's delegation clause pre-authorizes this arrival (the refs' value class arriving with its consuming chapter — the aggregate-note convention, so no ch12 row is affected). Absent map, absent key, and a PRESENT EMPTY list: all legal, zero refs (the admitted-form surface is C13's) — the shared value class deliberately declares no nonempty rule, diverging from every other list of the surface (receipt: PROBE-P4-4's empty-authored-list case). ch12-C7's no-field-typed sentence now describes the state before this arrival, per the standing aggregate-note practice — ch12 needs no edit. FORWARD SCOPE: the model also exhibits a MAP-SHAPED, mode-tagged ref member (the mode layer's); today's member class is deliberately string-only, and that member form arrives as D7 format growth with the chapter that takes the mode layer. |
155:| C7 | Resolution is ENTRY-BELTED (ADR-019 D10, the construct this surface carried into the vocabulary): every ref member that passed its own shape lane must resolve against the keys of the catalog WHOSE VALUE IS A VALID ENTRY — validity measured by the catalog's own declared entry node, one definition governing both channels; key existence alone is not resolution. The declared lanes run at ADMISSION on both channels (the moment the ch11 sync fixed; the model's invariant record agrees). Structural authority: `[vc-blockidlist]`'s membership rule, carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain, all accumulated. When a ref names an entry the declaration refuses, both facts report — the entry's finding and the coded finding at the ref site — each fixable on its own (PROBE-P4-4). Containment, why C10's dispatch-time miss is integrity drift: nothing the render reads escapes the validated set, and dispatch only ever consumes store-admitted templates. DECIDED HERE (v2): the resolution lane measures EVERY shape-passing occurrence — a duplicated unresolved ref reports per occurrence beside C8's duplicate finding (receipts: PROBE-P4-3 DUP_GHOST, PROBE-P4-4; rationale and old→new mapping: the completeness pass). This row is the refs-validated-at-definition-load invariant's home (disposition test). |
156:| C8 | Duplicates: a repeated id within one ref list draws the per-occurrence duplicate finding at the declared grain — structural authority `[vc-blockidlist]`'s duplicate rule. DECIDED HERE (v2): a member that failed its OWN shape lane is invisible to every LIST-LEVEL lane — no duplicate finding, no membership finding; its own finding is the trace (receipts: PROBE-P4-3 DUP_BAD_SHAPE, PROBE-P4-4; rationale and mapping: the completeness pass). C9's audit is NOT a list-level lane and is deliberately outside this rule — its reference set reads the raw authored members, as its own row states. The edge family's success negative: the compound CLEAN case — a populated catalog, every entry referenced, every ref resolving — admits with zero findings (receipt: PROBE-P4-4, measured gate-free; the gate position's clean case needs an injected registry, so its member belongs to P5's build-driven fixture family). |
158:| C10 | Render order and provenance — SEMANTIC, runtime (realized at the render build): blocks emit by source — the role position first, the step position second, the gate position last — and inside one source the authored list sequence holds. The gate leg walks the admitted gates record in that record's own enumeration, each event's pipeline in its authored sequence, each binding's list likewise, with C11 deciding what may emit. That record enumerates as authored except where the substrate hoists integer-like quoted keys to the front AND re-sorts them ascending among themselves — DERIVED from the recorded substrate measurement PROBE-CB3 (the predecessor's frozen Context holds the record) and RE-EXECUTED on the live engine at this basis (receipt: PROBE-P4-4's key-order case), named measurer: the P5 render build re-executes the probe before pinning order fixtures. An id met a second time emits nothing new — its first position stands — while the provenance list records every emitter as encountered (the dedup-with-retained-provenance invariant's home, disposition test). The admitted-form construction order is thereby a contract-visible property, not a free refactor. Body text comes solely from the admitted catalog; after admission no lookup can fail, so a dispatch-time miss signals kernel-integrity drift and must abort loudly rather than degrade. |
161:| C13 | The admitted-form surfaces — DERIVED, named measurer: the P5 build's acceptance family plus the parity gate. On admission success the admitted template carries: the catalog record (always present — the declared default covers absence; a present catalog arrives validated, C1 having refused every non-map form); and normalized ref-list fields FOR EVERY ref position (empty list where nothing was authored), produced by the normalizer (ADR-019 D3 — derivation is never validation) as SIBLING fields at the fixed-keyset levels (the roles entry, the step, the admitted binding) — beside, never inside, the raw agent-config maps. TYPE GRAIN (carried decision): the shared raw types mark the normalized fields with `?`, while every admitted VALUE carries them — the standing admitted-form optionality pattern; marking them required was weighed and refused, since it would invalidate each hand-built raw fixture in the tree. The domain type's `contextBlocks?` field already has this shape. CHANNEL GRAIN (the producer-monopoly precedent `advancesRound`, carried WHOLE): the TWO admission-produced positions — the roles entry's and the step's — are DECLARED channel-gated to the direct channel at P5, while the binding's `contextBlockRefs` is C6's authored key and stays channel-both; a caller holding an admitted value thereby re-admits it and a FILE author meets the unknown-key refusal, AND the fill RECOMPUTES each produced position from its authored source, overwriting any caller-supplied value — the monopoly half, which is what keeps C7's containment true on the direct channel. FIELD SPELLINGS, decided here for P5 to declare: the normalized fields carry their authored sources' names — `promptConcernRefs` as the sibling at the roles entry and the step, `contextBlockRefs` on the binding. Realization needs a NEW normalizer hook (D7 format growth landing at P5's own act, never silently), the ref fill running BEFORE the binding rebuild so an unauthored list lands as the empty list, not an undefined carry; and the `[n-effective-config]` hook's carry list must grow `contextBlockRefs`, or the rebuild drops the authored refs — the carry list plus the hook's produced field is the admitted binding's whole keyset. Two named build traps: admission rebuilds only `steps` today (the roles side is new work), and the role-side source is NESTED inside the config map (the first nested-source normalization). P5 also flips the three l2b domain-registry rows (the catalog and ContextBlockRef rows with the definition packet, carrying their realized type names — `BlockId`, the catalog record type; the ContextBlock row with the render side, its witness the packet's rendered block member type) — named here exactly BECAUSE omitting the flip trips no gate; a realized row needs its type name AND the registry's two sync surfaces, and if P5 ships as one packet the assignment collapses onto it unchanged. Rendering consumes these admitted surfaces and nothing rawer, never re-validating at dispatch; the catalog's entry type is the ONLY typed place body text can live (this row homes the single-body-source invariant, disposition type/schema). The raw config maps retain the authored key untouched for the ch12 cascade to read (C5). The ROLE-ENTRY and STEP keysets do not widen for FILE authors — the normalized positions are admission-produced, and a file authoring one meets the unknown-key refusal, while the direct channel admits them under the channel grain above; the binding's `contextBlockRefs` is the stated exclusion: there the AUTHORED key (C6) is itself the normalized position's source and is carried, never refused. |
164:| C16 | The shipped catalog — SEMANTIC, shipped-artifact duty: the canonical template file gains a catalog carrying the emit-envelope entry and BOTH roles gain a default-config ref to it (carried decision: every actor emits, the block is role-symmetric; this is the first agent-config-family authoring in a shipped template file). The entry's text gets written when the owning packet lands, sourced from the interim knowledge carrier `v1-prompt-parity-audit.md` (§2's Class A envelope content, under §5's interim-carrier ruling); retiring that carrier is bound to the EC chapter's definition of done. Ripple: the named fixture families (canonical-file equality pin moving with the file; effective-config and full-packet equality re-pins (the full-packet member doubling as C12's tree-wide presence ripple); committed-provenance re-pins; the golden-trace fixture disposition — its snake-spelled fixture sites migrate onto the typed key together with a resolving catalog entry, which realizes the L0c slot; absence-consumers swept by key AND value). MEMBERSHIP CRITERION for the re-pin families: a suite belongs iff it CONSUMES the canonical file or the shared fixture — directory location decides nothing, and suites constructing their own templates stay outside. The criterion bounds the CATALOG-CONTENT ripple families only — C12's always-present-field ripple is tree-wide and this criterion never narrows it. Member enumeration stays the owning packet's. |
167:| C19 | The lane inventory: this surface's definition-static lanes are the lanes the ch13v2-rowed VALIDATION nodes introduce — enumerated by the declaration itself — plus C9's one semantic lane. Named exclusions, four: `[d-root]` and `[vc-agentconfig]` are SHARED validation nodes whose pre-existing lanes belong to their own surfaces (only the growth is this chapter's); `[n-effective-config]` is a NORMALIZER cited for C13's duty — derivation introduces no lane (ADR-019 D3); and `[d-capability-profile]` is a raw, lane-less node (its file-channel refusal is `[d-root]`'s own unknown-key lane). Channel scope is an ENGINE property of the one declaration: no walk/rung realization split exists or is re-ratified, and for the catalog and the AUTHORED ref positions (C4/C6) admission answers identically on the two channels — accept or refuse — with only source-bearing attributes differentiating the finding sets (receipt: PROBE-P4-4). Declared, deliberate asymmetries are not defects of that rule: `[d-capability-profile]` is channel-GATED today (receipt: PROBE-P4-4), and the two admission-PRODUCED positions C13 channel-gates join it at P5 — arrivals this row forward-scopes. Named P5 build surfaces: the C9 lane; the C13 normalizer hook WITH its declaration growth (the two channel-gated produced-position nodes under their decided spellings), carry-list growth and registry flips; and the C16/C18 fixture duties. |

exec
/bin/zsh -lc "rg -n -o 'emit-envelope|reviewer-severity-ontology|no-converged-before-round-2' v3/model | sort -u | head -100
rg -n 'PROBE.*(emit-envelope|reviewer-severity-ontology|no-converged-before-round-2)|all three ids' v3/implementation/ch13-rederivation-arm/p4 v3/implementation/contracts/ch13-context-block-v2-contract.md" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/model/code/action-template-config.new.txt:34:reviewer-severity-ontology
v3/model/code/action-template-config.new.txt:36:no-converged-before-round-2
v3/model/code/action-template-config.new.txt:67:reviewer-severity-ontology
v3/model/code/action-template-config.new.txt:75:no-converged-before-round-2
v3/model/code/auto-action-template-config.new.txt:34:reviewer-severity-ontology
v3/model/code/auto-action-template-config.new.txt:36:no-converged-before-round-2
v3/model/code/auto-action-template-config.new.txt:67:reviewer-severity-ontology
v3/model/code/auto-action-template-config.new.txt:74:no-converged-before-round-2
v3/model/code/emit-contract-template-config.new.txt:112:no-converged-before-round-2
v3/model/code/emit-contract-template-config.new.txt:42:reviewer-severity-ontology
v3/model/code/emit-contract-template-config.new.txt:44:no-converged-before-round-2
v3/model/code/emit-contract-template-config.new.txt:92:reviewer-severity-ontology
v3/model/code/l0c-template-config.new.txt:18:reviewer-severity-ontology
v3/model/code/l0d-template-config.new.txt:22:reviewer-severity-ontology
v3/model/code/l0e-template-config.new.txt:31:reviewer-severity-ontology
v3/model/code/l0f-mode-template-config.new.txt:36:reviewer-severity-ontology
v3/model/code/l0f-mode-template-config.new.txt:38:no-converged-before-round-2
v3/model/code/l0f-mode-template-config.new.txt:77:reviewer-severity-ontology
v3/model/code/l0f-mode-template-config.new.txt:84:no-converged-before-round-2
v3/model/code/l0f-template-config.new.txt:36:reviewer-severity-ontology
v3/model/code/l2-template-config.new.txt:37:reviewer-severity-ontology
v3/model/code/l2a-template-config.new.txt:57:reviewer-severity-ontology
v3/model/code/l2b-template-config.new.txt:29:reviewer-severity-ontology
v3/model/code/l2b-template-config.new.txt:31:no-converged-before-round-2
v3/model/code/l2b-template-config.new.txt:62:reviewer-severity-ontology
v3/model/code/l2b-template-config.new.txt:69:no-converged-before-round-2
v3/model/code/l3-template-config.new.txt:30:reviewer-severity-ontology
v3/model/code/l3-template-config.new.txt:32:no-converged-before-round-2
v3/model/code/l3-template-config.new.txt:63:reviewer-severity-ontology
v3/model/code/l3-template-config.new.txt:71:no-converged-before-round-2
v3/model/code/l5-template-config.new.txt:36:reviewer-severity-ontology
v3/model/code/l5-template-config.new.txt:38:no-converged-before-round-2
v3/model/code/l5-template-config.new.txt:82:reviewer-severity-ontology
v3/model/code/l5-template-config.new.txt:89:no-converged-before-round-2
v3/model/code/release-template-config.new.txt:35:reviewer-severity-ontology
v3/model/code/release-template-config.new.txt:37:no-converged-before-round-2
v3/model/code/release-template-config.new.txt:68:reviewer-severity-ontology
v3/model/code/release-template-config.new.txt:76:no-converged-before-round-2
v3/model/core-model.html:10897:reviewer-severity-ontology
v3/model/core-model.html:10899:no-converged-before-round-2
v3/model/core-model.html:10943:reviewer-severity-ontology
v3/model/core-model.html:10950:no-converged-before-round-2
v3/model/core-model.html:12066:reviewer-severity-ontology
v3/model/core-model.html:12068:no-converged-before-round-2
v3/model/core-model.html:12116:reviewer-severity-ontology
v3/model/core-model.html:12136:no-converged-before-round-2
v3/model/core-model.html:1332:reviewer-severity-ontology
v3/model/core-model.html:1711:reviewer-severity-ontology
v3/model/core-model.html:1997:reviewer-severity-ontology
v3/model/core-model.html:2836:reviewer-severity-ontology
v3/model/core-model.html:3354:reviewer-severity-ontology
v3/model/core-model.html:3429:no-converged-before-round-2
v3/model/core-model.html:3429:reviewer-severity-ontology
v3/model/core-model.html:3437:no-converged-before-round-2
v3/model/core-model.html:3443:no-converged-before-round-2
v3/model/core-model.html:3827:reviewer-severity-ontology
v3/model/core-model.html:3860:reviewer-severity-ontology
v3/model/core-model.html:3862:no-converged-before-round-2
v3/model/core-model.html:3893:reviewer-severity-ontology
v3/model/core-model.html:3900:no-converged-before-round-2
v3/model/core-model.html:4665:reviewer-severity-ontology
v3/model/core-model.html:4667:no-converged-before-round-2
v3/model/core-model.html:4698:reviewer-severity-ontology
v3/model/core-model.html:4706:no-converged-before-round-2
v3/model/core-model.html:5809:reviewer-severity-ontology
v3/model/core-model.html:5811:no-converged-before-round-2
v3/model/core-model.html:5842:reviewer-severity-ontology
v3/model/core-model.html:5850:no-converged-before-round-2
v3/model/core-model.html:6673:reviewer-severity-ontology
v3/model/core-model.html:6675:no-converged-before-round-2
v3/model/core-model.html:6706:reviewer-severity-ontology
v3/model/core-model.html:6714:no-converged-before-round-2
v3/model/core-model.html:7584:reviewer-severity-ontology
v3/model/core-model.html:7586:no-converged-before-round-2
v3/model/core-model.html:7617:reviewer-severity-ontology
v3/model/core-model.html:7624:no-converged-before-round-2
v3/model/core-model.html:8847:reviewer-severity-ontology
v3/model/core-model.html:8849:no-converged-before-round-2
v3/model/core-model.html:8888:reviewer-severity-ontology
v3/model/core-model.html:8895:no-converged-before-round-2
v3/model/core-model.html:958:reviewer-severity-ontology
v3/model/sections/10-l2b.html:103:reviewer-severity-ontology
v3/model/sections/10-l2b.html:22:no-converged-before-round-2
v3/model/sections/10-l2b.html:22:reviewer-severity-ontology
v3/model/sections/10-l2b.html:30:no-converged-before-round-2
v3/model/sections/10-l2b.html:36:no-converged-before-round-2
v3/implementation/contracts/ch13-context-block-v2-contract.md:150:| C2 | Block ids are ONE namespace with ONE grammar governing the catalog's keys and all ref-list members alike — structural authority `[vc-block-id]` (cited by `[d-block-key]` and `[d-block-ref]`). The namespace is NEW and deliberately distinct from the gate-token and provider-token namespaces; all three ids the model exhibits satisfy it, the digit-bearing one being the grammar's designated positive case (build-driven). A key the grammar refuses draws the key lane's finding at the declared grain — DECIDED HERE (v2): the key lane runs on BOTH channels — strictly fail-louder (receipts: PROBE-P4-4's direct-channel bad-key case, PROBE-P4-3 BAD_KEY_SIBLING; mapping and rationale: the completeness pass). Key-identity on the file channel follows the engine's preserved key types (the boolean-key case: the key lane reports it, and no string ref can address it — each lane reporting its own finding, C9's audit adding its own once realized). |

exec
/bin/zsh -lc "rg -o --no-filename 'emit-envelope|reviewer-severity-ontology|no-converged-before-round-2' v3/model | sort -u
rg -n 'emit-envelope' v3/model || true
rg -n 'all three ids the model exhibits' v3/implementation/contracts/ch13-context-block-v2-contract.md" in /Users/felho/dev/pairflow
 succeeded in 0ms:
no-converged-before-round-2
reviewer-severity-ontology
150:| C2 | Block ids are ONE namespace with ONE grammar governing the catalog's keys and all ref-list members alike — structural authority `[vc-block-id]` (cited by `[d-block-key]` and `[d-block-ref]`). The namespace is NEW and deliberately distinct from the gate-token and provider-token namespaces; all three ids the model exhibits satisfy it, the digit-bearing one being the grammar's designated positive case (build-driven). A key the grammar refuses draws the key lane's finding at the declared grain — DECIDED HERE (v2): the key lane runs on BOTH channels — strictly fail-louder (receipts: PROBE-P4-4's direct-channel bad-key case, PROBE-P4-3 BAD_KEY_SIBLING; mapping and rationale: the completeness pass). Key-identity on the file channel follows the engine's preserved key types (the boolean-key case: the key lane reports it, and no string ref can address it — each lane reporting its own finding, C9's audit adding its own once realized). |

exec
/bin/zsh -lc "rg -n 'first agent-config-family authoring|first agentConfig-family authoring|canonical file.*first|FIRST.*agentConfig' v3/implementation/ch13-rederivation-arm/p4 v3/implementation/contracts/ch13-context-block-v2-contract.md
rg -n 'agentConfig|defaultAgentConfig' v3/templates || true" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/implementation/contracts/ch13-context-block-v2-contract.md:164:| C16 | The shipped catalog — SEMANTIC, shipped-artifact duty: the canonical template file gains a catalog carrying the emit-envelope entry and BOTH roles gain a default-config ref to it (carried decision: every actor emits, the block is role-symmetric; this is the first agent-config-family authoring in a shipped template file). The entry's text gets written when the owning packet lands, sourced from the interim knowledge carrier `v1-prompt-parity-audit.md` (§2's Class A envelope content, under §5's interim-carrier ruling); retiring that carrier is bound to the EC chapter's definition of done. Ripple: the named fixture families (canonical-file equality pin moving with the file; effective-config and full-packet equality re-pins (the full-packet member doubling as C12's tree-wide presence ripple); committed-provenance re-pins; the golden-trace fixture disposition — its snake-spelled fixture sites migrate onto the typed key together with a resolving catalog entry, which realizes the L0c slot; absence-consumers swept by key AND value). MEMBERSHIP CRITERION for the re-pin families: a suite belongs iff it CONSUMES the canonical file or the shared fixture — directory location decides nothing, and suites constructing their own templates stay outside. The criterion bounds the CATALOG-CONTENT ripple families only — C12's always-present-field ripple is tree-wide and this criterion never narrows it. Member enumeration stays the owning packet's. |
v3/implementation/ch13-rederivation-arm/p4/arm-round2-d11-out.txt:176:v3/implementation/contracts/ch13-context-block-contract.md:344:| C14 | The shipped-catalog presence rule: the canonical `v3/templates/local-pair-v0@1.yaml` gains THREE insertions — a root `contextBlocks` catalog carrying the entry id `emit-envelope`, and a NEW `defaultAgentConfig: { promptConcernRefs: [emit-envelope] }` subtree under EACH of the two roles (an untruncated draft-time sweep found the file authoring neither key; this makes the canonical file the first agentConfig-family authoring in an authored template FILE — the l0c TS fixture authors the family in-test today — DECIDED HERE: role-level on BOTH roles, because every actor emits and the block is role-symmetric). The entry's PROSE is packet-time authored (the interim carrier of the emit-envelope knowledge — `v1-prompt-parity-audit.md` §5; its retirement is bound into the DoD of the chapter that takes the §1.3 EC candidate). Ripple, the named families (member enumeration the owning packet's, R-ALTITUDE-LINE): (i) the ch8-P2 equality pin FOLLOWS the file in the same commit (fixture + file move together, the pin test unchanged — the ch11-P4 Y1/Y2 precedent; both pin sides pass through admission, so C17's normalized fields appear symmetrically); (ii) the `effectiveAgentConfig` assertions and full-packet deep-equals — spanning the CLI/journey suites AND the kernel/testkit deep-equal fixtures (C11's family, one sweep) — re-pin, P2; (iii) the COMMITTED-provenance family: `issued_agent_config` assertions re-pin to the new resolved values (the C12-named byte surface — the shared fixture flows through the ch12-C8 cascade into every committed transition); MEMBERSHIP is by `fixtureTemplate`/canonical-file CONSUMPTION, not by directory (the floor and testkit suites, the runner delivery-loop suite, and the root CT files are in; a suite building its own template is out) — P2; (iv) the l0c golden-trace re-pin (a ch12 trace: BOTH fixture sites move per C4's disposition and gain the catalog entry) — P2; (v) consumers of the keys' former ABSENCE are swept by VALUE and by key (R-ABSENCE-CONSUMERS). |

exec
/bin/zsh -lc 'git rev-parse --short=8 HEAD && shasum -a 256 v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts && git status --short' in /Users/felho/dev/pairflow
 succeeded in 0ms:
dc821f5b
81f1daa9ac04a393e8a7d31d8111778430e3166d89b8e9ee0294c47c6269fe7d  v3/implementation/contracts/ch13-context-block-v2-contract.md
9368e5253d1163fa0f6ffa18290fef5ef4007de4f293862eeee522c1b7ea3c49  v3/src/definition/schema/templateFormat.ts

exec
/bin/zsh -lc "rg -n -o 'contract states no structural attribute|A rule visible in both places is a defect|with the declared default: an absent catalog admits|carrying the ratified code "'`unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain|per-occurrence duplicate finding at the declared grain|default: '"\\{\\}|unique: \\{ grain: \"perOccurrence\"|code: \"unresolved_context_block_ref\"' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/src/definition/schema/templateFormat.ts:106:unique: { grain: "perOccurrence"
v3/src/definition/schema/templateFormat.ts:110:code: "unresolved_context_block_ref"
v3/src/definition/schema/templateFormat.ts:408:unique: { grain: "perOccurrence"
v3/src/definition/schema/templateFormat.ts:549:default: {}
v3/implementation/contracts/ch13-context-block-v2-contract.md:149:with the declared default: an absent catalog admits
v3/implementation/contracts/ch13-context-block-v2-contract.md:155:carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain
v3/implementation/contracts/ch13-context-block-v2-contract.md:156:per-occurrence duplicate finding at the declared grain
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:159:contract states no structural attribute
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:160:A rule visible in both places is a defect

exec
/bin/zsh -lc "rg -n -o 'this is the first agent-config-family authoring in a shipped template file' v3/implementation/contracts/ch13-context-block-v2-contract.md v3/implementation/ch13-rederivation-arm/p4 || true
rg -n 'agentConfig|defaultAgentConfig' v3/templates || true" in /Users/felho/dev/pairflow
 succeeded in 0ms:
v3/implementation/contracts/ch13-context-block-v2-contract.md:164:this is the first agent-config-family authoring in a shipped template file

codex
### IN-SCOPE 1 — C2 invents a third model-exhibited block id

CLASS: IN-SCOPE  
WHAT: C2 claims the model exhibits three block ids, but the model plane contains only two distinct ids; no receipt supports the count.  
WHERE: `ch13-context-block-v2-contract.md:150`  
EVIDENCE:

```text
$ rg -o --no-filename 'emit-envelope|reviewer-severity-ontology|no-converged-before-round-2' v3/model | sort -u
no-converged-before-round-2
reviewer-severity-ontology

$ rg -n 'emit-envelope' v3/model
[no output]
```

EXPECTED: A model-plane cardinality claim must match the model bytes and carry an executed receipt or a DERIVED marker with named measurer.

### IN-SCOPE 2 — Normative rows duplicate declaration-owned structural attributes

CLASS: IN-SCOPE  
WHAT: C1, C7 and C8 normatively repeat the declaration’s default, issue-code/grain and duplicate-grain attributes. This contradicts current ADR-019 D4 and falsifies the zero-restatement claim.  
WHERE: Contract rows C1/C7/C8; declaration lines 106/110/549; ADR-019 lines 159–160.  
EVIDENCE:

```text
$ rg -n -o 'contract states no structural attribute|A rule visible in both places is a defect|with the declared default: an absent catalog admits|carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain|per-occurrence duplicate finding at the declared grain|default: \{\}|unique: \{ grain: "perOccurrence"|code: "unresolved_context_block_ref"' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts
v3/src/definition/schema/templateFormat.ts:106:unique: { grain: "perOccurrence"
v3/src/definition/schema/templateFormat.ts:110:code: "unresolved_context_block_ref"
v3/src/definition/schema/templateFormat.ts:408:unique: { grain: "perOccurrence"
v3/src/definition/schema/templateFormat.ts:549:default: {}
v3/implementation/contracts/ch13-context-block-v2-contract.md:149:with the declared default: an absent catalog admits
v3/implementation/contracts/ch13-context-block-v2-contract.md:155:carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain
v3/implementation/contracts/ch13-context-block-v2-contract.md:156:per-occurrence duplicate finding at the declared grain
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:159:contract states no structural attribute
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:160:A rule visible in both places is a defect
```

EXPECTED: A row cites declaration tags and states decisions without normatively carrying declaration-owned defaults, codes or grains.

### IN-SCOPE 3 — C16 carries an unreceipted repository-wide “first” claim

CLASS: IN-SCOPE  
WHAT: C16 says this will be the first shipped-template agent-config authoring. The assertion is currently true by direct sweep, but neither the row nor any PROBE-P4 receipt carries that measurement, and the row has no DERIVED marker or named measurer.  
WHERE: `ch13-context-block-v2-contract.md:164`  
EVIDENCE:

```text
$ rg -n -o 'this is the first agent-config-family authoring in a shipped template file' v3/implementation/contracts/ch13-context-block-v2-contract.md v3/implementation/ch13-rederivation-arm/p4 || true
v3/implementation/contracts/ch13-context-block-v2-contract.md:164:this is the first agent-config-family authoring in a shipped template file

$ rg -n 'agentConfig|defaultAgentConfig' v3/templates || true
[no output]
```

EXPECTED: This measurable repository-wide assertion carries an executed PROBE-P4 receipt or a DERIVED marker with a named measurer.

HEAD: `dc821f5b`  
Hashes: contract `81f1daa9ac04a393e8a7d31d8111778430e3166d89b8e9ee0294c47c6269fe7d`; declaration `9368e5253d1163fa0f6ffa18290fef5ef4007de4f293862eeee522c1b7ea3c49`  
Counts: IN-SCOPE 3 / CARRIED-SCOPE 0 / UNRUN 0  
Claim targets: 1 fully; 2 fully; 3 partially—independent spot-check of old C2/C8/C10/C17/C19; 4 fully; 5 partially—current authored positions and `capabilityProfile` exercised, P5-produced positions are not yet executable  
Gates: `pnpm v3:packet-lint` PASS; `pnpm v3:typecheck` PASS; `pnpm v3:test` PASS—72 files, 2063 tests  
A draft a három IN-SCOPE eltérés miatt még nem kész a humán ratifikációs aktusra.
tokens used
162 665
### IN-SCOPE 1 — C2 invents a third model-exhibited block id

CLASS: IN-SCOPE  
WHAT: C2 claims the model exhibits three block ids, but the model plane contains only two distinct ids; no receipt supports the count.  
WHERE: `ch13-context-block-v2-contract.md:150`  
EVIDENCE:

```text
$ rg -o --no-filename 'emit-envelope|reviewer-severity-ontology|no-converged-before-round-2' v3/model | sort -u
no-converged-before-round-2
reviewer-severity-ontology

$ rg -n 'emit-envelope' v3/model
[no output]
```

EXPECTED: A model-plane cardinality claim must match the model bytes and carry an executed receipt or a DERIVED marker with named measurer.

### IN-SCOPE 2 — Normative rows duplicate declaration-owned structural attributes

CLASS: IN-SCOPE  
WHAT: C1, C7 and C8 normatively repeat the declaration’s default, issue-code/grain and duplicate-grain attributes. This contradicts current ADR-019 D4 and falsifies the zero-restatement claim.  
WHERE: Contract rows C1/C7/C8; declaration lines 106/110/549; ADR-019 lines 159–160.  
EVIDENCE:

```text
$ rg -n -o 'contract states no structural attribute|A rule visible in both places is a defect|with the declared default: an absent catalog admits|carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain|per-occurrence duplicate finding at the declared grain|default: \{\}|unique: \{ grain: "perOccurrence"|code: "unresolved_context_block_ref"' v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md v3/implementation/contracts/ch13-context-block-v2-contract.md v3/src/definition/schema/templateFormat.ts
v3/src/definition/schema/templateFormat.ts:106:unique: { grain: "perOccurrence"
v3/src/definition/schema/templateFormat.ts:110:code: "unresolved_context_block_ref"
v3/src/definition/schema/templateFormat.ts:408:unique: { grain: "perOccurrence"
v3/src/definition/schema/templateFormat.ts:549:default: {}
v3/implementation/contracts/ch13-context-block-v2-contract.md:149:with the declared default: an absent catalog admits
v3/implementation/contracts/ch13-context-block-v2-contract.md:155:carrying the ratified code `unresolved_context_block_ref` on exactly this lane, per-site findings at the declared grain
v3/implementation/contracts/ch13-context-block-v2-contract.md:156:per-occurrence duplicate finding at the declared grain
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:159:contract states no structural attribute
v3/adr/ADR-019-declared-schema-for-structural-definition-rules.md:160:A rule visible in both places is a defect
```

EXPECTED: A row cites declaration tags and states decisions without normatively carrying declaration-owned defaults, codes or grains.

### IN-SCOPE 3 — C16 carries an unreceipted repository-wide “first” claim

CLASS: IN-SCOPE  
WHAT: C16 says this will be the first shipped-template agent-config authoring. The assertion is currently true by direct sweep, but neither the row nor any PROBE-P4 receipt carries that measurement, and the row has no DERIVED marker or named measurer.  
WHERE: `ch13-context-block-v2-contract.md:164`  
EVIDENCE:

```text
$ rg -n -o 'this is the first agent-config-family authoring in a shipped template file' v3/implementation/contracts/ch13-context-block-v2-contract.md v3/implementation/ch13-rederivation-arm/p4 || true
v3/implementation/contracts/ch13-context-block-v2-contract.md:164:this is the first agent-config-family authoring in a shipped template file

$ rg -n 'agentConfig|defaultAgentConfig' v3/templates || true
[no output]
```

EXPECTED: This measurable repository-wide assertion carries an executed PROBE-P4 receipt or a DERIVED marker with a named measurer.

HEAD: `dc821f5b`  
Hashes: contract `81f1daa9ac04a393e8a7d31d8111778430e3166d89b8e9ee0294c47c6269fe7d`; declaration `9368e5253d1163fa0f6ffa18290fef5ef4007de4f293862eeee522c1b7ea3c49`  
Counts: IN-SCOPE 3 / CARRIED-SCOPE 0 / UNRUN 0  
Claim targets: 1 fully; 2 fully; 3 partially—independent spot-check of old C2/C8/C10/C17/C19; 4 fully; 5 partially—current authored positions and `capabilityProfile` exercised, P5-produced positions are not yet executable  
Gates: `pnpm v3:packet-lint` PASS; `pnpm v3:typecheck` PASS; `pnpm v3:test` PASS—72 files, 2063 tests  
A draft a három IN-SCOPE eltérés miatt még nem kész a humán ratifikációs aktusra.
