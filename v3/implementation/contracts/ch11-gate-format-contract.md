# ch11 — gate-format contract

```json
{"contract_draft": {"chapter": "ch11", "surface": "gate-format", "status": "realized"}}
```

## Context (non-normative by declaration)

**Sources.** Plan §11 (ratified 2026-07-11, arm-round closed at
ffb42804; realigned at the ch11 model fix — the RATIFIED MODEL STATE is
`453d3be9` with the delta evidence `ch11-model-sync-delta.md` @ de33d245:
single-authority definition admission, GateRegistration, effective
configs, definition-issue channel); the model units `l2-pseudocode/*` + `l2a-pseudocode/*`, the
golden-trace Config views (`code/l2-template-config.new.txt`,
`code/l2a-template-config.new.txt`), and their ledger slices (§2
invariants, §3 rejections); the ch8 `template-format` contract (the
base format this surface EXTENDS — its C7 forward declaration names
this chapter's key as the first expected additive growth);
`topics/v3-gate-policy-config-design-synthesis.md` (direction only —
where it diverges from the ratified model [`inconclusive` verdicts,
deferred process gates, authored implementation/execution keys,
allow|block-only decisions], the MODEL wins: verdicts are
allow|warn|block, execution is inline-only, evaluator axes are
registry-intrinsic).

**Seed-row disposition** (plan §11.3's indicative set → rows; no seed
item is undisposed): gate-declaration grammar → C1/C2/C3/C4/C5;
evaluator-id grammar + the C10 relation → C6 (+C7 paths); process-config
keys + validator lane matrix + channel mapping → C13–C17/C20/C21/C28;
`runtimeContext` key → C18/C19; GateDecision JSON schema → C25;
registry composition + per-evaluator config grammar → C8/C9/C10/C11;
evidence-persistence shape → C26/C33/C34; module-home ADR +
`ports/gate.ts` reconciliation → C29 (ADR-013).

**Control-Model answers (round-0 skeleton).**

- *Business invariant:* gate-before-commit — a blocking decision
  returns before the commit; version, step, and round are untouched
  (no round burned). Decisions derive from the policy projection
  only.
- *Control model:* the kernel owns the pipeline rung; evaluators are
  read-only, stateless deciders; the process runner owns the spawn
  ("the kernel owns the contract, the runner owns the spawn" — the
  spawn is ch 9's); the registry is static, injected composition.
  Round advancement: ADMISSION expands the (authored or absent)
  `round` declaration into explicit per-transition flags — the kernel
  consumes ONLY the flags, never an inference (C39).
- *Read-path:* `gate_projection` (derived; exposes no raw
  transcript); retained allow/warn decisions on the committed
  transcript read surface.
- *Forbidden fallback:* an unresolved evaluator never skips
  (`gate_evaluator_unavailable`); a runner failure never allows
  through (runner outcome, block); malformed decision JSON is never a
  business block (runner outcome); nothing on this surface is
  fail-open.
- *Allowed resolution:* config defaults, MATERIALIZED once at
  admission into the effective config — absent `output.mode` ⇒ `exitCode`; absent dispositions ⇒
  `blockTransition`; absent `reason` entries ⇒ the fixed per-bucket
  defaults; absent `previous_reviewer_verdict` config ⇒
  `{required: true}`.
- *Missing-data:* absent `gates` key = ungated step; a config a
  registration demands but does not receive is an ADMISSION issue; a
  process gate in a workflow with no runtime context is an admission
  issue (+ the HANDLE runtime backstop, C36); absent `round` key = NO
  advancing transitions — declared-only advancement, round remains 1
  after activation; an authored list is EXHAUSTIVE (replace, never
  merge) (C38).

**Substrate probe record** (yaml@2.9.0 via the v3 package's pinned
dependency, `scratchpad/gate-probe/probe.mjs`, 2026-07-11;
re-executed and reproduced by the round-1 substrate lens):

| Probe | Question | Result |
|---|---|---|
| GP1 | does the nested `gates` shape (event-type → list of gate maps) parse clean? | yes — zero diagnostics; `toJS` yields the natural object/array graph |
| GP2 | what is an unquoted `0:` map key? | node-level key is the NUMBER `0` (`"nonzero"` is a string); `toJS` stringifies it to `"0"` — the numeric-key identity class is real at the node level and invisible after `toJS` |
| GP3 | are dotted plain scalars (`external.process`) strings? | yes — plain strings, zero diagnostics |
| GP4 | integer forms for `timeoutMs`? | `900.0`, `0x384`, `9e2` ALL resolve to integral `900` (`Number.isInteger` true); `"900"` resolves to a string — the ch8-C8 source-form trap reproduced on this surface |
| GP5 | does an empty gate list parse? | yes — clean parse; the nonempty rule must be validator-owned |
| GP6 | the round-declaration forms (session probe 2026-07-12, the reopen round; same pinned yaml@2.9.0; re-executed and EXTENDED by the round-1 substrate lens)? | flow and block forms parse clean, members are plain strings; an EMPTY `advanceOnArrivalAt` list parses clean (the nonempty rule is validator-owned — GP5's twin); a SCALAR `round:` value parses clean (the container-precondition lane is real); an unquoted numeric member resolves to the NUMBER at the node level (GP2's identity class at list-member grain — and unlike numeric MAP keys, `toJS` does NOT stringify list members); DUPLICATE members parse clean (validator-owned). Lens re-run extensions: a NULL `round:` value (nothing after the colon) parses clean as present-null; a string/map `advanceOnArrivalAt` value parses clean; an empty `round: {}` map parses clean; an unknown key in the `round` map parses clean; a QUOTED numeric member (`"0"`) is a STRING at the node level |

**Draft metrics** (template §5, recorded at the 2026-07-12 ratification):
rounds to ratify: 1 full five-lens panel round + 3 delta-reconciliation
close passes + 1 external-arm round (with clean re-check) + the ratifier's
read sweep (6 catches — among them the model gap that produced the ratified
model fix 453d3be9); new-decision rows: 11 (C1, C6, C7, C11, C13, C15, C17,
C18, C24, C26, C33); post-ratification reopenings: 0. (The Opus panel and
the sweep are review EVIDENCE — not arms; the arms field names the
transitional cross-model arm only, per the template's letter.)

**Reopen record (2026-07-12, the ch11-P2c authoring round).** Reopened
from `ratified` on the plan §11.4 P2c row's PRE-DECLARED decision
point: the round-declaration format surface had no ratified row (the
model's "advances_round is declared transition semantics … never
inferred from … target equality" vs the realized ch-4
`target === template.start` heuristic). The user resolved the
draft-routing STOP (`1:open-choice`) to REOPEN/EXTEND (over a
transitional stance). Seed disposition (every seed item landed):
authoring grammar → C37; absent-key default → C38;
expansion/normalization + the kernel consumption rule → C39;
validator lanes → C40; the per-transition override's deferral → C41.
Sources: the model's 08-l2 section prose
(`model-src/sections/08-l2.html`, the round-semantics paragraph —
part of the ratified model state 453d3be9; named HERE because the
Sources list above enumerates units and Config views, not section
prose) + the golden-trace Config views
(`code/l2-template-config.new.txt` line 9, the `round:` declaration —
in the Sources list above). The C38 absent-key default was RE-DECIDED
at the ratifier read (start-arrival → declared-only/NONE — the
ratifier's authority); the C39/C40 staging split (value-level lanes
P2c, source-form lanes P4) resolved at the same read, on the
ratifier's manual arm round; and the ratifier resolved the ensuing
STOP `2:meaning-changing-alignment` by ESTABLISHING §8.2 rule 2's
general DEVIATION CLAUSE (a non-behavior-preserving default only as
the ratifier's explicit per-key act — the plan amendment rides the
re-ratification commit; C38 is the clause's first exercise).
**Reopen metrics** (template §5, recorded at the 2026-07-12
re-ratification): rounds to re-ratify: 4 panel rounds (2 full + 2
targeted) + 7 lens-4 reconciliation passes + 5 agent-invoked arm runs
(1 refine with clean re-check; the rest clean re-checks on the
ratifier-fold rounds, 1 carrying a timing finding dispositioned
timing-by-design) + the ratifier's 3 manual-arm rounds (the C38
declared-only re-decision + the staging split; the lifecycle/baseline
precision + the wrapper-only clause; the STOP-2 deviation-clause
resolution) + 3 top-level closes (the first two voided by
ratifier-read content folds); reopen-delta new-decision rows: 3
(C37, C38, C41 — cumulative 14); post-ratification reopenings: 1
(closed by this act).

**Second reopen record (2026-07-19, the ch12 ratification — acts
A of its decision set).** Reopened from `realized` (the post-close
escape hatch, the ratifier's resolved STOP — plan §12.1 item 3's
mandate): C18/C19/C21/C30 converted to POINTER forms delegating the
runtimeContext value domain and its growth stance to
`contract:ch12-runtime-core#C2–#C4`/`#C5`/`#C23`; the bare `required`
form retired by the same act (human-approved 2026-07-18, ratified
2026-07-19). Reopen-delta new-decision rows: 0 (pointer conversions;
the decisions live in the ch12 draft). The pointer rows realize
VACUOUSLY (the C22 retired-in-place precedent) — the successor
semantics realize at ch12's own close.

**Third reopen record (2026-07-23, the ch9 draft-ratification act —
its carried reopen, ch9-C27).** Reopened from `realized` (the
post-close escape hatch): the seven fixed reason tokens renamed to
their `sys:` forms at the six token-spelling sites (C10, C11, C17,
C25, C26's classification reference, C31), and C31 replaced by the
ratified successor text — the set-disjointness promise NARROWED to
the POSITIONAL rule (a reason token never occupies a rejection-name
position; system-vs-authored and system-vs-registry disjointness BY
CONSTRUCTION via the `:` — ADR-018; the ratifier's option-A decision,
the fail-closed registry-name check declined). Reopen-delta
new-decision rows: 0 (the decisions live in the ch9 draft +
ADR-018). Code realization = ch9-P0 (the map's C31 entry delegates).

**Close metrics (recorded at the ch11 close, 2026-07-18):**
post-ratification reopenings: 2 (the 2026-07-12 round-declaration
reopen — closed by re-ratification the same day; the 2026-07-19
ch12-ratification pointer reopen — C18/C19/C21/C30 delegated to the
ch12 successor, closed by re-ratification within the same act); every row realized
across ch11-P0…P4 per the realized map below — the `realized` flip +
map in ONE act (this commit).
**Fourth reopen record (2026-07-26, the ch13 draft-ratification act —
the context-block key).** Reopened C4 (the binding keyset grows the
optional `contextBlockRefs`, delegation `contract:ch13-context-block#C6`),
C30 (the non-surface clause converted to the landed form, camelCase
authored key named), and C41's comparative parenthetical (tensed, both
mentions) — one act, prepared texts and itemized map duties in the ch13
draft's Context (its C16 carrier row). Reopen-delta new-decision rows: 0
(pointer/tense conversions; every decision lives in the ch13 rows).

**Dated update (2026-07-23, the ch9-act reopen):** post-ratification
reopenings 2 → 3 (the `sys:` rename reopen above — closed by
re-ratification within the same act).
**Dated update (2026-07-26, the ch13-act reopen):** post-ratification
reopenings 3 → 4 (the ch13 draft-ratification act's C4/C30/C41 reopen —
closed by re-ratification within the same act).

**Dated annotation (2026-08-13, the ch13 boundary — pointer
maintenance, LEAVE-WITH-RECORD; rows byte-frozen).** The
`contract:ch13-context-block#C6` pointers in C4, C30, C41, the fourth
reopen record above and two `realized_map` entries PREDATE that
draft's supersede (2026-08-05): the pointed-at semantics — the
`contextBlockRefs` value class and resolution — are carried UNCHANGED
by the successor surface's row `contract:ch13-context-block-v2#C6`
(realized at the ch13 close). This is pointer maintenance, not
meaning drift; the ch13 boundary weighed REPOINT (a realized-reopen
ceremony for zero semantic content) and recorded this annotation
instead, at the spot where a reader meets the stale pointers. The
frozen predecessor file remains readable beside its successor.

## Contract rows (every normative statement is a C-row)

| ID | Rule |
|---|---|
| C1 | A step map gains the OPTIONAL `gates` key — the ch8-C7 forward declaration's "first expected" additive growth (ch8-C9's step keyset becomes `role`, `instruction`, `transitions`, + optional `agentConfig`, + optional `gates`). Its value is a map of event-type → gate list. An absent `gates` key means the step has no gated transitions. DECIDED HERE — a recorded divergence from the model's exhibited Config view, which nests gates INSIDE the transition entry (`on_converged: {target, gates}`): ch8-C24's additive-only evolution forbids turning `transitions`' scalar targets into maps, so the binding moves to a parallel step key; the (step, event type) grain is preserved (C2). |
| C2 | The `gates` map is OPEN-KEY (ch8-C13 vocabulary): its keys are event types under ch8-C10's id grammar, and every key MUST be a member of `keys(step.transitions)` — a gate bound to a non-transition event type is an ADMISSION issue (dead config, the silent-typo class). The binding grain is (step, event type), realizing the model's `gates_for(step, event_type)`. |
| C3 | Each event-type key's value is a NONEMPTY list of gate maps; authored order IS pipeline order (the runtime's ordered, first-block-wins evaluation consumes it; the format fixes only the order). An empty list is an ADMISSION issue — the parser accepts it (probe GP5), so the rule is admission-owned. |
| C4 | A gate map is FIXED-KEYSET: `uses` (required) + `config` (optional map) + `contextBlockRefs` (optional — value class and semantics owned by `contract:ch13-context-block#C6`). NO `implementation` / `execution` / `id` keys exist — the implementation and execution axes are evaluator-INTRINSIC (registry-resolved from `uses`), never authored; unknown keys are ADMISSION issues per the ch8-C13 fail-closed culture. |
| C5 | `config` presence is evaluator-specific: REQUIRED for `external.process` (C13) and `declarative.threshold` (C10); OPTIONAL for `pairflow.previous_reviewer_verdict` (C11). A missing `config` where the registration requires it is an ADMISSION issue (C20). |
| C6 | `uses` grammar: a nonempty string matching `^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)+$` — two or more dot-separated lowercase segments (probe GP3: dotted plain scalars parse as strings). Evaluator ids are VALUES, never error-path segments; ch8-C10's dot-ban (the path-segment grammar of the four ch8 id classes) is UNTOUCHED: C10 governs ids that become C21 path segments, `uses` values never do (C7 fixes the gate error-path form). DECIDED HERE — the relation to C10 is stated, not inferred; no C10 row is modified. |
| C7 | The ch8-C21 dotted-path grammar EXTENDS with LIST-INDEX segments for list containers: a gate's error path is `steps.<stepId>.gates.<eventType>[<i>]` (0-based), its fields `steps.<stepId>.gates.<eventType>[<i>].uses` etc. The extension is grammar-additive; existing ch8 paths are unchanged. DECIDED HERE. |
| C8 | The Block A evaluator registry composition is EXACTLY `declarative.threshold`, `pairflow.previous_reviewer_verdict`, `external.process` — STATIC composition (dynamic module loading is a ledger Absent), kernel-side, injected. ADMISSION (`admit_definition`) RESOLVES every `uses` against the INJECTED gate catalog at definition load — an unknown id is the `gate_evaluator_unavailable` DEFINITION ISSUE (the compiler receives the catalog by injection at the composition root; `definition/` still never imports `gates/`); HANDLE keeps the same-named rejection as the runtime availability BACKSTOP (registry drift across process generations — C35). |
| C9 | `external.process` is the ONLY registry member with implementation=process; `declarative.threshold` is declarative and `pairflow.previous_reviewer_verdict` is packaged — both in-process inline. ALL three are execution=inline; no deferred-execution member exists in Block A (`gate_execution_not_supported` covers the axis, the model's L2 lane). |
| C10 | `declarative.threshold` config keyset: exactly `metric`, `op`, `value` — ALL three required (the model's realized tiny DSL, projected from the golden-trace Config views: `config: {metric: round, op: ">=", value: 2}`; the section's own note: "deliberately tiny: a metric, an op, and a value — not a general expression language" — the `broad-declarative-dsl` ledger Absent is that GENERAL expression language, untouched by this row). Block A allowlists: `metric` ∈ { `round` } (an allowlisted `gate_projection` path), `op` ∈ { `">="` } (authored as a quoted string), `value` an authored integer ≥ 1 under C12. Semantics: block with the FIXED reason `sys:round_below_min` iff `projection.round < value`; allow otherwise. The reason-token derivation for FUTURE metrics/ops arrives additively with those members, never generalized here. |
| C11 | `pairflow.previous_reviewer_verdict` config: OPTIONAL; when present, exact keyset `{required}` with the sole legal value `true` — projecting the golden-trace Config views' `config: {required: true}`; ABSENT ≡ `{required: true}` (behavior-preserving default). The advisory `required: false` variant is UNREALIZED in Block A — authoring it is an ADMISSION issue (a reserved future toggle, the fail_instance pattern). DECIDED HERE — the false-variant exclusion and the optionality; the config keyset itself is the model's. Semantics: allow iff the projection history carries at least one prior committed transition whose `step_id` equals `projection.current_step` (the l2 trace's "prior reviewer verdict" — the reviewer's earlier committed PASS from the same review step); otherwise block with the FIXED reason `sys:no_previous_verdict`. DECIDED HERE — the model exhibits only the allow lane; the same-step semantic and the block token are this draft's decisions. |
| C12 | EVERY authored integer field on this surface (the threshold `value`, `timeoutMs`) follows ch8-C8's plain-scalar decimal source-text rule: raw source text matches `^[1-9][0-9]*$`, the scalar is anchor-free, non-aliased, and tag-free, and the resolved value is a safe integer. Probe GP4 reproduces the C8 trap on this surface (`900.0`, `0x384`, `9e2` all resolve to integral 900) — one grammar for all authored integers, the realized ch8 ladder reused. |
| C13 | `external.process` config keyset (fixed): `command` (required nonempty string), `timeoutMs` (required, C12), `output` (optional map with the single key `mode`), `onExit` (conditional per C15), `onRunnerError` (optional, C16), `onTimeout` (optional, C16), `reason` (optional, C17). Unknown keys are ADMISSION issues (the ch8-C13 fail-closed culture at the admission seam). The model's mandatory pair is `command` + bounded timeout — both required here. `command` is ONE POSIX shell command line, interpreted with `sh -c`, with the workspace root as cwd — DECIDED HERE, ratifier-precised (v1-faithful; the l2a trace's `"pnpm test"` presupposes shell word-splitting; the STRING form is kept deliberately — it matches the existing command slots and v1 behavior; an argv-list alternative was considered and rejected for its larger model/slot-surface divergence). Windows/native-shell portability is NOT a Block A property — stated, not implied. ch 9's real runner realizes the spawn; spawn mechanics beyond the contract — the timeout's kill-signal semantics, environment inheritance, stdout size bounding — are NAMED ch-9 runner-contract territory (deferred explicitly, never silently). |
| C14 | `output.mode` ∈ { `exitCode`, `gateDecisionJson` }; ABSENT ⇒ `exitCode` (the model's default, resolved once). An explicit unknown mode is the `invalid_process_gate_config` ADMISSION issue. |
| C15 | In exitCode mode (explicit or defaulted): `onExit` is REQUIRED, a FIXED-KEYSET map with BOTH `zero` and `nonzero` REQUIRED, each value ∈ { `allow`, `warn`, `block` } (the model's both-buckets rule; buckets map only to realized verdicts — `route` is never legal). The authored keys are the WORDS `zero`/`nonzero` — DECIDED HERE: the model's exhibited bucket key is the QUOTED string `"0"` (`on_exit["0"]`), and this surface renames it to `zero` on BOTH sides — the authoring grammar here, and the `classify_process_result`/`validate_gate_config` realizations, which read the `zero` bucket where the model text reads `"0"` (the rename is stated so neither side silently forks). Rationale: probe GP2 — an UNQUOTED `0:` key is a NUMBER at the node level while `toJS` stringifies it; the word keys exclude the numeric-key identity class AND the quoted-vs-unquoted authoring ambiguity outright. In gateDecisionJson mode a present `onExit` is an ADMISSION issue (unconsumed config = dead config, fail-closed) — a draft-decided hardening ABOVE the registration validator's model text, which inspects `on_exit` only under exitCode mode; one authority, one channel (C20), so no seam-scope split exists. |
| C16 | `onRunnerError` / `onTimeout`: when present, the ONLY legal value is `blockTransition`; ABSENT ⇒ `blockTransition` (the model's default). The value `failInstance` is rejected DISTINCTLY (the model's `gate_config_not_supported` — a reserved future disposition, its own lane); any other value is the generic invalid lane. The authored camelCase tokens map to the model's `block_transition`/`fail_instance`. |
| C17 | `reason` (optional): a FIXED-KEYSET map `{zero?, nonzero?}`, each value a token matching `^[a-z][a-z0-9_]*$` — the PER-BUCKET `GateDecision.reason` of exit-bucket decisions (the l2a trace's authored `test_failed` rides the `nonzero` entry). An absent entry ⇒ that bucket's fixed default `sys:exit_zero` / `sys:exit_nonzero`, MATERIALIZED into the effective config at admission (plan §8.2 rule 5 inherited by name: existing defaults are STABLE — changing one is breaking). EVERY exit-bucket decision carries a reason — allow, warn, and block alike (the model's `classify_process_result` returns `reason: exit_reason(result.exit_code)` UNCONDITIONALLY, and `exit_reason` is exit-code-DEPENDENT: the per-bucket map realizes that faithfully — a single flat token would stamp an allow with a failure reason, the audit absurdity the ratification read caught; an allow decision's reason is retained via C27). The authored reasons NEVER apply to runner outcomes: `sys:runner_error`, `sys:timeout`, and `sys:malformed_gate_decision_json` are FIXED distinct tokens (the model's audited-distinctly rule). This realizes the model's unspecified `exit_reason(exit_code)` as per-bucket authored-or-default — the AUTHORED-REASON direction has design provenance: the gate-policy synthesis memo's normalized shape authored `reason_code` per gate binding; the model's Config view then DROPPED it for the threshold (fixed reason, C10) and left the process gate's `exit_reason` undefined — this row closes the open half in the memo's direction. DECIDED HERE (the per-bucket map form is the ratifier's option-(b) decision at the ratification read). |
| C18 | The template top level's OPTIONAL `runtimeContext` key (ch8-C7 additive growth; ch8-C7's five-key root keyset becomes five + this optional key — the aggregate after later growth lives in C37). Its value domain and grammar are OWNED by `contract:ch12-runtime-core#C2–#C4` (ownership moved by the ch12 ratification act — human-approved 2026-07-18, ratified 2026-07-19 — the consuming chapter this row named). The bare string `required` is RETIRED by that same act: the removed form fails LOUD with its migration text (the §8.2 rule 3 mechanic, the act itself the authority) — the recorded exception to this row's original "existing files keep their exact meaning" promise, legal only as this explicit human re-ratification. |
| C19 | The process↔workspace lane: a template declaring ANY `external.process` gate whose `runtimeContext` requirement RESOLVES to `none` (authored `none` or absent key) yields the `runtime_context_required_for_process_gate` ADMISSION issue — the trigger's grammar is owned by `contract:ch12-runtime-core#C5`; an ILLEGAL `runtimeContext` value yields ONLY its own container finding, this lane suppressed as its dependent (the C21 container-precondition rule). The model's HANDLE `ready(∅)` runtime backstop (C36) is unchanged. |
| C20 | SINGLE-AUTHORITY ADMISSION (the ratified model fix, 453d3be9): parse (the ch8 pipeline — syntax + source metadata) → `admit_definition` over the INJECTED gate catalog = THE one semantic validation + normalization point (structure, registration resolution, per-registration `validate_and_normalize_config`, the `requires_runtime_context` cross-rule, defaults materialized ONCE, issue ACCUMULATION with the all-or-nothing return) → the admitted/compiled template — the `DefinitionStore`'s ONLY output (the TRUST lives in the store PORT CONTRACT; the branded type is its API expression, not a runtime mechanism). Kernel CREATE validates INSTANCE inputs only (task, binding coverage, ref existence). The testkit builds templates THROUGH the same admission (tests import the compiler; the kit's own import boundary — ADR-005/ch8-C32 — is untouched: harness types narrow to the admitted type). Admission failures are DEFINITION ISSUES on ch8's load channel — never registry rejections (ch8-C23 unchanged; the registry is 54 post-fix); the named codes REUSE the former rejection tokens. A second admission point may exist ONLY at a real definition-trust boundary (dormant trigger — the federation era). |
| C21 | The gate ADMISSION lane matrix (canonical — the ONE channel; every lane an admission finding in ch8-C21's `{path, message}` form, the NAMED lanes additionally carrying their issue CODE — the former rejection tokens; the P4 packet derives its driven-lane checklist from THIS list PLUS C40's source-form round lanes — C40's value-level lanes land at P2c per its realization split). CONTAINER PRECONDITIONS: the containers this surface introduces — the `gates` value, each event-type's list, each gate map, each `config`/`output`/`onExit`/`reason` map — JOIN ch8-C21's container-precondition rule: a container that is MISSING (where required) or NOT ITS REQUIRED KIND yields ITS OWN finding and suppresses its dependent lanes (e.g. `gates: "x"` is ONE finding at `steps.<id>.gates`, never a key-iteration cascade). The lanes: non-transition event key; empty gate list; gate-map unknown key; `uses` MISSING; `uses` grammar violation; unknown-but-grammatical `uses` → code `gate_evaluator_unavailable` (C8); `config` missing where required (C5); threshold `metric`/`op`/`value` missing, non-allowlisted, or C12-invalid; threshold-config unknown key; `previous_reviewer_verdict` config unknown key or `required` ≠ `true`; process `command` missing, empty, or non-string → code `invalid_process_gate_config`; process `timeoutMs` missing/C12-invalid → code `invalid_process_gate_config`; process-config unknown key; `output` unknown inner key; `output.mode` unknown → code `invalid_process_gate_config`; `onExit` missing or a bucket (`zero`/`nonzero`) missing → code `invalid_process_gate_config`; `onExit` bucket value non-allowlisted → code `invalid_process_gate_config`; `onExit` surplus key; `onExit` unconsumed (gateDecisionJson mode); disposition `failInstance` → code `gate_config_not_supported`; disposition other-unknown → code `invalid_process_gate_config`; `reason` violation (non-map value, unknown key beside `zero`/`nonzero`, token-grammar violation); the `runtimeContext` value violates its ratified grammar — owned by `contract:ch12-runtime-core#C2–#C4`; ONE container finding, dependent lanes suppressed; process gate with a requirement resolving to `none` → code `runtime_context_required_for_process_gate` (C19, grammar `contract:ch12-runtime-core#C5`; suppressed under an illegal-value container finding). |
| C22 | RETIRED-IN-PLACE at the model fix (the row id stays; the rule it carried was the two-seam design's circularity — the testkit bypass justifying a second validator). The standing rule is C20's: NO unadmitted template reaches the kernel — the admitted type is the only kernel input; evaluators receive the EFFECTIVE config by construction and carry no re-validation duty; testing an invalid config = testing the compiler (asserting its issue list), never handing a bad template to the kernel. |
| C23 | `GateInvocation` wire shape (the process gate's stdin, ONE UTF-8-encoded JSON document, snake_case = model-verbatim): exact top-level keyset `{instance_id, template_ref: {id, version}, step_id, event_type, expected_version, config, projection}` — `config` is the EFFECTIVE (admission-materialized) config — the one downstream form per the ratified model fix (the model's authored-snapshot line was updated); the declared/authored form lives in the source YAML for authoring and diagnostics, never on the wire, `projection` is C24's compact shape. The runner passes NOTHING else (no argv payload; stdin carries the invocation). |
| C24 | `gate_projection` compact wire shape: `{round, current_step, event_type, history}`; `history` = the ordered (transcript-order) list of `{step_id, event_type, role}` for every COMMITTED actor transition — no payloads, no raw transcript (the model's derived-view rule); an entry's `step_id` is the step the transition was emitted FROM. The SAME shape feeds in-process evaluators and the process wire inside C23. DECIDED HERE — the Block A field list of the model's `derive_policy_view`. |
| C25 | `GateDecision` JSON wire (gateDecisionJson mode, the process's stdout decoded as UTF-8, ONE JSON document under a STRICT single-value parse — leading/trailing whitespace legal, ANY other surrounding or trailing content is unparseable; snake_case = model-verbatim): exact keyset `{verdict (required), reason?, message?, evidence_refs?}` — `verdict` ∈ { `allow`, `warn`, `block` } (allowlisted; `route` is invalid until the routing slice); `reason`/`message` nonempty strings; `evidence_refs` a list of nonempty strings (an empty LIST is legal). Unparseable stdout, a non-object root, a missing or non-allowlisted `verdict`, ANY unknown top-level key, or ANY field violating its stated shape — a wrong-typed field, an EMPTY `reason`/`message` string, an empty `evidence_refs` ELEMENT — ⇒ `sys:malformed_gate_decision_json` → a runner outcome under `onRunnerError`'s disposition, NEVER a business block (the model's own lane). |
| C26 | Evidence on EVERY process-gate run (the l2a checker invariant, realized IN FULL in this chapter — ratifier-decided at the ratification read: the COMPLETE artifact contract enters now; only the MEASUREMENT is ch 9's, never the fields — no partial-invariant middle state): each run persists ONE evidence record, addressed by `log_ref` (the model's name kept; the ref addresses the WHOLE record — the model's "log + JSON artifact" pair is ONE Block A record), with the complete v1-faithful field list: `log` (the captured output text), `kind` (`ok` / `timeout` / `runner_error`), `exit_code` (present iff kind=`ok`), `duration_ms` (non-negative integer), `head_sha`, `git_status_hash`. The workspace-fact fields are the RUNNER's declared values: the ch-11 testkit runner mints DETERMINISTIC fakes; the ch-9 real runner MEASURES them. PERSISTENCE GUARANTEE (the `ProcessGateRunner` port contract): `run()` has DURABLY persisted the record on its substrate BEFORE returning — a returned `log_ref` MUST resolve; the testkit runner exposes its persisted records for assertion, and the record exists INDEPENDENTLY of any kernel commit, so blocked/timeout/runner_error runs are evidenced too (their refs return on the Rejected surface, C33). The checker binds BOTH halves: the store-visible half (every committed process-gate decision carries resolving refs) and the run-level half (every run — allow/warn/block/timeout/runner_error — has its persisted record). `kind` records PROCESS EXECUTION, independent of decision classification: a malformed-JSON run is kind=`ok` (the process ran and exited) whose C25 classification is `sys:malformed_gate_decision_json` — this bridges C26's three kinds to C29's six-outcome drive. |
| C27 | Retained decisions on the transcript read surface: every committed ACTOR transition entry carries `gateDecisions` — `[]` when the transition ran no gates (the ch6 known-empty culture), else the ORDERED allow/warn decisions as `{uses, verdict ∈ {allow, warn}, reason?, message?, evidence_refs?}` — block never commits (gate-before-commit). The store column behind it is THE ch11 schema bump, fenced per ADR-003. |
| C28 | CLI surface: NO new verbs and NO new flags. The new ADMISSION lanes ride ch8-C31's `{stage, findings}` machine shape and ch8-C38's write-lane doc UNCHANGED (they are C21-form admission findings; named lanes carry their issue code); gate rejections at HANDLE ride the ch6 rejection surface like every registry name (54 post-fix). |
| C29 | Module home (ADR-013, riding this draft: `proposed` with the content commit, `accepted` by the ratification act; amends ADR-001): a NEW `src/gates/` module holds the builtin inline evaluators + the static registry (imports domain/ports only); the registry/catalog reaches BOTH the kernel AND the definition compiler (admission) as INJECTED dependencies at the composition root (the port-parametric rule holds — neither `kernel/` nor `definition/` imports `gates/`); `domain/` gains the ledger-named gate values (the template's gate bindings, `GateDecision`); `ports/gate.ts` is RECONCILED — the ch3 placeholder shapes (`GateRunner` pass|fail, `ProcessRunner` `{exitCode, output}`, their scripted fixtures) are REPLACED by the ledger shapes (`GateRegistration` — the inline-evaluator variant carrying `evaluate` —, the catalog/registry port, `ProcessGateRunner` with C34's result kinds), a named replacement sweep, never a parallel seam; the testkit's scripted players re-shape onto the six-outcome drive (allow/warn/block/timeout/runner_error/malformed — ch11-P3); the port's result type is C34's `ProcessResult` shape. The fixed reason-token VALUES are this contract's business (C10, C11, C17); their code placement (a domain constant set vs `gates/`-local) is packet freedom. |
| C30 | Growth stance: the gate-map keyset (C4), the per-evaluator config keysets and allowlists (C10, C11, C13), the `runtimeContext` value domain — ownership and growth stance moved to `contract:ch12-runtime-core#C2–#C4`/`#C23` by the ch12 ratification act (human-approved 2026-07-18, ratified 2026-07-19; the same act retired the bare `required` form — this row's additive-only promise no longer binds that domain) —, and the `round` map keyset (C37 — with its absent-key default C38 stable per §8.2 rule 5, and the per-transition override an explicit partial-realization deferral, C41) grow ONLY additively per plan §8.2, and existing DEFAULTS are stable per §8.2 rule 5 (inherited by name — C17's per-bucket defaults included). The authored gate-binding key `contextBlockRefs` (the model's `context_block_refs`) joined the binding keyset at the ch13 ratification act — value class and semantics: `contract:ch13-context-block#C6`; before ch13 it was explicitly not a key of this surface. |
| C31 | A runner-outcome block REJECTS at HANDLE as `gate_blocked(reason=<fixed token>)` — the SAME rejection name as a business block, audited distinctly BY the reason (the l2a trace: `gate_blocked(reason=sys:runner_error)`). Reason tokens — authored (C17, grammar `^[a-z][a-z0-9_]*$`) and fixed (`sys:round_below_min`, `sys:no_previous_verdict`, `sys:exit_zero`, `sys:exit_nonzero`, `sys:runner_error`, `sys:timeout`, `sys:malformed_gate_decision_json`) — are `gate_blocked` REASON PAYLOAD, never registry rejection names: a reason token never occupies a rejection-name position (the POSITIONAL boundary). System-vs-authored disjointness holds BY CONSTRUCTION (the authored grammar cannot express `:` — ADR-018), and no `sys:`-prefixed token can equal a registry name (no registry name contains `:`); an authored token that SPELLS a registry name remains payload-only and positionally harmless — the earlier set-disjointness promise is NARROWED to this positional rule (re-ratified at the ch9 draft act, 2026-07-23). |
| C32 | Process-returned free text is UNTRUSTED (the C22 mirror for the outbound seam): `reason`, `message`, and `evidence_refs` elements arriving via C25 are opaque diagnostic/display values — never re-parsed, never policy or path input, never interpolated into commands; they are retained VERBATIM on the transcript read surface (C27) under the ch6/ch7 untrusted-free-text culture (the debug bundle's redaction boundary applies to them as to every payload-adjacent string). The AUTHORED reason (C17) is grammar-bound; the PROCESS-returned reason is not — the asymmetry is deliberate and confined by this row. |
| C33 | Evidence propagation (canonical; realizes the model's `with_evidence` and the section's "recorded on the committed transcript entry, or returned in the `Rejected` outcome on a block"): an exit-bucket decision carries `evidence_refs = [log_ref]` (the model's classify, verbatim); a gateDecisionJson decision carries the PROCESS-returned `evidence_refs` with the runner's `log_ref` APPENDED as the last element when not already present (string equality — the `with_evidence` merge realized; order and dedup are DECIDED HERE); a runner outcome carries `[log_ref]` (the `runner_outcome` unit). Propagation: allow/warn decisions retain their refs on the committed transcript entry (C27); a block's refs are RETURNED on the rejection surface — `Rejected(gate_blocked(reason), evidence_refs)`, model-verbatim (the l2a HANDLE). |
| C34 | The `ProcessResult` port shape (the model's `ProcessGateRunner` unit, verbatim fields): `{kind ∈ {ok, timeout, runner_error}, exit_code?, stdout?, log_ref, duration_ms}` — `exit_code` and `stdout` present iff kind=`ok` (`stdout` is the UTF-8-decoded text C25 consumes; `exit_code` an integer), `log_ref` a nonempty string, `duration_ms` a non-negative integer. C26's evidence record derives from this shape; the testkit's six-outcome drive scripts it. |
| C35 | HANDLE runtime backstop #1 — REGISTRY AVAILABILITY: the gate rung's `gateRegistry.resolve(uses)`; an unresolved id is `Rejected(gate_evaluator_unavailable)` — normally UNREACHABLE under a well-formed, unchanged registry (admission resolved every id at load); the lane guards REGISTRY DRIFT (an old instance loading under a new process generation's composition). Runtime-only; its admission twin is C21's resolve lane. |
| C36 | HANDLE runtime backstop #2 — WORKSPACE EMPTINESS: a process gate reached with `instance.runtime_context = ready(∅)` is `Rejected(runtime_context_required_for_process_gate)` — the model's l2a HANDLE lane and the registry's DUAL name (its compile twin is C21's declaration-level lane, C19). Realized by ch11-P3 directly from the l2a HANDLE unit. |
| C37 | The template top level gains the OPTIONAL `round` key (the ch8-C7 forward declaration's second additive growth on this surface — the root keyset becomes the ch8 five + the two ratified optional keys: `runtimeContext` (C18) and this). Its value is a FIXED-KEYSET map with the single key `advanceOnArrivalAt` (camelCase realization of the model's exhibited `advance_on_arrival_at` — the C13/C16/C18 rename culture, stated so neither side silently forks), whose value is a NONEMPTY list of step ids: arrival at a listed step advances the round (the model's authoring sugar, exhibited in the golden-trace Config views). Each member MUST be a member of `keys(steps)`; a TERMINAL id (ch8-C17's `terminal` list — DISJOINT from `keys(steps)` by that row, so the membership rule enforces this exclusion) is NOT a legal member — DECIDED HERE: the model ties round advancement to work-cycle starts ("the start step being a round-start"), a terminal arrival starts no work cycle, and an advancing terminal arrival would mutate the final-state round for nothing. (A sink STEP — empty `transitions`, legal per ch8-C12 — IS in `keys(steps)` and is a legal member: it dispatches work.) NO start-value key exists: round always starts at 0 — the model's fixed convention, not configurable (no authoring surface for it, now or later). Unknown keys in the `round` map are ADMISSION issues (the ch8-C13 fail-closed culture). The authoring key and C40's SOURCE-FORM lanes land with the P4 format walk; C40's VALUE-LEVEL lanes land at P2c (C40's realization split). |
| C38 | ABSENT-KEY DEFAULT (DECIDED HERE — the model exhibits the key but leaves the absent case unstated; RE-DECIDED at the ratifier read 2026-07-12, superseding the panel rounds' start-arrival default): an absent `round` key declares NO advancing transitions — the C39 expansion runs on the EMPTY set, every per-transition flag is false, and after ACTIVATION the instance's round remains 1 for the rest of active execution (the CREATE-state round is 0 and activation sets 1 — the model's fixed lifecycle, untouched by this row). Rationale: the LEAST-INFERENCE reading of declared transition semantics — an undeclared advancement is inferred from NOTHING, not even as a default (a start-arrival default would re-admit the banned graph-shape inference at the authoring layer); the ch-4 `target === template.start` heuristic was PROVISIONAL kernel scaffolding (`instance.ts`'s own forward pointer: "Formal round machinery ... is L2"), never a ratified format default — no RELEASED v3 template-compatibility baseline exists to preserve (the repo's canonical `v3/templates/local-pair-v0@1.yaml` is an in-repo development artifact; its gaining the round declaration is an explicit P4 migration), and §8.2 rule 5 binds from THIS ratification on, to THIS default. The model's exhibited start-arrival behavior is RESTORED per template by EXPLICIT declaration (the shipped template file gains its `round:` declaration at P4; the l2 trace's direct-constructed template declares it at P2c). CONSEQUENCE (stated, deliberate): a template binding a round-reading gate (C10) with NO round declaration is LEGAL-but-INERT — round never exceeds 1, a `value ≥ 2` threshold never passes; authoring-side detection of that shape (an admission cross-rule or a lint warning) is a NAMED later additive decision, deliberately NOT taken here. The (empty) default is MATERIALIZED once at admission (the C14/C17 culture; the admitted form carries no absent state), and an AUTHORED list is EXHAUSTIVE: exactly the listed steps advance — nothing is implicit, the start step included (replace, not merge). RELATION to plan §8.2 rule 2 + ch8-C7 (stated, not inferred): both expect an additive key's default to be BEHAVIOR-PRESERVING, and this default DEVIATES — a declaration-absent file does NOT keep the pre-key (ch-4 provisional heuristic) round behavior. The deviation is legal under §8.2 rule 2's DEVIATION CLAUSE — a non-behavior-preserving default ONLY as the ratifier's explicit per-key ratification act, recorded in the realizing chapter's contract row — and THIS row is that clause's FIRST exercise (the clause and this exercise are ratified by the same re-ratification act), scoped to the `round` key alone: every other key's behavior-preserving obligation stands. ch8-C7's realized text is untouched — its own post-realized carrier clause ENUMERATES the successor surface as a permissible carrier ("a new ratification act on this draft vs a successor surface — ... the process layer's call at that chapter"), and the selection is exactly the process-layer call this reopen exercises. |
| C39 | NORMALIZATION + KERNEL CONSUMPTION (realized by ch11-P2c): admission (`admit_definition` — C20's single authority) expands the declaration — authored or defaulted (C38) — into explicit per-transition `advancesRound` flags on the admitted template: a transition (step, event type) → target carries `advancesRound = (target ∈ advanceOnArrivalAt)`; NO flag is synthesized for activation, which has no incoming edge (the model's own clause). The kernel consumes ONLY the normalized flags: `advances_round` is declared transition semantics — NEVER inferred from "role names, graph shape, target equality, or naming" (model-verbatim ban; the realized ch-4 `target === template.start` heuristic is exactly the banned form and is REPLACED, not wrapped). STAGING: until P4 lands the authoring key (C37), the FILE channel carries ONLY C38's default path — no loadable YAML can author `round` (the ch8 unknown-key rejection stands), so a file-loaded instance's round remains 1 after activation for the whole window; directly-constructed templates may author the declaration from P2c on, through the same admission (under C40's value-level lanes, which land WITH this staging). Round stays transcript-reconstructable over the pinned admitted template (the `l2/round-is-canonical-reconstructable` checker's format-side guarantee — the checker itself is ch11-P2c packet material). The flags' domain-type placement (a parallel step key vs a transition-record field) is packet freedom — the C29 pattern. |
| C40 | The round ADMISSION lanes (JOINING C21's container-precondition rule and the ONE channel — UNCODED findings in ch8-C21 `{path, message}` form at the paths `round`, `round.advanceOnArrivalAt`, `round.advanceOnArrivalAt[<i>]` under C7's list-index grammar): `round` present but not a map — a scalar, a list, or NULL (`round:` with an empty value is PRESENT-null and lands HERE, never on C38's absent-key default; GP6 + the lens re-run: a scalar and a null both parse clean — ONE finding, dependent lanes suppressed); `round` unknown key; `advanceOnArrivalAt` MISSING (an empty `round` map); its value not a list (a string or map value parses clean per GP6), or an EMPTY list (parses clean per GP6 — the nonempty rule is admission-owned, the C3 empty-list culture: authored dead config fails closed); a member that is not a string (GP6: an unquoted numeric member resolves to a NUMBER — the GP2 identity class at list-member grain); a member not in `keys(steps)` — an UNKNOWN id (the C2 dead-config/silent-typo class) or a TERMINAL id (C37's exclusion: a deliberate-but-illegal reference, not a typo; ONE lane, message granularity is packet freedom); DUPLICATE members (parse clean per GP6 — validator-owned, the same dead-config class). REALIZATION SPLIT (staged with C39's channels — the ratifier-arm finding, 2026-07-12): the VALUE-LEVEL semantic lanes — the EMPTY list, a member not in `keys(steps)` (the terminal exclusion included), DUPLICATE members — bind EVERY admission channel and land at P2c WITH C39's normalization (the direct channel's input is a TYPED domain value, so container kinds and member primitiveness are foreclosed by construction — but these value rules are NOT type-expressible and must be admission-checked on every channel); the SOURCE-FORM lanes — `round` not a map, unknown key, `advanceOnArrivalAt` missing, a non-list value, a non-string member — are format-walk lanes and land at P4 with C37. |
| C41 | The model's "with per-transition override" clause is an explicit PARTIAL-REALIZATION disposition in Block A (DECIDED HERE, the model relation STATED — deliberately NOT the then-standing C30 `context_block_refs` non-surface pattern: `context_block_refs` was another chapter's semantics (landed at ch13 as the authored key `contextBlockRefs` — `contract:ch13-context-block#C6`), while the override is ratified model capability on THIS surface): the model's loader expansion carries a per-transition override; Block A realizes the AUTHORING half as the template-level list ONLY, and the override's authoring grammar (unexhibited in the model — prose only, no Config view shows it) stays UNREALIZED — the §11.2 partial-realization class (the dispatch_intent/RECEIVE precedent: a projection-time disposition call, not a scope change), carried BY NAME in plan §11.1 item 2 (aligned at this reopen's re-ratification — never a silent narrowing). The deferral is authoring-grammar-ONLY: the normalized PER-TRANSITION flags (C39) are exactly the model's expansion target, so the override capability stays REPRESENTABLE in the admitted form (an override changes which flags are SET, never the flag shape) and its grammar enters ADDITIVELY with its own ratified row when a concrete need arrives (§8.2's no-speculative-keys rule). |

## Ratification history

```json
{"ratification": {"date": "2026-07-12", "arms": ["agent-invoked codex gpt-5.6-sol/high — one review round + clean re-check"], "commit": "1748443f"}}
```

```json
{"ratification": {"date": "2026-07-12", "arms": ["agent-invoked codex gpt-5.6-sol/high — five runs across the reopen (refine + clean re-check; three clean re-checks on the ratifier-fold rounds)", "the ratifier's manual arm — three rounds (NO-GO: the C38 declared-only re-decision + the C39/C40 staging split; refine: the lifecycle/baseline precision + the wrapper-only trace clause; NO-GO STOP 2: resolved by the §8.2 rule 2 deviation clause)"], "commit": "67bea60a"}}
```

```json
{"ratification": {"date": "2026-07-19", "arms": ["the ch12 draft loop's agent-invoked codex arm (gpt-5.6-sol/high) — the prepared pointer texts reviewed within the ch12 rounds (two full rounds + six re-checks, final clean)"], "commit": "7cb22aadb20d37dece52eaa0f75f8b494ff55688"}}
```

```json
{"ratification": {"date": "2026-07-23", "arms": ["the ch9 draft loop's agent-invoked codex arm (gpt-5.6-sol/high) — the prepared reopen payload reviewed within the ch9 rounds (one full round + four re-checks, final CLEAN)"], "commit": "45fcac9602ce8f396a1d09348669762cf5b565a0"}}
```


```json
{"ratification": {"date": "2026-07-26", "arms": ["none external — the ch13 draft loop's internal five-lens Opus panel (4 full + 1 targeted round + clean top-level close); the codex arm consciously waived by the owner at GO (2026-07-26)"], "commit": "f507cda2220489a214de2580e5b8c58e2f061e23"}}
```

## Realized map (empty until chapter close)

```json
{"realized_map": {
"C1": "ch11-P2a D1 (domain Step.gates value) + ch11-P4 F2 (format-walk step keyset) + ch11-P3b W3 (reachability honesty: no shipped channel authors gates until P4) — domain/template.ts Step.gates field (the GateBinding/GatePipeline types are domain/gate.ts); definition/validate.ts F2 walk; admit.test.ts / validate.test.ts",
"C2": "ch11-P2a A4 + D1 (the Step gate lookup realizing the gates_for(step, event_type) grain) — definition/admit.ts event-type membership (gates bound to keys(transitions)); domain/template.ts; admit.test.ts A4 lane (+ ch11-P4 F3 gates-subtree walk, validate.ts)",
"C3": "ch11-P2a A4/D3 — definition/admit.ts nonempty-list admission + authored pipeline order; admit.test.ts (+ ch11-P2b K1 ordered first-block-wins eval, kernel/kernel.ts)",
"C4": "ch11-P4 A1 — definition/admit.ts gate-binding fixed-keyset (uses + optional config; + contextBlockRefs at ch13 — delegation: ch13#C6), unknown-key fail-closed; admit.test.ts A1 lane ; reopened at the ch13 ratification (2026-07-26) — successor contract:ch13-context-block#C6 (that draft superseded 2026-08-05; the semantics live unchanged in contract:ch13-context-block-v2#C6, realized at the ch13 close); realized vacuously (delegation — the successor realizes at ch13's close)",
"C5": "ch11-P2a G3/A4 (threshold required) + ch11-P3a V2 (process required) — definition/admit.ts config-presence; gates/threshold.ts / gates/process.ts validate (the verdict gate's OPTIONAL config is G5's lane — derived, refs C11/C21/C30 — gates/previousReviewerVerdict.ts)",
"C6": "ch11-P4 A2 — definition/admit.ts `uses` grammar lane (^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$), distinct from resolution; admit.test.ts A2 lane",
"C7": "ch11-P2a R4/A5 (+ ch11-P4 path forms) — definition/admit.ts list-index error paths steps.<id>.gates.<evt>[<i>]; admit.test.ts [<i>]-segment path asserts",
"C8": "ch11-P2a R3/A3 + G2 (the packet's two-member slice) + ch11-P3a G1 (the three-member chapter-end catalog) + ch11-P2b K2/T1 (runtime resolve backstop + one-catalog injection at the composition roots) — gates/registry.ts + definition/admit.ts uses-resolution (gate_evaluator_unavailable); registry.test.ts / admit.test.ts (HANDLE backstop is C35)",
"C9": "ch11-P2a R1/R2 (inline members) + ch11-P3a G1/G2 (process member, no evaluate) + ch11-P3b X1 (the HANDLE process branch — the reject→run flip) — ports/gate.ts GateRegistration variants + gates/registry.ts + kernel/kernel.ts; registry.test.ts compile-negatives",
"C10": "ch11-P2a G3/G4 — gates/threshold.ts validateAndNormalize (metric/op/value allowlists) + sys:round_below_min semantics; threshold.test.ts config + boundary grid",
"C11": "ch11-P2a G5/G6 — gates/previousReviewerVerdict.ts config ({required:true}, absent-default, sys:no_previous_verdict) + same-step semantics; previousReviewerVerdict.test.ts",
"C12": "ch11-P3a V3 (value-level: safe integer >= 1) + ch11-P4 F6 (ch8-C8 source ladder) — gates/process.ts; definition/validate.ts F6 lane (the threshold value allowlist is G3's, anchored at C10 — gates/threshold.ts)",
"C13": "ch11-P3a V2 (registration keyset) + R1 (the run(command,{cwd,stdin,timeoutMs}) port signature) + ch11-P3b X2 (run) — gates/process.ts validateAndNormalize; ports/gate.ts; kernel/processGate.ts; admit.test.ts / process.test.ts",
"C14": "ch11-P3a V2 (output.mode keyset + exitCode default) + ch11-P3b M1 (classify) — gates/process.ts; kernel/processGate.ts; processGate.test.ts M1 kind×mode grid",
"C15": "ch11-P3a V2 (onExit both-buckets keyset, zero/nonzero rename) + ch11-P3b M1 (bucket classify) — gates/process.ts; kernel/processGate.ts; process.test.ts",
"C16": "ch11-P3a V2 (disposition keyset, failInstance distinct) + ch11-P3b M3 (runner_outcome) — gates/process.ts; kernel/processGate.ts; process.test.ts",
"C17": "ch11-P3a V2 (reason per-bucket keyset + defaults materialized) + ch11-P3b M1/T3 (per-bucket reason on the wire) — gates/process.ts; kernel/processGate.ts; process.test.ts",
"C18": "ch11-P3a D1 (declaration consumed) + ch11-P4 F1/A3/F4 (root keyset + illegal-value lane + the walk's runtimeContext slot pass-through) + ch11-P3b S2/W3 (the start-seam ref lanes + the P4 deferral naming) — definition/admit.ts / definition/validate.ts; admit.test.ts / validate.test.ts ; reopened at the ch12 ratification (human-approved 2026-07-18, ratified 2026-07-19) — successor contract:ch12-runtime-core#C2–#C4; realized vacuously (delegation — the successor realizes at ch12's close)",
"C19": "ch11-P3a G2/V5 (V5 the new-decision owner of the implemented template-grain cross-rule — empty manifest refs, the lane text carries the C19 anchor) + ch11-P2a A7 (the pre-P3 explicitly-ruled-out branch — no dead code before the operand exists) — definition/admit.ts requiresRuntimeContext cross-rule (runtime_context_required_for_process_gate, template-grain single finding); admit.test.ts V5 lane ; reopened at the ch12 ratification (human-approved 2026-07-18, ratified 2026-07-19) — successor contract:ch12-runtime-core#C5; realized vacuously (delegation — the successor realizes at ch12's close)",
"C20": "ch11-P2a A1/A2/A6 + D6 (the branded AdmittedTemplate type) + T1 (the testkit admitted-type path) + ch11-P3a V6 (single authority over BOTH channels) + ch11-P4 F3/F7 (the walk defers all semantics to admission + file-channel accumulation) — definition/admit.ts single semantic-authority admission + DefinitionStore admitted-only contract; admit.test.ts all-or-nothing arm",
"C21": "ch11-P2a A9 + G3/G5/G8 (the inline config lanes: threshold, reviewer-verdict, container-precondition/own-property) + ch11-P3a V2/V4 + ch11-P4 A1-A3/F4/F7 — definition/admit.ts one-channel lane matrix ({path,message} + code carrier); admit.test.ts path+code lanes + load.test.ts file-channel sweep ; reopened at the ch12 ratification (human-approved 2026-07-18, ratified 2026-07-19) — successor contract:ch12-runtime-core#C2–#C4 + #C5; realized vacuously (delegation — the successor realizes at ch12's close)",
"C22": "RETIRED-IN-PLACE — no rule to realize; realized vacuously by the C20 single-admission form (ch11-P2a A1/A6, definition/admit.ts); ch11-P2b K4 carries the no-unadmitted-template kernel rule",
"C23": "ch11-P3b X3 + T1 (the call-recording kit driving the wire asserts) + ch11-P2a A5 (the authored form pinned to source YAML, never on the wire) — kernel/processGate.ts GateInvocation stdin wire (effective config, snake_case keyset); ports/gate.ts; processGate.test.ts X3 lane",
"C24": "ch11-P2b V1-V3 + ch11-P2a D5 (the domain GateProjection shape — the evaluate signature's second input) — kernel/gateProjection.ts derive {round,current_step,event_type,history}; domain/gate.ts; gateProjection.test.ts (+ ch11-P3b X4 same shape on the process wire)",
"C25": "ch11-P3b M2 — kernel/processGate.ts GateDecision strict single-value JSON parse + sys:malformed_gate_decision_json runner outcome; processGate.test.ts M2 lanes",
"C26": "ch11-P3a R3/T1 + ch11-P3b W2/T2 — ports/gate.ts evidence record shape + cli/failClosedProcessGateRunner.ts durable persist + testkit/scriptedProcessGateRunner.ts fakes; storeCheckers.ts STORE-VISIBLE-half checker + scriptedProcessGateRunner.test.ts run-level persist-before-return half (only the measurement is ch 9's)",
"C27": "ch11-P2b K5 + S1-S4/O2/O3/O4 (read pass-through on detail/timeline/tail + the bundle-keyset-unchanged stance) + ch11-P3b E1 (process-decision evidence retention) — kernel/kernel.ts retained ordered gateDecisions on the committed transition + store/sqliteStore.ts schema bump (ADR-003); kernel.test.ts / sqliteStore.test.ts",
"C28": "ch11-P4 Y7 (+ ch11-P2b K7/O3, ch11-P2a A9/T4) — cli/dev/main.ts validate + write-lane surface the same {stage,findings} docs, no new verbs/flags; dev/journey tests",
"C29": "ch11-P2a G1/R5/T2 (+ ch11-P3a G1) + ch11-P2b T1 (KernelDeps.gates — the one-catalog wiring at the composition roots) + ch11-P3a T1 (the scripted six-outcome runner kit) — src/gates/ module + gates/registry.ts injected catalog + ports/gate.ts ledger-shape reconciliation; ADR-013 accepted; registry.test.ts",
"C30": "growth STANCE — no code realizes it; the ratified stance stands, WITNESSED by the additive keyset pattern (ch11-P2a G5's pairflow.previous_reviewer_verdict config allowlist, gates/previousReviewerVerdict.ts; the ch11-P2c/P4 round keyset, domain/template.ts + definition/validate.ts) + the §8.2 additive-evolution culture; the former non-key witness RETIRED at the ch13 ratification (2026-07-26 — the key joined C4's keyset; the witness was mis-sited anyway: the binding unknown-key lane lives in admit.ts) — successor contract:ch13-context-block#C6 (that draft superseded 2026-08-05; the semantics live unchanged in contract:ch13-context-block-v2#C6, realized at the ch13 close), realized vacuously (delegation); prior reopen at the ch12 ratification (human-approved 2026-07-18, ratified 2026-07-19) — successor contract:ch12-runtime-core#C23",
"C31": "ch11-P3b M4/X1 (+ ch11-P2b O1/O5) — kernel/kernel.ts builds the gate_blocked(reason=<fixed token>) rejection (processGate.ts returns the GateDecision it consumes); the 2026-07-23 re-ratification NARROWS disjointness to the POSITIONAL rule (by-construction via the sys: forms, ADR-018) — the earlier authored-token REALIZATION GAP is RESOLVED by the narrowed rule (no runtime check owed); the sys: spellings' code realization is DELEGATED to ch9-P0 (until it lands, code carries the pre-rename spellings — a declared, packet-owned catch-up, the ch12-P0 precedent); processGate.test.ts / kernel.test.ts",
"C32": "ch11-P3b E2 + ch11-P2b O5 (the untrusted-confined verbatim-retention culture at the Block-A grain) — kernel/processGate.ts + kernel/kernel.ts process-returned reason/message/evidence_refs retained verbatim, never re-parsed/interpolated; kernel.test.ts E2-confinement suite",
"C33": "ch11-P3b E1 (+ ch11-P2b K4/O1) — kernel/processGate.ts evidence_refs propagation (exit-bucket [log_ref], gateDecisionJson append-dedup, runner outcome) + kernel/kernel.ts Rejected(...,evidence_refs) pass-through; processGate.test.ts E1 lanes",
"C34": "ch11-P3a R1/R2 + T1 (the six-outcome membership owner — the scripted playback kit) — ports/gate.ts ProcessResult shape ({kind, exitCode?, stdout?, logRef, durationMs} — the TS grain of C34's snake_case model spelling) + testkit/scriptedProcessGateRunner.ts six-outcome drive; scriptedProcessGateRunner.test.ts",
"C35": "ch11-P2b K2 — kernel/kernel.ts gate rung gateRegistry.resolve backstop (gate_evaluator_unavailable, registry drift); kernel.test.ts K2 lane",
"C36": "ch11-P3b S1/S5 — kernel/kernel.ts ready(∅) workspace-emptiness backstop (runtime_context_required_for_process_gate; processGate.ts already receives a non-null cwd); kernel.test.ts C36-backstop S5 suite",
"C37": "ch11-P2c D1 (the domain round?: {advanceOnArrivalAt} authoring shape) + ch11-P4 F1/F5 (root keyset + source-form) — domain/template.ts; definition/admit.ts / definition/validate.ts (the membership/terminal-exclusion value lanes are A2's, anchored at C40; A3 is the derived one-channel accumulation row); admit.test.ts / validate.test.ts",
"C38": "ch11-P2c A1/A4/K2 + D1 (absent = the none-default at the domain grain) (empty-set default, round stays 1 after activation) + ch11-P4 F8/Y1/Y2/Y5 (file default + shipped template restoration + the gated journey owning journey.test.ts) — definition/admit.ts, templates/local-pair-v0@1.yaml; admit.test.ts / journey.test.ts",
"C39": "ch11-P2c A1/K1/D2 + D3 (admitted-form completeness as an admission postcondition) + T1 (the checkRoundReconstruction checker lane + the AdmittedTemplate harness narrowing) + ch11-P4 F8 (the file-grain membership expansion) — definition/admit.ts advancesRound expansion + kernel consumes flags only (replaces target===start heuristic) + testkit/storeCheckers.ts reconstructable checker; admit.test.ts / kernel.test.ts",
"C40": "ch11-P2c A2/A3 (value-level: empty list, member membership incl. terminal, duplicates) + ch11-P4 F5 (source-form lanes) — definition/admit.ts; definition/validate.ts F5 lane; admit.test.ts / validate.test.ts",
"C41": "PARTIAL-REALIZATION disposition — the deferral is the ratified content: the per-transition override's authoring grammar stays unrealized, and C39's normalized advancesRound flags (ch11-P2c A1, definition/admit.ts) keep the override representable in the admitted form for an additive future row ; reopened at the ch13 ratification (2026-07-26) — the comparative parenthetical tensed (annotation-only, no landing change)"
}
}
```
