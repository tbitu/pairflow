
force_release(instance) → void                                     # LC4 — the same release ERRAND (full form), force-opened. IDENTICAL to LC2 initiate_release — release_safe (LC1 INV-5), provider, the marker-first CAS-claim → provider.release → RUNTIME_CONTEXT_RELEASED — EXCEPT it skips the single guard (boundary_event ∉ release_boundaries(template)): an operator delete is the authority to release regardless of the declared boundaries. release_safe is NOT skipped — a forced release is still a safe release. Resolves the gap where delete_requested ∉ a run’s release.boundaries would otherwise no-op
