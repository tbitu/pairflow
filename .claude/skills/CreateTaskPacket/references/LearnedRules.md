# Learned Rules — the failure-class registry

Distilled from `v3/implementation/process-log.md` (the source of
record; every rule cites its provenance line). Maintenance: updated at
**chapter boundaries only** (README §7 — capture, don't fix), from the
boundary review's verdicts — or at an explicit USER-RATIFIED process
revision (the §7 "unless the issue blocks" clause; precedent: the
2026-07-17 ch11-P3a reset). A rule enters here when the log marks it
adopted or standing; WATCH items enter marked as such.

| Id | Rule | Provenance |
|---|---|---|
| R-CLAIM-NEGATIVES | A gate's or contract's negative tests derive from its DECLARED claim, never from the implemented rule list — a blocklist passing an unlisted violation is the recurring failure class. | README §4 step 2; log 2026-07-07 ch3 aftermath (2nd occurrence) |
| R-DIMENSIONS | Enumerate a claim's DIMENSIONS before deriving tests; a fix scoped to the dimension just caught repeats the loop. When a row declares independence of N NAMED things, membership = the cross-product of those N names QUOTED from the rule (never paraphrased, never read off a fixture), and every construction-unreachable cell is named with its reason (ch13 boundary; the P0 receipt: one axis missing from a from-memory list let two wrong classifiers survive 1818 green tests). Known ladder: value shapes → descriptors → prototypes → numeric identity. The ladder applies to EVERY new validator over a numeric domain (`-0` requires `Object.is` — `Number.isSafeInteger(-0)` is true, `-0 < 0` is false; absorbed R-NUMERIC-LADDER, ch13 boundary). | log 2026-07-07 ch4 aftermath 3–4; ch5 chapter rule; ch6 aftermath 2 (numeric) |
| R-EXECUTION | A logged instruction is not execution — a sweep or test obligation counts only when it RAN. Packets phrase obligations as execution ("driven by test X"), never intention. | log 2026-07-07 ch4 aftermath 4; ch5 chapter rule |
| R-ALIGNED-UP | A packet decision contradicting ratified plan text flows UP into the plan IN THE SAME COMMIT, marked "aligned at <packet-id> pre-approval". Never silent, never deferred. | log 2026-07-07 ch5 boundary (standing convention) |
| R-EMPTY-SLICE | Operability packets (CLI/floor/tooling — zero kernel semantics) declare an EMPTY ledger slice explicitly; "coverage axes unchanged" is an assertion the close verifies, not an omission. | ch6 practice (all five packets); plan §6 |
| R-DELEGATION-CLOSURE | A claim that DELEGATES its definition ("P1-declared", "per ledger §X", "the canonical body") is expanded IN-PACKET — the delegated source's FULL rule set (field lists, presence iffs, enum domains) — with a stated PROOF BOUNDARY for rules the packet's own surface cannot decide. Registry/model-sourced FIELD LISTS are the same duty — ledger §4 entity names alone under-specify a shape (absorbed R-FIELD-LISTS, ch13 boundary). DRAFT LAYER (ch13 boundary; two regions, two reopens): a contract-draft may not delegate a channel's discipline to an actor's care without stating what the CODE does there — a delegation names AUTHORING care, never runtime behavior; draft-time litmus: "what does the code do when this actor gets it wrong?". | AuthorPacket step 4.5; log 2026-07-08 ch7-P2 round 8; adopted at the ch7 boundary; ch4 boundary (field lists) |
| R-CLAIM-FORM-PROBES | A mechanized guardrail's negative probes derive from the CLAIM's form dimensions (e.g. import FORMS: static / dynamic / re-export), never from the implemented rule's shape — R-CLAIM-NEGATIVES applied at the lint/tooling layer. SCOPE EXTENSION (ch12 boundary): applies to ONE-OFF verification/reconciliation scripts too — fresh checker tooling ships WITH adversarial negative fixtures (delete / duplicate / swap / typo mutations) in the SAME commit; tooling proven green on the good tree but never driven to FAIL is the false-green class. A GUARD'S TOKENIZER IS PART OF THE GUARD (ch13 boundary; the P4 receipt: whitespace-split masked 12 copied runs the word-normalized form caught) — the text-splitting/normalization step gets its own negative probes, same standing as the rule it feeds. | log 2026-07-10 ch7-P3 aftermath (the dynamic-import hole); adopted at the ch7 boundary; extended at the ch12 boundary (the ADR-015 sweep's two false-green classes: count-closure circularity, order blindness) |
| R-UNTRUNCATED-SWEEP | A completeness/sweep claim rides its UNTRUNCATED measurement — a truncated output is not a measurement, and a truncation-SATURATED output (hit count == the cut limit, e.g. `head -20` returning exactly 20 lines) is itself the overflow signal: re-run before claiming a "full set". | README §5.5 finding policy; log 2026-07-11 ch8-P2 (arm gate 1: a head-cut grep dropped the 21st hit — the live `domain/template.ts` debt comment); adopted at the ch8 boundary |
| R-LANE-SENSITIVITY | "Driven" means ABLE TO FAIL: a lane's test must catch a plausible violation of its row's MEANING — presence is not sensitivity. Known blind classes: keyset-only asserts passing wrong content; single-form fixtures blind to split/ordering regressions; a rule's stated-but-undriven half; projected-field equality passing full-row divergence; a CONFINEMENT/EXCLUSIVITY claim asserted as an enumeration of watched sub-parts — assert over the WHOLE surface MINUS the named permitted delta (ch13 boundary; the p1b family-8 receipt); a machine-readable-field claim asserted by token-in-serialization — assert on the FIELD (ch13 boundary; the p1b family-11 receipt: the lane stayed green after the `code` attribute was deleted with its token left in the message). BINDS TWICE (promoted 2026-07-17 — the ch11-P2b/P2c second occurrence): at authoring against the packet's lane texts, and at build close against the BUILT test bodies — the arm gate-2 mandatory sensitivity pass; correct lane texts still build weak asserts, so the guarantee is only purchasable at the built bodies. | ReviewPacket lens-3 duty 2 + README §5.5 build-close gate; log 2026-07-11 ch8-P2 (arm gate 2: four green-but-blind lanes); adopted at the ch8 boundary; twice-binding promoted 2026-07-17 (ch11-P2b/P2c) |
| R-ACTIVATION-JOURNEY | An ACTIVATION packet (previously-built foundation wired into a live path reachable from a shipped entrypoint) ships ≥1 journey smoke THROUGH that entrypoint — subprocess, production bindings, full lifecycle. Deterministic actors only: a stub bound via the SHIPPED actor-config surface is legal, an injected seam is not; real-LLM runs are a separate non-CI tier. | template §2 write-time disciplines; log 2026-07-11 (the §8.9 P2 journey ratification); adopted at the ch8 boundary |
| R-ALTITUDE-LINE | Test obligations are authored as DISCIPLINE + FAMILY INVENTORY (parameterized membership, owner named), never fixture-level enumeration — enumeration is BUILD work; a spec-time finding demanding it is `deferred-to-build` BY RULE, and the build-close arm gate's sensitivity pass owns the member-by-member check against the BUILT bodies. Spec-time findings remain: missing family, missing discipline, wrong membership rule, contradiction, ratified-row conflict. | README §5.5 finding policy + template §2; user-ratified 2026-07-17 (the ch11-P3a process reset — the PROCESS2 comparative study) |
| R-CLAIM-GRAMMAR | Every completeness/universality claim takes a CLOSED form: MEASURED (untruncated receipt — R-UNTRUNCATED-SWEEP), PARAMETERIZED ("every member of the declared <set> satisfies X", owner named), or SCOPED (named exclusions + deferral home). Bare wholesale rhetoric is a finding — exhaustivity rhetoric attracts completeness findings indefinitely; a closed form converts them into checks against declared sets. THE RATIFICATION-SIDE HALF (ch13 boundary — the five-member executed-probe family consolidated): the grammar binds at WRITE time and binds the RATIFYING side too — a MEASURED label travels with its receipt INTO THE RECORD (not the conversation); a DERIVED value names its measurer; a CITED attribution is grep-verified before it rides (an unverified citation is the citation-shaped unrun "measured"); and a CONSOLIDATING edit earns its widened scope only with a per-case executed probe. | template §2 write-time disciplines + ReviewPacket lens-3 duty 7; user-ratified 2026-07-17 (ch11-P3a reset) |
| R-PRESENT-TENSE | The packet's operative reading path speaks in the present — zero round references, fold provenance, or review residue (litmus: would the sentence survive on a day-one authoring?); provenance lives in the machine blocks / flags / process log / git. Intent notes are not scars; scar findings are bookkeeping-class. REFERENCE ECONOMY (ch12 boundary; scope EXTENDED at the ch9 close to FORWARD OBLIGATIONS — a verdict, deferral, or carried item a future session must pick up is a reference in this sense, and its carrier is a conscious choice: derivation-visible plan surface or machine marker, never prose-only): a cross-reference earns its upkeep iff benefit > rot-risk × miss-cost — a reference worth keeping is PROMOTED to a machine-checked class (lint / coverage / adr-check / the `DEFERRED(chN)` marker) where the value justifies it; where it does not, DROP the reference rather than maintain a silently rotting pointer. The default when in doubt is DROP. | template §2 write-time disciplines + ReviewPacket lens-4 duty 3; user-ratified 2026-07-17 (ch11-P3a reset); reference-economy default adopted at the ch12 boundary (the P4 artifact-linkage item) |
| R-INSTRUMENT-PROBE | A completeness-bearing sweep is INSTRUMENT-ROBUST: it records SITE LISTS, never bare counts; when the result can be tool/mode-dependent (binary-classified files, line-wrapped or em-dash comment forms), the receipt uses a byte-level scan or names the exact tool+mode; two environments measuring differently is an automatic RE-MEASURE trigger, never an assumption that one side erred. R-UNTRUNCATED-SWEEP's instrument-side sibling. | log 2026-07-18 ch11 boundary (the P4 receipt rounds: the binary-skipping ugrep wrapper vs stock BSD grep divergence; the NUL-classified admit.ts; the wrapped/em-dash pointer forms); adopted at the ch11 boundary |
| R-ABSENCE-CONSUMERS | When a shared fixture/value FLIPS state (a field is born or removed), the ripple sweep enumerates the value's CONSUMERS — searching the shared value's NAME — never only the new token: consumers of its ABSENCE are token-invisible. | log 2026-07-18 ch11 boundary (the P4 build STOP: the storeCheckers declaration-absent fixture broke outside the declared boundary); adopted at the ch11 boundary |
| R-DERIVED-PROBES | The builder's red-on-break mutation-probe list is DERIVED from the packet's declared family inventories — >=1 probe per family, materialized as a build-report TABLE (family -> what breaks -> expected red -> observed); a probe with NO test able to red, or one that stays GREEN, is a build-time finding (the arm-gate-2 green-but-blind class caught at build). Ad hoc probe picks never satisfy the rule. EXECUTION PROTOCOL (ch12 boundary): probes run EXCLUSIVELY through the probe runner (`tools/v3-plan/probe_runner.py`) — scratchpad-copy backup BEFORE the mutation, restore FROM THAT COPY (git-based restore is FORBIDDEN for mutation rollback: git knows the committed state, not uncommitted work — three live incidents), `cmp` byte-verification after, and a RECEIPT file per probe (exact command, exit code, red test names, restore-verified) that the table cites by id — the table is receipt-backed evidence, never LLM prose; arm-flagged "plausibly blind" lanes get an EXECUTED verification through the same runner before their fold is recorded. | log 2026-07-18 ch11 boundary (the P3b interpretive layer's candidate; P4 gate-2 yield 4 test-evidence / 0 product beside 4 ad hoc probes); adopted at the ch11 boundary; execution protocol + receipts adopted at the ch12 boundary (restore incidents: ch11-P0 git-restore, ch12-P1a git-checkout + mis-pathed backup; the self-report gap raised by the owner) |

## How a rule gets here

1. Friction happens → one line in `process-log.md` the moment it happens.
2. Chapter boundary review issues the verdict (gate / rule / README edit /
   non-issue / WATCH). Admission bias (adopted at the ch12 boundary): a
   single-occurrence candidate defaults to WATCH (severity-weighted
   exceptions only); when a rule is warranted, mechanize (lint/test/
   checker) or extend an existing lens duty before adding prose — the
   prose registry's size is the guarded budget.
3. Verdicts that shape packet AUTHORING land here with their provenance;
   verdicts that shape the build loop land in README §4. This file never
   invents a rule the log does not carry.

## How a rule leaves

Retirement is a first-class boundary verdict with the same standing as
addition (adopted at the ch12 boundary). Each boundary review asks, per
rule: zero catches this chapter? absorbed by a mechanical check? a
duplicate of a lens duty, or persistently co-firing with another rule?
A retired rule's row is DELETED — the registry speaks present
(R-PRESENT-TENSE); the retirement's reasons and date live in the
boundary's process-log entry and git history. The boundary entry carries
the per-rule catch tally that feeds this sweep (a catch matching several
rules credits EACH of them; persistent overlap is itself a duplication
signal, same standing as a zero-catch rule).
